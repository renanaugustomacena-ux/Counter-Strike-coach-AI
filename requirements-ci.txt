# CI-specific requirements for GitHub Actions (CPU-only runners)
# Forces CPU-only PyTorch — no GPU available on CI runners.
#
# Uses version ranges (requirements.txt) rather than the lock file to avoid
# conflicts between CUDA-pinned versions and CPU index resolution.
# For reproducible distribution builds, use requirements-lock-cpu.txt instead.
#
# Usage: pip install -r requirements-ci.txt

--extra-index-url https://download.pytorch.org/whl/cpu

-r requirements.txt
