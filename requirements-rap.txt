# RAP Coach experimental architecture — optional dependencies
# Required ONLY when USE_RAP_MODEL=True in settings.
#
# These packages provide the LTC (Liquid Time-Constant) neural network and
# Hopfield associative memory layers used by the RAP Coach memory system.
#
# Install: pip install -r requirements-rap.txt
# Then set USE_RAP_MODEL=True in the application settings.

# >=1.0.1: ncps 0.0.7 shipped a stray print("alloc!") in torch/ltc_cell.py
# executed on every LTC construction, polluting training stdout (TASKS#60);
# upstream removed it in 1.x. RAP test suite verified green on 1.0.1.
ncps>=1.0.1,<2.0
hflayers>=1.3.0,<2.0
