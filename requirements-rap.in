# =============================================================================
# Macena CS2 Analyzer — RAP Coach Optional Dependencies
# =============================================================================
#
# Required ONLY when USE_RAP_MODEL=True in settings.
#
# Compile to a hashed lockfile via:
#
#   uv pip compile --generate-hashes \
#       --output-file requirements-rap-lock.txt \
#       --constraint requirements-lock.txt \
#       requirements-rap.in
#
# The --constraint pin to requirements-lock.txt prevents accidental upgrade of
# shared transitives (numpy, torch) to versions different from production.
#
# =============================================================================

# SEC: justified — ncps tracks tied to torch + ML-research stability; explicit
# range pin lets us follow patch fixes without forcing a major-version bump.
ncps>=0.0.7,<2.0

# SEC: justified — same reasoning as ncps; community-maintained Hopfield
# implementation. TASKS#65: the 'hflayers' dist name does not exist on
# PyPI — the package installs from the ml-jku repo (import name hflayers);
# commit-pinned for supply-chain integrity, matching the lock files.
hopfield-layers @ git+https://github.com/ml-jku/hopfield-layers.git@f56f929c95b77a070ae675ea4f56b6d54d36e730
