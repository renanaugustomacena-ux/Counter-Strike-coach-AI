# Cluster 13a — tests/ (a-l)

Scope: `Programma_CS2_RENAN/tests/` files with basename starting a-l (conftest.py .. test_lifecycle.py) plus `automated_suite/`. `data/` contains only `.gitkeep` (fixture dir placeholder). `tests/__init__.py` starts with `_` (outside both agents' letter rule) — checked here: see Files read.

## Files read

- [x] conftest.py
- [x] __init__.py (1-line peek, out-of-letter-rule gap)
- [x] automated_suite/__init__.py
- [x] automated_suite/README.md
- [x] automated_suite/README_IT.md
- [x] automated_suite/README_PT.md
- [x] automated_suite/test_e2e.py
- [x] automated_suite/test_functional.py
- [x] automated_suite/test_smoke.py
- [x] automated_suite/test_system_regression.py
- [x] automated_suite/test_unit.py
- [x] test_amp_autocast.py
- [x] test_analysis_engines.py
- [x] test_analysis_engines_extended.py
- [x] test_analysis_gaps.py
- [x] test_analysis_orchestrator.py
- [x] test_animation_killswitch_all.py
- [x] test_auto_enqueue.py
- [x] test_baselines.py
- [x] test_belief_model_extended.py
- [x] test_blind_spots_extended.py
- [x] test_blind_synthesis.py
- [x] test_build_pipeline_testonly_safe.py
- [x] test_build_tools_contracts.py
- [x] test_charts.py
- [x] test_chat_streaming.py
- [x] test_checkpoint_normalizer_versioning.py
- [x] test_chronovisor_highlights.py
- [x] test_chronovisor_scanner.py
- [x] test_coach_manager_flows.py
- [x] test_coach_manager_tensors.py
- [x] test_coaching_dialogue.py
- [x] test_coaching_dialogue_tools.py
- [x] test_coaching_dialogue_tutor_mode.py
- [x] test_coaching_engines.py
- [x] test_coaching_service_contracts.py
- [x] test_coaching_service_fallback.py
- [x] test_coaching_service_flows.py
- [x] test_collapse_metrics.py
- [x] test_concept_temperature_saturation.py
- [x] test_config_dotenv.py
- [x] test_config_extended.py
- [x] test_config_resolution.py
- [x] test_coper_pathway.py
- [x] test_data_pipeline_contracts.py
- [x] test_data_quality.py
- [x] test_database_layer.py
- [x] test_database_wal_enforcement.py
- [x] test_db_backup.py
- [x] test_db_governor_integration.py
- [x] test_debug_ingestion.py
- [x] test_deception_flash_baits.py
- [x] test_dem_validator.py
- [x] test_demo_format_adapter.py
- [x] test_demo_parser.py
- [x] test_deployment_readiness.py
- [x] test_design_token_references.py
- [x] test_detonation_overlays.py
- [x] test_dimension_chain_integration.py
- [x] test_drift_and_heuristics.py
- [x] test_dry_run_checkpoint_integrity.py
- [x] test_elite_csv_builder.py
- [x] test_ema_hopfield_drift_invariants.py
- [x] test_embedding_collapse_detector.py
- [x] test_engagement_range_extended.py
- [x] test_entropy_extended.py
- [x] test_experience_bank_db.py
- [x] test_experience_bank_dedup.py
- [x] test_experience_bank_logic.py
- [x] test_faceit_integration.py
- [x] test_feature_extractor_contracts.py
- [x] test_feature_kast_roles.py
- [x] test_features.py
- [x] test_game_theory.py
- [x] test_game_tree.py
- [x] test_grenade_thrown_extraction.py
- [x] test_handoff_regressions.py
- [x] test_heatmap_projection.py
- [x] test_hltv_stat_parsing.py
- [x] test_hltv_sync_entrypoint.py
- [x] test_hltv_table_registry.py
- [x] test_hybrid_engine.py
- [x] test_ingestion_atomic_claim.py
- [x] test_ingestion_boundary.py
- [x] test_ingestion_pipeline.py
- [x] test_ingestion_tickrate.py
- [x] test_integration.py
- [x] test_jepa_insight_adapter.py
- [x] test_jepa_model.py
- [x] test_jepa_training_pipeline.py
- [x] test_jepa_window_fetcher.py
- [x] test_knowledge_graph.py
- [x] test_known_maps_ssot.py
- [x] test_label_source_monitor.py
- [x] test_lifecycle.py

## Invariants encoded by tests

### Test harness (conftest, automated_suite)

- __conftest.py__ — The suite's constitution. (1) Venv guard P6-03: pytest exits(2) unless running inside a venv, bypassed by `CI`/`GITHUB_ACTIONS` env vars (conftest.py:23-30). (2) `integration` marker auto-skipped unless `CS2_INTEGRATION_TESTS=1` (conftest.py:47-56) — the project-wide real-data gate. (3) Path stabilization: repo root injected into sys.path; `KIVY_NO_ARGS=1` set globally (conftest.py:35-40). (4) Three DB tiers: `in_memory_db` (schema via `SQLModel.metadata.create_all`, PT2-18 caveat that any raw-SQL schema in `init_database()` would diverge, conftest.py:66-72), `seeded_db_session` (CI-portable realistic data), `real_db_session` (production database.db, skip if absent). (5) `rap_model`/`rap_inputs` fixtures lock RAP input contract: view/map/motion `(B,3,64,64)`, metadata `(B,seq=5,METADATA_DIM)`, skill_vec `(B,10)`, seed 42 (conftest.py:349-370). (6) `isolated_settings` snapshots/restores `config._settings` and `_SETTING_NAME_TO_GLOBAL` globals, redirects SETTINGS_PATH to tmp (conftest.py:416-443). (7) `seeded_hltv_session` builds only `_HLTV_TABLES` (conftest.py:461-465). (8) `match_data_dir` per-match shard fixture is explicitly MINIMAL — no `match_metadata` table where the 26-NORM-01 per-shard tick_rate lives (conftest.py:586-591).
- __tests/__init__.py__ — empty file (0 bytes); package marker only. (Falls outside both agents' a-l/m-z letter rule; recorded here.)
- __automated_suite/__init__.py__ — 1-line comment, package marker.
- __automated_suite/README.md / README_IT.md / README_PT.md__ — Layered-suite doctrine: smoke → unit → functional → regression → E2E staging, E2E gated behind `CS2_INTEGRATION_TESTS=1`, functional tests must mock all external systems (no network/Ollama/Steam), regression tests freeze prior bugs. Cites `tools/headless_validator.py` as a separate gate run after pytest. IT and PT are faithful translations of EN — trilingual parity holds (all 80 lines, same tables/commands).
- __automated_suite/test_smoke.py__ — Build-alive gate: imports `TeacherRefinementNN` (legacy NN), `init_database`, i18n; asserts __METADATA_DIM == 25__ from `feature_engineering.vectorizer` (test_smoke.py:65-68) — hard-locks the metadata vector width; ModelFactory must expose TYPE_LEGACY/TYPE_JEPA/TYPE_VL_JEPA/TYPE_RAP/TYPE_ROLE_HEAD (test_smoke.py:83-84). Note: `test_database_init` calls real `init_database()`/`get_db_manager()` un-gated — touches the real DB file in a "smoke" layer.
- __automated_suite/test_unit.py__ — Locks `extract_match_stats` arithmetic: kd_ratio=kills/deaths, accuracy=hits/shots, `opening_duel_win_pct` ignores rounds with value -1 (no duel) (test_unit.py:29-32). LOC-02: unknown i18n key returns raw key string (test_unit.py:75-80); trilingual 'dashboard' key must exist for en/pt/it.
- __automated_suite/test_functional.py__ — Single test: settings round-trip via `save_user_setting`/`load_user_settings` under `isolated_settings`. Thin but honest.
- __automated_suite/test_e2e.py__ — `@integration` + `slow` + 120s timeout; full `CoachTrainingManager().run_full_cycle()` on real DB with skip-gate at <5 PlayerMatchStats; asserts CoachState exists with status and current_epoch>=1 after training (test_e2e.py:62-72). Comment records measured cost: 340 MB/s reads, 45+ min on the data box (test_e2e.py:14-16).
- __automated_suite/test_system_regression.py__ — Locks that `PlayerMatchStats` model carries `dataset_split` and `accuracy` fields (pure model construction, no DB) (test_system_regression.py:9-32); integration query test skip-gates on empty DB and never injects synthetic rows into production DB.

### Ingestion (demo parsing, pipeline, tick rate)

- __test_auto_enqueue.py__ — `@integration` class on the real DB (gated). Locks IngestionTask contract: default status "queued", retry_count 0, is_pro flag persists, created_at/updated_at populated and recent, ordering by created_at. Documents SQLite datetime asymmetry: in-session aware UTC, after round-trip naive UTC — test normalizes both sides (test_auto_enqueue.py:114-125). Cleanup only deletes rows with `/__test_auto_enqueue__/` path prefix — never real data.
- __test_faceit_integration.py__ — FACEIT API boundary. __R4 MED__: RFC 7231 allows HTTP-date `Retry-After`; the old bare `int()` raised uncaught ValueError (handler only caught RequestException), crashing instead of backing off. Contract: HTTP-date header => 60s fallback backoff; numeric Retry-After capped at 300s; persistent 429 raises typed `FACEITAPIError` "Rate limit exceeded" (test_faceit_integration.py:37-74).
- __test_feature_extractor_contracts.py__ — Tier-2 FeatureExtractor contracts. Shape exactly (25,), float32; METADATA_DIM==25 again. Slot semantics locked: [0] health/100, [1] armor, [2]/[3]/[5]/[6]/[7] binary helmet/defuser/crouch/scope/blind, [12]/[13] sin/cos(yaw), [19] weapon class (ak47=>0.6, unknown=>0.5, `weapon_` prefix stripped), [20-24] context features (0.0 without context; time/bomb/teammates/enemies/economy with). No NaN/Inf for empty, all-zero, or extreme (1e10 / negative) inputs. Batch extraction row-identical to individual. __26-VEC-01__: the P3-A batch quality gate must count only THIS batch's clamps via a thread-local counter — the old process-global delta let a concurrent thread trip a false DataQualityError (test_feature_extractor_contracts.py:248-276). __D1/D2 quality gates__: D2 clamp logging — first `_CLAMP_VERBOSE_LIMIT` (10) occurrences verbose, then one aggregate "suppressed" line per window, counters stay exact; __R4-14-01__ (0,0,0)-position warning follows the same discipline (teacher daemon had logged hundreds per pass); D1 single-sample sliding gate trips CRITICAL exactly once (hysteresis) with a state-manager notification at >5% clamp rate in the window; batch rows never feed the single-sample window; P3-A batch gate raises DataQualityError at 10% clamped rows (test_feature_extractor_contracts.py:302-467).
- __test_feature_kast_roles.py__ — Phase 14. KAST doctrine (kast.py): kill, assist, survive, or traded-within-window all earn KAST; trade window = 5s × tick rate (320 ticks @64, 640 @128 — tick-rate aware); too-late trade => False; no events => survival; non-death events ignored; __self-kill counts as neither K nor S => False__ (test_feature_kast_roles.py:138-178). calculate_kast_percentage averages per-round booleans; empty => 0.0. estimate_kast_from_stats bounded [0,1] (the "loud fallback" estimator — cf. test_demo_parser event-KAST). Role features: empty stats => UNKNOWN conf 0.0; `_heuristic_classify_role` maps entry/awper/support signatures; deviation features = (value-baseline)/baseline against ROLE_SIGNATURES (0.70 vs 0.35 => 1.0); UNKNOWN role => {}; every known role has >=3 coaching-focus metrics. CoachingDialogueEngine helpers: `_classify_intent` keyword routing (positioning/utility/economy/aim/general); system prompt embeds player name, demo, focus, recent insight titles; `_build_chat_messages` = full history + augmented user message; offline fallbacks mention "Offline"/"knowledge base"; get_history returns a copy; clear_session wipes state; constants MAX_CONTEXT_TURNS=6, RETRIEVAL_TOP_K=3 (test_feature_kast_roles.py:644-663).
- __test_ingestion_atomic_claim.py__ — __F-0037__: exactly ONE runner wins each queued task. Pre-fix, `process_queued_tasks` snapshot-SELECTed the queue and blindly wrote status='processing' — every concurrent trigger surface (home screen, settings, console ingest, batch_ingest, ingest_pro_demos) double-parsed the same demos. Contract: `_claim_task` first claim wins, second loses; a task pre-claimed by another runner is skipped, its 'processing' status untouched, exactly one demo processed (test_ingestion_atomic_claim.py:57-93).
- __test_ingestion_boundary.py__ — Ingestion/HLTV isolation doctrine: demo ingestion must NEVER reach into hltv_metadata.db (previous refactors bolted ProPlayerLinker/NicknameResolver/get_pro_baseline onto the hot path — cross-DB FK violations, HLTV-availability coupling). Two guards: (1) fresh-subprocess probe — `import run_ingestion` must pull ZERO modules matching 7 forbidden markers (baselines.pro_baseline, .pro_player_linker, .nickname_resolver, .meta_drift, data_sources.hltv, hltv_scraper, hltv_sync_service) (test_ingestion_boundary.py:26-71); (2) tripwire — `_save_player_stats` for both is_pro paths must never call `get_hltv_db_manager` (test_ingestion_boundary.py:74-137). Header says: "Do not weaken the assertions; trace the leak back."
- __test_ingestion_pipeline.py__ — `_check_duplicate_demo` 3-path detection: IngestionTask row OR PlayerMatchStats demo_name => duplicate; status='error' tasks NOT counted (retry allowed). `_is_profile_ready`: needs profile + >=1 NON-pro demo (MIN_DEMOS_FOR_COACHING=1); pro demos don't count; Steam/FACEIT NOT required (v1.0 gate relaxation, test_ingestion_pipeline.py:161-178). Stat sanitization: NaN/Inf => 0.0, rating clamped [0,5], negative kills/adr => 0 — but note these three tests re-implement the sanitization inline rather than calling `_save_player_stats` (weak/tautological, test_ingestion_pipeline.py:181-219). generate_corrections re-checks: <=3, importance-sorted, empty => [], P3-07 tuples.
- __test_ingestion_tickrate.py__ — __GAP-01__: `_parse_demo_header_meta` tick-rate doctrine — 64 and 128 returned as-is (floats preserved); out-of-range (16, 512), non-numeric, missing, or falsy 0 => fallback 64.0 with "GAP-01" WARN; missing/literal "unknown" map => "de_unknown"; parse_header exception => ("de_unknown", 64.0) never propagates (test_ingestion_tickrate.py:47-116). Integration variant uses DP-06 SSOT `get_pro_demo_base()` and accepts tick rates 32-256. __F6.1/DQ-02__: `_build_match_tick_dataframe` derives `is_blinded` from `flash_duration > 0` (CS2 removed the legacy signal), preserves legacy recorded values, defaults False; asserts 1:1 rows — "tick decimation FORBIDDEN" re-pinned (test_ingestion_tickrate.py:147-187).
- __test_grenade_thrown_extraction.py__ — __GAP-02__: `run_ingestion._extract_and_store_events` must build MatchEventState rows for grenade_thrown with weapon passthrough, tick, player and __throw origin resolved from tick state__ (X/Y/Z looked up by steamid at that tick); rows with unresolvable player (unknown steamid, empty user_name) are SKIPPED — no empty-string player rows pollute the event table; a parser exception on grenade_thrown logs a warning and continues, never aborts the event pipeline (test_grenade_thrown_extraction.py:93-201). Integration (gated): physical invariant thrown >= detonated on a real demo. __DP-06 SSOT__ note: demo discovery via `get_pro_demo_base()` — the old hardcoded 'New Volume' mount died with a disk rename and skipped the test forever (test_grenade_thrown_extraction.py:23-31).
- __test_features.py__ — F9-08 pure-logic `extract_match_stats`: avg_kills/deaths, kd_ratio=total kills/total deaths, opening_duel_win_pct counts wins over decided duels (1=win, -1=loss, 0=none), clutch_win_pct, accuracy=hits/shots, rating in (0, 5.0), econ_rating>0 with spending; empty df => {}; all-zero => 0.0 everywhere, no ZeroDivisionError.
- __test_dem_validator.py__ — DEMValidator contract: magic `PBDEMS2\0` => "CS2", `HL2DEMO\0` => "CSGO"; missing file / too small (<~min size) / above MAX_FILE_SIZE / bad magic => invalid with specific error text; processing time estimated ~1s per 10MB (test_dem_validator.py:102-114).
- __test_demo_format_adapter.py__ — Proposal 12 protobuf-aware adapter; explicitly "no mocking of file I/O, every assertion checks a SPECIFIC value". validate_demo: CS2 magic => valid, version "cs2_protobuf"; legacy CS:GO magic => INVALID "unsupported", version "csgo_legacy" (FORMAT_VERSIONS marks csgo_legacy supported=False); unknown header => "unknown"; warnings on non-4-byte-aligned size and files <50MB. get_field_mapping must cover canonical keys (player_*, kill_attacker/victim/weapon/headshot). Changelog dates chronologically ordered. Integration: `ingestion/integrity.validate_dem_file` must delegate to adapter (source must import demo_format_adapter, must NOT re-declare `DEM_MAGIC = b"HL2DEMO"`), accepts CS2, raises ValueError "unsupported" on legacy (test_demo_format_adapter.py:212-240).
- __test_demo_parser.py__ — parse_demo/parse_sequential_ticks return EMPTY DataFrame for missing files (never raise). HLTV 2.0 rating via production `_apply_hltv2_columns`: rating_* columns carry RAW components (rating_kpr=kpr, rating_survival=1-dpr, rating_kast=kast ratio, rating_adr=raw ADR), econ_rating=avg_adr/85.0, baseline stats => rating in (0.9,1.3), above-baseline => >1.0. __Supreme invariant (R4 MED, 2026-07-16): tick decimation is FORBIDDEN — the legacy `rate` stride parameter must not exist in parse_sequential_ticks' signature__ (test_demo_parser.py:172-179). __Event-driven KAST (runtime E2E 2026-07-17)__: closed-form KAST estimate saturated at 1.0 for any >=1 kill/round player (8/10 pros stamped 1.0) — `_compute_event_kast` now derives KAST from real per-round K/A/S/T events: multikill rounds count once, trade window honored, empty rounds count as survival, missing round column => {} (test_demo_parser.py:182-260). __Final scoreboard totals from LAST tick, never max()__ (2026-07-22 PGL Astana: warmup carried previous match's live scoreboard, max() gave +146 phantom kills): match-start reset respected, disconnects keep own final, monotonic demos equal max semantics (test_demo_parser.py:263-325).
- __test_data_pipeline_contracts.py__ — Tier-2 parser data-quality contracts on FeatureExtractor. Dict and ORM-object inputs must produce IDENTICAL vectors of shape (METADATA_DIM,), all finite; missing attributes fall to defaults. __Vector slot semantics locked__: index 18 = economic phase (pistol 0.0 / eco 0.33 / force 0.66 / full_buy 1.0 by equipment_value), index 17 = deterministic map-id hash (same map => same value across calls — immune to Python hash randomization; None map => 0.0) (test_data_pipeline_contracts.py:106-190).
- __test_debug_ingestion.py__ — `extract_match_stats` formula checks: avg over rounds; __kd_ratio = sum(kills)/max(1, sum(deaths))__ (3/1=3.0); empty DataFrame => {}; all-zero input => kd_ratio 0.0, accuracy 0.0, no ZeroDivisionError (test_debug_ingestion.py:39-80).
- __test_blind_synthesis.py__ — Pass-2 finding frozen: modern CS2 demos emit ZERO `player_blind` events (CS:GO-era event), so flash_assists/utility_blind_time/utility_enemies_blinded were 0.0 everywhere while coach_manager compared those zeros to pro baselines (test_blind_synthesis.py:1-8). `_synthesize_blind_events_from_ticks` reconstructs blind events from per-tick flash_duration transitions attributed via flashbang_detonate: transition => one event with victim/thrower/duration/tick; re-flash while blind => second event; detonation outside window => transition SKIPPED, never fabricated ("unattributable transitions must be skipped, never guessed", test_blind_synthesis.py:84-89); sub-`_BLIND_EPSILON` jitter and decay emit nothing; no detonations at all => empty.

### Storage (database, backup, experience bank, HLTV)

- __test_hltv_stat_parsing.py__ — __R4 HIGH (2026-07-16)__: `_safe_float` blindly mapped every comma to a decimal point, so HLTV thousands-grouped counters ("39,606" rounds) collapsed to 39.606 => 39, poisoning every ratio built on them. Disambiguation pinned: digit-grouped commas = thousands separators; lone comma = decimal (legacy); percent suffix stripped; "-"/"N/A"/"nan"/""/None/garbage => 0.0 never raise (test_hltv_stat_parsing.py:20-51). __R4 MED__: sub-page endDate must be today's UTC date — the hardcoded "2026-05-06" constant silently excluded all newer stats forever; test asserts the constant is GONE (test_hltv_stat_parsing.py:54-71).
- __test_hltv_sync_entrypoint.py__ — __F-0014__: `start_detached` must spawn `python -m <importable module>` — pre-fix it launched the deleted Kivy-era main.py (child died instantly, stderr DEVNULL, PID file recorded a corpse); test asserts target module is importable and "main.py" absent from the command. Dormant path must RE-test connectivity after the dormant sleep instead of returning while the WR-15 notification promised "Retrying in 6 hours" (test_hltv_sync_entrypoint.py:26-68).
- __test_hltv_table_registry.py__ — __R4 CRIT (2026-07-16)__: `_reconcile_stale_schema()` DROPS any hltv_metadata.db table not in `_HLTV_TABLES`; Phase H1 added four Pro* models without registering them, so __every startup destroyed the H1 tables and their scraped data__. Pinned: every `Pro*` table=True SQLModel must be registered (reflection-based); h1 tables proevent/protournament/prohead2head/promaprecord explicitly present; HLTV and `_MONOLITH_TABLES` stay disjoint; functional probe — second startup preserves H1 rows (test_hltv_table_registry.py:33-83). __R4 MED__: SQLite treats NULLs as distinct in UNIQUE indexes — the old 3-column constraint never fired (XOR guarantees one subject NULL); partial unique indexes must reject duplicate (subject, map) while allowing same map across subjects. __#47/GAP-14 non-destructive reconciliation__: additive drift adds the missing column in place preserving rows (honest NULL); non-additive drift preserves old rows in a `proevent_stale_*` snapshot that survives later orphan purges while a fresh empty table is created; true orphan junk tables still dropped (test_hltv_table_registry.py:122-213).
- __test_database_layer.py__ — Phase 3. DatabaseManager: get_session commits on success, rolls back on exception; upsert dedupes PlayerMatchStats on (player_name, demo_name) — second upsert updates in place, count stays 1 (test_database_layer.py:101-121). __CFG-1__ hardening is best-effort: `_restrict_db_permissions` must swallow EPERM on foreign-owned DB files, never abort startup (test_database_layer.py:146-157). StateManager: daemon status per hunter/digester/teacher/global; parsing/training progress persisted to CoachState; set_error creates an ERROR ServiceNotification; unknown daemon or missing state => status "Unknown". StatCardAggregator: persist_player_card creates/updates ProPlayer + ProPlayerStatCard keyed on hltv_id (update keeps a single card); missing ids logged, not crashed; __V-2__: KAST normalized to ratio [0,1] on persist (68.0 => 0.68) (test_database_layer.py:418-439); full spider payload stored in detailed_stats_json.
- __test_database_wal_enforcement.py__ — Every new SQLite connection on BOTH DatabaseManager and HLTVDatabaseManager must get journal_mode=WAL, synchronous=NORMAL(1), busy_timeout=30000 — re-applied per connection; pool locked to size=1, max_overflow=4 (test_database_wal_enforcement.py:102-112). Per-match shard DBs (via `match_data_dir` fixture) must also be WAL. Session commit/rollback lifecycle re-verified at pool level.
- __test_db_backup.py__ — Phase 2 gate. backup_monolith uses sqlite3.backup() Online Backup API (comment records why the old "may hang" class-skip expired post-P0-05, test_db_backup.py:19-26); backup passes PRAGMA integrity_check and carries data. __Free-space guard (2026-07-17 incident)__: a 154GB monolith on 54GB-free disk filled the volume to 100% and killed a concurrent schema migration — `_check_backup_free_space` must refuse when db_size+`_BACKUP_HEADROOM_BYTES` exceeds free space (test_db_backup.py:65-92). restore_backup: False on missing/empty file, integrity-verified on success. rotate_backups prunes oldest beyond keep_count. Alembic env.py must define AND call `_pre_migration_backup()` inside run_migrations_online (source-grep test). __ST-BK-01__: BackupManager.create_checkpoint refuses DBs above `_BACKUP_MAX_DB_BYTES` and when disk free is insufficient — the startup auto-backup previously had NO size guard against the multi-hundred-GB monolith symlink (test_db_backup.py:243-293).
- __test_db_governor_integration.py__ — Phase 10. DatabaseGovernor.audit_storage reports tier1_2_size/tier3_count/tier3_total_size/anomalies (missing monolith => "not found" anomaly); verify_integrity light=SELECT 1, full=PRAGMA quick_check; rebuild_indexes issues REINDEX; prune_match_data delegates delete_match. E2E RAP pipeline (skipif no ncps/hflayers): ModelFactory.get_model("rap") forward returns advice_probs/value_estimate(B,1)/gate_weights, no NaN; __skill_vec modulates pedagogy (value_estimate) but NOT strategy (advice_probs)__ (test_db_governor_integration.py:150-174); compute_sparsity_loss returns non-negative scalar.

- __test_experience_bank_db.py__ — Full ExperienceBank DB doctrine (in-memory, FAISS disabled => brute-force; __WR-88__ deterministic hash-embedder replaces the 400MB SBERT download). add_experience: stores base64 embedding (__AC-32-01__), game_state JSON, pro metadata, context_hash; __dedups on (context_hash + action) with UPDATE semantics__ (distinct rows need distinct actions — test_experience_bank_db.py:522-545). retrieve_similar: map filter, min_confidence, top_k, outcome_filter, __increments usage_count in DB__, exact context-hash match earns a ranking bonus. synthesize_advice: empty => generic 0-confidence; user failure => suggests successful alternative actions by name; pro refs included. record_feedback: matched action + good outcome => outcome_validated, effectiveness>0, times_advice_given/followed counted; bad outcome => effectiveness<0; different action => neutral 0.0 and not "followed"; confidence clamped [0.1, 1.0]. decay_stale_experiences: >90-day-old, USED, UNvalidated rows decay ×0.9; validated rows never decay. extract_experiences_from_demo: player_death where player is attacker => "kill" exp, victim => "death"; unrelated events ignored; __pro demos confidence 0.7 vs user 0.5__ (test_experience_bank_db.py:721-745).
- __test_experience_bank_dedup.py__ — __CHAT-07 (AUDIT §8.7)__: `_dedup_experiences` greedily filters retrieval rows sharing `(action, outcome, map, pro_player_name)` (user saw three identical "aggressive_push -> traded on dust2" rows); uniques first, spillover fills remaining top_k slots; different pro names are distinct; empty-safe. __R4 MED DET-01__: `sample_for_replay` must have a `seed` parameter defaulting to None => GLOBAL_SEED (signature-inspection test — weak but intentional) (test_experience_bank_dedup.py:91-103).
- __test_experience_bank_logic.py__ — Pure data structures: ExperienceContext defaults (health_range "full", equipment_tier "full", 5v5), deterministic hash, query string includes map/side/phase/position/health/NvM and __omits default "full health"__; SynthesizedAdvice field integrity.

### NN / JEPA

- __test_deployment_readiness.py__ — Migrated remnants of the 118-rule brain_verification framework; 60s timeout. Across ALL 5 ModelFactory types (legacy/jepa/vl_jepa/rap/role_head, RAP skipped without ncps): Rule 99 — 100 consecutive forward passes with zero NaN/Inf; Rule 45 — CPU batch=1 seq=10 median latency budgets legacy 10ms / jepa 20ms / rap 50ms, × `CS2_LATENCY_MULTIPLIER` (default 3.0) for CI (test_deployment_readiness.py:41-46); Rule 47 — batch-size invariance: batch=4 output[0] must equal batch=1 output within 1e-5 using fresh model instances (hidden-state contamination guard); Rule 51 — 5 same-seed runs identical within 1e-6 (legacy+jepa only); Rules 9/112 — OOD all-zeros/all-999 inputs => finite outputs, NaN input either clean error or tolerated. Deployment verdict aggregation: pass rate must be >=75% ("not RED") and NaN in <=10% of passes. Weak spot: verdict test re-implements lighter versions of the other tests and 0.75 is a loose bar.
- __test_dimension_chain_integration.py__ — Tier-3 "most critical invariant in the ML pipeline": __vectorizer.METADATA_DIM == nn.config.INPUT_DIM == 25__; __OUTPUT_DIM == 10__ (strategy layer, distinct from 25); __HIDDEN_DIM == 128__; TRAINING_FEATURES and MATCH_AGGREGATE_FEATURES both length METADATA_DIM; legacy model consumes FeatureExtractor output directly and emits OUTPUT_DIM (test_dimension_chain_integration.py:17-124). Weak spot: `test_jepa_model_accepts_metadata_dim_input` catches TypeError and degrades to `hasattr(model, "forward")` — near-tautological (test_dimension_chain_integration.py:53-68).
- __test_dry_run_checkpoint_integrity.py__ — __B4 / Law-7__ mock-free, `@integration`+slow, F-0043 data-gate (>=5 PlayerMatchStats): runs the REAL `run_full_training_cycle.py` as a subprocess against the real 429M-row monolith with `BRAIN_DATA_ROOT` redirected to tmp. Both directions locked: `--dry-run` writes NO `.pt` checkpoint anywhere; a real `--epochs 1` run MUST write one (inverse guard against silently-disabled saving) (test_dry_run_checkpoint_integrity.py:94-122). Scope note documented: dry-run still commits dataset-split labels to the monolith — only checkpoint writes are guaranteed absent. CUDA disabled to avoid 4GB GPU contention; 900s subprocess cap.
- __test_ema_hopfield_drift_invariants.py__ — TEST-COV (AUDIT §3.2) silent-failure regressions. __NN-16__ EMA aliasing: apply_shadow/restore must CLONE tensors — in-place param mutation must not corrupt stored shadow, and zeroing leftover backup refs must not mutate restored weights (test_ema_hopfield_drift_invariants.py:26-61). __NN-MEM-01__ Hopfield partial-load guard: a state_dict omitting `hopfield.*` weights must leave `_hopfield_trained=False` (else associative recall runs on random prototypes); full checkpoint must re-arm it True. Meta-note: the previous revision imported a class that never existed (RecurrentBeliefState) and skipped forever — the guard had zero real coverage (test_ema_hopfield_drift_invariants.py:67-111). __MOE-01__: RAPStrategy top-2 routing must produce a non-zero output row per sample (zero row = indexing bug). __DRIFT-01__: TickFeatureDriftMonitor flags per-dimension drift on the 25-dim vector (5σ shift on one feature detected by name); check_drift without fit_reference raises RuntimeError.
- __test_embedding_collapse_detector.py__ — __P9-02 hard-stop__ (Modernization Report §9 + N=260 supplement §5.1.4): embedding variance < 0.01 across `patience=2` consecutive validation epochs must ABORT training via `EmbeddingCollapseError`; single bad epoch or recovery-in-between never aborts; variance == threshold is healthy; __NaN and negative variance fail closed as collapse__; reset() clears state; error message must include "P9-02" and diagnostic hints naming InfoNCE/EMA/VICReg/data (test_embedding_collapse_detector.py:40-117).
- __test_amp_autocast.py__ — bf16 AMP doctrine for `nn.config.amp_autocast`: on CPU hosts the context is a transparent no-op (fp32 out); on GPU matmul must come out bf16 while master grads stay fp32 and finite (no GradScaler needed since bf16 keeps fp32 exponent range). Rationale documented: ROCm gfx1201 fp32 GEMM untuned ~1.3 TFLOPS vs ~118 bf16 (test_amp_autocast.py:1-7). GPU tests skipif no CUDA — never run in CPU CI.
- __test_checkpoint_normalizer_versioning.py__ — __GAP-07__ persistence sidecar doctrine: `save_nn` writes `.pt.meta.json` with schema_version="v1", __metadata_dim=25__, 25 feature_names, heuristic_config incl. "health_max" (test_checkpoint_normalizer_versioning.py:42-57). load_nn raises `StaleCheckpointError` on metadata_dim / feature_names / schema_version mismatch, missing feature_names, or corrupt sidecar JSON ("cannot parse sidecar" — fail-loud). Legacy .pt without sidecar: WARN "no metadata sidecar" but still loads (backward compat). extra_meta round-trips under "extra". __CTF-1__: checkpoint_hashes.json registry written by save_nn must end with a newline (committed file, end-of-file-fixer lint). Atomicity: sidecar write failure rolls back BOTH files — no .pt in final position, no temps left (test_checkpoint_normalizer_versioning.py:160-181).
- __test_collapse_metrics.py__ — JEPA representation-collapse metrics, CPU-only deterministic. `compute_collapse_metrics` surface is exactly {std_mean, std_min, effective_rank, cosine_offdiag_mean}; collapsed (near-identical rows) => std<0.01, cosine>0.99, effective_rank<1.5; healthy randn => std>0.05, |cosine|<0.3, rank>8; metrics scale-invariant (×37 changes nothing); higher-rank [B,T,D] input flattened; single-row degenerate input => zeros, no crash. `compute_ema_drift`: 0.0 for identical params, monotone with divergence. Motivation captured: collapse means predictor satisfies objective trivially while loss looks healthy (test_collapse_metrics.py:1-6).
- __test_concept_temperature_saturation.py__ — __PRE-6__ (Modernization Report §8.3): VL-JEPA `concept_temperature` is a learned scalar clamped to [0.01, 1.0]; saturation against EITHER boundary = degenerate concept space (binary collapse at 0.01, uniform at 1.0). MaturityObservatory must flag after `CONCEPT_TEMP_SATURATION_PATIENCE` (10) consecutive saturated epochs — 9 is not enough; one healthy epoch resets streak; warning is a one-shot latch (`_concept_temp_warning_logged`) that resets on recovery so a NEW episode re-emits; models without the attr => None temperature, no alarm, no crash (test_concept_temperature_saturation.py:40-140). Notes that project loggers run propagate=False (logger_setup.py:208) so caplog can't intercept — the latch property is asserted instead.

- __test_jepa_insight_adapter.py__ — __F1.1 (W5.1)__ JEPA insight adapter. delta = 2*(sigmoid_out - 0.5); |delta| < 0.1 noise dropped; axis vocabulary maps features (health=>survival, enemies_visible=>positioning); direction semantics — semantically void directions ("be blinded more") produce NOTHING (test_jepa_insight_adapter.py:73-77). Maturity ladder: "doubt" caps at 1 insight, prefixes "Observation:", halves confidence; "mature" speaks plainly, top-3 ranked by |delta|; unknown maturity falls back to doubt. __26-HYB-01 loader discipline__: weights-or-None — missing checkpoint or StaleCheckpointError => None, never a random-weights model. Setting gate: flag off => byte-identical no-op (F1.2), loader never called. [T,25] input auto-batched to [1,T,25]. __F1.2 service wiring__: flag-off touches nothing; candidates persist as CoachingInsight severity "Info"; adapter explosions NEVER raise (non-blocking contract); tier->ladder mapping 0-49 doubt / 50-199 learning / >=200 conviction (test_jepa_insight_adapter.py:228-244). __F-0029 head-trained gate__: adapter refuses pretrain-only checkpoints — arming requires sidecar `extra.head_trained == True`; missing sidecar refused; TrainingOrchestrator stamps `head_trained: False` for jepa pretrain and map/view_resolution 64 for rap (test_jepa_insight_adapter.py:247-304). __15f/#64 contract pin__: `_TARGET_FEATURES` == vectorizer `FEATURE_NAMES[:10]` — NOT MATCH_AGGREGATE_FEATURES[0:10] (both docs had claimed to be "the first 10"; this pins which vocabulary the JEPA head means) (test_jepa_insight_adapter.py:307-320).
- __test_jepa_model.py__ — JEPA architecture doctrine. JEPAEncoder (B,T,25)->(B,T,latent); predictor latent->latent; model defaults 3 experts, is_pretrained=False; forward_jepa_pretrain returns (pred, target) at latent dim; forward_coaching (B,T,25)->(B,OUTPUT_DIM=10), role bias supported; freeze_encoders sets is_pretrained=True and kills grads; __target_encoder is EMA — no gradients expected__ (test_jepa_model.py:139). VL-JEPA: subclass with NUM_COACHING_CONCEPTS=16 concept embeddings; forward_vl returns concept_probs (softmax sums to 1), logits, coaching_output, latent, top-3 concepts; ConceptLabeler heuristics (low HP+enemies => positioning_exposed[2]; high equip in pistol => economy_wasteful[6]; low KAST => trade_isolated[10]); `vl_jepa_concept_loss` diversity term punishes collapsed identical embeddings; JEPA checkpoint loads into VL-JEPA strict=False with missing keys exactly the concept layers; full pretrain->align->freeze->finetune cycle; COACHING_CONCEPTS ids sequential 0..15 with dimension in 5 categories. __F-0023__: training loss consumes temperature-SCALED logits (logits/tau, tau clamped [0.01,1.0]) matching softmax inference; at tau=0.10 sigmoid range must exceed [0.05,0.95] (raw logits were confined to [0.269,0.731]); tau must RECEIVE gradient from the concept loss (test_jepa_model.py:539-584).
- __test_jepa_training_pipeline.py__ — jepa_train.py CI coverage (60s timeout). JEPAPretrainDataset: (context, target) float32 windows, non-overlapping sequential slices, short/single-tick sequences pad without crash, empty ok. Contrastive loss finite, >=0, gradient flows, lower for matching pairs, batch=1 fallback. __P1-05__ negative sampling: negatives never include self, in bounds; batch 2 => exactly 1 mutual negative; batch 1 => zeros fallback. __J-6__ cosine EMA momentum: starts at ema_base 0.996, ends 1.0, monotone, always in [base, 1]. __NN-JM-04__: `update_target_encoder` RAISES RuntimeError if target encoder still has requires_grad (must be frozen before EMA); EMA moves target toward context (test_jepa_training_pipeline.py:317-349). Checkpoints round-trip incl. is_pretrained flag. __NN-33__: empty dataset => pretrain returns None. Pretrain freezes both encoders after, no NaN/Inf weights, EMA updates target. Finetune keeps encoders frozen, updates LSTM head. __15f/TASKS#64 fixture doctrine__: model fixture uses OUTPUT_DIM=10 — the old output_dim=25 fixture "exercised a shape combination that cannot exist in production and green-lit the undefined 25-target finetune contract" (test_jepa_training_pipeline.py:59-69). __26-RANGE-01 (R4 HIGH)__: `train_jepa_finetune` with 25-dim targets vs 10-dim head must raise a NAMED ValueError (was an opaque MSELoss broadcast error); 10-dim targets pass (test_jepa_training_pipeline.py:697-719). __W1.4 / REPR-01 / Law 17__: JEPATrainer warns loudly when a resumed pretrained model lacks saved EMA counters; counters present rehydrate `_ema_step`/`_ema_total_steps` silently (test_jepa_training_pipeline.py:648-691). Post-training embedding variance > 0.001 and cross-input cosine < 0.99 (anti-collapse). `_MIN_TICKS_FOR_SEQUENCE >= 20`, `_MAX_TICKS_PER_SEQUENCE <= 10000`, INPUT_DIM == METADATA_DIM.
- __test_jepa_window_fetcher.py__ — __R4 CRIT (2026-07-16)__: JEPA consumers slice batch rows positionally (rows 0-9 context, row 10 target) — the old flat `_fetch_jepa_ticks` feed returned randomly-subsampled unordered rows spanning players and demos, so "next-step prediction" trained on unrelated pairs. `_fetch_jepa_windows` contract: every window is exactly window_len contiguous ticks from a SINGLE player, sorted, no duplicates; DET-01 same-seed determinism and B1 seed rotation preserved; end-of-stream anchors that cannot fill a window are DROPPED, never padded or mixed (test_jepa_window_fetcher.py:89-118).
- __test_label_source_monitor.py__ — __G-01__ concept-routing telemetry: trainer marks each concept-alignment batch `label_source ∈ {round_stats, skipped_no_round_stats}`; legacy "heuristic" name raises ValueError. LabelSourceMonitor sliding window: alarm when skipped rate strictly > threshold (at-threshold does NOT alarm) after min_samples; window eviction by timestamp; alarm is a LATCH (no per-batch log spam; survives recovery until reset); reset clears window+alarm but persistent totals survive (test_label_source_monitor.py:50-144).

### NN / RAP + coach manager

- __test_data_quality.py__ — __OI-1__ regression on `nn.data_quality.run_pre_training_quality_check`: the completeness enumeration used to sit in ONE try/except — first unreadable shard aborted the loop at iteration 0, failure logged at DEBUG, report printed "Complete: 0, Incomplete: 0" and still said PASS (test_data_quality.py:1-7). Now: one bad shard is counted incomplete without aborting (1 complete / 2 incomplete of 3), the failure logs at WARNING naming the match id, and the report still passes on good data.
- __test_chronovisor_highlights.py__ — Phase 7 Chronovisor→MatchVisualizer integration. CriticalMoment.to_highlight_annotation: keyed on peak_tick; severity classification thresholds >0.3 "critical", >0.15 "significant", else "notable" (test_chronovisor_highlights.py:37-72); includes scale/context_ticks/suggested_review; context_ticks defaults to 128; to_dict excludes match_id by design. Cross-scale dedup: micro preferred over standard at same peak; same scale => higher severity wins; distant peaks both kept. MatchVisualizer.render_critical_moments produces a nonempty .png (empty list => None; missing positions auto-placed). Weak spot: `test_scale_marker_sizes_in_source` greps visualizer source text for "scale_marker_sizes"/"micro"/"standard"/"macro" instead of asserting behavior (test_chronovisor_highlights.py:356-365).
- __test_chronovisor_scanner.py__ — Phase 7. ScanResult.is_empty_success / is_failure semantics. ANALYSIS_SCALES: exactly 3 scales named micro/standard/macro; micro locked at window_ticks=64, lag=16, threshold=0.10. Dedup keeps higher severity within MIN_GAP_TICKS. `_analyze_signal_at_scale`: flat signal => no moments; short input => empty list, no crash. __R4 MED__: lag is expressed in TICKS and must be converted to entry units before indexing (timeline entries are one-per-window ~stride apart) — a step change on a 20-entry timeline must be detected with lag=64 ticks/32-tick spacing (old per-tick lag could never analyze timelines <65 entries) (test_chronovisor_scanner.py:266-282); `_timeline_entry` keys the entry to the window's LAST tick with the single RAP output value (test_chronovisor_scanner.py:284-296). Weak spot: `test_spike_detected` asserts only `isinstance(moments, list)` — tautological (test_chronovisor_scanner.py:245-255).
- __test_coach_manager_flows.py__ — The largest doctrine file for CoachTrainingManager (in-memory DB, CI-portable). (1) Maturity: gate at 50 matches; tiers CALIBRATING <50, LEARNING 50-199, MATURE >=200; confidence multipliers 0.5/0.8/1.0; None counter treated as 0. (2) Dataset splits: chronological 70/15/15 by match_date (10 => 7/1/2), train strictly before val before test, pro and user split independently; 1 match => test; splits computed per int(n*.70)/int(n*.85). (3) `_calculate_deltas`: z-normalized (target - current), clipped to [-1,1], length == len(TARGET_INDICES). (4) `_prepare_tensors`: X shape (n, len(MATCH_AGGREGATE_FEATURES)), y (n, len(TARGET_INDICES)), float32, no NaN, y clipped. (5) `_get_pro_baseline_vector`: dict {mean,std} uses mean; scalar used directly; defaults avg_kills=0.75, avg_adr=80.0. (6) __26-SCHEMA-02__ (owner decision 2026-07-17): prerequisites are PRO-ONLY — zero pro demos => "Professional demos required", message must NOT mention Steam/FACEIT (connect-state fields dropped); 5/10 => "Gathering Pro Baseline"; profile row does not change outcome; check_prerequisites catches OSError but deliberately NOT RuntimeError (unexpected errors propagate) (test_coach_manager_flows.py:561-622). (7) Skill radar: calibrating => status+empty data; mature => values clipped [-100,100]. (8) run_full_cycle with failed prereqs ends status "Idle". (9) __B1-XL (2026-07-02)__: above `_ID_MATERIALIZE_CAP` the JEPA tick fetch uses seeded id-space rejection sampling (the original exact materialization OOMed on the 429M-row monolith); __DET-01__ same (seed, corpus) => same ids; different seed rotates the window; eligibility (is_pro/demo) respected; scale cache (COUNT/min/max) paid once per (split, corpus) and reuse preserves determinism; below cap keeps exact-uniform path (test_coach_manager_flows.py:764-837). (10) __P4-A__ eligibility-aware splits: splits only over demos with complete shards, ineligible => "unassigned", stale splits reset; `_get_completed_demo_names()=None` = "signal unavailable" keeps legacy full-split (rationale: 259 historical schema-only shards would otherwise pack TRAIN, 2026-07-21) (test_coach_manager_flows.py:840-901). (11) Completeness enumeration fault tolerance (2026-07-26): one unreadable shard is skipped, treated as incomplete (conservative), NOT fatal; total failure or all-unreadable => None (test_coach_manager_flows.py:904-985). (12) __Whole-match eligibility__ (2026-07-26): shard presence != match completeness — HLTV `-p1`/`-p2` half-map fragments (2-9 rounds, sometimes zeroed aggregates) passed the shard gate into TRAIN. `MIN_COMPLETE_MAP_ROUNDS` floor is inclusive at 13 (fastest decided map 13-0; 12 = completed half rejected); all-zero avg_kills/avg_adr => ineligible; a match must NEVER straddle two splits (per-row microsecond match_date spread had put one demo's 10 players across TRAIN/VAL on the live DB — train/test leak); empty RoundStats = "wholeness unknown" keeps legacy behavior (test_coach_manager_flows.py:988-1178).

- __test_coach_manager_tensors.py__ — Dimension-chain doctrine: `len(TRAINING_FEATURES) == len(MATCH_AGGREGATE_FEATURES) == METADATA_DIM` (=25), no duplicates, TARGET_INDICES in bounds (test_coach_manager_tensors.py:20-62). __Bug #4 / W3 test-debt fix__: old tests demonstrated `dict.get(None)` semantics on an inline re-implementation and never called production ("passed whether or not Bug #4 was fixed") — replaced by driving the REAL `_prepare_tensors` with None-laden rows: NULL DB values must become 0.0, never NaN (walrus None guard) (test_coach_manager_tensors.py:65-124). DEMO_TIERS contiguous covering [0, inf); TIER_CONFIDENCE all in [0,1], MATURE=1.0, CALIBRATING lowest. Weak spot: `test_baseline_defaults_cover_all_features` copy-pastes the 25 defaults from production source into the test and only checks key coverage (test_coach_manager_tensors.py:172-206).

### Analysis engines

- __test_deception_flash_baits.py__ — __F-0021__ deepened: CS2 demos emit no `player_blind` events and production tick frames carry NO `event_type` column — the old detector returned degenerate 1.0 (all-bait) whenever flashes existed without player_blind rows (test_deception_flash_baits.py:1-7). Contract: `is_blinded` transitions within the window make flashes effective (mixed => 0.5); legacy player_blind events still supported; flashes with NO blind signal of any kind => honest 0.0 ("dark"), never 1.0; signal column present but never firing => honest 1.0; production PlayerTickState-shaped frames (no event_type) => 0.0.
- __test_analysis_engines.py__ — WinProbabilityPredictor: hard invariants 0 alive => prob 0.0, 0 enemies => 1.0; even match in (0.3,0.7); man-advantage >0.80; economy monotonicity. DET-01 note: predictor runs on Xavier random weights (no checkpoint) so class seeds torch RNG (autouse `torch.manual_seed(42)`) after an observed order-dependent flake (test_analysis_engines.py:22-29). RoleClassifier cold start => FLEX with confidence 0.0; ROLE_PROFILES exist for AWPER/ENTRY/SUPPORT/IGL/LURKER. EconomyOptimizer: money 800/r1 => "pistol" conf>0.9; 5500 => "full-buy"; 1200+loss_bonus => "eco" with "save" in reasoning; 2500 down 2 => force/half-buy.
- __test_analysis_engines_extended.py__ — Phase 4 coverage. Momentum: multiplier clamped to [MULTIPLIER_MIN, MULTIPLIER_MAX], neutral=1.0, tilted<=0.80-ish, hot>=1.25, MR12 half-switch at round 13 resets momentum (test_analysis_engines_extended.py:73-83). BeliefState.threat_level: visible/5 scaling, inferred threat decays with information_age. DeathProbabilityEstimator: hp brackets full/damaged/critical, is_high_risk threshold between 0.5 and 0.7, calibrate() from df sets priors with full<critical; empty df leaves uncalibrated. EntropyAnalyzer: empty/single position => 0.0 entropy; utility ranked by effectiveness. WinProbabilityNN: input dim 12, output (B,1) bounded [0,1], gradients flow. __PlattScaler 26-WINPROB-03__: Newton-Raphson must converge to BCE MLE — identity optimum A=-1,B=0 on calibrated data, Brier reduction on overconfident model; regression guard `abs(a)<100` against pre-fix Hessian-sign bug that ran A,B to ~3e8 and collapsed all probabilities to 0 (test_analysis_engines_extended.py:430-473).
- __test_analysis_gaps.py__ — Phase 13. RoleClassifier scoring formulas locked numerically: `_score_awper` = ratio*1.5 without threshold, `0.8+(r-t)*0.5` above threshold; `_score_lurker` = `0.7+(r-t)*0.8`; IGL prefers balanced kd. `_consensus`: agreement boosts conf by +0.1 (capped 1.0); neural overrides heuristic only if neural_conf > heuristic_conf + 0.1 (test_analysis_gaps.py:168-190). classify_team dedupes AWPers (<=1); audit_team_balance flags multi-AWPer/multi-Lurker/no-Entry/no-Support, all-same-role => CRITICAL. Role scores normalize to sum 1.0. DeceptionAnalyzer: __F-0021__ — all-bait flash verdict requires a blind signal column that never fired; frame with no signal source at all is "dark" => 0.0, not 1.0 (test_analysis_gaps.py:407-429). Sound deception: all-crouch clamps to 0, no-crouch = 1.0. Constants: FAKE_EXECUTE_WINDOW=5.0s, UTILITY_FOLLOWUP_WINDOW=3.0s, __FLASH_BLIND_WINDOW_SECONDS=2.0 (R4 MED 26-TICK: seconds-based, converted per-demo — replaced FLASH_BLIND_WINDOW_TICKS=128 which hardcoded 64-tick)__; deception weights sum to 1.0 (test_analysis_gaps.py:560-575).
- __test_baselines.py__ — Pro baseline doctrine (processing/baselines). HARD_DEFAULT_BASELINE: every metric has mean+std, std>0. `_get_default_pro_baseline` stamps `_provenance="hard_default"`. `calculate_deviations` z-score math: (val-mean)/std; std==0 => z=0 but raw delta kept; missing player key => omitted. TemporalBaselineDecay: HALF_LIFE_DAYS=90.0, MIN_WEIGHT=0.1, weight 1.0 today/future, ~0.5 at half-life, clamped floor, monotone decreasing; meta shift detection needs ~20% change, skips zero old-mean; metric key mapping rating_2_0->rating, kpr->avg_kills, dpr->avg_deaths. RoleThresholdStore: MIN_SAMPLES_FOR_VALIDITY=30 (get_threshold returns None below), 9 canonical threshold keys, learn_from_pro_data uses 75th percentile, source="hltv", cold start exits after >=3 valid thresholds. __F-0018__ fusion priority: baseline layers must emit ONLY empirical keys — hard defaults come from fusion base; earlier layer's empirical value survives later layers (test_baselines.py:481-524). __F-0019__ CSV scale: KAST/HS% published as percent get normalized to ratio [0,1], and ratio-styled CSVs must NOT be double-divided (test_baselines.py:527-547). __OI-7__: MIN_DEMO_BASELINE_ROWS=30 (three matches) — 10 rows (one 5v5 match) rejected to prevent std collapse/inflated z-scores (test_baselines.py:550-569).
- __test_belief_model_extended.py__ — 5s timeout on all. `extract_death_events_from_db` on empty DB => empty df with exactly columns ["health","died"]. auto_calibrate with partial columns calibrates HP priors only, weapon_lethality={} and threat_decay=None. __AC-05__: DeathProbabilityEstimator.calibrate needs >=30 samples (MIN_CALIBRATION_SAMPLES) else priors stay at `_DEFAULT_PRIORS` — distinct from AdaptiveBeliefCalibrator's 100-sample guard (test_belief_model_extended.py:85-121). Weapon lethality clamped to `_LETHALITY_BOUNDS` (0.1, 3.0). __A-01__: non-finite polyfit coefficients => calibrate_threat_decay returns None (test_belief_model_extended.py:164-197). __P3-10__: get_death_estimator is a double-checked-locking singleton — identical object across 2 racing threads. estimate() bounded [0,1] for extreme/negative/999 HP and unknown weapons. __R4 HIGH / WR-65__: THREAT_DECAY_LAMBDA must be a ClassVar, not a dataclass field — as a field the 0.1 default was baked into __init__ making the calibrator's class-level assignment a silent no-op; test asserts class assignment reaches new instances and the exact decay formula `(0 + 5*exp(-λ)*0.5)/5` (test_belief_model_extended.py:375-401).
- __test_blind_spots_extended.py__ — BlindSpotDetector: rounds with empty/missing game_state are skipped => []; empty spots => "No strategic blind spots detected" sentinel; training plan title-cases each situation and defaults top_n=3; BlindSpot.priority == frequency * impact_rating, defaults 0 (test_blind_spots_extended.py:102-131).
- __test_integration.py__ — Light cross-module smoke (120s timeout): EliteAnalytics role lookup + tournament baseline (skips when untracked CSVs absent); WinProbabilityTrainerNN forward on a `predict_win_prob` state dict produces a valid sigmoid in [0,1] (explicitly tolerates untrained saturation); `get_pro_baseline()` returns dict with rating mean > 0; extra datasets listing is a list of nonempty strings. Mostly smoke-grade.
- __test_drift_and_heuristics.py__ — Phase 3 gate. DriftMonitor(z_threshold=2.5): 4σ-shifted batch => is_drifted with feature named; seeded matching batch => no drift. `should_retrain`: >=3 of last 5 reports drifted => True; 2/5 => False; fewer than window => False (test_drift_and_heuristics.py:96-181). HeuristicConfig defaults locked: impact_kill_threshold=1.0, impact_adr_threshold=100.0, health_max=100.0, context_gate_l1_weight=1e-4; to_dict/from_dict JSON round-trip; unknown keys ignored; missing file => defaults. HeatmapEngine must expose `generate_differential_heatmap_data` (attr check only — weak).
- __test_engagement_range_extended.py__ — EngagementRangeAnalyzer: kill distance is full 3D euclidean (sqrt50 case), 0.0 for identical points, correct with negative coords; NamedPositionRegistry unknown map => None / empty list; analyze_match_engagements([]) returns full dict structure with zero-valued EngagementProfile, empty observations and annotated_kills (test_engagement_range_extended.py:109-142).
- __test_entropy_extended.py__ — Shannon position entropy: never negative (single point, duplicates, collinear, negative coords, epsilon deltas), uniform > clustered, empty => 0.0. __Thread safety__: 4 concurrent `compute_position_entropy` calls sharing the default grid buffer must match serial results across 10 stress rounds (lock on shared buffer) (test_entropy_extended.py:84-124). Utility effectiveness rating clamped to [0,1] even when entropy delta exceeds max_delta.
- __test_analysis_orchestrator.py__ — Phase 6 integration. AnalysisOrchestrator must own belief/deception/momentum/entropy/game_tree/blind_spot analyzers. 7 losses => "Tilt" momentum insight; 7 wins => "Hot"; empty rounds => zero insights, no error. Every insight is a `CoachingInsight` with severity in {Info, Medium, High}. __26-ORCH-01 (W1.1)__: notification-send failure in `_record_module_failure` must log a warning containing "26-ORCH-01", never be swallowed (test_analysis_orchestrator.py:188-207). __F-0031__: production player_stats never carry `*_thrown` keys — orchestrator derives utility stats from RoundStats rows for (player, demo); explicit keys still take precedence; no data + no keys => silent (test_analysis_orchestrator.py:210-272).

- __test_heatmap_projection.py__ — __F-0017 / C-03__: the heatmap grid follows the single-Y-flip convention shared with `tensor_factory._world_to_grid` — a world point near the north edge (y ≈ radar pos_y) lands in the TOP rows; the test cross-checks the doctrine against TensorFactory itself before asserting hotspot labels (pro-heavy north cluster must have larger world_y than user-heavy south) (test_heatmap_projection.py:25-50). Hotspot reverse projection round-trips world coordinates within 2 grid cells. Dead-Kivy surface removal pinned: HeatmapData / generate_heatmap_data / create_texture_from_data / generate_heatmap_texture must NOT exist (test_heatmap_projection.py:71-75).
- __test_game_theory.py__ — The Phase 6 game-theory omnibus. Belief: default priors match documented HP brackets; is_high_risk strictly `>` 0.6-ish threshold (0.6 => False, 0.7 => True); calibrate lowers full-HP prior when data shows 10% deaths; AdaptiveBeliefCalibrator clamps priors to `_PRIOR_BOUNDS` max 0.95, needs MIN_SAMPLES=100, weapon lethality within [0.1, 3.0]. Deception: empty round => all-zero metrics; F-0021 semantics re-pinned. Momentum: tilt after 7 losses (<0.85). Entropy: identical pre/post => zero delta; ranked by effectiveness. ExpectiminimaxSearch: node budget respected; leaf eval 0.0/1.0 at team/enemy elimination; best action in {push,hold,rotate,use_utility}, empty root => "hold"; suggest_strategy contains "Recommended". BlindSpot priority = frequency×impact. EngagementRange: 3-4-5 triangle distance; range classes close(<~500)/medium/long/extreme; profile percentages sum to 1.0; AWPer close-heavy triggers observation; <5 kills => none; NamedPositionRegistry covers 9 competitive maps with >=2 positions each (incl. de_train); annotate unknown => "Unknown Position". All Phase 6 factory functions return correct types. __R4 HIGH (2026-07-16) format-aware half-switch__: MR12 momentum resets ONLY at round 13, not 16 (old code reset at both, wiping mid-half streaks); mr_format=15 resets at 16 (test_game_theory.py:992-1021). __Economy half-switch is pistol__: round 13 (MR12) with $800 must never be "full-buy"; true overtime (round 25, $10k) IS full-buy; round 24 regulation not labeled Overtime (test_game_theory.py:1024-1046). __R4 MED batch 9__: transposition-table key includes node type (max vs min never share a slot) and depth semantics — shallow cache entries must not satisfy deeper queries while deep entries are reusable shallow (test_game_theory.py:1053-1090); flash-blind window scales with tick rate (200-tick gap: bait at 64tps, effective at 128tps); belief calibration DROPS eco/force rounds instead of fabricating health=60/30 (which mis-calibrated damaged/critical priors to eco death rates) (test_game_theory.py:1111-1156); `get_win_predictor` must wire the standard checkpoint when present (`_checkpoint_loaded` True) and flag heuristic mode when absent — "the bare factory ran production on random weights" (test_game_theory.py:1158-1177).
- __test_game_tree.py__ — Phase 15 OpponentModel doctrine: economy classes eco(<2000)/force(2000-3999)/full_buy(>=4000, default); eco favors push, full-buy favors hold; CT/T side adjustments; outnumbered enemies hold more, advantaged push more; low time => push up hold down; probabilities always sum to 1.0, all positive, exactly 4 actions. Learning: learn_from_match stores per-(map, economy) profiles, ignores <5-event samples, blends into priors only after count>=10; event->action inference (first_kill/entry_frag=>push, *_thrown=>use_utility, rotation=>rotate, default hold). `_apply_action` transitions: push trades one player each side and shifts map control; hold burns 15s (clamped at 0); use_utility consumes utility; never mutates input; map_control clamped [0,1]. suggest_strategy reports "opponent model: static|adaptive", node count, confidence label (high/moderate/marginal). Constants: DEFAULT_NODE_BUDGET=1000; `_MAX_ACTIONS` = the four actions; default opponent probs sum to 1.0.

### Coaching (dialogue, services)

- __test_coaching_service_flows.py__ — End-to-end CoachingService flows on in-memory SQLite. Notable fixture doctrine: uses StaticPool + check_same_thread=False because COPER runs `_generate_coper_insights` in a separate thread via `_run_with_timeout` — default SingletonThreadPool would give each thread an empty DB (test_coaching_service_flows.py:41-56). Traditional mode saves one CoachingInsight per correction; __C-01__: empty corrections produce a generic "Match Analysis Complete" fallback insight, not nothing (test_coaching_service_flows.py:200-227). COPER failure (real ExperienceBank init) falls back to traditional and still saves insights; non-dict tick_data hits the COPER guard — logged warning, silently skipped, NO fallback (by design). get_latest_insights: per-player filter, limit, ordered created_at DESC. `_save_corrections_as_insights`: severity High iff |z|>=2 else Medium; RAG corrections use rag_title/rag_description and severity "Info". Longitudinal coaching needs >=3 matches. `_baseline_context_note` extra edges: scalar baseline ok, zero mean skipped (div-by-zero), "aim" focus reads avg_hs+avg_kills.
- __test_coper_pathway.py__ — Pre-flag-flip safety net for the whole COPER chain (no Ollama/FAISS/SBERT). ExperienceContext.to_query_string: includes map, "T-side", phase, position, "NvM" alive counts, health only when not full; compute_hash = first 16 hex chars of SHA256 over `map:side:phase:position`, deterministic (test_coper_pathway.py:141-168). infer_round_phase brackets by equipment_value: <1500 pistol, 1500-2999 eco, 3000-3999 force, >=4000 full_buy; missing key => pistol; NON-DICT => "full_buy" (defensive) (test_coper_pathway.py:196-234). __P3-07__: deviations may arrive as tuples/lists from JSON deserialization — first element is the z-score. ExperienceBank: embeddings serialize to compact base64 with legacy JSON-string fallback; add_experience persists context+action+outcome+pro attribution; brute-force retrieval when vector index is None filters by map and min confidence (MIN_RETRIEVAL_CONFIDENCE 0.3); retrieve_pro_examples returns only pro-sourced rows; synthesize_advice on empty bank => confidence 0.0 generic narrative; pro references formatted "name (action -> outcome)"; record_feedback False for unknown id; counts split total/pro/user. Helpers: `_health_to_range` same 80/40 brackets as CoachingService; `_infer_action` is_scoped=>scoped_hold, crouching=>crouch_peek, victim default held_angle, attacker default pushed; `_action_to_focus` pushed=>aggression, held_angle=>positioning, scoped_hold=>aim, unknown=>positioning; cosine similarity zero-vector => 0.0. `_run_with_timeout`: (result, False) on success, (None, True) on timeout, re-raises exceptions, passes args/kwargs. Mode notification levels: COPER = "Level 1", Traditional = "Level 4"; COPER timeout emits WARNING "timed out" but the mode label still reads Level 1 (attempted mode) (test_coper_pathway.py:969-1020). __C-02__: `_generate_coper_insights` guard rejects non-dict tick_data without crash.
- __test_chat_streaming.py__ — F2 (TASKS#33) streaming chat. LLMService.chat_stream parses Ollama /api/chat NDJSON; __DR-14__: every on_chunk callback receives the FULL accumulated text, not fragments (`["Hold ","Hold the ","Hold the angle."]`, test_chat_streaming.py:34-46); system prompt prepended as messages[0], stream=True. CoachingDialogueEngine.respond_stream: __F5-06__ history discipline — exactly one user+assistant pair committed once; __DR/F2.3__ cancellation mid-stream returns "" and commits NO history bubble (test_chat_streaming.py:116-129); stall (requests Timeout) falls back to `_fallback_response` and the fallback text IS committed to history. Worker injects progress_callback only when `wants_progress=True`. __F3 (TASKS#37)__ session ML context: injected for positioning/aim/utility/economy/general intents ONLY when `using_pro_reference` is true; `player_query` keeps its own mention-based path; cached once per session (`_get_ml_analysis_for_players` called exactly once), NN failure caches "" and never raises; clear_session resets cache (test_chat_streaming.py:197-246).
- __test_coaching_dialogue.py__ — Phase 8 CoachingService helpers. `_format_coper_message`: narrative + "N similar situations" + confidence% + optional "Pro Examples:" block (absent when no refs) + optional baseline note. `_baseline_context_note`: empty stats or baseline => ""; unknown focus defaults to rating. `_health_to_range` boundaries: >=80 "full", 40-79 "damaged", <40 "critical". `_infer_round_phase` delegates to round_utils: 800 => "pistol", 5000 => "full_buy", empty dict => "pistol".
- __test_coaching_dialogue_tools.py__ — __DP-03__ zero-trust LLM tool-calling. `_validate_rounds`: filters non-ints/out-of-range(1-50), dedupes, sorts, caps at 5 (`[5,"7",0,51,"junk",None,7,3,12,15,20]` => `[3,5,7,12,15]`, test_coaching_dialogue_tools.py:65-68). `_canonical_demo`: case/whitespace-insensitive against known-demos whitelist; SQL-injection strings, ints, None => None. `_tool_list_matches`: team/map filters, bogus map filter ignored (whitelist), "No matches found". `_execute_tool`: unknown tool => "ERROR..."; unknown demo error mentions `list_matches` recovery. Demo candidate resolution: mentions narrow by map; ambiguity => `_resolve_demo_name` None + disambiguation block instructing "Ask the user"; session demo fallback from `_player_context`. Match inventory block counts matches/full-round-data and is cached by identity. `_respond_via_tools` agentic loop: tool round results fed back as role="tool" messages with tool_call_id; string JSON arguments parsed; `tools_supported=False` => None with zero LLM calls; "[LLM Connection Error]" content => None; pre-set cancel => "" with zero calls (test_coaching_dialogue_tools.py:177-250).
- __test_coaching_dialogue_tutor_mode.py__ — __CHAT-02 (AUDIT §8.2)__ third-person tutor mode: `_to_third_person` deterministically rewrites 2nd->3rd person when injecting pro-player insights for a user with no personal data ("failures here mean the LLM will be re-fed fabricated 'your stats' critiques authored for a different player"). Locks: You->They / Your->Their with case preservation, `[player]` attribution prefix, no double prefix on rerun, attribute=False strips prefix, word boundaries ("Contour" never becomes "conttheir"), stats/percents preserved. __R4 MED (2026-07-16)__: `_ROUND_PATTERN` word boundary — trailing 'r' of any word ("tips foR 5 players") must NOT match round_query (strongest-signal intent); "round 5", "R12", "rounds 5-10", "round 5 to 10" all still match with correct groups (test_coaching_dialogue_tutor_mode.py:80-107).
- __test_coaching_engines.py__ — Phase 11. ExplanationGenerator: SILENCE_THRESHOLD=0.2 (|z|<0.2 => "" — coach silence doctrine), severity boundaries SEVERITY_HIGH=1.5 / MEDIUM=0.8, abs-value based (z=-2.0 => "High"); skill_level<=2 simplifies phrasing with "Goal". PlayerCardAssimilator (pro_bridge): invalid/None detailed_stats_json => {} not crash; __P3-02__: get_coach_baseline returns per-round rates DIRECTLY (kpr 0.75 => avg_kills 0.75, not multiplied); zero dpr => kd_ratio=kpr; default hs ratio 0.45; invalid detailed metrics fall back entry_rate=0.25/utility_damage=45.0; archetypes: impact>~1.3 "Star Fragger", kast>=~0.80 "Support Anchor", AWP-dominant weapons "Sniper Specialist", else "All-Rounder". TokenResolver `_build_token_dict`: five sections (identity/core_metrics/tactical_baselines/granular_data/metadata); malformed JSON => granular {}; compare_performance_to_token: deltas = player - pro, is_underperforming by rating delta. nn_refinement: weighted_z *= (1 + adj); no matching key => unchanged; other fields preserved. correction_engine: avg_kast importance 1.5, unknown 1.0; max 3 corrections; confidence scales with rounds_played up to CONFIDENCE_ROUNDS_CEILING=300; tuple deviations use first element. longitudinal_engine: negative slope => "Regression"/Medium, positive => "Improvement"/Positive, confidence<~0.5 filtered, stability_warning upgrades to High, max 3 insights, zero slope => none.
- __test_coaching_service_contracts.py__ — __Bug #8__: COPER path must tolerate non-dict truthy tick_data (list) without AttributeError — fallback chain absorbs it; empty-dict tick_data is falsy so COPER silently skips (test notes this as accepted-but-undocumented behavior, test_coaching_service_contracts.py:165-192). Mode flags read from settings USE_RAG_COACHING/USE_HYBRID_COACHING/USE_COPER_COACHING. `_baseline_context_note` directionality: below mean => "below", above => "above". get_coaching_service is a module singleton. Weak spots: `test_coper_takes_precedence_when_enabled` and `test_hybrid_selected_when_coper_conditions_not_met` only assert flag values and describe the condition in comments — no behavior exercised (test_coaching_service_contracts.py:45-62); happy-path COPER test forgives all non-AttributeError exceptions.
- __test_coaching_service_fallback.py__ — __P9-03__ fallback chain doctrine: COPER -> Hybrid -> Traditional+RAG -> Traditional. Selection: COPER needs use_coper AND map_name AND tick_data (missing tick_data => Hybrid; missing both and hybrid off => Traditional); Hybrid needs flag + player_stats; Traditional calls `generate_corrections(deviations, rounds)`; RAG mode calls `_enhance_with_rag`. Exception-driven: ExperienceBank import failure inside COPER falls to Hybrid when on, else Traditional (test_coaching_service_fallback.py:198-257). Architecture tests require CoachingService and generate_new_insights docstrings to document COPER/Hybrid/Traditional/Fallback — doc-as-contract (test_coaching_service_fallback.py:265-292).

- __test_hybrid_engine.py__ — Hybrid ML-RAG engine (classes marked `@integration` — gated). Priority ladder: |Z|>2.5+high conf => CRITICAL, >2.0 & conf>0.6 => HIGH, >1.0 & conf>0.4 => MEDIUM, else LOW; confidence via real MetaDriftEngine monotone in inputs; feature-category matching (avg_adr matches positioning AND aim, econ_rating economy); insights sorted by priority; save path uses injected in-memory DB. __F-0028 / 26-HYB-01__: the engine must have NO model seam — no `model`, `_get_ml_predictions`, `_load_model` attributes; model inference enters the chain ONLY via JEPAInsightAdapter (test_hybrid_engine.py:232-242). __Direction-aware insights (runtime E2E 2026-07-17)__: lower-is-better metrics invert the Z before judging — deaths 7σ above pro cohort must be "needs work", never "Strong Deaths … Keep it up!"; low deaths IS a strength; higher-is-better unchanged (test_hybrid_engine.py:245-304).

### Services / lifecycle / config

- __test_handoff_regressions.py__ — Engineering-handoff audit pinboard, one class per finding. __WR-31 (CRITICAL)__: restore_backup deletes stale `.db-wal`/`.db-shm` before copying (test_handoff_regressions.py:74-118). __WR-57/C-01__: Traditional coaching must NEVER produce zero insights — empty corrections save a generic "Match Analysis Complete". __WR-58__: COPER timeout routes to Hybrid (when on) before Traditional. __WR-56__: `_build_chat_messages` preserves the full history including the last assistant response. __WR-44 superseded by R4 MED__: raw `time_in_round` is HONEST — not clamped at 115s (bomb plant extends rounds; the pin flattened post-plant temporal signal); warmup ticks before round 1 map to that round with honest elapsed seconds, not fillna(0) bogus values; the [0,1] guarantee lives only in the vectorizer's `min(t/115, 1)` saturation of feature 20 (test_handoff_regressions.py:251-294). __CORE-10__: PlaybackEngine._interpolate_angle NaN handling (nan,b)=>b, (a,nan)=>a, (nan,nan)=>0.0. __WR-21__: refresh_settings updates module-level globals (ACTIVE_THEME, FONT_SIZE). __DS-08 SSRF__: FACEIT demo download rejects `file://` URLs => None (test_handoff_regressions.py:351-366).
- __test_config_dotenv.py__ — __CFG-ENV-01__: repo-root `.env` was documented but never parsed — every documented override silently no-opped. Loader contract now pinned: `os.environ.setdefault` semantics (real environment wins over file), inline comments stripped, quoted values keep `#`, junk lines ignored, missing file is a no-op (test_config_dotenv.py:1-63).
- __test_config_extended.py__ — Phase 9 config surface. stabilize_paths returns existing dir and injects into sys.path; mask_secret: <=~4 chars/None => "\*\*\*\*", 8+ => first4...last4. get_setting default semantics; get_all_settings returns a COPY. Constants: MIN_DEMOS_FOR_COACHING=1, MAX_DEMOS_PER_MONTH=10; DATABASE_URL is `sqlite:///...database.db`; DB/LOG/DATA/MODELS dirs must exist.
- __test_config_resolution.py__ — 3-level resolution: defaults -> user_settings.json -> keyring/env. Defaults locked: CS2_PLAYER_NAME="", USE_COPER_COACHING=True, __USE_RAP_MODEL=False, USE_JEPA_MODEL=False__, USE_HYBRID_COACHING=False, SETUP_COMPLETED=False; DEFAULT_DEMO_PATH=$HOME (test_config_resolution.py:17-42). JSON overrides merge with defaults; corrupted/non-dict JSON falls back to defaults without crash. save_user_setting: atomic (no .tmp left), creates file, preserves other keys, updates in-memory; refresh_settings picks up external writes; concurrent save/read threads produce no errors or corrupt JSON. __Match-data path doctrine (2026-07-26)__: `$HOME` must NEVER become the per-match shard root — PRO_DEMO_PATH used to default to `~`, so `_resolve_match_data_path()` always chose `$HOME/match_data` and auto-migration moved the whole production shard corpus there (observed: 45 shards + ENOSPC, then 313 shards taking root disk 42GB->2GB). Now: PRO_DEMO_PATH default != $HOME; unset resolves to in-project `backend/storage/match_data`; explicit `$HOME` REFUSED; a deliberate real directory is honoured (test_config_resolution.py:263-306). File defines its own local `isolated_settings` fixture that shadows conftest's (resets `_settings` wholesale rather than snapshotting globals).

- __test_knowledge_graph.py__ — Phase 5 KnowledgeGraphManager (raw-SQLite entity/relation graph): WAL mode; idempotent init; entity upsert replaces type and observations (JSON list); relations unique per (from, to, type) via INSERT OR IGNORE, same pair with different types distinct; metadata JSON round-trips; query_subgraph returns 1-hop OUTGOING relations only, depth>1 tolerated (still 1-hop), nonexistent entity => {entity: None, neighbors: []} (test_knowledge_graph.py:167-239).
- __test_lifecycle.py__ — AppLifecycleManager: mutex name locked to `Global\MacenaCS2Analyzer_Unique_Lock_v1`; project_root exists; ensure_single_instance returns bool (Windows-only value assertions relaxed because the module-level singleton may hold the mutex); shutdown is a safe no-op without a daemon and does NOT terminate an already-exited daemon; launch_daemon with missing script => None. Mostly smoke-grade.
- __test_known_maps_ssot.py__ — __CP0 decision #2__ map SSOT: `core.known_maps` is the one authority (KNOWN_MAP_NAMES incl. train, KNOWN_MAP_IDS with `de_`-prefixed ids, is_known_map accepts de_cache/office, sniff_map_from_text parses demo filenames); the SEVEN converted consumers (qt match_utils, coach_screen, and five tools/ scripts) must literally contain "known_maps import"; regex drift-guard: no consumer may re-declare a 3+-map set literal (mirage...inferno...nuke pattern) (test_known_maps_ssot.py:11-44).

### UI (qt_app, charts, tokens)

- __test_animation_killswitch_all.py__ — __F-0034__: EVERY `Animator` helper (fade_in/fade_out/pulse/slide_out/reveal_stagger/collapse_width) must honor `MACENA_UI_ANIMATIONS=0` by returning None/[] and jumping to end state (opacity 1.0 / 0.0 / mid 0.5, final geometry) — the regression was that only count_up/sweep_ring did. Runs offscreen (`QT_QPA_PLATFORM=offscreen`), forces `animations_enabled -> False` via autouse monkeypatch (test_animation_killswitch_all.py:18-22). Note tests assert on `graphicsEffect().opacity()` — implies kill-switch path still installs a QGraphicsOpacityEffect (cross-check with the Qt graphics-effects ban on Linux).
- __test_design_token_references.py__ — __R4 HIGH (2026-07-16)__ token-reference contract: statically scans every qt_app .py for `tokens.<name>` attribute references and pins each to a real frozen `DesignTokens` dataclass field — kills the runtime-only AttributeError class (pro_player_detail_screen referenced never-existent `tokens.surface_card`). Includes a guard-the-guard test that the scan actually covers pro_player_detail_screen (test_design_token_references.py:24-43).
- __test_detonation_overlays.py__ — CS2 grenade truth constants in the ACTIVE Qt tactical map (legacy Kivy map removed; test repointed): GRENADE_RADII HE=350, MOLOTOV=180, SMOKE=144, FLASH=1000 game units (test_detonation_overlays.py:76-81); every NadeType maps to a semantic palette key resolved per-paint via `TacticalMapWidget._palette()` so overlays theme-track (CS2/CSGO/CS1.6); palette values valid QColors. Source-grep half: `_draw_detonation_overlay` defined AND called (count >= 2). Whole file skips without PySide6.
- __test_charts.py__ — P3 design-atlas QPainter chart set. Geometry pure functions locked numerically: RadarChart `_vertex` polar math (idx=0 is top); RatingSparkline `_y` with 6px pads, zero-span input maps to center; UtilityBarChart `_w` proportional with zero-max guard and clamps at 0/full-width; EconomyChart `_half_x` puts the half boundary at the LEFT edge of the round's slot, `_bar_rect` never negative even with zero max. All widgets must `grab()` a non-null pixmap offscreen and stay safe on empty input; EconomyChart/MomentumChart keep the pre-rewrite `plot()` public API. __License gate__: zero "QtCharts"/"QChart" references allowed anywhere in apps/qt_app (GPL-only module) — rglob scan (test_charts.py:247-256).

### Tools / build / deployment

- __test_build_pipeline_testonly_safe.py__ — __F-0040__: `console build run --test-only` must NEVER run "Project Sanitization" (`Sanitize_Project.py --yes` deletes database.db, hltv_metadata.db, match_data/, models/, logs/) — the old stage order sanitized BEFORE the test-only early return and wiped live data. test-only must still run "Unit Tests"; full build keeps sanitize as stage 1 (test_build_pipeline_testonly_safe.py:29-45). Requires `rich` (importorskip) — skipped on CI runners without it.
- __test_elite_csv_builder.py__ — __F-0020__: `tools/build_elite_csvs.py` + EliteAnalytics per-component degradation. Builder writes HLTV-style CSVs with exact headers (`Name,Rating1.0,K/D,ADR,Headshot %,KAST,Impact`) and __converts stored ratios to percent for CSV__ (0.41 -> 41.0 — the write-side counterpart of F-0019's read-side normalization); top_100 sorted by rating desc; demo CSVs exclude `is_pro=0` rows; `apply=False` (dry-run) writes nothing; missing DBs skip cleanly. Round-trip: builder output feeds `EliteAnalytics.analyze_user_vs_elite` (z_scores + tournament_z_scores); __missing top_100 CSV must NOT blank the other components__ — elite_rating_avg degrades to 0 while z_scores stay populated (test_elite_csv_builder.py:104-135).
- __test_build_tools_contracts.py__ — __F-0041__: three drifts stay dead in `Programma_CS2_RENAN/tools/build_tools.py`: (a) AST-scan proves no `shell=True` anywhere; (b) `cmd_verify` accepts the writer's own `{"binaries":[...]}` manifest schema (exit 0); (c) `--verify-only` reads the "hashes" key of integrity_manifest.json; (e) spec target `packaging/cs2_analyzer_win.spec` exists and phantom "macena.spec" reference is gone (test_build_tools_contracts.py:31-76). Also rich-gated.

## Suspicious/contradictory findings

1. __conftest.py docstring/code mismatch__: `seeded_db_session` docstring claims "12 RoundStats" but the code seeds 4 rounds × 2 players = 8 rows (conftest.py:88 vs conftest.py:233-250). Harmless but stale.
2. __Smoke layer touches the real DB__: `automated_suite/test_smoke.py::test_database_init` calls the production `init_database()`/`get_db_manager()` with no gate or tmp redirection (test_smoke.py:42-59) — contradicts the automated_suite README's own convention that only E2E touches real data. Same pattern in `test_auto_enqueue.py` (marked integration) is fine; the smoke one is not marked.
3. __Tautological/weak tests spotted__: `test_chronovisor_scanner.py::test_spike_detected` asserts only `isinstance(moments, list)` ("may or may not detect", test_chronovisor_scanner.py:245-255); `test_dimension_chain_integration.py::test_jepa_model_accepts_metadata_dim_input` degrades to `hasattr(model, "forward")` on TypeError; `test_coaching_service_contracts.py::TestCoachingModeSelection` (first two tests) asserts flag values only, behavior described in comments; `test_ingestion_pipeline.py::TestStatSanitization` re-implements the sanitization inline instead of calling `_save_player_stats`; `test_coach_manager_tensors.py::test_baseline_defaults_cover_all_features` copy-pastes production defaults into the test. The suite is generally self-aware about this failure mode (W3 note in test_coach_manager_tensors.py:65-70 documents replacing exactly such a test), so these stragglers are worth flagging.
4. __Source-grep tests as contracts__: several invariants are enforced by grepping production source text rather than exercising behavior — `test_chronovisor_highlights.py::test_scale_marker_sizes_in_source`, `test_db_backup.py::TestAlembicPreMigrationHook`, `test_demo_format_adapter.py::TestIntegration` (partially), `test_detonation_overlays.py::TestTacticalMapOverlayIntegration`, `test_known_maps_ssot.py::test_consumers_import_the_authority`. Brittle to refactors and satisfiable by dead code, though cheap and better than nothing.
5. __Animation kill-switch vs graphics-effects ban__: `test_animation_killswitch_all.py` asserts on `w.graphicsEffect().opacity()` (test_animation_killswitch_all.py:39,48,57) — the kill-switch path evidently still installs QGraphicsOpacityEffect. The project's Qt rules ban graphics effects on Linux; worth confirming the killswitch path is exempt or platform-guarded (speculation flagged as such — production code not read in this cluster).
6. __`_health_to_range` triplicated__: identical 80/40 health-bracket logic is tested independently on CoachingService (test_coaching_dialogue.py:120-133), ExperienceBank (test_coper_pathway.py:739-752), and again in test_experience_bank_db.py:572-582 — three copies of the same domain constant with no shared SSOT test.
7. __Two divergent KAST implementations both under test__: demo_parser's `_compute_event_kast` (test_demo_parser.py:182-260) and feature_engineering's `calculate_kast_for_round` (test_feature_kast_roles.py:19-178) implement the same K/A/S/T doctrine separately (both 5s trade windows); plus `estimate_kast_from_stats` as documented fallback. Tests keep them consistent in spirit but nothing pins parity between the two event-driven implementations.
8. __CI-vs-local skip surface__: RAP-dependent tests skip without ncps/hflayers; rich-dependent build tests skip without rich; GPU AMP tests skip without CUDA; integration tests skip without CS2_INTEGRATION_TESTS=1; EliteAnalytics tests skip without untracked CSVs. On a minimal CI runner a large fraction of the doctrine in this cluster never executes — consistent with the memory note that hollow local gates are a known trap.
9. __test_ingestion_atomic_claim's claim contract vs test_run_worker_claims__ (m-z cluster) both cover claims — sibling agent should cross-check they pin the same semantics (F-0037).
10. __tests/__init__.py and data/ are empty__ — no issues; noted only for campaign completeness.
