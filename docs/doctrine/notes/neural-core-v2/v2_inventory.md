INVENTORY — legacy neural core vs. v2 (read-only, HEAD 50031cb, branch docs/refresh-2026-09-04, 2026-09-11)

Path shorthand (all absolute): R = /media/renan/WORK_RECOVERED1/PROIECT/Counter-Strike-coach-AI ; P = R/Programma_CS2_RENAN. Line numbers are from the files as read today.

======================================================================
1. THE 25-DIM PRODUCTION FEATURE CONTRACT
======================================================================

1.1 Definitions
- METADATA_DIM = 25 — P/backend/processing/feature_engineering/vectorizer.py:34.
- FEATURE_NAMES tuple — vectorizer.py:240-266; import-time assert len==METADATA_DIM — :267-270.
- Lazy re-export (`FEATURE_NAMES`, `METADATA_DIM`, `FeatureExtractor`, `DataQualityError`) — P/backend/processing/feature_engineering/__init__.py:26-33, 47-50.
- nn side: INPUT_DIM = METADATA_DIM — P/backend/nn/config.py:183-185; OUTPUT_DIM = 10 :186; HIDDEN_DIM = 128 :187; BATCH_SIZE = 32 :180; RAP_POSITION_SCALE = 500 :218.
- ModelFactory forwards `input_dim=METADATA_DIM` / `metadata_dim=METADATA_DIM` to every model — P/backend/nn/factory.py:56, 64, 72, 80, 98.

1.2 The "two vocabularies of 25" (docs D5: CORREZIONE_NUCLEO_NEURALE.md:642-644, PARTE_III:178)
- Vocabulary A (tick-level): vectorizer.FEATURE_NAMES (above). Duplicated verbatim as `TRAINING_FEATURES` in P/backend/nn/coach_manager.py:57-90 with `raise ValueError` if len != METADATA_DIM (:91-92). Only consumer: `self.feature_names = TRAINING_FEATURES` (:164) — never read functionally; pinned by tests (P/tests/test_coach_manager_tensors.py:20-27, test_dimension_chain_integration.py:90-95, R/tools/headless_validator.py:1812-1818).
- Vocabulary B (match-aggregate, PlayerMatchStats columns): `MATCH_AGGREGATE_FEATURES` coach_manager.py:98-132, `raise` if len != METADATA_DIM (:133-137); `TARGET_INDICES = range(OUTPUT_DIM)` (:139). Consumers: `_prepare_tensors` (:1088-1107, X (N,25) float32 from `model_dump()`, y = `_calculate_deltas`), `_calculate_deltas` (:1109-1160; hard-coded FEATURE_SCALES z-score, clip [-1,1]), `_get_pro_baseline_vector` (:1162-1202; defaults dict), `_get_user_baseline_vector` (:1271-1287). Feeds AdvancedCoachNN (P/backend/nn/model.py:28-135, alias TeacherRefinementNN :171) via `train_nn` (P/backend/nn/train.py:15-64) in Phase 2/3 (coach_manager.py:1064-1073).
- Third 12-column aggregate list with a StandardScaler: P/backend/processing/data_pipeline.py:33-46 (`feature_cols`), scaler fit/transform :107-110, persisted to `backend/storage/fitted_scaler.joblib` (:48, not present on disk). `ProDataPipeline()` is instantiated in coach_manager.py:163 but `run_pipeline()` has no caller anywhere in P/, R/tools, batch_ingest.py or run_full_training_cycle.py (grep) — dead. No StandardScaler exists on the tick path.
- Ambiguity pinned by a test: P/backend/coaching/jepa_insight_adapter.py:33-46 `_TARGET_FEATURES` = FEATURE_NAMES[:10]; P/tests/test_jepa_insight_adapter.py:307-320 asserts this and its docstring records that MATCH_AGGREGATE_FEATURES[0:10] is "a DIFFERENT 25-dim vocabulary".

1.3 Ordered slots, source column (P/backend/storage/db_models.py PlayerTickState :161-219), transform (vectorizer.py), normaliser constant (P/backend/processing/feature_engineering/base_features.py HeuristicConfig :18-58)
 idx name             source column           transform                                              where
  0  health           health (:193)           /health_max=100                                        vectorizer:282; base:34
  1  armor            armor (:194)            /armor_max=100                                         :283; base:35
  2  has_helmet       has_helmet (:197)       1/0; if None → armor>0 heuristic                       :285-289
  3  has_defuser      has_defuser (:198)      1/0                                                    :291
  4  equipment_value  equipment_value (:200)  /equipment_value_max=10000, NOT clipped (>1 possible)  :292; base:36
  5  is_crouching     is_crouching (:195)     1/0                                                    :294
  6  is_scoped        is_scoped (:196)        1/0                                                    :296
  7  is_blinded       is_blinded (:203)       1 if flash_duration>0 OR is_blinded (DB rows have no flash_duration; ingestion already derived is_blinded = flash_duration>0 at P/run_ingestion.py:1515-1520)  :300-305
  8  enemies_visible  enemies_visible (:202)  min(n/enemies_visible_max=5, 1)                        :313-314; base:37
  9  pos_x            pos_x (:187) [fallback keys x/X]  clip(x/pos_xy_extent=4096, -1, 1)            :316,323; base:38
 10  pos_y            pos_y (:188)            same                                                   :317,324
 11  pos_z            pos_z (:189)            clip(z/pos_z_extent=1024, -1, 1)                       :318,325; base:39
 12  view_yaw_sin     view_x (:190) (= demoparser `yaw`, run_ingestion.py:1473)  sin(radians)         :328-330
 13  view_yaw_cos     view_x                  cos(radians)                                           :331
 14  view_pitch       view_y (:191) (= `pitch`, run_ingestion.py:1474-1476)  /pitch_max=90, not clipped  :332; base:40
 15  z_penalty        pos_z + map_name (:218) compute_z_penalty = min(|z − z_cutoff|/500, 1); 0.0 for single-level or unknown map; 0.0 + one-time warning if map_name absent  :336-353; P/core/spatial_data.py:402-424
 16  kast_estimate    get_val("kast", get_val("avg_kast", None))  → NO such column on PlayerTickState → 0.0 always from DB rows; injection of avg_kast forbidden (DATA-01, P/backend/nn/jepa_train.py:152-157)  :356-363
 17  map_id           map_name                (md5(map_name) % 10000)/10000; 0.0 if no map_name       :365-369
 18  round_phase      equipment_value         0 / .33 / .66 / 1.0 at thresholds 1500/3000/4000; 0 if equip==0  :371-381; base:43-45
 19  weapon_class     active_weapon (:199)    WEAPON_CLASS_MAP lower(), "weapon_" prefix stripped; knife 0, grenade .1, taser/c4 .05, pistol .2, smg .4, rifle .6, sniper .8, heavy 1.0; unknown → 0.5 (_UNKNOWN_WEAPON_DEFAULT :143), numeric handle 0xFFFFFF → 0.0  :40-140, :384-414
 20  time_in_round    time_in_round (:213)    min(t/115, 1)  (literal)                               :427-430
 21  bomb_planted     bomb_planted (:214)     1/0                                                    :432-435
 22  teammates_alive  teammates_alive (:215)  min(n/4, 1)  (literal)                                 :437-440
 23  enemies_alive    enemies_alive (:216)    min(n/5, 1)  (literal)                                 :442-445
 24  team_economy     team_economy (:217)     min(v/16000, 1)  (literal)                             :447-450
- Slots 20-24 read tick attribute first, then the optional `context` dict (:425-450). map_name auto-resolved from tick.map_name unless "de_unknown" (:657-664).
- Finalisation: NaN/Inf → nan_to_num(nan=0, posinf=1, neginf=-1) (:548); clamp telemetry D1/D2 module globals (:146-185, :453-547); batch gate P3-A raises DataQualityError above 5% clamped rows (:736-746). Config snapshot per batch (:700-732); class-level `configure()/update_heuristics()` (:594-612) = mutable global state (docs D6).
- HeuristicConfig loaded from P/backend/storage/heuristic_config.json (base_features.py:60-85) — file does not exist on disk → defaults; the whole config is serialised into every checkpoint sidecar (P/backend/nn/persistence.py:123-142) — see the on-disk sidecar P/models/global/jepa_brain.pt.meta.json (heuristic_config block).
- FeatureExtractor API: `extract(tick_data dict|obj, map_name=None, context=None, _config_override=None) -> np.float32[25]` (:614-676); `extract_batch(list, map_name=None, contexts=None) -> np.float32[N,25]` (:678-748); `get_feature_names()` (:750-757); `validate_feature_parity(vec, label)` raises ValueError if last dim != 25 (:759-778).

1.4 Constant / dead / redundant features (measured on real ticks, R/docs/research/verify_math_claims_2026-09-05.json, 30 pairs, 1.2M ticks)
- Fraction of 11-tick windows in which the feature is exactly constant: health .997, armor .997, has_helmet .999, has_defuser 1.0, equipment_value .993, is_crouching .969, is_scoped .999, is_blinded .998, enemies_visible .933, pos_x .448, pos_y .447, pos_z .655, yaw_sin/cos .459, pitch .492, z_penalty .98, kast_estimate 1.0 (unique values {0.0}), map_id 1.0 (7 distinct values corpus-wide), round_phase .999 (verified pure function of equipment_value), weapon_class .937, time_in_round .16, bomb_planted .999, teammates_alive .996, enemies_alive .994, team_economy .979.
- Docs verdicts: D1 feature 16 constant zero (CORREZIONE_NUCLEO_NEURALE.md:626-628), D2 map_id hash + ordinal weapon_class (:630-632), D3 round_phase redundant (:634-636), D4 literal divisors for 20-24 (:638-640), D6 global state (:646-648). PARTE_III §2.1 (:73-141) defines CS2_V2 = 21 numeric + 3 categorical (map_id vocab, weapon_class vocab, side) with `source_column` per field, and §2.2 (:143-153) requires v1 `extract/extract_batch` to remain untouched and a lossless `remap_v1_to_v2` from indices [0..15, 20..24].

1.5 Every place the 25-vector is built (all go through FeatureExtractor; grep of non-test callers)
 a. Training (JEPA/RAP): P/backend/nn/training_orchestrator.py:836 `FeatureExtractor.extract_batch(raw_items)` inside `_prepare_tensor_batch` (:820-928) → torch (b,25) → JEPA slices context (1,10,25), target (1,1,25), negatives (1,5,25) (:859-893); RAP metadata (B,32,25) (:1385, :1028).
 b. Training drift telemetry (opt-in DRIFT_RETRAIN_ENABLED, default False P/core/config.py:222): training_orchestrator.py:252, :278 (TickFeatureDriftMonitor, P/backend/processing/validation/drift.py:153-190).
 c. CLI JEPA path: P/backend/nn/jepa_train.py:134-166 `_load_tick_sequence` (raw sqlite3 rows → dicts → `extract_batch(tick_dicts, map_name=first row)` → (T,25)).
 d. RAP/Chronovisor/overlay: P/backend/processing/state_reconstructor.py:85-103 (`contexts` built from PlayerTickState fields :88-97; `extract_batch(ticks, map_name=self.map_name, contexts)`; `validate_feature_parity("training")`; metadata (1,T,25) :103; vision tensors from TensorFactory :106-113). Callers: coach_manager.get_interactive_overlay_data :1349-1368, run_ingestion._execute_rap_logic :212-224, chronovisor_scanner.
 e. Live inference: P/backend/nn/inference/ghost_engine.py:162-185 (`extract(tick_data, map_name, context)` → (1,1,25), `validate_feature_parity("inference")`).
 f. Coaching insights: P/backend/services/coaching_service.py:801-850 `_generate_jepa_insights` → `extract_batch(rows[-256:])` (:824) → `generate_jepa_insights(window [T,25])` (jepa_insight_adapter.py:249-280) → CoachingInsight rows (:829-843). Gated by USE_JEPA_MODEL (default False, core/config.py:236).
 g. Tools: R/tools/verify_math_claims.py:198; R/tools/eval_harness.py:105-112 (instance `.extract` per row, ≥500 ticks :97-103); R/tools/observe_training_cycle.py:189; R/tools/headless_validator.py:1785-1858.
 h. NOT builders: P/backend/processing/tensor_factory.py (map/view/motion rasters (3,H,W); TensorConfig 128/224 :84-93, TrainingTensorConfig 64/64 :95-110); data_pipeline.py (12-col aggregate scaler, dead); coach_manager window fetchers return ORM rows (vectorisation happens in the orchestrator).

======================================================================
2. LEGACY JEPA — FILE BY FILE
======================================================================

2.1 P/backend/nn/jepa_model.py (1153 lines)
- JEPAEncoder(input_dim, latent_dim=256): Linear(in,512)-LN-GELU-Dropout(.1)-Linear(512,latent)-LN, per-tick MLP, [B,T,in]→[B,T,latent] (:43-65). No temporal mixing (docs J3).
- JEPAPredictor(latent_dim=256): Linear(l,2l)-LN-GELU-Dropout-Linear(2l,l), [B,l]→[B,l] (:75-96).
- JEPACoachingModel(input_dim, output_dim, latent_dim=256, hidden_dim=128, num_experts=3) (:112-162): `context_encoder`, `target_encoder` (independent init), `predictor`, `lstm` LSTM(latent,hidden,2 layers,dropout .15) :133, `experts` ×3 Linear-ReLU-Linear :136-139/177-179, `gate` Linear(hidden,3) :143, `log_temperature` = log(0.07) :151, buffers `moco_queue` (4096,latent) + `moco_queue_ptr` :154-158, `_moe_aux_loss` :161. State-dict keys therefore: context_encoder.projection.*, target_encoder.projection.*, predictor.predictor.*, lstm.*, experts.{0,1,2}.*, gate.*, log_temperature, moco_queue, moco_queue_ptr.
- forward(x, role_id) = forward_coaching (:164-175). forward_jepa_pretrain(x_context[B,Tc,in], x_target[B,Tt,in]) → (pred[B,l], target_pooled[B,l]); mean-pool over T, target under no_grad (:181-207). enqueue (:209-223). _sparse_moe top-2 + Switch aux loss (:225-263). forward_coaching → sigmoid [B,output_dim] (:265-289). forward_selective (:291-350). _apply_role_bias (:352-363). freeze_encoders/unfreeze_encoders toggle `is_pretrained` (:365-381). update_target_encoder(momentum=0.996) EMA with NN-JM-04 requires_grad guard (:383-405).
- Losses: jepa_contrastive_loss(pred[B,l], target[B,l], negatives[B,N,l], temperature=0.07) InfoNCE = cross_entropy over [pos, negs]/τ on L2-normalised vectors (:408-430). vicreg_regularization(emb, λ_var=25, λ_cov=1) returns 0.0 when B<2 (:433-449).
- VL-JEPA: NUM_COACHING_CONCEPTS=16 :460; COACHING_CONCEPTS/CONCEPT_NAMES :473-554; ConceptLabeler :557-894 — label_tick (heuristic on the 25 input features, leakage; index map :582-599), label_from_round_stats(RoundStats) outcome labels (:735-780, fields kills, deaths, assists, damage_dealt, trade_kills, was_traded, opening_kill/death, he_damage, molotov_damage, flashes_thrown, smokes_thrown, equipment_value, round_won, round_rating), label_batch (:862-894). VLJEPACoachingModel(input_dim, output_dim, latent_dim=256, hidden_dim=128, num_experts=3, num_concepts=16) adds concept_embeddings Embedding(16,l), concept_projector, concept_temperature=0.10 (:910-937); forward_vl → dict{concept_probs, concept_logits, concept_logits_scaled, top_concepts, coaching_output, latent} (:939-999); get_concept_activations (:1026-1055). vl_jepa_concept_loss(logits, labels, emb, α=.5, β=.1) = α·BCE + β·(−mean std) (:1058-1096).

2.2 P/backend/nn/jepa_trainer.py (654 lines)
- JEPATrainer(model, lr=1e-4, weight_decay=1e-2, drift_threshold=2.5, t_max=100) (:79-173): freezes target_encoder (:89-91); "concept" params at 0.05×lr (:93-113); SequentialLR = LinearLR warm-up 5% → CosineAnnealingLR (:114-127); fp16 GradScaler when CUDA (:129-132); accumulation 4, clip 1.0 (:134-136); EMA cosine τ 0.996→1.0 with REPR-01 rehydration from `model._saved_ema_step/_saved_ema_total_steps` (:138-155); DriftMonitor (:157-161); EmbeddingCollapseDetector(0.01, 2) (:167); LabelSourceMonitor (:173).
- set_total_steps(epochs, batches_per_epoch) (:175-177); _scheduled_ema_momentum (:179-191).
- encode_raw_negatives(negatives[B,N,feat], seq_len) → [B,N,l] via target_encoder on expanded sequences (:193-212) — LIVE: called by train_step (:259-260) and orchestrator._eval_step_jepa (training_orchestrator.py:717) because production negatives are raw (1,5,25). (PARTE_III:424 lists it as never called — inaccurate; the docs' J14 "costly no-op" verdict is the substantive point.)
- train_step(x_context[B,Tc,25], x_target[B,Tt,25], negatives, step_optimizer=True) → {loss, embedding_variance (None if B<2), pred_embedding_pooled[l], grad_norm, temperature, vicreg} (:214-299): tabular mask .3 + noise .03 (:301-308); MoCo augmentation with 64 random queue rows (:310-322, live); loss = InfoNCE(τ=exp(log_temperature) clamped [.01,1]) + 0.01·VICReg (inert at B=1, D-13 note :270-275); enqueue (:278).
- _optimizer_step unscale→clip→step→update→zero_grad→EMA (:324-334). _log_embedding_diversity (:336-360).
- DEAD in production: train_epoch (:362-437; only caller retrain_if_needed :519-521, which has no callers — J7 table CORREZIONE_NUCLEO_NEURALE.md:436), check_val_drift (:439-468), retrain_if_needed (:470-528).
- train_step_vl (:530-654): InfoNCE + VICReg + BCE on `concept_logits_scaled`, D-14 one RoundStats per sample (:31-70); only reachable with model_type "vl-jepa", which no entry point passes (run_full_training_cycle choices all/jepa/rap; coach_manager uses "jepa").

2.3 P/backend/nn/jepa_train.py (869 lines) — second, CLI-only implementation
- `_DB_PATH` hard-coded to P/backend/storage/database.db (:33-34); `_open_db` raw sqlite3 WAL (:39-46).
- `_MIN_TICKS_FOR_SEQUENCE = 20` (:51), `_MAX_TICKS_PER_SEQUENCE = 500` (:55).
- JEPAPretrainDataset(sequences, context_len=10, target_len=10, seed=42): random start, short sequences sliced (not zero-padded) (:58-108) → {"context": (10,25), "target": (10,25)}.
- `_load_tick_sequence(demo, player, max_ticks)`: `SELECT * FROM playertickstate WHERE demo_name=? AND player_name=? ORDER BY tick LIMIT ?` (exact raw-name equality) → extract_batch (:111-166). load_pro_demo_sequences(limit=100) (:169-241) and load_user_match_sequences (:244-300) filter `is_pro`, `sample_weight > 0`, `UPPER(dataset_split)='TRAIN'` (D-15/D-15b :189-216).
- Pretrain: only context_encoder + predictor optimised (:361-365), bf16 amp (:433-439), CosineAnnealingLR(T_max=epochs) (:371), 8 in-batch negatives from targets via `_jepa_negative_indices` (:303-321, :445-446), τ fixed 0.07 (default arg, :449), EMA cosine per batch (:456-460), early stop on TRAIN loss (:513, :579), TensorBoard + EmbeddingProjector (:489-499). train_jepa_pretrain(model, num_epochs=50, batch_size=16, learning_rate=1e-4, num_negatives=8, log_dir=None) (:472-595) freezes encoders and stashes `_training_metadata`, `_saved_ema_*` (:377-414).
- train_jepa_finetune (:598-724): raises ValueError "26-RANGE-01" when y dim != output_dim (:632-641); MSE on forward_coaching + MoE aux (:695-707).
- save_jepa_model(model, path, optimizer, trainer) rich dict {model_state_dict, is_pretrained, input_dim, output_dim, param_count, save_timestamp, optimizer_state_dict?, ema_step, ema_total_steps, training_metadata} atomic (:727-792); load_jepa_model(path, input_dim, output_dim) (:795-830). NOTE: the archived checkpoints in P/models/global/archive_pre_rebuild_2026-09-01/ are `save_nn` state_dicts with .pt.meta.json sidecars and are loaded by verify_math_claims via `torch.load + load_state_dict(strict=True)` (R/tools/verify_math_claims.py:487-490) — load_jepa_model's dict format is not needed even for the benchmark.
- __main__ `--mode pretrain|finetune`, `--model-path` default MODELS_DIR/jepa_model.pt (:833-869). No production caller.

2.4 Third implementation (dead): P/backend/nn/train.py `train_nn(..., config_name="default")` (:15-64) branches to `_train_jepa_self_supervised` (:67-179; JEPA_EPOCHS=5, SelfSupervisedDataset context 10 / prediction 5 — P/backend/nn/dataset.py:25-63, per-sample randperm negatives ≤8, EMA per batch) only when config_name == "jepa"; coach_manager._train_phase calls `train_nn(X_train, y_train, X_val, y_val, model=base_model, context=context)` without config_name (:1071-1073) → never reached.

2.5 P/backend/nn/training_orchestrator.py (1779 lines) — JEPA branches
- Class constants: `_DEFAULT_TRAIN_SAMPLES = 50_000`, `_DEFAULT_VAL_SAMPLES = 10_000` (:38-39, overridable by settings TRAIN_SAMPLES/VAL_SAMPLES :76-81), `_JEPA_CONTEXT_LEN = 10` (:45), `RAP_SEQ_LEN = 32` (:934).
- `__init__(manager, model_type="jepa", max_epochs=100, patience=10, batch_size=32, callbacks=None, accumulation_steps=4, train_samples=None, val_samples=None, dry_run=False)` (:47-140): jepa/vl-jepa → JEPATrainer, model_name "jepa_brain"/"vl_jepa_brain", lr 1e-4 (:117-123); rap → requires USE_RAP_MODEL else ValueError (:125-138); unknown → ValueError "Unknown model type" (:139-140); `_neg_rng = default_rng(42)` (:88); negative pool max 500, 5 negatives (:92-97); D-13 collectors (:107-112).
- `_load_or_init_model` ModelFactory + load_nn + B3.2 best_val restore from sidecar `extra.best_val_loss` (:161-219).
- `_run_epoch_loop(trainer, model, val_data, context)` (:314-454): per-epoch re-fetch seed GLOBAL_SEED+epoch; collapse gate D-13 (within-batch mean else cross-window variance of pooled embeddings) → `trainer.embedding_collapse_detector.update` (:354-377); `trainer.scheduler.step()` per epoch only if `_last_train_batch_count > 0` (:388-400); progress → `manager._update_state` (:402, :1775-1779); callbacks on_epoch_end (:409-416); save_nn(model_name, extra_meta={best_val_loss, head_trained False}) on improvement (:418-436), save_nn(f"{model_name}_latest") every epoch (:440-446), both skipped in dry_run; patience stop (:449-452).
- `_checkpoint_extra_meta` (:456-476): jepa/vl-jepa → {"head_trained": False}; rap → map/view_resolution from TrainingTensorConfig.
- `_finalize_training` (:478-522): P3-C zero-tensor fallback rate >30% → False; on_train_end final_metrics {best_val_loss, final_epoch, model_type, fallback_rate}.
- `run_training(context=None) -> bool` (:524-623): `run_pre_training_quality_check()` gate (P/backend/nn/data_quality.py:57-169: ≥1000 tick rows, zero-position rate ≤10%, train_rows>0, shard completeness) → False on FAIL; trainer kwargs `lr`, `t_max=max_epochs` (+`accumulation_steps` for rap) (:541-546); preflight fetch epoch 0 (:551-552); `_MIN_TRAINING_SAMPLES = 100` real rows counted as sum(len(window)) (:562-571); probe batch prepared with is_train=False and RNG state restored (:589-596); `on_train_start(model, config={model_type, max_epochs, batch_size, lr, probe_batch})` (:598-612); `set_total_steps(max_epochs, ceil(len(preflight)/accum))` (:614-618).
- `_fetch_batches(is_train, epoch)` (:625-659): JEPA → `manager._fetch_jepa_windows(is_pro=True, split, seed=42(+epoch if train), n_windows=sample_size//11, window_len=11)`; RAP → `_fetch_rap_windows(is_pro, split[, max_demos=10 if max_epochs<=1])`.
- `_train_step_dispatch` (:661-695), `_eval_step_dispatch` (:697-702), `_eval_step_jepa`: InfoNCE on forward_jepa_pretrain with negatives encoded by `encode_raw_negatives` and learned τ detached (D-18) (:704-725), `_eval_step_rap` (:727-743).
- `_run_epoch` (:745-818): drops batches with len<2 (:777), gradient accumulation, denominators count processed batches only, collects `embedding_variance` / `pred_embedding_pooled` (:789-795).
- `_prepare_tensor_batch(raw_items, is_train=True)` (:820-928): described in 1.5a; returns None for empty, b<11, or b<5 without warm pool; negatives from cross-match pool (:871-882) else in-batch (:883-886); pool fed by TRAIN batches only (:900-905); VL adds `round_stats=[rs]` (last context tick's round) via `_fetch_round_stats_for_batch` (:911-924).
- RAP-only remainder: `_prepare_rap_batch` (:948-1047; output view/map/motion (B,T,3,64,64), metadata (B,T,25), target_strat (B,10), target_val (B,1), val_mask (B,), target_pos (B,3), timespans (B,T)), prefetch caches (:1049-1124), per-tick collection with PlayerKnowledge (:1126-1262), `_rap_compute_target_pos` (:1265-1282), `_rap_compute_timespans` (:1285-1310), `_resolve_tick_rate` (:1313-1350; MatchMetadata.tick_rate in [32,256] else DEFAULT_TICK_RATE=64, P/core/tick_rate.py:25), `_rap_segment_windows` (:1353-1407), `_build_sample_knowledge` (:1409-1470), `_resolve_map_name` (:1485-1533), `_fetch_round_stats_for_batch` (:1537-1606, exact-name join — see §3), `_compute_advantage` (:1651-1709), `_classify_tactical_role` (:1711-1773).

2.6 P/backend/nn/coach_manager.py — fetchers and phases
- `_fetch_jepa_ticks(is_pro, split=TRAIN, seed=42, sample_size=5000)` (:634-774): PlayerMatchStats(is_pro, split) → demo names with `.dem_<player>` suffix stripped (MATCH_STATS_DEMO_SUFFIX_RE, db_models.py:17) ∩ completed shards (`_get_completed_demo_names` :1015-1062, MatchMetadata.match_complete) → COUNT (cached per process :695-716) → exact id sampling below `_ID_MATERIALIZE_CAP = 2_000_000` (:12) else id-space rejection sampling `_sample_ids_rejection` (:776-832) → chunked fetch (500 ids) (:766-771). Empty on zero eligible rows (D-20 :725-733).
- `_fetch_jepa_windows(is_pro, split=TRAIN, seed=42, n_windows=4500, window_len=11)` (:834-907): each anchor expanded with `demo_name==, player_name==, tick>=anchor.tick ORDER BY tick LIMIT window_len` (:869-878); drop if len != window_len (:879-880); D-22 drop if `rows[0].round_number != rows[-1].round_number` (:881-892). No `tick[i+1]-tick[i]==1` check — contiguity is row order.
- `_fetch_rap_windows(is_pro, split, window_size=96, max_demos=None)` (:909-1013): per (demo, player) first `_PER_PLAYER_TICK_CAP = 1_000` ticks only (:971, :992), non-overlapping windows of 96.
- Phases `_execute_training_phases` (:526-586): 1 JEPA via TrainingOrchestrator("jepa") (:588-602, ValueError → warning), 2 pro AdvancedCoachNN → save_nn("latest") (:543-551), 3 user model save_nn("latest", user_id="user") (:559-561), 4 RAP orchestrator (ValueError when USE_RAP_MODEL False → warning :604-615), 5 role head (:617-632), maturity counter (:580). `run_full_cycle` (:293-330) calls `assign_dataset_splits` (:320). `assign_dataset_splits` (:397-509) 70/15/15 per demo, pro/user separately, gates :343-395 (shard complete, ≥MIN_COMPLETE_MAP_ROUNDS=13 :47, nonzero aggregates), UNASSIGNED otherwise.
- `_update_state(status, detail)` (:1075-1079) is the only manager method the orchestrator calls besides the two fetchers.

2.7 P/backend/nn/collapse_metrics.py (97 lines) — keep as-is (CORREZIONE_NUCLEO_NEURALE.md:674, :961)
- `compute_collapse_metrics(embeddings[N,D] | [...,D]) -> {std_mean, std_min, effective_rank, cosine_offdiag_mean}` on L2-normalised, uncentred rows; zeros when N<2 (:41-81). `compute_ema_drift(online_params, target_params) -> float` L2 (:84-97). Consumers: P/backend/nn/tensorboard_callback.py:273-305 (`embed/*` scalars; needs `model.context_encoder`, optional `model.target_encoder`, probe dict with a "context" tensor — `_extract_probe_context` :38-62), R/tools/verify_math_claims.py:62-65, P/tests/test_collapse_metrics.py.

2.8 P/backend/nn/early_stopping.py — EmbeddingCollapseDetector(threshold=0.01, patience=2).update(v) raises EmbeddingCollapseError; NaN/negative count as collapse; reset() (:39-102). EarlyStopping(patience=10, min_delta=1e-4) (:105-152). Docs: keep as gate with new inputs (:961).

2.9 P/backend/nn/persistence.py (309 lines)
- `BASE_NN_DIR = MODELS_DIR` (:16) = USER_DATA_ROOT/models (P/core/config.py:389) = P/models; `_META_SCHEMA_VERSION = "v1"` (:21); StaleCheckpointError (:24-29).
- `get_model_path(version, user_id=None)` → P/models/global/{version}.pt or P/models/{user_id}/{version}.pt (:32-39); factory bundle fallback (:42-45).
- Hash registry P/models/checkpoint_hashes.json (:48-50; git-tracked; currently holds entries for six historical mounts incl. the live WORK_RECOVERED1 paths for jepa_brain, jepa_brain_latest, rap_coach, rap_coach_latest); `_register_checkpoint_hash` (:62-90), `_verify_checkpoint_hash` (unregistered → allowed) (:93-115).
- Sidecar `<name>.pt.meta.json` (:118-120); `_build_current_meta` = {schema_version, metadata_dim, feature_names, heuristic_config} (:123-142); `_validate_loaded_meta` requires schema_version=="v1", metadata_dim==25, feature_names==FEATURE_NAMES (:145-181).
- `save_nn(model, version, user_id=None, extra_meta=None)` atomic state_dict + sidecar + hash (:184-221). `load_nn(version, model, user_id=None)` search user→global→factory user→factory global; hash verify; sidecar validate (missing → warning); `torch.load(weights_only=True)` + `load_state_dict(strict=True)`; size mismatch → StaleCheckpointError; nothing found → FileNotFoundError (:224-309).
- On disk now (P/models/global): jepa_brain.pt (7.97 MB, 2026-09-11 05:13) sidecar extra {best_val_loss 4.5255e-07, head_trained false}; jepa_brain_latest.pt; rap_coach.pt / rap_coach_latest.pt (11.4 MB, 2026-09-11 11:40, extra map/view_resolution 64); archive_pre_rebuild_2026-09-01/ (jepa_brain, jepa_brain_latest, latest.pt, rap_coach*). None of the .pt files are git-tracked (only README*/checkpoint_hashes.json).

2.10 P/backend/nn/config.py — GLOBAL_SEED 42 (:13), set_global_seed (:16-39, deterministic algorithms warn_only unless CS2_NONDETERMINISTIC=1), seeded_generator (:42-51), get_device (CUDA_DEVICE setting; ROCm → `torch.backends.cudnn.enabled = False` :125-132) (:89-162), amp_autocast bf16 (:165-176), throttling/intensity (:202-211).

2.11 P/backend/nn/factory.py — TYPE_* (:36-41); get_model (:44-112); get_checkpoint_name: jepa→"jepa_brain", vl-jepa→"vl_jepa_brain", rap→"rap_coach", rap-lite→"rap_lite_coach", role_head→"role_head", default→"latest" (:115-142).

2.12 What the docs say to keep (evidence)
- FiLM: P/backend/nn/layers/superposition.py SuperpositionLayer(in_features, out_features, context_dim=METADATA_DIM), y = sigmoid(W_γ·ctx)·(Wx+b) + W_β·ctx, β zero-init, gate telemetry (:14-121); used only by P/backend/nn/experimental/rap_coach/strategy.py:42. Keep as conditioning mechanism (CORREZIONE_NUCLEO_NEURALE.md:964, :768, :1049).
- collapse_metrics.py — keep whole (:674, :961).
- PlayerKnowledge: P/backend/processing/player_knowledge.py dataclasses VisibleEntity/LastKnownEnemy/HeardEvent/UtilityZone (:69-114), PlayerKnowledge (:116-162: own state incl. own_team, teammate_positions, visible_enemies, last_known_enemies, heard_events, utility_zones, bomb state, position_is_fallback), PlayerKnowledgeBuilder(fov_degrees, hearing_range_gunfire, memory_decay_tau, memory_cutoff_ticks, tick_rate=64) (:252-284), build_knowledge(player_tick, all_players_at_tick, recent_player_history, recent_all_players_history, active_events) (:286+). Keep (:675, :962).
- Chronovisor algorithm: P/backend/nn/experimental/rap_coach/chronovisor_scanner.py ScanResult (:37-53), ScaleConfig (:56-64), ANALYSIS_SCALES micro/standard/macro = window 64/192/640 ticks, lag 16/64/128, thresholds .10/.15/.20 (:67-90), CriticalMoment (:93-138), `_timeline_entry` one value per window keyed to last tick (:269-279), `_analyze_signal` (:281-308), `_analyze_signal_at_scale` lag differences → same-sign grouping within window → peak (:310-398), `_deduplicate_across_scales` MIN_GAP 64 ticks, micro>standard>macro (:400-440). Deprecated shim P/backend/nn/rap_coach/chronovisor_scanner.py:1-16. Keep algorithm + ScanResult, convert units to seconds, add smoothing, feed surprise (PARTE_III:455-461; PARTE_I:594-598, :963).
- Observability discipline: CallbackRegistry/TrainingCallback events on_train_start(model, config), on_epoch_start(epoch), on_batch_end(batch_idx, loss, outputs), on_epoch_end(epoch, train_loss, val_loss, model, optimizer), on_validation_end, on_train_end(model, final_metrics), close (P/backend/nn/training_callbacks.py:29-107); TensorBoardCallback + build_run_dir (P/backend/nn/tensorboard_callback.py:65-306) with probe-batch contract (:140-167); MaturityObservatory (P/backend/nn/maturity_observatory.py:89-430; reads RAP-only attributes per docs R12); EmbeddingProjector (P/backend/nn/embedding_projector.py:69-205); LabelSourceMonitor. Keep (:961, :967).
- Also keep per docs §9 (:953-967): persistence save/load + hash registry, set_global_seed/seeded_generator/get_device, temporal split + eligibility gates, D-22, no zero padding, data_quality gate.

======================================================================
3. PLAYER-NAME HANDLING (D8)
======================================================================

3.1 Where names are normalised (lower/strip) — roundstats family
- P/backend/processing/round_stats_builder.py `_collect_player_names` :262-288 (death-event attacker/user names `.str.strip().str.lower()`; roster keys already lowercase — P/backend/data_sources/trade_kill_detector.py `build_team_roster` :51-99, key `name_str.lower()` :92); accumulators keyed by lowercased name (:291-358); every event processor lowercases (:389-390, :413, :444-445, :480-481, :614-615, :652-653); `enrich_from_demo` target filter (:901); RoundStats rows inserted with those names (`persist_round_stats_and_enrichment` :940-1044, insert :1002-1006). Enrichment UPDATE on PlayerMatchStats uses `func.lower(PlayerMatchStats.player_name) == player_key` (:1008-1014) — already case-insensitive. Persist call site: P/run_ingestion.py:518-521.
- P/run_ingestion.py:797 (`_pname` = strip().lower()) is the shard event-extractor state index, not the RoundStats writer (the docs cite this line as the roundstats normalisation — CORREZIONE_NUCLEO_NEURALE.md:656, PARTE_III:162; functionally the lowercasing lives in round_stats_builder).

3.2 Where raw names are kept
- PlayerTickState (monolith): P/run_ingestion.py `_build_legacy_tick_dataframe` :1451-1544 passes `player_name` through unchanged (:1469; only `.astype(str)` at :1308-1309), bulk `to_sql("playertickstate")` :1700-1705. Source: demoparser2 `player_name`/`name` (P/backend/data_sources/demo_parser.py:574-578, rename :661-664), lowercased only for target filtering (:682-686).
- MatchTickState (per-match shard): `_build_match_tick_dataframe` :1316-1448, `player_name` raw (:1329), `team` from team_name (:1333), plus steamid.
- PlayerMatchStats: `_save_player_stats` `p_name = row["player_name"]` :544 → `PlayerMatchStats(player_name=p_name, demo_name=clean stem, ...)` :588-595; row from demo_parser `_extract_stats_with_full_fields` :114-135 (raw). Second constructor at run_ingestion.py:119-121 (skill vector, not persisted).

3.3 Joins between the two families in training/eval
- training_orchestrator._fetch_round_stats_for_batch :1537-1606: `RoundStats.demo_name == item.demo_name AND RoundStats.player_name == item.player_name` (:1566-1570), keyed (demo, player, round_number) (:1586-1593) — exact raw-vs-lowercase → misses for capitalised names; VL-JEPA path only. Test pins round_number semantics (P/tests/test_training_orchestrator_logic.py:232-305).
- jepa_train._load_tick_sequence: playermatchstats (raw) → playertickstate (raw) exact (:136-141) — consistent today but exact.
- coach_manager._fetch_jepa_ticks/_fetch_jepa_windows: demo-level only (:664-686), player equality within PlayerTickState (:873).
- RAP `_build_sample_knowledge`: shard vs monolith raw==raw (:1437-1440).
- R/tools/verify_math_claims.py: `select_pairs` from roundstats (:116-138) → `resolve_stored_name(con, demo, player)` = `SELECT DISTINCT player_name FROM playertickstate WHERE demo_name=?` matched by `strip().casefold()` (:141-151) → `fetch_ticks` exact on the stored spelling (:154-163); `attach_round_won` joins (demo, round, player.strip()) (:277-289). Reused by R/tools/measure_episode_lengths.py:56,397 and R/tools/measure_event_horizons.py:471-482.
- R/tools/eval_harness.py:360-379 reads RoundStats.round_won directly (no name join).

3.4 Measured (R/docs/research/name_join_coverage_2026-09-05.json, read-only run against the "New Volume7" DB copy, 225 demos)
- roundstats 139 distinct names vs playermatchstats 146; 61 roundstats names without exact match; 67 PMS names changed by strip/lower (66 case-only, 1 leading/trailing whitespace, 0 non-ASCII/NFKC).
- Tick existence probes: roundstats (demo, player) pairs hit playertickstate by exact equality 1211/2250 = 53.8%; resolved via strip/casefold 2250/2250 = 100%, 0 ambiguous.
- steamid unusable as key: playermatchstats.steamid NULL in all 2252 rows; playertickstate.steamid NULL in all 25,974 sampled rows; roundstats has no steamid column.
- Migration size (PARTE_III:164-169): 1040/2252 PMS rows (46.2%) and ≈189.5M playertickstate rows would be touched by `UPDATE ... lower(trim())`.
- roundstats.side: 48,570 rows, 0 null, exactly 5 CT / 5 T per (demo, round), first flip at round 13 for 94.2% of pairs (rest = short/fragment demos), overtime flips at 28/31/34/37/40 — consistent with `_team_num_to_side` (round_stats_builder.py:163-186).
- PlayerTickState has no team/side column (db_models.py:161-219; F-0025 note training_orchestrator.py:1227-1231): side is obtainable only via roundstats (name join) or the shard `matchtickstate.team`.
- Note the two DB copies: PARTE_III:65 reports 1,031 playermatchstats rows / all UNASSIGNED (repo symlink P/backend/storage/database.db), the JSON above reports 2,252 rows on /media/renan/New Volume7/AI/database.db (183 GB). Counts differ; I did not open either DB.

======================================================================
4. EPISODE / WINDOW SEMANTICS AVAILABLE TODAY
======================================================================
- round_number / time_in_round: P/backend/data_sources/round_context.py `extract_round_context` (round_freeze_end + round_end events; round_start = last freeze_end before round_end, else previous round_end) :23-119; `assign_round_to_ticks(df, rc, *, tick_rate)` backward merge_asof on round_start_tick :170-247 — warm-up ticks → round 1, time 0.0 (:219-235); `time_in_round = (tick − round_start_tick)/tick_rate`, NOT clamped (:225-235; pinned by P/tests/test_handoff_regressions.py:251-294). Consequence: a `round_number` segment spans freeze_end→next freeze_end, i.e. includes post-round and the next freeze time (R/tools/measure_episode_lengths.py:21-25).
- tick_rate per demo from the .dem header, [32,256] else 64 (P/run_ingestion.py:1145-1201); enrichment call :1646-1651 (P/backend/processing/tick_enrichment.py:34-94: bomb state :97-161 resets at round_start ticks; alive counts :164-219; team_economy = Σ balance per (tick, team) :222-240; enemies_visible geometric FOV 90°, 4000 u, no occlusion :243-354; map_name stamped :91).
- Tick contiguity: native ticks 1:1, no decimation (demo_parser.py:548-555; run_ingestion.py:1319-1322, 1455-1456). Rows per player-match mean ≈182k (name_join JSON e_migration). Census (R/docs/research/episode_lengths_2026-09-05.json, 120 pairs, 14.39M rows): consecutive-row tick diff ≠ 1 in 0.049% (7,101 gaps, sizes 2–7, max 5, zero gaps ≥ 8); 1,653 round changes; strict episodes 8,871 vs 1,773 with a 7-tick tolerance (80% of strict breaks are 2–7 tick gaps); 20.9% of episodes dead-majority, 16.7% of ticks dead; 0 duplicate rows. Tick rate median 64.0 Hz (verify_math_claims T1).
- Round-boundary drop D-22: coach_manager.py:881-892 (test P/tests/test_training_loop_honesty.py:77-132). Window = context 10 + target 1 (training_orchestrator.py:45, :644; coach_manager default window_len=11 :840).
- MIN/size constants: orchestrator `_MIN_TRAINING_SAMPLES = 100` rows (:563); P3-E batch len<2 dropped (:777); JEPA batch needs b ≥ 11 (:850-856) and b ≥ 5 for in-batch negatives (:883-893); `_n_contrastive_negatives = 5` (:97); jepa_train `_MIN_TICKS_FOR_SEQUENCE = 20`, `_MAX_TICKS_PER_SEQUENCE = 500`; RAP `window_size = 96` → `RAP_SEQ_LEN = 32` (3 windows/batch), `_PER_PLAYER_TICK_CAP = 1_000`, curriculum 8→32 over `_ltc_curriculum_epochs = 5` (:113-114, :936-946), `_MIN_POV_DENSITY = 0.5` (:1362); state_reconstructor `sequence_length = 32`, 50% overlap (:33-52, :122-134); coaching_service window `rows[-256:]` (:824); eval_harness ≥ 500 ticks (:97-103); verify_math_claims CONTEXT_LEN 10, HORIZONS (1,8,32,128,640), DEATH_HORIZON 128 ticks (:72-75); MIN_COMPLETE_MAP_ROUNDS = 13 (coach_manager.py:47); data_quality min_samples 1000 / zero-position ≤ 10% (data_quality.py:57-59).
- Split: `assign_dataset_splits` (coach_manager.py:397-509); DatasetSplit enum stored on disk as the NAME ("TRAIN") — raw SQL must use `UPPER(dataset_split)` (jepa_train.py:195-210; P/tests/test_jepa_train_split_filter.py:78-115). Docs D9: all rows UNASSIGNED as of 2026-09-05 (PARTE_III:63-67).

======================================================================
5. LABELS AVAILABLE WITHOUT NEW INGESTION
======================================================================
- roundstats (db_models.py:779-841; unique (demo_name, round_number, player_name) :796-798; indexes ix_rs_demo_player, ix_rs_demo_round, ix_rs_kast): side, kills, deaths (0/1), assists, damage_dealt, headshot_kills, trade_kills, was_traded, thrusmoke_kills, wallbang_kills, opening_kill, opening_death, he_damage, molotov_damage, flashes_thrown, smokes_thrown, equipment_value (left at 0 unless backfilled — round_stats_builder.py:957-959; R/tools/populate_round_stats.py), round_won, mvp, kast (bool), round_rating (`compute_round_rating` :189). Written by run_ingestion.py:518-521 → persist_round_stats_and_enrichment. Read today by: ConceptLabeler.label_from_round_stats (jepa_model.py:735-780) via training_orchestrator._fetch_round_stats_for_batch (VL only); coach_manager `_get_demo_round_counts` (:333-341, eligibility); P/backend/analysis/momentum.py:232 (round_won); P/backend/processing/round_reconstructor.py:484 (round_won); P/backend/services/coaching_dialogue.py:1514, :1673; R/tools/eval_harness.py:360-379 (round_won as win-prob calibration truth); R/tools/mine_coaching_experience.py:152; P/backend/knowledge/pro_demo_miner.py:296 (AVG round_rating); R/tools/repair_kast.py:54; verify_math_claims `attach_round_won` :277-289; measure_event_horizons E2 (round_won, opening_death, opening_kill, kast).
- playertickstate transitions (per player stream): death = prev.health>0 and health<=0 (round_reconstructor.py `_emit_death` :148-160; verify_math_claims :259-260 death_within_128; measure_event_horizons death/damage/contact); damage = health drop (:124-145); first contact = enemies_visible 0→N (`_emit_engagement` :180-200; verify_math_claims :261); weapon_switch (:163-177); is_blinded, bomb_planted per tick (db_models :203, :214). P/backend/analysis/movement_quality.py:295-546 also keys on health<=0.
- playermatchstats (db_models.py:54-158): rating (CHECK 0..5 :59), dataset_split (:77-79), is_pro (:149), sample_weight (ghost = 0.0, excluded by jepa_train :204-215), match_date/match_date_source (:66-73), data_quality (:80), avg_kast (:104), kpr/dpr/rating_* (:122-128), trade/utility enrichments (:131-145). Read by coach_manager `_prepare_tensors`, data_quality.py:90-98 (split counts), R/tools/coach_answer_eval.py:165 (rating order), jepa_train loaders.
- Per-match shard (match_<id>.db, id = sha256(demo_name) mod 2^63−1, P/backend/storage/match_data_manager.py:109-130): matchtickstate with `team`, `money`, `is_alive`, per-round counters, steamid (run_ingestion.py:1323-1447); MatchMetadata(demo_name, map_name, tick_rate, match_complete) (:321-350); accessors get_metadata :707, get_events_for_tick_range :730, get_all_players_at_tick :805, get_all_players_tick_window :880, list_available_matches :923. Side per tick is available here without name normalisation issues (raw==raw).
- Probe baselines already measured (verify_math_claims T6, AUROC, GroupKFold by demo): round_won raw-25d 0.76 vs archived encoder 0.677 vs random encoder 0.681; death_within_128 raw 0.83/0.84 vs 0.736/0.704 (positive rate 0.95%); enemy_visible_within_128 raw 0.949 vs 0.942 (positive rate 70%).

======================================================================
6. ENTRY POINTS, CLI, AND CONTRACT-PINNING TESTS
======================================================================
6.1 Entry points
- R/run_full_training_cycle.py: parser :92-178 — `--dry-run`, `--resume` (documented no-op), `--epochs` (100), `--model-type {all,jepa,rap}` default all, `--tb-logdir`, `--no-tensorboard`, `--patience` (10; 5 in dry-run :214), `--train-samples`, `--val-samples`, `--seed`, `--eval-baseline/--no-eval-baseline` (default on unless dry-run :221). main :181-286: set_global_seed :189-195; CoachTrainingManager :207; callbacks TensorBoard + MaturityObservatory + EmbeddingProjector :21-64; `assign_dataset_splits` :217; pre/post eval via subprocess R/tools/eval_harness.py :67-89; Phase 1 `TrainingOrchestrator(manager, "jepa", max_epochs, patience, callbacks, train_samples, val_samples, dry_run)` :229-239; GPU release :242-249; Phase 2 RAP :253-264; exit 3 on aborted phase :269-274; any exception → exit 1 :281-283. Observed: with USE_RAP_MODEL=False (default, P/core/config.py:240) `--model-type all` reaches Phase 2, `TrainingOrchestrator("rap")` raises ValueError (training_orchestrator.py:128-132) → exit 1 after a completed JEPA phase (adjacent issue, docs D7).
- R/train.sh: venv `.venv` else `~/.venvs/cs2analyzer` (:32-40); env EPOCHS (100), MODEL_TYPE (all); short-flag rewrite (:51-62); logs/train_<ts>.log; exit code of python (:69-77).
- R/tools/ingest_pro_demos.py: `--full`, `--retrain-only`, `--no-train` (:6-10); retrain = `CoachTrainingManager().run_full_cycle()` (5-phase manager path, not run_full_training_cycle) :55-62, :256-263.
- R/batch_ingest.py:290-299: `assign_dataset_splits()` + `manager.run_jepa_pretraining()` + supervised phases.
- P/backend/control/ml_controller.py:165-178 (`run_full_cycle(context)`), P/core/session_engine.py:461-476 (teacher daemon `run_full_cycle()`), coach_manager `__main__` :1418-1419, jepa_train `__main__` :833-869, train.py `run_training` :271-302 (legacy supervised), R/tools/observe_training_cycle.py:344-351 (orchestrator jepa 1 epoch), R/tools/headless_validator.py:855 (init smoke).

6.2 Tests that pin current contracts (P/tests unless stated)
- test_metadata_dim_contract.py: METADATA_DIM==25 (:64-70); len(FEATURE_NAMES)==25 (:73-80); exact tuple order (:35-61, :83-96); FEATURE_NAMES[17]=="map_id" (:99-111; docstring claims rap_coach/trainer.py reads feature_vec[17] — grep finds no such read, stale claim); no duplicates (:114-121).
- test_feature_extractor_contracts.py (467 lines): output shape (25,) float32 (:20-39); METADATA_DIM 25 (:41-44); health/armor in [0,1] (:50-72); binary slots 2,3,5,6,7 ∈ {0,1} (:74-97); sin/cos range (:99-107); ak47→0.6, unknown→0.5, "weapon_" strip (:109-134); no NaN for empty/zeros/extremes (:137-176); slots 20-24 == 0.0 without context (:182-192) and >0 / ==1 with context (:194-212); batch shape (3,25) and batch==individual (:218-246); P3-A thread-local gate (:248-276); names count/unique (:279-299); D1/D2 throttle/gate tests (:302-467).
- test_dimension_chain_integration.py: METADATA_DIM==INPUT_DIM (:17-25), ==25 (:27-31), OUTPUT_DIM==10 (:33-37), default model accepts (1,25) (:39-51), jepa accepts (1,10,25) (:53-68), extract→model (:70-88), len(TRAINING_FEATURES)==25 (:90-95), len(MATCH_AGGREGATE_FEATURES)==25 (:97-102), HIDDEN_DIM==128 (:104-108), output last dim==10 (:110-124).
- test_coach_manager_tensors.py: both 25-lists lengths/uniqueness (:20-50); TARGET_INDICES in bounds (:52-62); `_prepare_tensors` None→0.0 not NaN (:65-124); tiers/confidence (:127-166); baseline defaults cover all aggregate features (:169-206).
- test_nn_config_reproducibility.py: INPUT_DIM==METADATA_DIM==25 (:121-125); JEPACoachingModel(INPUT_DIM, INPUT_DIM) encoder shape (2,10,·) (:136-146); seed/device tests (:27-118).
- test_data_pipeline_contracts.py: object vs dict input parity (:14-104); round_phase 800→0.0, 2000→0.33, 3500→0.66, 5000→1.0 (:106-143); map_id deterministic, distinct per map, 0.0 when None (:146-190).
- test_handoff_regressions.py WR44 (:251-294): raw time_in_round unclamped (130 s), warm-up → 0.0, vectorizer slot 20 saturates at 1.0.
- test_jepa_model.py: encoder (4,10,25)→(4,10,128) (:29-37); predictor (4,128) (:39-47); init num_experts 3, is_pretrained False (:49-57); forward_jepa_pretrain (4,128) (:59-71); forward_coaching (4,15,25)→(4,10) with/without role (:73-91); freeze/unfreeze (:93-108); contrastive loss finite ≥0 (:110-123); grads on context_encoder/predictor only (:125-142); integration/save-load (:145-190); VL subclass/forward_vl/concept probs/labeler/losses (:191-419); JEPA→VL migration: missing keys exactly concept_embeddings, concept_projector, concept_temperature, no unexpected (:420-465); factory "vl-jepa" type; 16 concepts (:516-532).
- test_jepa_training_pipeline.py (719 lines): fixtures at production shape input 25 / output 10 (:38-83); dataset returns context/target of metadata_dim, no overlap (:89-158); contrastive/negatives/EMA schedule/freezing (:160-352); save/load round trip, is_pretrained preserved (:355-402); train_jepa_pretrain runs/freezes/EMA-updates (:405-469); finetune runs/keeps encoders frozen (:471-514); user sequences padding (:516-559); `_MIN_TICKS_FOR_SEQUENCE >= 20`, `_MAX_TICKS_PER_SEQUENCE <= 10000`, INPUT_DIM==METADATA_DIM (:561-577); end-to-end smoke (:583-646); REPR-01 warning (:648-695); 26-RANGE-01 guard: 25-dim targets raise, 10-dim pass (:697-719).
- test_jepa_train_split_filter.py: pro loader selects TRAIN (uppercase + legacy lowercase), excludes VAL/TEST/NULL/ghost (:64-70); user loader (:72-75); ORM stores DatasetSplit NAME (:78-115).
- test_jepa_window_fetcher.py: windows of 11, single player, ascending unique ticks (:89-99); deterministic per seed (:101-104); seeds differ (:106-109); end-of-stream anchors dropped (:111-118).
- test_jepa_collapse_feed.py: `_log_embedding_diversity` → None at B=1, float>0.01 at B=16 (:37-46); detector aborts after two 0.0 epochs (:48-55); cross-window feed completes when healthy / aborts when collapsed / within-batch precedence (:58-116). PARTE_III:557 marks the first and last of these for retirement.
- test_jepa_insight_adapter.py: deltas→insights (:24-84); loader refuses missing/stale/head_trained False, arms on True (:86-291); `_checkpoint_extra_meta()` == {"head_trained": False} for jepa, map/view_resolution 64 for rap (:293-304); `_TARGET_FEATURES == FEATURE_NAMES[:10]` (:307-320).
- test_training_orchestrator_logic.py: model_type jepa→"jepa_brain", vl-jepa→"vl_jepa_brain", rap (needs ncps + USE_RAP_MODEL) → "rap_coach", invalid → ValueError "Unknown model type" (:21-77); patience 10 / best_val inf (:79-89); EarlyStopping semantics (:92-132); `_prepare_tensor_batch([])` → None (:135-148); `_fetch_batches` [] when manager empty (:150-163); `_neg_rng` seeded/reproducible (:166-194); `_resolve_tick_rate` warns "26-ORCH-02" and falls back to 64 (:197-229); `_fetch_round_stats_for_batch` uses item.round_number, no round-1 fallback, None when all miss (:232-305).
- test_training_orchestrator_flows.py (1111 lines): `_fetch_batches` passes windows through and calls `_fetch_jepa_windows(is_pro=True, split="train"/"val", seed=42(+epoch), n_windows=50_000//11 or 10_000//11, window_len=11)` (:372-419); JEPA batch keys and shapes context (1,10,25), target (1,1,25), negatives (1,5,25); None for 3, 6 or exactly 10 ticks; target follows context (:427-547); map-name resolution, advantage, tactical role, seed rotation, loss denominators, scheduler zero-step guard (:37-370, :555-720).
- test_training_loop_honesty.py: P3-C >30% → False (:35-46); 44 rows abort / 110 pass (:49-74); D-22 same-round kept / cross-round dropped via real SQL (:77-132); val loss uses learned τ (:135-170).
- test_training_abort_signal.py: run_training False on quality-gate abort, no data, P3-C; True on completion (:20-65).
- test_probe_batch_wiring.py: raw ORM rows unconsumable; prepared batch consumable; probe prep leaves `_neg_rng` untouched; unconsumable probe warns "unconsumable" (:59-100).
- test_checkpoint_normalizer_versioning.py: sidecar keys schema_version "v1", metadata_dim 25, 25 feature_names, heuristic_config.health_max (:42-56); round trip (:59-67); StaleCheckpointError on metadata_dim / feature_names / schema_version / missing names / corrupt JSON (:70-120); legacy no-sidecar loads with warning (:123-139); extra round-trips (:142-147); registry newline (:150-157); atomic rollback of both temp files (:160-181).
- test_model_factory_contracts.py: kwargs propagation (:135-162); checkpoint names default→latest, jepa→jepa_brain, vl-jepa→vl_jepa_brain, rap→rap_coach, role_head→role_head; unknown raises (:165-189).
- test_collapse_metrics.py: key set, collapsed/healthy thresholds, scale invariance, [B,T,D] flattening, N=1 → zeros; EMA drift (:29-85).
- test_dry_run_checkpoint_integrity.py (integration, CS2_INTEGRATION_TESTS=1): `--dry-run` writes no .pt; `--epochs 1` writes one (:94-122).
- test_persistence_stale_checkpoint.py, test_embedding_collapse_detector.py, test_tensorboard_collapse_logging.py, test_operator_stop_propagation.py, test_rap_training_dry_run.py, test_state_reconstructor.py, test_tensor_factory.py: names listed in my grep; they pin StaleCheckpointError/FileNotFoundError semantics, detector counting, `embed/*` scalars per epoch with a fixed probe, TrainingStopRequested propagation, RAP USE_RAP_MODEL gate, 32-tick 50%-overlap windows and 64×64 training config parity.

======================================================================
7. DEPENDENCIES
======================================================================
- .venv (Python 3.12.3): torch 2.13.0+rocm10.0.0 (hip 7.15), safetensors 0.8.0 (importable), numpy 2.4.3, pandas 2.3.3, polars 1.44.1, pyarrow 25.0.1, demoparser2 0.42.0, scikit-learn 1.8.0, sqlmodel 0.0.38, SQLAlchemy 2.0.49, tensorboard 2.21.0, huggingface_hub 0.36.2.
- Pins: R/requirements.in:51-52 `torch>=2.1.0,<3.0`, `numpy==2.4.3`, :45 `pandas==2.3.3`, :38 `demoparser2==0.42.0` (same in requirements.txt:11-22); R/requirements-lock.txt / requirements-lock-cpu.txt / requirements-dist.txt pin `torch==2.13.0+cpu`, `safetensors==0.8.0`, `polars==1.42.1`, `pyarrow==25.0.0`, `numpy==2.4.3`, `pandas==2.3.3`. safetensors is a transitive lock entry only (not in requirements.in/.txt); installed venv drifts from the locks on torch (+rocm vs +cpu), polars (1.44.1 vs 1.42.1) and pyarrow (25.0.1 vs 25.0.0).

======================================================================
INTERFACES V2 MUST RESPECT
======================================================================
1. v1 vectorizer untouched: FEATURE_NAMES order/25, `extract`/`extract_batch` shapes, round_phase/map_id/weapon_class encodings, slot-20 saturation, D1/D2/P3-A gate behaviour (tests in 6.2). PARTE_III §2.2 mandates `extract_v2` beside them.
2. nn/config: INPUT_DIM==METADATA_DIM==25, OUTPUT_DIM==10, HIDDEN_DIM==128 must stay true for the legacy models; v2 dimensions must come from the schema, not from these constants (PARTE_III:178).
3. ModelFactory: existing type strings and checkpoint names unchanged; new "jepa_v2" (checkpoint "jepa_v2_encoder", PARTE_III:410) must be added without altering `get_checkpoint_name` for the old types; TrainingOrchestrator must keep raising ValueError("Unknown model type ...") for unknown types and mapping jepa→"jepa_brain", vl-jepa→"vl_jepa_brain".
4. Orchestrator contracts: `run_training() -> bool` (quality gate, no-data, P3-C paths); `_prepare_tensor_batch([]) -> None`; JEPA batch dict shapes (1,10,25)/(1,1,25)/(1,5,25) and the None rules; `_fetch_batches` keyword signature to `manager._fetch_jepa_windows`; `_checkpoint_extra_meta()` == {"head_trained": False} for jepa; probe batch handed to `on_train_start` must be a dict with a tensor under "context" (or any tensor value) for `embed/*`; `manager._update_state(status, detail)` is the only progress channel.
5. persistence: sidecar v1 = {schema_version "v1", metadata_dim 25, feature_names, heuristic_config, extra}; v2 checkpoints must still write a sidecar that `_validate_loaded_meta` handles (PARTE_III:157 prescribes schema_version "v2" + schema_fingerprint + a `SchemaMismatchError(StaleCheckpointError)` branch) and keep hash-registry newline, atomic pair write, `load_nn` search order and FileNotFoundError/StaleCheckpointError semantics; `save_nn` serialises `state_dict()` only (no rich dict).
6. Window fetcher semantics: contiguous single-player rows, deterministic per seed, drop never pad (J-5, WR-53), D-22 (test_jepa_window_fetcher, test_training_loop_honesty).
7. Split machinery: `assign_dataset_splits` + DatasetSplit stored as NAME; any raw SQL must filter `UPPER(dataset_split)='TRAIN'` and `sample_weight > 0`.
8. collapse_metrics API (four keys, float values, zeros for N<2) and EmbeddingCollapseDetector(threshold, patience).update/reset.
9. TensorBoard collapse telemetry expects `model.context_encoder` (and optional `model.target_encoder`) attributes; a v2 encoder without these names silently disables `embed/*` (tensorboard_callback.py:265-267, :299-305).
10. Callback event signatures (training_callbacks.py:38-63); `on_train_end(final_metrics={best_val_loss, final_epoch, model_type, fallback_rate})`.
11. Insight adapter seam: `generate_jepa_insights(tick_window [T,25] | [1,T,25], maturity_state)` behind USE_JEPA_MODEL; `_TARGET_FEATURES == FEATURE_NAMES[:10]`; `head_trained` sidecar gate; CoachingInsight(player_name, demo_name, title, severity, message, focus_area). PARTE_III §9.1 replaces this with a `probes_calibrated` gate — the tests in test_jepa_insight_adapter.py:247-320 pin the current behaviour until then.
12. Entry-point chain: coach_manager.run_full_cycle → `_execute_training_phases` → `run_jepa_pretraining` must re-raise TrainingStopRequested (test_operator_stop_propagation); ML controller / session engine / ingest_pro_demos / batch_ingest all call the manager, not run_full_training_cycle.
13. Dry-run must never write a checkpoint; real run must (test_dry_run_checkpoint_integrity).
14. If RAP stays trainable: RAPCoachModel(metadata_dim=25, output_dim=10, use_lite_memory) forward(view_frame, map_frame, motion_diff, metadata (B,T,25), timespans) (P/backend/nn/experimental/rap_coach/model.py:17-95); RAPTrainer(model, lr=1e-4, t_max=100, accumulation_steps=4).train_step(batch, step_optimizer) → dict with "loss" (trainer.py:30-101); state_reconstructor output dict view/map/motion/metadata; TrainingTensorConfig 64/64 stamped in sidecar extra.
15. Name joins: any new label↔tick join must normalise (`strip().casefold()` per resolve_stored_name; PARTE_III:163 wants `normalize_player_name` in `backend/storage/naming.py`) and must not assume steamid.

======================================================================
SAFE TO DELETE / REPLACE (dead paths the docs condemn, with evidence)
======================================================================
- P/backend/nn/jepa_train.py: whole CLI path (`train_jepa_pretrain`, `train_jepa_finetune` — raises by design :632-641, `_jepa_pretrain_*`, `JEPAPretrainDataset`, loaders with hard-coded `_DB_PATH` :33-34). No production caller (only tests, docs). PARTE_III:423; PARTE_I J7 :430-453. `save_jepa_model/load_jepa_model` are not needed for the archived checkpoints (they are save_nn state_dicts — see 2.3).
- P/backend/nn/jepa_trainer.py: `train_epoch` (:362-437), `check_val_drift` (:439-468), `retrain_if_needed` (:470-528), the in-trainer `DriftMonitor` (:157-161) — no callers. `encode_raw_negatives` and `_augment_with_moco_queue` are live in the orchestrator path (2.2) — the docs condemn them as no-ops (J4/J14), not as unreachable; retire together with the InfoNCE objective, not as dead code.
- P/backend/nn/train.py `_train_jepa_self_supervised` (:67-179) and P/backend/nn/dataset.py `SelfSupervisedDataset` (:25-63): unreachable (config_name never "jepa"). PARTE_III:427.
- P/backend/nn/training_config.py (`TrainingConfig`, `JEPATrainingConfig`): imported only by R/tools/headless_validator.py:272 module list. P/backend/nn/ema.py `EMA`: only headless_validator.py:283, 1112, 1248. PARTE_III:425; PARTE_I:449.
- P/backend/nn/jepa_model.py: LSTM/experts/gate/`_sparse_moe`/`forward_coaching`/`forward_selective`/`_apply_role_bias` (head never trained: every orchestrated checkpoint carries head_trained False; adapter refuses it), `ConceptLabeler.label_tick/label_batch` (leakage), `VLJEPACoachingModel`/`forward_vl`/`vl_jepa_concept_loss` + `train_step_vl` (no entry point passes "vl-jepa"). PARTE_III:426; PARTE_I J8 :455-468, J9 :470. Keep `JEPAEncoder`/`forward_jepa_pretrain`/`jepa_contrastive_loss` only for the A/B baseline.
- P/backend/nn/coach_manager.py `TRAINING_FEATURES` (:57-92): verbatim duplicate of FEATURE_NAMES with no functional consumer (tests + headless validator only) — replace by importing FEATURE_NAMES; `MATCH_AGGREGATE_FEATURES` must stop sharing METADATA_DIM (D5) — its models (AdvancedCoachNN in model.py/train.py, Phases 2/3) are marked "ritirare" (PARTE_I:108, :689; PARTE_II §11.1).
- P/backend/processing/data_pipeline.py `ProDataPipeline`: instantiated (coach_manager.py:163) but `run_pipeline()` never called; scaler file absent. Dead.
- P/backend/nn/rap_coach/chronovisor_scanner.py: deprecated shim (:1).
- P/run_ingestion.py `_execute_rap_logic` (:202-228) with the literal `confidence=0.85` (:225): PARTE_III §9.3 (:528-530) removes it.
- "vl-jepa" wiring in factory (:60-66, :121-122) and orchestrator (:117-123, :676-684) once the VL head goes.
- P/models/checkpoint_hashes.json: 17 of 21 entries point at mounts that no longer exist (New Volume, /workspace, New Volume1, 7c51b2a8-…, WORK_RECOVERED) — data hygiene, not code.
- Tests to move to tests/legacy per PARTE_III:557 / PARTE_I App. E (:1154-1161): test_jepa_collapse_feed::{test_single_window_batch_is_unmeasurable_not_collapsed, test_within_batch_variance_still_takes_precedence}; test_jepa_model::{test_positioning_exposed, test_economy_wasteful_pistol_round, test_trade_isolated_low_kast, test_label_batch_2d, test_label_batch_3d, test_jepa_coaching_forward, test_jepa_coaching_with_role, test_forward_vl_output_shape, test_concept_probs_sum_to_one}; test_jepa_training_pipeline::{test_finetune_*, test_target_is_last_tick, test_context_target_no_overlap}; test_rap_coach::{test_advantage_gap, test_with_skill_vec, test_diagnose_shape}. Docs explicitly keep: test_contrastive_loss (A/B), test_freeze_unfreeze_encoders, test_model_save_load, test_jepa_window_fetcher.py, test_metadata_dim_contract.py (extended), all collapse_metrics tests (PARTE_I:1163).

Adjacent issues noticed (not acted on): (a) `--model-type all` exits 1 at the RAP phase with default settings (6.1); (b) R/tools/eval_harness.py:370-376 reads `r.team_economy`, `r.enemy_economy`, `r.teammates_alive`, `r.enemies_alive`, `r.team_side` on RoundStats rows — none of those columns exist (db_models.py:803-840), so that section can only return status ERROR via the broad except at :389; (c) test_metadata_dim_contract.py:99-111 justifies index 17 with a `feature_vec[17]` read in rap_coach/trainer.py that does not exist; (d) `_fetch_rap_windows` trains RAP on only the first 1,000 ticks of each player (coach_manager.py:971-992); (e) the current jepa_brain sidecar records best_val_loss 4.5e-07, consistent with the docs' degenerate-objective diagnosis (J2/J4).
