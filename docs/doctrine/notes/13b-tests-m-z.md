# Cluster 13b — tests/ (m-z)

Scope: every `Programma_CS2_RENAN/tests/test_*.py` whose basename starts with m..z
(84 files, ~17.1k lines). README.md read for orientation (IT/PT are translations of it;
not counted in the 84): it codifies the suite doctrine — "no mock data for domain logic:
real DB data or pytest.skip", "zero tolerance for synthetic data", GLOBAL_SEED=42
everywhere, `@integration` gated by CS2_INTEGRATION_TESTS=1, and a conftest venv guard
that exits pytest rc 2 outside the cs2analyzer venv unless CI is set. automated_suite/
and a-l files are cluster 13a's scope.

## Files read

- [x] test_manual_entry_normalization.py
- [x] test_map_manager.py
- [x] test_match_data_dangling_symlink.py
- [x] test_match_data_logger_fallbacks.py
- [x] test_match_data_no_implicit_migration.py
- [x] test_match_date_backfill.py
- [x] test_match_date_resolver.py
- [x] test_match_shard_schema_contract.py
- [x] test_meta_drift_attribution.py
- [x] test_metadata_dim_contract.py
- [x] test_model_factory_contracts.py
- [x] test_models.py
- [x] test_moe_sparse_gate.py
- [x] test_momentum_extended.py
- [x] test_movement_quality_tickrate.py
- [x] test_nn_config_reproducibility.py
- [x] test_nn_extensions.py
- [x] test_nn_infrastructure.py
- [x] test_nn_training.py
- [x] test_observability.py
- [x] test_onboarding.py
- [x] test_onboarding_training.py
- [x] test_operator_stop_propagation.py
- [x] test_parse_guard.py
- [x] test_parse_timeout_real.py
- [x] test_persistence_stale_checkpoint.py
- [x] test_phase0_3_regressions.py
- [x] test_playback_engine.py
- [x] test_player_knowledge_tickrate.py
- [x] test_pro_demo_base_no_home.py
- [x] test_pro_demo_miner.py
- [x] test_profile_service.py
- [x] test_qt_core.py
- [x] test_rag_knowledge.py
- [x] test_rap_coach.py
- [x] test_rap_training_dry_run.py
- [x] test_rap_window_fetcher.py
- [x] test_rating_components_contract.py
- [x] test_reset_pro_data_dryrun.py
- [x] test_role_vocabulary_guard.py
- [x] test_round_stats_enrichment.py
- [x] test_round_utils.py
- [x] test_run_worker_claims.py
- [x] test_screens_no_gui_thread_db.py
- [x] test_security.py
- [x] test_security_hardening.py
- [x] test_services.py
- [x] test_session_engine.py
- [x] test_set_secret_degrade.py
- [x] test_single_pytest_config.py
- [x] test_skill_assessment.py
- [x] test_skill_model.py
- [x] test_spatial_and_baseline.py
- [x] test_spatial_engine.py
- [x] test_state_reconstructor.py
- [x] test_storage_manager_archive.py
- [x] test_strategy_label_migration.py
- [x] test_tactical_features.py
- [x] test_tactical_frame_widgets.py
- [x] test_team_side_resolution.py
- [x] test_telemetry_client.py
- [x] test_temporal_baseline.py
- [x] test_tensor_factory.py
- [x] test_tensorboard_collapse_logging.py
- [x] test_tensorboard_integration.py
- [x] test_tensorboard_run_layout.py
- [x] test_theme_live_restyle.py
- [x] test_tick_rate_propagation.py
- [x] test_tick_rate_ssot.py
- [x] test_tools_regressions.py
- [x] test_trade_kill_detector.py
- [x] test_trade_timing.py
- [x] test_training_abort_signal.py
- [x] test_training_callbacks.py
- [x] test_training_orchestrator_flows.py
- [x] test_training_orchestrator_logic.py
- [x] test_ui_harness.py
- [x] test_ui_smoke.py
- [x] test_utility_economy_extended.py
- [x] test_v1_blockers.py
- [x] test_vector_index_contracts.py
- [x] test_verify_all_safe_gate.py
- [x] test_winner_normalization.py
- [x] test_z_penalty.py

## Invariants encoded by tests

### ingestion

- **test_pro_demo_base_no_home.py** (33) — **F-0008**: `config.get_pro_demo_base` never falls back to `$HOME` ($HOME always exists, so a $HOME default silently pointed pro-demo sweeps at the whole user profile; the baseline run rglobbed home and fed an arbitrary .dem to the parser → F-0006). Unset → in-project path containing "Programma_CS2_RENAN"; missing configured path → still never $HOME; existing configured path honoured.
- **test_run_worker_claims.py** (87) — **F-0015**: `run_worker._recover_stale_tasks` uses the **P4-B SSOT** stale threshold (default 30 min), not the old hardcoded 5-minute copy that re-queued demos mid-parse (duplicate processing): 10-min-old processing task NOT recovered, 45-min-old → queued; `_release_claim` requeues a claimed-but-skipped pro task instead of leaving it stuck in 'processing' (rw:82-88). NB comment: run_worker imported inside tests because the entry script self-inserts into sys.path (S5) and breaks `import tools` at collection.
- **test_trade_kill_detector.py** (367) — `data_sources/trade_kill_detector`. TradeKillResult ratios zero-safe. `assign_round_numbers` via searchsorted side="right" (boundary tick → next round), index-preserving. `detect_trade_kills`: trade = enemy killed within `trade_window` (default 192 ticks) of a teammate's death in the SAME round; different rounds never linked; unknown players skipped; roster/columns missing → empty result; trade detail carries trade_tick/killers/victims/response_ticks/round; custom window honored (192 detects a 150-gap, 100 does not). `get_player_trade_stats`: all roster players initialized to zeros, avg_response_ticks correct. **R4 LOW (26-TICK)**: the 64-tick-baked `TRADE_WINDOW_TICKS` export MUST NOT exist — the window is computed from the actual demo rate; the seconds-based SSOT `TRADE_WINDOW_S == 3.0` remains (tkd:360-367).
- **test_trade_timing.py** (192) — **GAP-03**: response_ticks was computed but never persisted to `PlayerMatchStats.avg_trade_response_ticks`. `_integrate_trade_kills` accumulates `trade_response_ticks_sum`/`trade_response_count` per trader and marks victim `was_traded`; zero/negative response_ticks (simultaneous tick, out-of-order data) still count the trade but NOT the average (count stays 0 → 0.0); `aggregate_round_stats_to_match` emits the average (75.0 from 100+50, 80.0 from a 3-trade round); no trades → 0.0 schema default; analyze_demo_trades exceptions swallowed with a "skipped" warning (tt:172-193).
- **test_parse_guard.py** (65) — **F-0006**: `data_sources/parse_guard.is_parse_error` absorbs pyo3 `PanicException` (BaseException subclass matched by NAME — live evidence: "range end index 16 out of range for slice of length 12" flew through every `except Exception`), ordinary exceptions, and demoparser2's OWN direct-Exception-subclass error; KeyboardInterrupt/SystemExit/GeneratorExit and unknown BaseExceptions ALWAYS propagate (pg:38-46). `run_ingestion._parse_demo_header_meta` on panic → defaults ("de_unknown", 64.0); KeyboardInterrupt escapes.
- **test_parse_timeout_real.py** (31) — **F-0013**: `demo_parser._run_with_parse_timeout` must RETURN (False, None) promptly on timeout with the hung worker orphaned — the old executor with-block did `shutdown(wait=True)` and blocked on the unkillable parse thread, making "prevent indefinite hangs" illusory. Real-thread test asserts elapsed <5s against a 30s-wedged worker (pt:11-27); fast path returns (True, result).

### storage

- **test_phase0_3_regressions.py** (574) — P6-02 umbrella for MASTER_REMEDIATION_PLAN Phase 0-3 fixes ("a fix without a regression test is incomplete"). **P0-04**: StateManager.get_state creates exactly ONE CoachState row under 100 concurrent calls / 10 threads; double-checked-locking singleton factory; invalid global status rejected, state stays "Paused" (p03:548-575). **P0-05**: backup via SQLite Online Backup API passes integrity_check with full row count. **P0-06**: connection closed via try/finally even when WAL checkpoint raises (pattern-level test, not against production code — weak). **P1-01**: EarlyStopping fires at plateau epoch 5 AND source-inspection asserts "EarlyStopping"/"early_stopper" appear in train.py (grep-style wiring check). **P1-03**: JEPAEncoder(latent 256) mean pairwise cosine sim of 20 random inputs < 0.9 (collapse probe at init). **P1-04**: `_prepare_splits` refuses < MIN_TRAINING_SAMPLES (returns 4×None) instead of train=val; explicit val bypasses check. **P1-05**: negative sampling excludes positive index (pure-python model of the algorithm — tautological, does not exercise production code). **P3-01**: `PlayerRole` canonical in core.app_types (ENTRY/AWPER/SUPPORT/LURKER/IGL/FLEX/UNKNOWN); role_classifier and role_features must re-export the SAME enum object; str-enum lowercase values for DB. **P3-02**: pro baselines use per-round KPR/DPR (0.75), never ×24 per-match totals — scale mismatch made z-scores absurd. **P3-07**: correction_engine treats float / (z,raw) tuple / [z,raw] list deviations identically.

- **test_storage_manager_archive.py** (105) — STOR best-effort archive contract: `archive_demo` must NEVER raise — a failed archive must not fail an otherwise-successful ingestion. 2026-07-21 batch run violated it: `target_dir.mkdir(exist_ok=True)` sat OUTSIDE the try and without parents=True, so a stale PRO_DEMO_PATH (unmounted disk) raised FileNotFoundError AFTER all tick/event data landed, marking a fully-ingested demo failed (sma:1-10). Missing parent chain → demo stays put, no raise; happy path moves into `ingested/`. **OI-8**: $HOME is never a demo root — unset/HOME-configured/missing DEFAULT_DEMO_PATH all fall back to the managed DATA_DIR/demos; valid path respected; `DEMO_ARCHIVE_PATH` override honored (sma:58-105).
- **test_strategy_label_migration.py** (142) — **GAP-09**: alembic migration `c3d4e5f6a7b8` (down_revision b2c3d4e5f6a7) adds nullable indexed `strategy_label` to coachingexperience; reversible round-trip (upgrade adds column+`ix_coachingexperience_strategy_label`, downgrade drops both) exercised on in-memory SQLite via MigrationContext; upgrade is IDEMPOTENT (exists-guarded); ORM field Optional; `docs/strategy_taxonomy.md` must exist and contain all five families (setpiece/economy/rotation/playbook/individual) (slm:136-142).

- **test_match_date_resolver.py** (58) — **OI-2** resolution ladder (`backend/ingestion/match_date_resolver.resolve_match_date`): yyyymmdd token → `filename_date` (UTC); HLTV `2023-` prefix → coarse `filename_year` (Jan 1); invalid 8-digit token falls through; file mtime rung when a .dem path is given; honest floor `ingested_at`; years <2000 rejected. `CHRONOLOGICAL_SOURCES` contains filename_date+filename_year but NOT file_mtime or ingested_at (mdr:50-54) — only filename evidence counts as real chronology for the anti-leak split.
- **test_match_shard_schema_contract.py** (87) — **POV-TBL-01** guard after the POV-RAP-FIX incident (Sprint A 2026-04-26): ORM pointed at `match_tick_state` while ingestion wrote `matchtickstate` → every RAP training batch silently dropped. Pins `MatchTickState.__tablename__ == "matchtickstate"` (no underscore!), `match_event_state`, `match_metadata`; fresh shard contains exactly those; ORM write + raw sqlite read hit the same table (shard:66-87).
- **test_meta_drift_attribution.py** (116) — `baselines/meta_drift.MetaDriftEngine.calculate_spatial_drift` (R4 HIGH 2026-07-16). Historic bug: JOINed `PlayerTickState.match_id` against `PlayerMatchStats.id` (unrelated ID spaces) with no map filter → per-map drift computed from random cross-map ticks. Fixed contract: attribution via demo_name with WR-76 suffix strip + `map_name` filter. Shifted centroids on mirage → drift > 0; identical nuke centroids → exactly 0 (contamination probe); unknown map → 0; single era → 0.
- **test_models.py** (74) — db_models default values: PlayerMatchStats avg_kast/rating 0.0; PlayerTickState health/armor 0; PlayerProfile role "All-Rounder"; IngestionTask status "queued" retry 0; CoachState status "Paused", cpu_limit 0.5, pro_ingest_interval 1.0; ServiceNotification severity default "ERROR", unread; ProTeam/ProPlayer link via hltv team_id. Pure smoke — weakest file in the storage cluster.

- **test_match_data_dangling_symlink.py** (134) — a dangling `match_data` symlink must raise `MatchDataUnavailableError`, NOT self-repair (2026-07-26 incident: `MatchDataManager.__init__` unlinked the broken symlink and invented an empty dir — app started with zero shards, no error, and the only pointer to the data was destroyed). Doctrine stated in docstring: "a dangling symlink is positive evidence that storage was configured and is currently unreachable — that is an error, not a prompt to invent empty storage"; merely-absent path (fresh install) IS created (sym:108-134). Symlink survives untouched and un-repointed; error message names both link and dead target; singleton factory caches nothing broken. Whole module skips honestly on Windows without symlink privilege (F-0003 can_symlink probe, sym:23-44).
- **test_match_data_no_implicit_migration.py** (130) — constructing MatchDataManager must NEVER relocate shards. 2026-07-26 incident: a path-comparison-triggered "one-time migration" `shutil.move`d the production shard corpus twice in one afternoon (45 shards ENOSPC mid-move, then 313 shards, root disk 42→2 GB free) because the trigger was config-dependent, not a persisted marker (mig:1-14). Spy asserts `migrate_match_data` never called on construction; `warn_if_shards_in_legacy_location` warns "will NOT be moved automatically" (count returned) and is silent when active==legacy/empty/missing; explicit `migrate_match_data()` still moves, skipping files already at destination.
- **test_match_data_logger_fallbacks.py** (30) — **F-0012**: fallback branches of `_default_window_ticks` must warn and return `MEMORY_WINDOW_SECONDS*64`; the old bare `logger.warning` raised NameError (module only defines `_logger`) from exactly the paths meant to keep RAP/POV fetching alive on sparse metadata. tick_rate=0 or lookup failure → 64; valid 128 used directly.
- **test_match_date_backfill.py** (130) — **OI-2** chronology backfill (`tools/backfill_match_dates.py`, loaded by file path). Dry-run writes nothing, reports "Upgradable rows: 2"; `--apply` upgrades ONLY weak sources: year-prefix demo → `filename_year` (date 2023-01-01), yyyymmdd → `filename_date`; `ingested_at` with no filename evidence stays; real `filename_date` never downgraded (bf:53-68). HLTV proevent name-containment rung (`_hltv_event_date`) resolves unique event matches. Ingestion wiring: `run_ingestion._save_player_stats` carries match_date + match_date_source through; default stays honest `ingested_at` (bf:119-130).

### nn/jepa

- **test_training_orchestrator_flows.py** (1108 lines) — the master contract for `backend/nn/training_orchestrator.TrainingOrchestrator`. Locks in:
  - `_resolve_map_name` ladder: metadata (auto `de_` prefix) → demo-name substring (8 known maps) → `de_mirage` fallback, with metadata cache and C-1/TASKS#60 "warn once per distinct demo" (flows:122-144).
  - `_compute_advantage` ∈ [0,1], ~0.5 balanced, alive-count/HP/equip/bomb weighted; weights `_ADV_W_ALIVE+_ADV_W_HP+_ADV_W_EQUIP+_ADV_W_BOMB == 1.0` exactly (flows:949-958); dead players excluded; bomb helps T/hurts CT; empty player list safe.
  - 10 tactical roles indices 0-9 contiguous (`ROLE_SITE_TAKE..ROLE_PASSIVE_HOLD`, flows:960-975); heuristics: equip<~500→SAVE, CT+bomb_planted→RETAKE, teammates ≥2000u away→LURK, visible enemy ≤~500u→ENTRY_FRAG vs ~1500u→AGGRESSIVE_PUSH, crouching CT→ANCHOR, teammates ≤~400u→SUPPORT.
  - **R4 CRIT batch contract**: each JEPA batch is ONE contiguous single-player window of `_JEPA_CONTEXT_LEN+1 = 11` ticks; `sample_size` is a per-epoch row budget → `n_windows = sample_size // 11` (flows:372-419). Defaults train=50_000/val=10_000 rows (B2, flows:755-784).
  - **B1 seed doctrine**: train seed = GLOBAL_SEED(42)+epoch (epoch 5→47); val seed pinned at 42 regardless of epoch (flows:403-419, 594-650); `_run_epoch_loop` refetches train windows every epoch with seeds [43,44,45].
  - **V-1/J-5**: <11 ticks → batch returns None (never zero-padded — "zero vectors encode physically impossible game states"); exactly 10 ticks → None (target would overlap context → misleading low loss); target = tick index 10 immediately after context[0:10] (flows:499-547). Shapes: context (1,10,METADATA_DIM), target (1,1,METADATA_DIM), negatives (1,5,METADATA_DIM).
  - **R4 HIGH loss denominator**: epoch loss averages over PROCESSED batches only, never len(batches) — skipped batches previously made val losses non-comparable, corrupting best-checkpoint/early-stop (flows:658-689); all-skipped epoch → 0.0.
  - **TASKS#59**: scheduler.step() skipped (with loud TASKS#59 warning) on epochs with `_last_train_batch_count == 0` (flows:697-744).
  - **B3**: patience default 10, configurable; `best_val_loss` restored from checkpoint sidecar `jepa_brain.pt.meta.json` `extra.best_val_loss` (missing sidecar/extra → +inf fresh start); saves persist it back; **F-0029**: pretrain saves stamp `head_trained: False` (flows:818-903). EMA `set_total_steps(max_epochs, ceil(batches/accum))` (B3.3).
  - **W1.6/NN-H-03**: negative-pool warmup→cross-match transition logs "negative pool warmed up" exactly once (flows:982-997).
  - **W2.5/DR-17**: train-vs-val drift monitor opt-in via `DRIFT_RETRAIN_ENABLED`; per-epoch warnings each drifted epoch, CRITICAL escalation exactly once, `_check_train_drift` never raises (flows:1000-1058).
  - **F-0024**: `EmbeddingCollapseDetector` (P9-02 hard stop) must fire from the PRODUCTION `_run_epoch_loop` via `_last_epoch_variances` — it was previously only wired inside `JEPATrainer.train_epoch`, which the orchestrator never calls; collapsed variance raises `EmbeddingCollapseError` within detector patience; healthy variance completes all epochs (flows:1061-1108).

- **test_metadata_dim_contract.py** (121 lines) — **P-X-01 ContractGuard, deliberately rigid**. Hard-pins `METADATA_DIM == 25` and the exact 25-name `FEATURE_NAMES` tuple order (health..team_economy) from `feature_engineering/vectorizer.py` — "the single binding contract between the feature pipeline and every NN input layer" (~331 modules) (dim:1-24). A change requires, in the same commit: updated expected constants, a `migration_manifest_<ts>.md`, and an Alembic migration. Also pins `FEATURE_NAMES[17] == "map_id"` because Pillar III's σ_z per-axis position loss in rap_coach/trainer.py reads `feature_vec[17]` by index — moving it is silent corruption (dim:99-111); no duplicate names.

- **test_model_factory_contracts.py** (225 lines) — Bug #2 fix (commit 3120c4b): `ModelFactory.get_model` MUST raise ValueError on unknown/typo/empty/None/case-mismatch model types (previously the `else` branch silently returned legacy TeacherRefinementNN) (mfc:96-132). Valid map: default→TeacherRefinementNN, jepa→JEPACoachingModel, vl-jepa→VLJEPACoachingModel, rap→RAPCoachModel (ncps+hflayers gated), role_head→NeuralRoleHead. Dimension kwargs propagate (lstm.input_size); default input_dim = METADATA_DIM. Checkpoint names as in test_nn_infrastructure; every `TYPE_*` constant produces a model. NOTE: skip set mentions a "rap-lite" type (mfc:215) not in the expected checkpoint map.

- **test_training_abort_signal.py** (48 lines) — **F-0043**: `run_training()` returns False on EVERY abort path (quality-gate fail, no data) and True only on completion — the CLI exits 3 on abort instead of reporting success with zero training done (T-DIAG root cause) (abort:1-3).

- **test_training_callbacks.py** (307 lines) — callback framework + TensorBoardCallback. TrainingCallback hooks all no-op by default; CallbackRegistry: **NN-L-13** same instance not added twice (cb:74-84), different instances fine; `fire` dispatches to all with kwargs, isolates errors (one bad callback never blocks others), unknown event no-op; `close_all` isolates close() errors. TensorBoardCallback: importable without tensorboard; when unavailable warns "TensorBoard unavailable" EXACTLY once per run — "a silent no-op is the bug" (cb:230-242); `CS2_TB_STRICT=1` upgrades degraded run to RuntimeError (cb:244-254); default is degrade-not-crash; `_active == _TB_AVAILABLE`; integrates with registry using a real stub module because registry.fire would swallow AttributeError from the histogram path (cb:280-307).

- **test_tensorboard_collapse_logging.py** (108) — collapse telemetry doctrine: `embed/*` scalars (std_mean, std_min, effective_rank, cosine_offdiag_mean, ema_drift) computed from a **FIXED probe batch sampled once at train start** — varying batches would conflate model change with input change, making the epoch-over-epoch trend "the entire signal" uninterpretable (tbc:1-7). Fixed probe + untrained model → identical values across epochs (1e-6); no probe or model without encoders → skipped silently, no embed/ tags.
- **test_tensorboard_integration.py** (183) — one SummaryWriter per run: TensorBoardCallback + EmbeddingProjector share ONE writer → exactly one events.out.tfevents.* file; event file readable back with embed/std_mean + embed/effective_rank tags. Probe shape tolerance: dict {"context": ...}, raw tensor, and sequence batches all work (the two training paths hand over different shapes); incompatible feature width disables the probe QUIETLY after exactly one warning (tbi:139-151). Production chain `run_full_training_cycle._build_callbacks` must contain TensorBoardCallback + MaturityObservatory + EmbeddingProjector, projector sharing `tb.writer` ("Layer 4 must be in the production chain"); `--tb_logdir` defaults to None so `build_run_dir()` per-run scoping isn't defeated (tbi:154-184).
- **test_tensorboard_run_layout.py** (47) — run directories unique per run and device-stamped: run id matches `^\d{8}T\d{6}Z-(cpu|cuda|rocm)$` under RUNS_DIR/model_type — "without the device in the run id a cheap smoke run is indistinguishable from a real one in the dashboard" (tbr:1-6); `resolve_device_tag` ∈ {cpu, cuda, rocm}, cpu when no GPU.

- **test_nn_config_reproducibility.py** (146 lines) — `backend/nn/config`. GLOBAL_SEED == 42; `set_global_seed` makes torch+numpy deterministic, sets `cudnn.deterministic=True` / `benchmark=False`; full training step reproducible to 1e-6. `get_device` cached, cpu/cuda only. **Dimension chain**: `INPUT_DIM == METADATA_DIM == 25` (cfg:121-125); JEPACoachingModel instantiates from config constants and context_encoder preserves (B,seq).

- **test_nn_extensions.py** (384 lines) — role head + config constants + datasets + AdvancedCoachNN/ModelManager. NeuralRoleHead: 5→5 softmax (sums to 1), log_softmax ≤0; constants ROLE_INPUT_DIM=ROLE_OUTPUT_DIM=5, FLEX_CONFIDENCE_THRESHOLD=0.35, MIN_TRAINING_SAMPLES=20, LABEL_SMOOTHING_EPS=0.02, ROLE_OUTPUT_ORDER len 5 (ext:60-71). `extract_role_features_from_stats`: rounds_played 0/missing → None; missing optional stats default. nn/config constants: BATCH_SIZE=32, INPUT_DIM=METADATA_DIM, OUTPUT_DIM=10, HIDDEN_DIM=128, LEARNING_RATE=0.001, EPOCHS=50, WEIGHT_CLAMP=0.5 (ext:141-159); `_INTEGRATED_GPU_KEYWORDS` includes uhd/iris. Datasets: ProPerformanceDataset coerces to float32; SelfSupervisedDataset len = N−context−prediction, raises ValueError "too short". AdvancedCoachNN: accepts 2D and 3D input → (B,out) tanh-bounded [−1,1]; role_id out-of-bounds clamps with warning; 1-D input raises "at least 2 dims". CoachNNConfig defaults hidden=128/experts=3/lstm_layers=2/dropout=0.2/layer_norm. ModelManager.save_version writes .pt + sidecar .json; `TeacherRefinementNN is AdvancedCoachNN` alias.

- **test_nn_infrastructure.py** (381 lines) — EMA, ModelFactory, persistence, SuperpositionLayer. EMA: shadow initialized from model, update moves it, apply_shadow/restore roundtrip exact, state_dict returns clones (mutation-safe), lower decay moves faster, 500 updates converge (atol 0.15). ModelFactory: types default/jepa/vl-jepa/rap/role_head → checkpoint names latest/jepa_brain/vl_jepa_brain/rap_coach/role_head (infra:190-201); unknown raises ValueError; kwargs pass through. Persistence: `get_model_path` global vs per-user dir; save/load roundtrip exact; **`StaleCheckpointError` on architecture mismatch** (Linear 10→20, infra:255-265); **NN-14**: missing checkpoint raises FileNotFoundError, no silent fresh model (infra:267-280). SuperpositionLayer (context-gated MLP, context_dim=METADATA_DIM): gate activations stored per forward, statistics dict {mean/std/sparsity/active_ratio/top_3/bottom_3}, no-forward → {"error":"no_activations_recorded"}; **NN-24**: `gate_sparsity_loss` must require grad and backprop into `context_gate.weight` (infra:340-350); different contexts → different outputs; tracing interval default 100.

- **test_nn_training.py** (190 lines) — EarlyStopping + TrainingController. EarlyStopping: improvement must beat `best - min_delta` STRICTLY (exactly min_delta does NOT count, nn_tr:71-77); defaults patience=10, min_delta=1e-4; reset() clears counter/best/should_stop and is reusable. TrainingController: `_cosine_similarity` (zero vector → 0.0); `_extract_features` → 6-dim normalized `(val−baseline)/scale` (kills baseline 15/scale 10, adr 75/25); missing attrs raise AttributeError (stats contract enforced, not defaulted); `_DEFAULT_MAX_DEMOS_PER_MONTH == 10`, `MIN_DIVERSITY_SCORE == 0.3` (nn_tr:186-190).

- **test_tick_rate_propagation.py** (76) — **C1.2 (AUDIT 26-TICK-01/26-TICK-03)**: per-demo tick rate into the RAP path. `TrainingOrchestrator._resolve_tick_rate` reads MatchMetadata.tick_rate from the shared cache (no extra DB round-trip), defaults 64 when absent/None, REJECTS out-of-range (valid window [32,256] per **DS-07**) — absurd 0/9999 "would poison the memory/flash windows and the LTC dt downstream". `_rap_compute_timespans` scales the **LTC dt** by per-item tick_rates: 64 ticks @64 t/s = 1.0s, @128 = 0.5s — otherwise "the Liquid Time-Constant integrates on a 2x-wrong scale" (trp:44-75).
- **test_tick_rate_ssot.py** (161) — **26-NORM-01 / C11** (owner decision 2026-07-17). (1) `core.tick_rate.resolve_tick_rate` ladder metadata → header → **None sentinel, never a fabricated default**; out-of-range metadata falls through; validity window [32,256]; `DEFAULT_TICK_RATE == 64` is "the one sanctioned literal". (2) **AST-based repo sweep**: NO production module (Programma_CS2_RENAN + tools, tests excluded) may bind a bare 64/64.0 to a tick-rate-ish name — assignments, AnnAssign/Field(default=), function defaults, call kwargs, getattr 3-arg, dict keys all scanned; a seeded violation self-test proves the ban bites (trs:131-161). Same systemic-contract spirit as test_design_token_references.

- **test_persistence_stale_checkpoint.py** (229) — Bug #1: `load_nn` originally set `model._stale_checkpoint = True` on architecture mismatch and NO caller ever checked it — models silently ran with RANDOM WEIGHTS (psc:1-16). Fixed contract: dimension mismatch raises `StaleCheckpointError` (match "is incompatible") which (a) contains the checkpoint path, (b) chains the original RuntimeError via `__cause__`, (c) is a RuntimeError subclass so legacy handlers still catch it (psc:188-193). Successful load → weights exactly match checkpoint; missing checkpoint → FileNotFoundError (NN-14, not "return model unchanged" as the stale module docstring line 14 still claims); corrupted/empty checkpoint re-raises (NN-14).

- **test_tensor_factory.py** (985 lines) — `backend/processing/tensor_factory` (Player-POV perception). Locks in:
  - `TensorConfig` defaults map_res=128/view_res=224/sigma=3.0/fov=90°/view_distance=2000; `TrainingTensorConfig` shrinks to 64/64 for training (tf:109-137).
  - Three tensor types, all (3,res,res) float32, values in [0,1], no NaN/Inf, empty ticks → zeros, unknown map → zeros. Legacy mode (no PlayerKnowledge): map Ch1=team accumulation, Ch2=player marker. POV mode: map Ch0=teammates+own pos, Ch1=visible enemies + last-known enemies at `decay_factor` intensity, Ch2=utility zones + bomb marker (tf:230-287).
  - View tensor: Ch0=FOV mask, Ch1=danger zone (1−accumulated FOV; more angles covered → strictly less danger, tf:333-344), POV Ch1=entities, Ch2=utility. Motion tensor: Ch0=trajectory trail (last TRAJECTORY_WINDOW ticks), Ch1=velocity (stationary <0.02, 4 units=max speed >0.5), Ch2=crosshair/yaw delta with 360° wraparound (350°→10° reads as 20°, tf:834-842). Legacy motion (no map metadata): uniform channels dx∈[−1,1], dy∈[−1,1], speed∈[0,1].
  - `_world_to_grid` origin→(0,0), center→~(64,64) at res 128; out-of-bounds negative; extreme coords silently ignored. `_normalize` divides by max, zero array unchanged. `get_tensor_factory` is a thread-safe singleton (10-thread race test, tf:734-758).
  - **R4 CRIT `_tick_yaw`** (tf:855-945): canonical yaw source is `view_x`; `yaw` attribute is legacy fallback; default 0.0. Historic bug: `getattr(tick,"yaw",0.0)` returned 0.0 for every real PlayerTickState row (DB rows have view_x/view_y, no `yaw` col) → FOV cone faced east for ALL training samples; the old `_make_tick` helper set both attrs so this suite couldn't see it. New `_make_db_shaped_tick` omits `yaw`; asserts east≠west FOV and 60° flick lights crosshair channel.
  - **R4 HIGH FOV orientation** (tf:948-985): grid rows grow south (Y flip), so FOV angle must negate row delta; yaw=90 (world north) cone mass must sit ABOVE player row, 270 below, 0 east, 180 west. Historic bug: every training sample had a vertically mirrored cone.

### nn/rap/moe

- **test_rap_coach.py** (633 lines) — full 7-layer RAPCoachModel contract (`backend/nn/rap_coach/{perception,memory,strategy,pedagogy,model,communication,trainer}`). Whole module skipped unless `ncps`+`hflayers` installed (rap:18-19). Locks in:
  - Perception: ResNetBlock identity vs projection shortcut; RAPPerception fuses view/map/motion (3×64×64 each) → (B,128); AdaptiveAvgPool2d handles any spatial size; no NaN.
  - Memory (LTC+Hopfield): input (B,seq,128+METADATA_DIM) → combined (B,seq,256), belief (B,seq,64), passable hidden state. **NN-MEM-01 (R4 MED)**: Hopfield stays BYPASSED until `notify_optimizer_step()` — arbitrary forwards without an optimizer step keep `_hopfield_trained=False`; the old forward-count gate (2 forwards → active) fired before any weight update under grad accumulation and this test used to codify the bug (rap:160-184).
  - Strategy: **RAP-AUDIT-09** context_dim = METADATA_DIM(25)+belief(64) = 89 (rap:193-194); default 4 experts, gate softmax sums to 1; **RAP-AUDIT-08** top-2 sparse routing (weak assert: only checks pred non-zero, rap:226-237).
  - Pedagogy: value head (B,1); skill_vec changes value; `calculate_advantage_gap` = actual−pred. CausalAttributor: 5 concepts incl. "Positioning","Utility"; attribution (B,5); optimal_view_delta changes Crosshair Placement (index 1); `_detect_utility_need` ∈ [0,1].
  - Full model forward returns exactly {advice_probs (B,10), belief_state (B,5,64), value_estimate (B,1), gate_weights (B,4), optimal_pos (B,3), attribution (B,5), hidden_state}; sparsity loss scalar >0 with gate, 0.0 with None; seed-deterministic; `heuristic_config.context_gate_l1_weight` honored.
  - Communication: confidence ≤0.5 → advice suppressed (returns None); skill tiers low/mid/high (2/5/9) all produce strings; default skill_level=5.
  - RAPTrainer: `train_step` returns {loss, sparsity_ratio, loss_pos, z_error}; loss decreases over 10 steps at lr=1e-3; **`z_axis_penalty_weight == 2.0`** (rap:566-572); `compute_position_loss` on pure-Z error 1.0 → z_err 1.0, loss > 1.5 (2.0×1.0 penalty dominates); without target_pos both loss_pos and z_error are 0.0; **RAP-AUDIT-10** CosineAnnealingLR scheduler exists.

- **test_rap_training_dry_run.py** (259 lines) — RAP path gating + LTC fix presence. (1) `TrainingOrchestrator(model_type='rap')` with `USE_RAP_MODEL=False` MUST raise ValueError matching "USE_RAP_MODEL" (dry:79-95); enabled → model_name `rap_coach`, lr 5e-5, and **TrainerClass module must contain `experimental.rap_coach`** (not the legacy nn/rap_coach tree, dry:97-109). (2) Integration smoke gated by `CS2_INTEGRATION_TESTS=1`: run_training with empty `_fetch_jepa_windows`/`_fetch_rap_windows` exits at the insufficient-data guard, proving RAP branch entered (R4 MED 2026-07-17: `_fetch_rap_ticks` no longer exists; mock model must yield fresh param iterators, dry:149-162). (3) **RAP-LTC-FIX** (commit 374f3fc): experimental memory.py must contain the "RAP-LTC-FIX" marker string and the LTC cell's `_ode_solver` must be the `_patched_ode_solver` closure (unsqueezes 1-D elapsed_time to (B,1) — ncps upstream fails for batch_size != state_size) (dry:196-255).

- **test_rap_window_fetcher.py** (127 lines) — `CoachTrainingManager._fetch_rap_windows` contract (R4 CRIT + WR-76, 2026-07-16), real in-memory SQLite. Historic bugs: `order_by(tick)` alone interleaved ALL players inside a window (position deltas/LTC timespans crossed players); legacy `PlayerMatchStats.demo_name` = `stem.dem_Player` never matched `PlayerTickState`'s bare stem → zero windows (WR-76). Pins: suffix stripped (rapwf:94-100); every window single-player (102-108); ticks strictly ascending, no duplicates (110-116); 120 ticks/player @ window 50 → exactly 4 windows of 50 (118-122); runs < window_size silently skipped (124-127).

- **test_training_orchestrator_logic.py** (305 lines) — Tier-2 orchestrator logic. Valid model_types: jepa→`jepa_brain`, vl-jepa→`vl_jepa_brain`, rap→`rap_coach` (rap needs USE_RAP_MODEL); unknown type raises ValueError "Unknown model type" — noted contrast: ModelFactory does NOT reject unknown types (logic:67-77). Defaults patience=10, best_val_loss=inf. Production `EarlyStopping(patience,min_delta)`: counter increments on non-improvement, resets on improvement, fires at limit. Empty batch → `_prepare_tensor_batch` returns None ("never all-zero tensors"), empty fetch → []. `_neg_rng` seeded and reproducible across instances. **26-ORCH-02 (W1.2)**: `_resolve_tick_rate` fallback paths (metadata lookup failure, cache explosion) return default 64 AND log a warning containing "26-ORCH-02" — fallbacks must be loud (logic:197-229). **R4 MED (G-01/26-TICK)**: `_fetch_round_stats_for_batch` keys concept labels on the item's REAL `round_number` — never the old `tick/(64*115)` estimate, never round-1 fallback (missing round → None per item; all-miss → None) (logic:232-305).

- **test_state_reconstructor.py** (127) — `processing/state_reconstructor.RAPStateReconstructor`. `segment_match_into_windows`: 50% overlap (step = seq_len//2), all windows exactly seq_len contiguous ticks, trailing remainder intentionally discarded, <seq_len ticks → 0 windows; default seq_len 32, metadata_dim = METADATA_DIM. **F-0026 training/inference resolution parity**: GhostEngine's inference tensor factory and RAPStateReconstructor must both use `TrainingTensorConfig` 64×64 — inference at the 128/224 default silently mismatched the trained perception (sr:104-127).

- **test_moe_sparse_gate.py** (197 lines) — **GAP-10 (MOE-02)** for `backend/nn/model.{_topk_sparse_gate, AdvancedCoachNN, CoachNNConfig}` (the classic coach NN, distinct from RAPStrategy's MoE). Locks in:
  - `_topk_sparse_gate(logits,k)`: exactly K non-zeros per row; selected weights renormalized to 1.0 via softmax over top-K logits, NOT clamped full softmax (preserves relative top-2 weighting, moe:5-8); k>E clamps to E (dense degenerate); k=1 → one-hot argmax.
  - **Zero-gradient invariant** (moe:70-102): non-routed experts get grad norm exactly 0.0 on their first Linear — verified by capturing the actual routing from the same eval-mode forward that produced the gradients (dropout=0 so routing inspection matches).
  - Entropy: sparse top-2 mean row entropy < dense softmax over same logits AND ≤ log(2); **Plan §7 acceptance**: post-warmup gate entropy < 0.5 nats, simulated with a ~3-nat top1-top2 logit gap → softmax ≈ (0.95,0.05) (moe:130-149).
  - Checkpoint signature: state_dict must have flat `gate.weight`/`gate.bias`, NOT legacy `gate.0.weight` — confirms `StaleCheckpointError` on legacy load is intentional (moe:162-171). num_experts=1 works, gate_top_k=1. Role bias survives sparse gating: role expert gets weight 0.5 even if outside top-K; output differs with role_id (moe:184-197).
  - Forward output (B,4) bounded in [−1,1] (tanh).

- **test_z_penalty.py** (152 lines) — vertical awareness from `core.spatial_data.{compute_z_penalty, classify_vertical_level}` + FeatureExtractor slot 15. Locks in: z-cutoffs Nuke=−495, Vertigo=11700; penalty 0.0 on boundary, linear ramp |z−cutoff|/500 saturating at 1.0, strictly monotone away from boundary (z:62-67); single-level maps (mirage/dust2/inferno) and unknown maps → 0.0. `classify_vertical_level`: upper/lower/transition with inclusive ±50 default band (−445 in, −444 out), custom band honored; single-level/unknown → "default". FeatureExtractor integration: `vec[15]` of the METADATA_DIM(25) vector carries z-penalty; >0 deep in Nuke upper, 0.0 on mirage or map_name=None (z:123-152).

### analysis

- **test_playback_engine.py** (194, UI-adjacent core) — `core.playback_engine` + `core.demo_frame`. Speed clamps to [0.25, 8.0]; angle interpolation handles 360° wrap (350→10 @0.5 ≈ 360). **R4 MED**: Inf yaw entered `while diff > 180: diff -= 360` and hung the UI clock callback forever (inf−360==inf) — `_interpolate_angle` must sanitize ALL non-finite values, not just NaN (pe:143-149); **DF-01 extension**: `PlayerState.__post_init__` sanitizes non-finite yaw → 0.0, not only x/y/z (pe:151-170).
- **test_player_knowledge_tickrate.py** (79) — **C1 (AUDIT 26-TICK-01)**: PlayerKnowledgeBuilder windows derive from per-demo tick rate. At 64 they equal legacy: flash 128t (2.0s), smoke 1152t (18s), molotov 448t (7s), memory cutoff 480t (7.5s), decay tau 160 (2.5s); at 128 exactly double; default 64; explicit overrides win over derivation (pk:16-46). **R4 HIGH sound window**: the 1s audible window must use the builder's rate — 100 ticks heard at 128 (0.78s) but rejected at 64 (1.56s); previously a `tick_rate=64` parameter default won silently because the call site never forwarded it (pk:48-79).
- **test_pro_demo_miner.py** (227) — `knowledge/pro_demo_miner.ProStatsMiner`. Archetype classification (rating 1.25/impact 1.20 → "Star Fragger"; kast 0.75 → "Support Anchor"); `_generate_player_knowledge` yields exactly 3 categories (pro_baseline, opening_duels, clutch_performance); integration test mines a real seeded ProPlayerStatCard from hltv_metadata.db with tracked cleanup. **R4 MED units**: `opening_duel_win_pct` is a RATIO on disk (0.46-0.77); knowledge text once compared `> 52` (percent literal no ratio could satisfy) so EVERY pro read "Disciplined opener." with "0.5%" — pins 0.60 → "Aggressive entry fragger."+"60.0%", 0.47 → "Disciplined opener."+"47.0%" (pm:197-227).

- **test_tactical_features.py** (84) — `analysis/utility_economy`. UtilityAnalyzer effectiveness scores pinned numerically (overall 0.63; smoke 0.278, flash 0.667, molotov 0.571, HE 1.0). EconomyOptimizer: pistol/full-buy/eco/force-buy/half-buy actions with weapon counts; **R4 HIGH (2026-07-16)**: round 13 under MR12 is the SECOND PISTOL round (money resets at half switch) — the old test codified "full-buy" there (tf2:71-78); true overtime round 25 with 10k → full-buy.
- **test_team_side_resolution.py** (70) — **F-0025**: RAP training resolves the REAL side in every vocabulary. PlayerTickState has no `team` column, so `getattr(item, "team", "CT")` was a constant "CT": every T-side sample got CT-perspective advantage labels and T-side tactical roles were UNREACHABLE (tsr:1-7). `core.team_codes.normalize_team`: "TERRORIST"→T, "counter-terrorist"→CT, numeric 2→T/3→CT, spectator/None→None. `_resolve_item_side` ladder: item.team → knowledge.own_team (shard vocabulary) → documented "CT" last resort. Advantage with shard vocabulary: TERRORIST sample dominated by CTs reads <0.5, CT same state >0.5; TERRORIST entry reaches ROLE_SITE_TAKE.
- **test_utility_economy_extended.py** (146) — utility_economy structure contracts: EconomyOptimizer exposes WEAPON_COSTS, FULL_BUY_THRESHOLD > FORCE_BUY_THRESHOLD; UtilityAnalyzer has a PRO_BASELINES entry for every UtilityType; ALL-ZERO throws (incl. rounds_played=0) never ZeroDivisionError — every effectiveness ∈ [0,1], economy_impact 0.0; UtilityReport shape: overall_score float, utility_stats covers every UtilityType with typed fields, recommendations list of str, economy_impact ≥ 0.
- **test_skill_model.py** (205) — `backend/nn/rap_coach/skill_model.SkillLatentModel` (DUPLICATE of processing/skill_assessment — same class names, separate module, tested against the REAL pro baseline, not a mocked one). Low stats → low axis scores per axis pair (mechanics=accuracy+avg_hs, positioning=survival+kast, utility=blind metrics, timing=opening+aggression, decision=clutch+impact). **R4 MED (2026-07-16)**: `utility_blind_time=0.0` is a REAL demo-derived value (player blinded nobody) and must rank LOW — the old contract conflated 0.0 with missing via `not val` and hid bad performance behind the 0.5 neutral fallback; None stays the unavailable sentinel → 0.5 (sm:60-85). Pro-level stats → curriculum level ≥7 with all 5 axes populated; level clamps 1-10; tensor (1,10) one-hot at level−1.
- **test_spatial_and_baseline.py** (215) — mixed spatial/baseline invariants. `distance_with_z_penalty`: Z diff < threshold → plain 3D distance; above → XY_dist + z_diff×factor (300×2.0+100=700). `NicknameResolver._fuzzy_match`: exact/case-insensitive/typo matches at 0.8 Levenshtein threshold ("simple"→"s1mple"; "monsey" ~0.67 → None). `validate_and_trim`: strict raises on out-of-band ADR; trim clamps to [0,200]. **Maturity soft gate**: DEMO_TIERS CALIBRATING [0,50) conf 0.5, LEARNING [50,200) conf 0.8, MATURE [200,∞) conf 1.0; boundaries contiguous; every tier has confidence (sab:80-124) — the coach's self-declared confidence scales with corpus size. **R4 MED landmark integrity**: hand-written landmark tables contradicted map_callouts.py (Mirage T-Spawn projected to pixel_x=6, extreme left edge) — landmarks now DERIVED from map_callouts; every landmark of every map must project inside the [0,1] radar frame (≥10 checked); Mirage T-Spawn pinned to the RIGHT half nx>0.5 (sab:127-180). **F-0019**: headshot_pct is ratio-scaled — strict validation rejects 37.0; trim mode self-heals percent-scale (37.0→0.37) like kast.
- **test_spatial_engine.py** (62) — `core.spatial_engine.SpatialEngine` coordinate math: dust2 (pos −2476/3239, scale 4.4) and mirage (−3230/1713, 5.0) origins map to normalized (0,0); formula `norm = (world−pos)/(scale·1024)` with Y inverted; normalized→pixel scaling for 1080p/4k; unknown map → safe (0.5,0.5) center fallback; `world_to_pixel` shortcut.

- **test_temporal_baseline.py** (257) — `baselines/pro_baseline.TemporalBaselineDecay` (Fusion Plan Proposal 11). Exponential decay half-life 90 days: today/future → 1.0, 90d → 0.5, 180d → 0.25, ≥365d clamps to MIN_WEIGHT 0.1; deterministic. **Timezone doctrine** (tb:110-123): SQLite round-trips datetimes NAIVE while the default reference is AWARE UTC — that exact pairing raised TypeError in production and "silently downgraded every temporal baseline to the legacy fallback"; both mixed pairings must compute, never raise. Weighted baseline: std floor 0.01; recent cards pull the mean; None last_updated → weight 0.5. `detect_meta_shift`: >5% mean change flagged, ≤4% not, old mean 0 skipped. `get_temporal_baseline` always returns a valid dict even without DB. `_metric_to_baseline_key` maps HLTV names → monolith names (kpr→avg_kills etc.), unknown passthrough.
- **test_role_vocabulary_guard.py** (55) — **F-0030**: `RoleClassifier.classify` must REFUSE stats dicts carrying none of the role-vocabulary keys instead of fabricating a confident role. Live incident: MATCH_AGGREGATE dicts (kd_ratio only) scored 0 in every affinity except the IGL balanced-KD bonus and normalization made it "IGL (100% confidence)" (rvg:1-7). Vocabulary-less dict → confidence 0.0, role FLEX; empty dict → 0.0; vocabulary-bearing dict must still score > 0.
- **test_momentum_extended.py** (134) — `analysis/momentum.MomentumTracker`: init neutral (multiplier 1.0, streak 0/"neutral", not tilted/hot); win streak monotonically raises multiplier >1, loss streak lowers <1; 12 alternating W/L stays within [0.90,1.10]; `predict_performance_adjustment` = exactly multiplier×base_rating, always finite, incl. MULTIPLIER_MIN/MAX bounds (mom:103-134).
- **test_movement_quality_tickrate.py** (168) — **AUDIT 26-TICK-02 / backlog C4**: `movement_quality` hardcoded 128 tick/s, doubling every real-time window on 64-tick GOTV demos. Pins: `_seconds_to_ticks` linear in rate, clamps to min 1 tick; `DEFAULT_TICK_RATE == core.constants.TICK_RATE == 64` (mq:46-49); **rate-equivariance property test**: identical real-time scenario (4s hold then >300u move, no enemy) yields the identical mistake set ["position_abandoned"] at 64 AND 128 tick; 2s hold flagged at neither (mq:100-133). AnalysisOrchestrator `_resolve_tick_rate`: prefers data-carried `tick_rate` column, falls back to canonical 64 on garbage column or unknown demo.

### coaching

- **test_vector_index_contracts.py** (54) — `knowledge/vector_index.VectorIndexManager._stack_uniform` (R4 MED batch 10), FAISS-independent. Ragged embeddings (100-dim hash fallback rows mixed with 384-dim SBERT) used to make np.stack raise through the whole retrieval path; now non-modal-dimension rows are DROPPED LOUDLY — warning names the skipped id, modal rows survive; empty input → (0,)/(0,0) (vic:25-54).
- **test_round_stats_enrichment.py** (353, ingestion) — `round_stats_builder` (Fusion Plan Phase 1). Formulas pinned: trade_kill_ratio = trade_kills/kills; was_traded_ratio = traded/deaths; wallbang/thrusmoke/noscope/blind kill pcts over total kills; HE/molotov damage and smokes per round; flash_assists summed; opening_duel_win_pct = openings won/(won+lost) — and the KEY IS ABSENT when the player had no opening duels (never a fabricated 0, rse:186-192); zero kills → 0.0 ratios; per-player filtering strict. **Q1-01** blind metrics: `utility_blind_time` sums blind_time_on_enemies; `utility_enemies_blinded` is the DISTINCT UNION across rounds (same enemy twice counts once); zero-safe; per-player isolated. **Schema coupling**: every enrichment key must be a PlayerMatchStats model field (rse:255-273). **CS2 patch 14160 (July 2026)**: round_end's 'round' prop shifted +1 (24-round match emits 2..25) — `_build_round_boundaries` renumbers rounds ORDINALLY from sorted tick order, matching round_context.py, else every per-round join misaligns by one; unsorted events keep winner paired to its tick; **H-18**: round i starts at round i−1 end_tick+1 (rse:300-354).
- **test_winner_normalization.py** (35, ingestion) — **F-0016**: `round_stats_builder._normalize_winner` handles every known dtype shape: registry-documented ints 2→T/3→CT (and 3.0), strings ("terrorist", " Counter-Terrorist ", "3"), None/"draw"/NaN → None. The old str()-cast compared "3".upper() == "CT" — never true — so `round_won` was silently always-False on int-emitting demos; `_build_round_boundaries` carries the normalized winner.
- **test_verify_all_safe_gate.py** (71, tools) — **F-0039**: the "safe" runner (`tools/verify_all_safe.py.is_safe_to_run`) must never schedule a tool whose BARE invocation mutates live data. Audit census found 13 such tools slipping past the six original prefixes; rebuild_monolith's delete-first phase + the runner's 120s timeout-kill = pro stats left empty (vas:1-8). All 13 census members must be gated (repair_*/flag_ghost/purge/mine_*/populate_*/rebuild_monolith/observe_training_cycle/ingest_pro_demos), plus interactive/long-running tools; 9 genuinely-safe or dry-run-default tools must STAY scheduled.
- **test_v1_blockers.py** (460) — the V1 production-readiness sprint umbrella. Coaching level visibility: traditional mode emits a "Coaching complete ... Level 4" notification to the UI; COPER timeout emits a WARNING "timed out" (v1:78-172). AnalysisOrchestrator: `_record_module_failure` notifies the user only after 3 CONSECUTIVE failures of a module ("Momentum ... 3 times"), single failure stays silent (v1:178-220). Toast severities: all 4 levels configured; INFO auto-dismisses (ms>0), CRITICAL requires manual dismissal (ms==0) — mirrors test_qt_core's no-caption assertion. CoachingService helpers duplicate ExperienceBank's `_health_to_range` thresholds (80/40); `_baseline_context_note` renders "20% below" from stats vs pro mean, empty on missing data; `_infer_round_phase` delegates. Exceptions hierarchy (CS2AnalyzerError base). Design tokens: 3 themes, invalid → CS2 fallback, toast colors per theme, spacing/radius > 0. Splash: 520×320 pixmap, status updates don't raise. Error codes registry non-empty (weaker duplicate of test_observability's checks).
- **test_rag_knowledge.py** (289, @integration) — `knowledge/rag_knowledge`. KnowledgeEmbedder: deterministic embeddings, no NaN, dim > 0. KnowledgePopulator: `add_knowledge` persists TacticalKnowledge with a JSON embedding of exactly embedding_dim floats. KnowledgeRetriever: retrieves by semantic query; map_name and category filters honored; **usage_count increments on retrieval** (rag:196-211). `generate_rag_coaching_insight(stats, map_name)` returns non-empty string containing map/coaching context. F9-20 hygiene: all fixtures touch ONLY `_TEST_RAG_`-prefixed rows, never production data.
- **test_round_utils.py** (309) — `knowledge/round_utils.infer_round_phase` thresholds: <1500 pistol, 1500-2999 eco, 3000-3999 force, ≥4000 full_buy; missing key → 0 → pistol; NON-dict input → "full_buy" fallback (ru:58-62 — inconsistent with empty-dict → pistol). ExperienceContext: query string embeds map/side/phase/NvN/position/"damaged health" (full health omitted); `compute_hash` = first 16 hex chars of sha256 over "map:side:phase:position-or-unknown", deterministic, sensitive to each component. ExperienceBank helpers: health ≥80 full / 40-79 damaged / <40 critical; action→focus map (pushed→aggression, held_angle→positioning, scoped_hold→aim, crouch_peek→movement, used_utility→utility, rotated→game_sense, unknown→positioning); `_infer_action` default: attacker "pushed", victim "held_angle"; cosine similarity zero-vec→0; `_infer_round_phase` delegates to round_utils.

- **test_rating_components_contract.py** (253) — the `rating_*` column SCALE contract. Doctrine: PlayerMatchStats `rating_*` columns carry RAW HLTV 2.0 components (rating_survival = 1−dpr, rating_kast = kast ratio, rating_impact = dpr-aware impact, rating_kpr = kpr, rating_adr = adr); baseline normalization happens ONLY inside the `rating` aggregate. History: aggregate_match_stats_sql once wrote baseline-normalized ratios while demo_parser wrote raw — same column, two meanings, every downstream Z-score vs pro_baseline silently corrupted for tool-written rows (rc:8-15). Pins THREE writers to the SSOT `rating.compute_rating_components`: (1) at formula-baseline inputs components equal the INPUTS, not 1.0 ratios; survival/kast explicitly ≠ normalized variants (rc:67-77); (2) demo_parser `_apply_hltv2_columns` vectorized replica matches SSOT row-by-row to 1e-9, and **impact_rounds is NOT aliased to rating_impact** (canonical semantics: share of rounds with ≥1 kill in [0,1], R4; rc:127-139); (3) `tools/aggregate_match_stats_sql._build_player_match_stats` persists SSOT values (dpr 0.625 → raw survival 0.375, never 0.375/0.317≈1.18). **W3 lockstep**: `tools/repair_rating_scale.py` (stdlib-only for WSL) duplicates `BASELINE_*` constants and the formula — both pinned equal to the SSOT (rc:208-253).

### services

- **test_profile_service.py** (138) — ProfileService guards: missing steam/faceit API key or empty steam_id → {"error": ...}, never a network call; `_parse_steam_response` tolerant of missing response/players ("Player not found") and missing fields (None nickname/avatar); `_apply_profile_fields` copies fields but NEVER overwrites `id` (prof:124-138).

- **test_security.py** (152) — repo-wide hygiene sweeps over production files (excludes tests/tools/venv): no hardcoded api_key/password literals (regex-based, mock_-prefixed vars exempt); `.env` and `database.db`/`*.db` in .gitignore ("`*.db-wal` does NOT cover .db"); no .pem/.key/.crt anywhere on disk (".gitignore only prevents NEW additions"); no bare `eval(`/`exec(` (method calls .exec()/.execute() exempt); `core/integrity_manifest.json` valid with non-empty 64-hex SHA-256 `hashes` dict (feeds the RASP boot check); no `print("DEBUG...")`; no `subprocess shell=True` (sec:141-152).
- **test_security_hardening.py** (267) — AUDIT §9 HIGH fixes; "if any of these regress, the pre-LLM and pre-DDL trust boundaries collapse" (sh:10). **BE-03** `_sanitize_llm_context`: strips ASCII control chars (NUL/BEL/ESC/DEL) but preserves \n/\t and unicode letters; caps at 300 chars (custom max_len honored). **BE-03 brace escape**: a poisoned insight containing `{x}` must survive `SYSTEM_PROMPT_TEMPLATE.format(...)` as literal text, never KeyError (sh:60-85). **DB-01** `_assert_safe_identifier`/`_assert_safe_col_type`/`_assert_safe_default_literal` in match_data_manager: whitelist `[A-Za-z_][A-Za-z0-9_]*` identifiers; col types limited (VARCHAR(255)/NUMERIC(10,2) ok, ENUM/OBJECT/injection rejected); default literals only numbers/NULL/TRUE/FALSE/quoted strings (CURRENT_TIMESTAMP and subselects rejected). **BE-01/BE-06** `_SAFE_BACKUP_LABEL_RE`: `[a-z0-9_-]` style ≤64 chars, rejects traversal/space/quote/NUL. **DB-06**: PRAGMA foreign_keys=ON must be issued per connection — the two tests re-implement the handler pattern rather than exercising the real DBManager listener (weak: guards the pattern, not the wiring; sh:230-267).

- **test_observability.py** (395) — logger_setup + error_codes. JSONFormatter fields ts/lvl/mod/thread/msg (+exc_type/exc_msg/traceback only when exc_info); thread-local correlation IDs (12-char auto-gen) injected by `_CorrelationFilter` into JSON as `cid`; `CS2_LOG_LEVEL` env override (invalid → INFO); `configure_log_level` retargets all cs2analyzer loggers; get_logger idempotent (no duplicate handlers); tool loggers write `logs/tools/<name>_*.json`; ErrorCode registry: every member has code/severity/module/description/remediation, no duplicate codes; `log_with_code` prefixes `[LS-01] ...`; retention deletes only *.log older than max_days, tolerates missing dir. **F-0011 single-handler architecture** (obs:338-396): children have NO handlers, exactly one FileHandler on the `cs2analyzer` root, `CS2_LOG_ROLE=daemon` → per-process `cs2_analyzer_daemon.log`, foreign logger names are re-parented under cs2analyzer.
- **test_services.py** (98) — service-layer smoke: CoachingService.generate_new_insights graceful on empty deviations (None or list); AnalysisService singleton, check_for_drift returns dict for unknown player; VisualizationService.plot_comparison_v2 with seeded DB data yields PNG buffer >100 bytes; CoachingDialogueEngine exposes is_available/start_session/respond.
- **test_session_engine.py** (476) — `core/session_engine` daemon lifecycle. `_cleanup_zombie_tasks`: 10-min-old 'processing' → 'queued' (implies `_ZOMBIE_THRESHOLD_SECONDS` ≤ 600), 1-min-old untouched, non-processing statuses ignored, empty DB safe, all zombies reset; constant bounded 60..1800 (se:463-472). `_check_retraining_trigger`: fires at initial 10-sample bootstrap; **R4 HIGH (2026-07-16)**: with last_trained=0 the old growth formula collapsed to `pro_count >= 0` and fired on ONE sample — the previous test CODIFIED the bug ("even pro_count=5 triggers"); cold start below 10 must wait (se:232-242); ≥10% growth triggers (111/100 yes, 105/100 no). `_commit_trained_sample_count`: **R4 MED** missing CoachState row must be CREATED (previous silent no-op left last_count 0 → teacher daemon re-entered cold-start retrain every cycle, se:290-308). `_get_current_baseline_snapshot` → {} on error; `_check_meta_shift` returns new baseline, old one on error. `signal_work_available` sets the event; `_monitor_stdin` sets shutdown on "STOP" or EOF.
- **test_telemetry_client.py** (46) — client↔server contract (**R4 HIGH ×2, 2026-07-16**): client sent `player_id` while the server's MatchTelemetry schema requires `player_name` (every request 422'd), AND treated only HTTP 200 as success while the endpoint declares 202 — success was UNREACHABLE end-to-end (tc:1-8). Payload is validated by constructing the REAL `backend.server.MatchTelemetry` pydantic model from the captured JSON; 202 → True, 422 → False.
- **test_onboarding.py** (87) — `backend/onboarding/new_user_flow.UserOnboardingManager` stage machine: 0 demos → AWAITING_FIRST_DEMO, 1-2 → BUILDING_BASELINE, ≥3 → COACH_READY; status invariants `demos_required==1`, `demos_recommended==3`, `coach_ready == (uploaded>=1)`, `baseline_stable == (uploaded>=3)` (onb:44-63); cache invalidation per-user and global safe.
- **test_onboarding_training.py** (156) — TrainingController math (F9-09/F9-02 anti-fabrication review documented in the docstring: synthetic stats OK for pure-formula tests). `_extract_features` z-centering: baseline player (kills 15, deaths 15, adr 75, hs 0.4, util 20, opening 0.5) → zero vector; good player → kills+ deaths− others+; bad player inverse (deaths NOT sign-inverted in the formula — direction semantics live in the consumer). Cosine similarity 1/0/−1/zero-vec→0. Real-DB integration: diversity score ∈ [0,1] (skips if <2 rows); `MAX_DEMOS_PER_MONTH == 10`.
- **test_set_secret_degrade.py** (29) — **F-0007**: `config.set_secret` degrades like get_secret — keyring backend failure returns False (never raises out of the UI save path), routing `save_user_setting` to the **FE-04 plaintext fallback** in user_settings.json; the value must actually land on disk (ssd:16-29).
- **test_single_pytest_config.py** (43, tools/infra) — **F-0044**: exactly ONE pytest config. A shadow `Programma_CS2_RENAN/pytest.ini` used to win rootdir resolution for scoped invocations, silently dropping strict-markers and the marker registry for single-file dev runs. Asserts the shadow file does not exist AND a subprocess `--collect-only` run of a scoped path reports `configfile: pytest.ini` with rootdir at repo root (spc:22-43).
- **test_skill_assessment.py** (353, analysis) — `processing/skill_assessment.SkillLatentModel`. 5 skill axes (mechanics/positioning/utility/timing/decision). `calculate_skill_vector` vs pro baseline: at-baseline player ≈0.5 on every axis, +2σ > 0.8, −2σ < 0.3; all-None stats → default 0.5 ×5; PARTIAL stats → only axes with data appear (no fabricated axes, sa:158-166); scores clipped [0,1], zero stats no NaN. **P-SA-01-2**: baseline metrics with std=0 or std<0 are SKIPPED (no div-by-zero). **P-SA-01**: percentile = sigmoid(1.702·z) — approximates Gaussian CDF within 1.5% over z∈[−2,2] (verified vs scipy norm.cdf, sa:262-269). `get_curriculum_level`: empty/zeros → 1, ones → 10, mid → 4-6, always [1,10], monotone in skill. `get_skill_tensor`: one-hot (1,10) float32, hot index = level−1.
- **test_operator_stop_propagation.py** (73) — **F-0032**: `TrainingStopRequested` must PROPAGATE out of `run_jepa_pretraining`, `run_rap_cycle` and `run_full_cycle` so MLController's graceful stop path is reachable. Pre-fix guards said `except StopIteration: raise` — an exception check_state() stopped raising at F5-16 — so operator Stop fell into broad `except Exception`, logged "Training Failed", started the NEXT phase, and reported a crash (stop:1-8). Stop must never be misreported as "Cycle Failed"; ordinary RuntimeError still reports Cycle Failed without raising.

### UI

- **test_screens_no_gui_thread_db.py** (68) — **F-0038**: no Qt screen method touches the DB on the GUI thread. The three historical breach sites (profile `_save`, tactical chronovisor wiring, wizard `_finish`) must run DB work through `Worker(QRunnable)`; pinned structurally (extracted DB functions are staticmethods `_ensure_profile_row`/`_resolve_match_id_for_cm`) and via an AST doctrine sweep: `get_db_manager` may appear in `*_screen.py` ONLY inside those two sanctioned staticmethod names (sndb:54-68).

- **test_qt_core.py** (1104) — the PySide6 frontend's core contract suite (offscreen QPA, no pytest-qt). i18n: known key → "Macena CS2 Analyzer", unknown key falls back to the key itself, `set_language("pt")` works, unknown lang rejected, en.json loaded. Screen contracts: all 14 screen modules importable; every screen class has callable `on_enter`; 12 simple screens construct with parent=None; SettingsScreen needs ThemeEngine. Worker: result/error/finished signals all delivered cross-thread (drained via processEvents). AppState._apply: emits only on change (diffing), same payload twice → zero re-emits, None payload no-op, notifications forwarded as (severity, message). ThemeEngine: THEME_NAMES == {CS2, CSGO, CS1.6}; QPalette derives from design tokens (Window=surface_base, Highlight=accent_primary); rating_color green >1.1 / red <0.9; rating_label thresholds (1.25 Excellent, 1.15 Good, 1.00 Average, 0.80 Below Avg); `engine.chart_bg == CS2_TOKENS.chart_bg` read from token SSOT, not hard-coded (qt:386-396 documents a prior CI break from a hard-coded hex). Typography role sizes come from tokens; weight override preserves size/family; mono keeps Mono family; unknown role → body. Component library: ProBadge side property; StatBadge rating variant uses rating_color; EmptyState 64px icon well + ghost link (cb AND signal); Toast "auto · 5s" caption, CRITICAL never auto-dismisses (no caption row); ProgressRing presets (48/64/80/128); ToastContainer MAX_VISIBLE eviction + sizeHint refit. ChatPanel: bubble anatomy with meta footnote, system messages render as caption not bubble, suggestion chips emit text, submit trims + clears + never emits empty, status header Online/Offline with token colors. Shared primitives (DriversList/TipBox/NumberedStep/DbRecordCard/MonoFooter) text roundtrips; MapTile fill = min(rating/1.5, 1) clamped; MetricBarRow frac clamps [0,1]; DeltaChip hides on zero/None AND on values that FORMAT to zero (+0.00 must not claim an arrow, qt:812-835). ProComparison pure functions: 8 radar axes in [0,100], missing axes → neutral 50/50 midpoint never spikes, one-sided keys skipped pairwise; `_h2h_winner` epsilon rule incl. None → no winner; frame-15 fixture outcome vector pinned; style archetypes (support/entry/anchor/damage/balanced) via i18n. Animations: MACENA_UI_ANIMATIONS=0 kill-switch sets values immediately (returns None); the LIVE count-up runs in a SUBPROCESS because a completed QVariantAnimation poisons Qt's global animation driver on Windows offscreen (reproduced 5/5 in-suite; qt:998-1044). Stepper: geometry formula pinned, labels must match step_count (ValueError), navigation clamps.

- **test_tactical_frame_widgets.py** (186) — tactical viewer frames 13/14. `load_map_zones`: mirage ships ≥8 normalized zones (all boxes inside [0,1], labeled), majors exactly {A, B, MID}, "mirage" prefix fallback, unknown map → [] (graceful degradation). TimelineWidget star hit-test: hit at peak_tick x seeks START tick; misses off-star and in caption strip; accepts scanner objects, dicts, and empties. **29.5 glyph kinds**: mistake→star, clutch→diamond, play→circle (case/whitespace tolerant), anything unknown → star; legend covers every mapped kind. **Locked Decision 8**: `_divergence_rows` renders ONLY what the ghost payload carries — always 6 rows, absent/malformed payloads → em-dash + neutral verdict, bare scalar → neutral, missing value keeps verdict with em-dash (tfw:142-187).
- **test_ui_harness.py** (119) — offscreen screenshot harness contracts. **i18n key parity**: en/it/pt JSON files must expose IDENTICAL key sets (a missing translation is a bug, uh:11-21). **Bundled-font contract**: 10 TTFs (Inter/Space Grotesk/JetBrains Mono weights) verified by parsing the TTF name table directly — Qt offscreen on Windows mis-reports family names via QBasicFontDatabase, so files are the truth (uh:24-76). `ThemeEngine.register_fonts` sets `_fonts_registered`. Subprocess run of `tools/ui_screenshot.py --screens home,match_history --themes CS2` must exit 0 and produce PNGs > 20KB each.
- **test_ui_smoke.py** (139) — the runtime regression net the screenshot harness can't provide: REAL MainWindow + every real screen composed exactly like `app.main()` minus backend (daemons/SBERT/polling intentionally not started — "the UI layer must never require them to stay alive", us:10-13). Walks every screen's on_enter/on_leave; injects `ui_fixtures` into match_history so painted paths run; live theme switch across all THEME_NAMES asserts `accent_primary` lands in qapp.styleSheet(); language round-trip en→it→pt→en; sidebar collapse/expand + three window resizes. `MACENA_UI_ANIMATIONS=0` for deterministic end states. Skipped ONLY on CI+Windows (GitHub Windows runner crashes natively hosting the embedded QWebEngineViews; Ubuntu leg is deploy truth) — matches the MEMORY.md local-gate trap note (us:22-34).
- **test_theme_live_restyle.py** (69) — theme staleness (CP0 decision #4): instance-styled FilterChip/StatusChip must restyle on a LIVE theme switch via the module-level theme relay — pre-fix they kept construction-time colors until recreated; `ThemeEngine.apply_theme` must ping `get_theme_relay().theme_changed` (tlr:61-69). StatusChip's theme-sensitive colors live on the child label, not the frame.
- **test_map_manager.py** (104) — `core.map_manager.MapManager` + `core.asset_manager.AssetAuthority` (kivy-gated, skipped in CI without kivy). Map path resolution absolute + existing; unknown map → checkered fallback SmartAsset (`is_fallback=True`, theme "fallback", path contains "FALLBACK_CHECKERED"); de_dust2 scale == 4.4; all 8 core competitive maps have real PNG assets; name normalization ("mirage", "de_mirage.dem", "maps/de_mirage" → same asset); dark theme variant distinct file; deprecated MapAssetManager alias still works. NOTE (possibly stale): imports kivy though the frontend is Qt — this suite may be permanently skipped.

### tools

- **test_manual_entry_normalization.py** (49) — **F-0042**: `tools/user_tools.cmd_manual_entry` normalizes percent-scale KAST/HS inputs ("78", "42") to ratio (0.78, 0.42) BEFORE writing `is_pro=True` rows — pro_baseline consumes them unfiltered, so a raw 71.0 poisons the baseline at ~100x (man:1-3,44-49). Drives the real interactive CLI via patched `input`.

- **test_reset_pro_data_dryrun.py** (28, tools) — **W3 (CP0 #6)**: `tools/reset_pro_data.py` bare invocation is DRY-RUN by default — prints the plan (mentions "--execute"), deletes NOTHING; exit 0. Second test greps source for `"--execute"` and `_backup_main_db_first` (weak grep-style wiring check).
- **test_tools_regressions.py** (359, tools) — R4 MED batch 11 + Pass-2 tool fixes. `register_orphan_matches._player_aggregates`: totals from `MAX(kills_total)` cumulative counters, NOT `SUM(MAX(kills_this_round))` (undercounts ~50% — per-round counters not reliably sampled at round-end peak); damage keeps SUM(MAX(damage_this_round)) (tr:6-45). `dead_code_detector` Phase C: `alias not in content` was always False (the import line itself contains the alias) — must now report truly-unused imports, skip `__future__`/star imports, and treat string usage (`__all__`) as usage. **Task #8 compile gate**: every script in tools/, Programma_CS2_RENAN/tools/, evals/ and repo root must ast.parse; ≥70 scripts collected or the gate is declared hollow (tr:81-109). `populate_match_results.derive_winner_from_roundstats`: winner is a SIDE label ("CT_start"/"draw"), never a team name — nothing in the DB links starting side to team name, so ~half of recorded winners were fabricated; group score = MAX across players (substitute-robust) (tr:112-177). `repair_tick_features`: case-insensitive join — monolith stores original parser case ("ZywOo") while the repair table lower-cases; the old exact join silently skipped every mixed-case player while reporting success (all four repaired flags are 25-dim training features); partial field sets touch only available columns. `wipe_for_reingest_safe._restore`: current -wal/-shm sidecars removed before extract, else SQLite replays a post-snapshot WAL onto the restored file, "silently mixing two database states" (tr:264-310). `mine_shard_strategies.load_shard`: window sizes derive from per-shard `match_metadata.tick_rate` (128-tick shard windows previously covered HALF the intended time, skewing rush/default/execute classification); missing/out-of-range → `_DEFAULT_TICK_RATE`.

## Suspicious/contradictory findings

1. **Stale cross-reference on ModelFactory strictness**: test_training_orchestrator_logic.py:68 says unknown types are rejected "unlike ModelFactory", but test_model_factory_contracts.py pins that ModelFactory DOES raise ValueError since commit 3120c4b. The logic-file docstring/comment predates the fix — harmless but misleading doctrine.
2. **Phantom "rap-lite" type**: test_model_factory_contracts.py:215 skips a `rap-lite` type in its TYPE_* sweep that appears nowhere in the checkpoint-name map or any other test in this cluster. Either a vestige or an untested registered type.
3. **Zombie vs P4-B stale-threshold tension**: test_session_engine expects `_cleanup_zombie_tasks` to requeue a 10-min-old 'processing' IngestionTask (`_ZOMBIE_THRESHOLD_SECONDS` bounded 60..1800, implied ≤600), while test_run_worker_claims (F-0015) pins run_worker's recovery to the P4-B SSOT default of 30 min and explicitly asserts a 10-min-old task is NOT recovered. Two daemons hold different staleness doctrines for the same table — session_engine can requeue mid-parse exactly what P4-B was raised to protect. (Matches the cluster-02 carry-forward "config ZOMBIE 300 vs P4-B 1800".)
4. **Duplicated SkillLatentModel**: `backend/processing/skill_assessment` (test_skill_assessment.py, mocked baseline) and `backend/nn/rap_coach/skill_model` (test_skill_model.py, real baseline) expose the same API in parallel. The R4 MED "0.0 is real data, None is missing" fix is only pinned on the rap_coach copy; the processing copy's tests still route through mocked baselines without that distinction. Drift risk between the two.
5. **Duplicated `_health_to_range`**: CoachingService (test_v1_blockers) and ExperienceBank (test_round_utils) each own an 80/40 threshold implementation, pinned in separate files with no shared SSOT.
6. **test_map_manager.py is kivy-gated** in a Qt app — `pytest.importorskip("kivy")` means the whole asset-registry contract (all 8 core map PNGs, dust2 scale 4.4) is likely permanently skipped in every environment. dust2 scale 4.4 is also independently duplicated in test_spatial_engine.
7. **Pattern-only tests (do not exercise production code)**: P1-05 negative-sampling and P0-06 connection-leak in test_phase0_3_regressions model the fix in pure Python; DB-06 in test_security_hardening re-implements the pragma handler instead of exercising the real DBManager listener; test_reset_pro_data_dryrun and P1-01's wiring check are source-grep assertions. All would keep passing if the production code regressed in ways the pattern doesn't capture.
8. **RAP-AUDIT-08 top-2 sparsity is weakly asserted**: test_rap_coach.py:226-237 only checks the prediction is non-zero and the gate is a full softmax — RAPStrategy's actual top-2 masking is unverified, in sharp contrast to the rigorous zero-gradient proof for `_topk_sparse_gate` in test_moe_sparse_gate.py. The two MoE implementations are held to different standards.
9. **test_persistence_stale_checkpoint.py docstring line 14** still claims "Missing checkpoints return the model unchanged" while its own NN-14 test asserts FileNotFoundError — internal doc drift.
10. **Two RAP trees under test**: test_rap_coach.py exercises `backend/nn/rap_coach/*` while test_rap_training_dry_run.py requires the production TrainerClass to come from `backend/nn/experimental/rap_coach` (and pins the RAP-LTC-FIX only there). The legacy tree keeps a full passing suite, which can mask which implementation is actually canonical.
11. **Tick-rate SSOT sweep blind spot**: the AST scanner in test_tick_rate_ssot.py checks assignments, defaults, call KEYWORDS, `getattr` and dict keys — but `header.get("tick_rate", 64.0)` (positional default to `.get`, e.g. the demo_loader.py:719 case carried from cluster 03) is not matched by any branch. The viewer-path literal survives the ban structurally, not by exemption.
12. **round_utils fallback asymmetry**: `infer_round_phase({})` → "pistol" (missing equipment = 0) but non-dict input → "full_buy" — opposite conservative directions for two flavors of bad input, both pinned as intended behavior (test_round_utils.py:54-62).
13. **Cross-file consistency confirmed (not a finding, a verification)**: METADATA_DIM=25 is pinned identically in test_metadata_dim_contract, test_nn_config_reproducibility and test_z_penalty (slot 15 = z_penalty, index 17 = map_id); the 11-tick JEPA window (context 10 + target 1) is consistent between orchestrator flows and V-1 tests; CRITICAL-toast behavior agrees between test_qt_core and test_v1_blockers; ZOMBIE (#3 above) is the only numeric contradiction found across the 84 files.
