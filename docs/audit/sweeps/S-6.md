# S-6 — cross-cutting censuses (subagent sweep, verified) — 2026-08-14

Method: delegated mechanical rg sweep (pointers-only), sanity checks
verified by the agent, conclusions re-verified against Pass-1 dossiers.
Production scope (Programma_CS2_RENAN + tools, tests/venv excluded).

## S6-A core→backend layering — 22 hits, ALL in core/session_engine.py
Every single edge lives in the session engine (mostly lazy in-function
imports). VERDICT: the "22 layering edges" concern DISSOLVES into one
architectural fact — session_engine is the boot/orchestration
composition root and legitimately composes backend services. W3 scope
shrinks to an ADR paragraph declaring it the sanctioned exception; no
decoupling wave needed. No OTHER core module imports backend.

## S6-B bare except — 0 real occurrences
3 grep hits are string literals inside headless_validator's own
bare-except CHECKER (which enforces the ban repo-wide). Discipline ✓.

## S6-C print() in production (non-tools) — 24 hits
- faceit_integration (8), jepa_model (10), dem_validator (4): ALL
  inside `__main__` debug blocks — members of the S5 ~18-block list
  (L10 debt, W3 batch decision: keep-as-CLI vs strip).
- LIVE-path prints (P3, W3 one-liners): help_system.py:41 (error path
  should use logger), training_callbacks.py:15 (default callback —
  arguably intentional console feedback; decide at W3).

## S6-D naive datetime.now() — 17 hits (exact worklist)
- Tools/validators (10): backup/manifest AGE math + report timestamps
  — consistently naive local-time on one machine, no cross-tz
  comparison; P3 cosmetic batch.
- Production-adjacent (4): logger_setup.py:244 filename stamp,
  report_generator.py:72/:80 report headers, user_tools.py:137
  manual_entry demo_name date. P3.
- d3_recover (2) + migrate_db (1) + reset_pro_data (1): report stamps.
- VERDICT: no correctness hazard found (no naive-vs-aware arithmetic
  across boundaries); W3 mechanical batch to timezone.utc where cheap.

## S6-E seed/determinism — 15 hits, EXEMPLARY
set_global_seed SSOT (config.py seeds random+np+torch) invoked with
P1-02 comments at every trainer entry (jepa_train ×3 incl. per-worker
42+worker_id, train.py ×2, win_probability_trainer); deterministic
probes in ML debugger (R4 LOW) + headless; experimental rap memory
hardcodes 42 (acceptable, experimental tree).

## S6-F `raise e` — 0 hits
Traceback-preservation discipline clean (module 07 lesson verified).
