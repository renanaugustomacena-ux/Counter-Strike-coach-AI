# S-5 — CLI/tools pre-batch sweeps (scheduled before B46) — 2026-08-14

Method: personal Grep censuses (no agents); per-candidate verification
happens in the owning B46–B61 batch reads.

## 1. sys.path-manipulation census
92 occurrences / 83 files. Pattern: the standard one-per-file repo-root
bootstrap (`sys.path.insert(0, <repo root>)`) so tools/CLIs import
`Programma_CS2_RENAN` when run directly — expected for every root CLI
and tools/ script. Multi-hit files to eyeball in-batch:
portability_test ×4 (B49), batch_ingest ×2 (B47), ui_screenshot ×2 +
ui_gallery ×2 (B50), seed_hltv_top20 ×2 (B58).
PRODUCTION hits (already read + documented in Phase F/D dossiers, listed
here for the L6 lens): core/config.py ×1, core/session_engine.py ×1
(PyInstaller/runtime bootstrap rationale) — no new exposure.

## 2. subprocess / shell usage census
shell=True: exactly ONE live use — Programma_CS2_RENAN/tools/
build_tools.py:252 with an inline `# nosec B602 — internal build tool`
annotation (verify at B61). Every other hit is GUARD code: the
headless_validator "[Security] no subprocess shell=True in production"
check (:2319-2406), tests/test_security.py:142-152, and
portability_test's own scanner. Production posture: shell=True-free and
double-enforced (validator + test). os.system: zero hits.

## 3. `__main__` entry inventory
130 occurrences / 125 files. Buckets:
- Root CLIs (5): console, schema, batch_ingest, goliath,
  run_full_training_cycle — the documented entry surface (B46/B47).
- tools/ + Programma_CS2_RENAN/tools/ (~60): one per script ✓ expected.
- tests/verify_* + forensics (~15): manual diagnostic runners (Phase T
  scope — the latent suites).
- **Production library modules w/ debug `__main__` blocks (~18)** — the
  L10 pass-2 list: backend/{coaching/hybrid_engine, analysis/
  win_probability, analysis/utility_economy, nn/{early_stopping,
  coach_manager, jepa_model, ema, jepa_train, train, training_monitor},
  knowledge/{pro_demo_miner, init_knowledge_base}, processing/
  validation/dem_validator, ingestion/csv_migrator ×2, data_sources/
  {faceit_integration, steam_demo_finder}, services/telemetry_client,
  storage/remote_file_server, server} + apps/qt_app/app.py +
  core/session_engine + run_ingestion + run_worker + hltv_sync_service
  (the last five are REAL entry points, not debug blocks). Most debug
  blocks were noted in their phase dossiers; the aggregate view feeds
  L10 (dead-or-debug entry surface) — W3 decision: keep as smoke
  harnesses vs strip.
- Double-`__main__` files to verify in-batch: migrate_db ×2 (B53),
  _infra ×2 (B61), portability_test ×2 (B49), csv_migrator ×2 (read at
  B07 — docstring example, recorded), json_tournament_ingestor ×2
  (read at B09).
