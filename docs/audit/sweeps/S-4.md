# S-4 (redo) — raw-SQL + session-callsite census — closed 2026-08-14

Original S4 was deferred; the census was ultimately performed file-by-file
during Pass 1 (every session callsite personally read) and assembled at
Pass 2 L3/L9 instead of as a standalone grep dump.

Verified conclusions (evidence = L3/L9 tables + per-batch dossiers):
- Session shape uniform: `Session(engine, expire_on_commit=False)` via
  manager context managers; no stray sessionmaker() constructions found.
- Raw SQL sites: text() PRAGMAs/SELECTs in inspectors (whitelist+bracket
  validated), D2A/reconcile (nosec B608 justified, module constants),
  schema.py (identifier-validated, TESTED), repair tools (parameterized).
  ZERO user-input-interpolated SQL found in Pass 1.
- Check-then-act inventory: F-0037 (run_ingestion family) is the one
  registered hazard; gated single-writer tools acceptable under locks.
