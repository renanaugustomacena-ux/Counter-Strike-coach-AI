# S-3 — UI pre-batch sweeps (scheduled before B34) — 2026-08-14

Method: personal Grep censuses (no agents), candidates verified during the
owning batch reads. Raw commands in the audit transcript.

## 1. QGraphicsEffect census (repo rule: effects ban — Linux crash)
**HITS REQUIRING BATCH SCRUTINY:**
- `core/animation.py` :30-47 — USES QGraphicsOpacityEffect, with a "SAFETY
  NOTE" header (:8-9) about mid-repaint crashes → B35 must read the
  mitigation and reconcile with the ban (MACENA_UI_ANIMATIONS gate?).
- `widgets/components/card.py` :29,:130-159 — USES QGraphicsDropShadowEffect
  for floating/frosted variants → B37 scrutiny.
- `themes/base.qss.template` :432 — comment claims DropShadow "(safe on
  mid-repaint widgets)" → B35 read the full comment.
- `main_window.py` :349 — fade animation DISABLED citing the crash (ban
  observed here).
Verdict pending B35/B37: either doctrine violation (P1-class on the Linux
deploy target) or the ban is narrower than the memory row (opacity-on-
mid-repaint only) — settle with the in-file safety notes + git context.

## 2. Hex-literal census (tokens must flow from design_tokens)
30 hits outside design_tokens.py/theme_engine.py:
- svg_icon_provider.py ×22, icons.py ×7 (icon-internal palettes — verify
  at B35 whether these are token-derived or hand hex), app.py ×1 (B34).
Everything else CLEAN — the one-way token flow held across 90+ UI files.

## 3. i18n-literal census (all visible strings via i18n.get_text)
556 get_text() calls repo-wide in qt_app. 32 raw-literal setter candidates
(setText/setPlaceholderText/setToolTip/setWindowTitle with a letter-leading
literal), concentrated in: tactical_viewer_screen ×9, settings_screen ×8,
wizard_screen ×5, user_profile_screen ×4, steam_config_screen ×4,
pro_player_detail ×1, faceit_config ×1. Each verified at its owning batch
(B42/B44/B45) — expected mix of data glyphs, key names, and genuine misses.
