# S-2 — rap_coach tree cross-check (scheduled before B25) — 2026-08-14

Method: direct read of every file in backend/nn/rap_coach/ (10 files, 61 LOC)
against backend/nn/experimental/rap_coach/ (canonical). No agent needed —
the "duplicate tree" premise dissolved on contact.

## Verdict: DISSOLVED — no divergent duplicate code exists.

backend/nn/rap_coach/ is a P9-01 backward-compatibility SHIM package:
- __init__.py: deprecation notice only.
- chronovisor_scanner / communication / memory / model / pedagogy /
  perception / strategy / trainer .py: each is a 4-16 line
  `from ...experimental.rap_coach.X import (...)  # noqa: F401` re-export
  with explicit __all__ and a "DEPRECATED shim — canonical location" header.
- skill_model.py: correctly points at the P9-01 EXTRACTION target
  (backend.processing.skill_assessment), not experimental — SkillAxes/
  SkillLatentModel are production utilities, documented as such.
- model.py shim additionally re-exports RAP_POSITION_SCALE from nn.config
  (the F3-05 SSOT) — consistent.

Every re-exported symbol verified to exist at its canonical location during
the B25 read. Zero drift, zero stale exports.

Residual: the shim package itself is W3 dead-code-candidate material ONCE the
S5/S7 caller census confirms no remaining imports of the old paths (tests may
still exercise the shims deliberately as compat guards — decide at CP0).
