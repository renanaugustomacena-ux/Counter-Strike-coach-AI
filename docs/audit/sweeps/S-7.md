# S-7 — test↔module cross-ref — closed 2026-08-14 (performed during Phase T)

Method: instead of a standalone matrix, every Phase-T batch dossier
(D-B62..D-B75) mapped its suites to the mirrored subsystems read in
Phases F–C, with doctrine-pin coverage recorded per batch. This file
records the ASYMMETRIES that survived.

## Under-tested (flagged for CP0/W3)
- Zero-coverage packages flagged at Phase K: backend/progress,
  backend/reporting, knowledge_base — still no dedicated suites.
- mine_coaching_experience's local phase-classifier copy (untested; the
  SSOT twin in round_utils IS tested via test_coper_pathway).
- repair_rating_scale constants mirror (no parity class — W3).
- Latent suites red-by-design on non-data boxes: e2e (skip-gate OK),
  dry-run integrity real-run leg (F-0043 exit-0 root cause).
- verify_*.py root scripts + forensics: manual harnesses outside pytest
  collection (deliberate; tier labels at W3).

## Well-covered highlights (no action)
- Every R4/WR/26-*/GAP war story catalogued in Phases F–C has a pinning
  test found in Phase T (see D-B63..D-B72 doctrine-pin maps).
- The three 2026-07-26 operational disasters each carry multi-test guards.
