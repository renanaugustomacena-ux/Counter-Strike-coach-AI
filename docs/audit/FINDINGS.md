# Findings Register

Severity: **P0** crash/data-loss/wrong-output · **P1** correctness/threading/resource under real use ·
**P2** contract drift, dead code, smells with behavioral risk · **P3** style (tooling autofix or batches ONLY).
`study_ref`: course module§ when a lesson genuinely applies (any module, not only the 8 required), else `-`.
Status: open / approved / fixed@sha / wontfix / deferred.

| ID | Sev | Subsystem | Location | Title | Evidence | study_ref | Proposed fix | Status |
|---|---|---|---|---|---|---|---|---|
| F-0001 | P1 | infra/integrity | integrity_manifest.json:5 + Programma_CS2_RENAN/core/integrity_manifest.json | Integrity manifest stale — gate RED at baseline | `--verify-only` FAIL: 45 changed, 15 new, 0 removed (2026-08-14); root manifest hashes phantom `Programma_CS2_RENAN/main.py` (file absent — Kivy-era entry point); two manifest files + .bak coexist — RESOLVED at B58+B76: core/integrity_manifest.json is authoritative (sync tool's target, 300+ entries, NO main.py key); root integrity_manifest.json is a STALE duplicate (v2.0, generated 2026-02-04, 18 hand-picked hashes incl. the deleted main.py and observability/rasp.py) — W1 resync deletes-or-regenerates the root copy; also reds headless_validator [Integrity] sampling (3/10 mismatch) | - | Isolated resync commit opens W1; remove phantom entries; clarify single authoritative manifest | fixed@9a12488 |
| F-0002 | P2 | ui/tactical | Programma_CS2_RENAN/apps/qt_app/screens/tactical_viewer_screen.py:416 | Bare tick-rate literal 64 violates SSOT 26-NORM-01 — default gate RED | Guard test test_tick_rate_ssot.py::test_production_code_has_no_bare_64 FAILS naming exactly this line; introduced during design-atlas rebuild; escaped review because CI never ran on the branch (F-0004). B72 LIVE re-run (2026-08-14): still EXACTLY ONE offender — `self._demo_tick_rate = 64` init placeholder (B42 verdict: loud _resolve_demo_tick_rate fallback already correct; only the literal needs the SSOT import). The AST scanner has a seeded-violation meta-test (scanner-must-bite), so the fix can't silently regress | 22§Code Smells: Magic Numbers | Import DEFAULT_TICK_RATE from core.tick_rate (both the :416 init placeholder and _resolve_demo_tick_rate's fallback). B42 VERDICT: NO severity upgrade — real playback rate comes from _parse_demo_header_meta per demo (26-TICK war story in-file: old default-64 played 128-tick demos at half speed, FIXED); the bare 64s are only the pre-load init value and a LOUD-warning fallback for unresolvable headers; dual-progress offset math uses the resolved rate | fixed@da2fb15 |
| F-0003 | P2 | tests/storage | Programma_CS2_RENAN/tests/test_match_data_dangling_symlink.py (6 tests) | Symlink tests unrunnable without Windows symlink privilege — local gate RED on standard Windows | 1 FAILED + 5 ERROR, all `OSError [WinError 1314] A required privilege is not held` at fixture `link.symlink_to(...)`; no capability probe/skipif; CI runners (admin) unaffected | - | Add symlink-capability probe fixture → pytest.skip when privilege absent (CPython support.can_symlink pattern) | fixed@da2fb15 |
| F-0004 | P1 | infra/ci | .github/workflows/build.yml (on.push.branches) | CI branch filter excludes the project's real branch convention — design branch had ZERO CI runs | Filter is `[main, develop, 'feature/**', 'fix/**']` but branches use `feat/*` (46-commit feat/frontend-design-atlas: no workflow runs per `gh run list`); Ubuntu deploy-truth leg never exercised; explains F-0002 escape | - | Add `feat/**`, `chore/**` to filter (keep existing); consider PR-early workflow; part of W1 | fixed@da2fb15 |
| F-0005 | P2 | tools/validator | tools/headless_validator.py ([Structure] Qt app check) | Validator asserts obsolete .qss theme files deleted by design mission — validator RED | `[FAIL] ... No .qss theme files found in apps/qt_app/themes/`; only base.qss.template is rendered since design-atlas rebuild (legacy cs2/csgo/cs16.qss deliberately removed) | - | Update structure check to assert themes/base.qss.template (+ qss_generator contract) instead of *.qss | fixed@da2fb15 |
| F-0006 | P1 | ingestion/parsing | run_ingestion.py:1108 + demo_parser.py:60,:651 + round_context + trade_kill_detector + forensic parser (every demoparser2 call site) | pyo3 PanicException bypasses demo-parse crash guards — one bad demo aborts ingestion | Docstring at :1099 promises "never aborts on metadata"; guard is `except Exception`; baseline integration run: PRO_DEMO_PATH absent → get_pro_demo_base() fell back to $HOME → test rglob'd the whole home dir and fed a random .dem to demoparser2 0.41.4 → Rust panic (`range end index 16 out of range for slice of length 12`, first_pass/parser.rs:82/92); PanicException (BaseException subclass) flew through — 3 integration tests red | 07§(pending read: exception hierarchy) | Catch `(Exception, pyo3_runtime.PanicException)` at all demoparser2 call sites; root-cause the panicking demo at B07/T-DIAG (truncated vs newer CS2 format vs parser bug; check upstream changelog) | fixed@5f46659 |
| F-0007 | P2 | core/config | Programma_CS2_RENAN/core/config.py:168 + :533 | Keyring failure crashes settings save — get/set robustness asymmetric | get_secret catches Exception→default (graceful); set_secret `raise RuntimeError` on keyring error; save_user_setting calls set_secret UNGUARDED for STEAM/FACEIT/STORAGE_API_KEY → on Linux (deploy target) with no keyring backend, saving an API key raises out of the settings-save path (likely UI) | 22§Error handling: gestiti esplicitamente | Make set_secret degrade like get_secret (return False, log) or guard the save path; plaintext-fallback path already exists (FE-04 chmod 600) | fixed@af44cab |
| F-0008 | P2 | core/config + tests | Programma_CS2_RENAN/core/config.py:461 + Programma_CS2_RENAN/tests/test_ingestion_tickrate.py:133-139 | get_pro_demo_base() falls back to $HOME — the exact anti-pattern the module documents against; integration test then rglobs the entire home dir | config.py:186-190 doctrine: "$HOME always exists — so a $HOME default silently relocated..."; :461 returns Path(expanduser("~")) when unset/missing; on this machine PRO_DEMO_PATH is absent → baseline integration swept the user profile and parsed an arbitrary .dem (fed F-0006) | 22§Config: fail-fast al boot | Return None (or sentinel) forcing callers to decide + skip; test should skip when no configured pro-demo base instead of sweeping $HOME | fixed@e55f850 |
| F-0009 | P2 | core/locks | Programma_CS2_RENAN/core/lock_files.py:190-197 | release() unlinks without ownership check — can delete a FOREIGN live lock | Docstring claims "releasing an unheld lock is a no-op" but path.unlink() removes whatever exists; a caller invoking release(name) without holding it destroys another process's live lock (defeats D-track↔HLTV-track mutual exclusion); acquire/steal path is TOCTOU-hardened but release is not | 22§Funzioni come Contratti (postcondizioni) | release() verifies lock-file PID == os.getpid() (or name ∈ _held_locks) before unlink; audit callers at Phase C | fixed@5b1b034 |
| F-0010 | P2 | core/lifecycle | Programma_CS2_RENAN/core/lifecycle.py:42-44 | Single-instance enforcement is a no-op on Linux — the real deployment target | `if sys.platform != "win32": return True` with comment "project currently targets Windows-only deployments" — contradicts Linux-deploy doctrine; two instances on Linux = concurrent SQLite writers, the exact hazard the mutex exists to prevent ("Fail closed to protect DB") | - | Cross-platform guard via in-house lock_files.py named lock (or fcntl/XDG socket) on non-Windows | fixed@d1d0b65 |
| F-0014 | P1 | daemons/hltv_sync | Programma_CS2_RENAN/hltv_sync_service.py:237 + :103-104 | start_detached launches deleted main.py (dead-on-arrival) AND dormant path exits while notifying "retrying" | :237 `main_script = SCRIPT_DIR / "main.py"` — Kivy entry point removed; Popen child dies instantly (stderr DEVNULL), PID file records a dead PID; :103-104 HLTV-unreachable path sleeps 6h then RETURNS (service exits) while the WR-15 notification promises "Retrying in 6 hours" — notification lies, daemon dead until manual restart | 22§Dead code + honest contracts | Point start_detached at a real entrypoint (python -m ... hltv_sync_service) or remove; dormant path `continue` instead of return; caller census at Phase C for exposure | fixed@0bb0b54 |
| F-0015 | P1 | daemons/run_worker | Programma_CS2_RENAN/run_worker.py:28 + :132-144 + backend/control/ingest_manager.py:~184 (_recover_stuck_tasks, same 5-min threshold — B32) | Worker keeps the 5-min stale threshold that P4-B documented as THE duplicate-processing bug; skip-after-claim leaks tasks into processing | :28 `_STALE_THRESHOLD = timedelta(minutes=5)` — session_engine's P4-B raised the same recovery to 30min because "large demos legitimately take 10+ minutes; 5-minute threshold caused premature reset and duplicate processing"; run_worker never got the fix → concurrent worker+digester (or two workers) re-queue demos mid-parse. :141-143 _should_skip_pro_task fires AFTER _fetch_next_task_data claimed the task as processing → skipped task stays claimed, stale-recovery requeues, infinite claim/skip churn | 07§ (timeout/threshold coherence) | Align threshold with ZOMBIE_TASK_THRESHOLD_SECONDS setting (single source); check skip condition BEFORE claiming (or release claim on skip); caller census at Phase C | fixed@13cf92b |
| F-0013 | P2 | data_sources/parsing | Programma_CS2_RENAN/backend/data_sources/demo_parser.py:39-50 + :584-595 | Parse timeout is illusory — `with ThreadPoolExecutor` joins the hung worker on exit | future.result(timeout) raises FutureTimeoutError, but the with-block exit calls shutdown(wait=True) which BLOCKS on the still-running parse thread (Python threads can't be killed): a hung demoparser2 call hangs parse_demo/parse_sequential_ticks (and the Digester daemon) forever despite the docstring's "prevent indefinite hangs"; slow demos pay full parse cost after "timing out" | 07§retry/timeout patterns | Manual executor + shutdown(wait=False, cancel_futures=True) with orphan-thread logging, or process-based isolation for parse calls; align with F-0006 fix | open |
| F-0012 | P1 | storage/shards | Programma_CS2_RENAN/backend/storage/match_data_manager.py:488,830,837 | Undefined name `logger` (module defines only `_logger`) — fallback paths raise NameError instead of warning | Grep-verified: only `_logger` is defined (:37); :830/:837 are the graceful-fallback branches of _default_window_ticks (shard metadata missing or tick_rate invalid → intended warn+64-fallback becomes NameError, crashing get_player_tick_window/get_all_players_tick_window — RAP/POV training + PlayerKnowledgeBuilder path); :488 masks the WR-14 storage-disconnect IOError. Branches provably untested (would fail on first execution) | 22§Dead/untested paths; 07§logger naming | Rename to `_logger` at 3 sites; regression test exercising the no-metadata fallback; also replace bare `rate = 64` (:823) with DEFAULT_TICK_RATE and reuse is_valid_tick_rate | fixed@6a94335 |
| F-0016 | P2 | processing/round-stats | Programma_CS2_RENAN/backend/processing/round_stats_builder.py:87,:297-298 + backend/data_sources/event_registry.py:49 | round_end `winner` dtype contract conflict — if parser emits int (as registry documents), round_won is silently always-False | Registry documents `winner: int (team)`; demo_parser returns the round_end df RAW (no normalization); builder str()-casts then compares `.upper() == "CT"/"T"` → str(3)="3" never matches → every RoundStats.round_won False (label poisoning: win-rate features, derive_winner_from_roundstats, UI round drill-down); tests feed only string winners (test_round_stats_enrichment.py:310-351) so gate is blind; sibling tool aggregate_match_stats_sql.py:341-350 defends BOTH str and int for team fields, proving mixed-type reality in this stack | 22§Funzioni come Contratti | T-DIAG probe: dtype of round_end winner on real demo. If int: side-map (2→T,3→CT) at boundary build + escalate P1; if str: fix stale event_registry doc. Either way add int-winner test case | open |
| F-0017 | P2 | processing/heatmap | Programma_CS2_RENAN/backend/processing/heatmap_engine.py:81,:182 (+ :110-134, :277-300 Kivy residue) | Heatmap grid double-flips Y (Kivy bottom-left-origin residue) — violates C-03 single-flip doctrine; latent vertical mirror for any Qt/matplotlib consumer of rgba_bytes | `ny = (pos_y − y)·scale` then `gy = (1−ny)·res` (double flip) vs tensor_factory._world_to_grid C-03 "single Y-flip only"; row 0 = map bottom = OpenGL blit convention; hotspot inverse projection self-consistent (world coords safe). Today dead surface: sole caller coaching_service.py:889 consumes only .hotspots; rgba_bytes has zero consumers; create_texture_from_data/generate_heatmap_texture require Kivy (not installed — honest RuntimeError; docstrings still advertise Clock.schedule_once); differential RGBA colorized then discarded every call (wasted compute) | 22§Dead code + convention drift | W3 Kivy-residue batch: delete texture path + align gy to C-03 single-flip (or document OpenGL-origin contract on the dataclass); stop building unused RGBA in the differential path (or return it lazily); L1 records the convention row | open |
| F-0018 | P2 | processing/baselines | Programma_CS2_RENAN/backend/processing/baselines/pro_baseline.py:76-95 + :275-279 + :321-324 | Baseline fusion layer-priority inversion — layers 2/3 merge HARD defaults into their returns, then the fusion loop copies every key, clobbering earlier empirical layers | Documented contract: "later layers override earlier … stronger data overrides weaker fallbacks". _load_pro_from_demo_stats merges HARD_DEFAULT for its missing metrics (e.g. rating_impact) → get_pro_baseline copies demo_data wholesale → hardcoded 1.10 overwrites CSV's empirical Impact whenever ≥10 pro demo rows exist and HLTV layer is empty; _load_pro_from_csv has the same internal merge (no-op today only because it runs first) | 22§Funzioni come Contratti | Layers 2/3 return ONLY empirically-derived keys (drop internal default-merges; fusion already starts from HARD_DEFAULT_BASELINE); regression test: fused baseline never contains a default where an earlier layer had empirical data | open |
| F-0019 | P2 | processing/baselines+validation | pro_baseline.py:299-306 (_CSV_COLUMN_MAP) + validation/sanity.py:29-36 (LIMITS) | Metric-scale incoherence: CSV layer ingests KAST/HS% as PERCENT into a ratio-scaled baseline; sanity's headshot_pct band is percent-scaled against a ratio-emitting parser | Empirical (R0 backups): PlayerMatchStats avg_hs/avg_kast are RATIO (0.318/0.944); stat_fetcher.py:588 stores kast/100 (ratio); HARD defaults ratio. (a) _CSV_COLUMN_MAP takes "KAST"→avg_kast and "Headshot %"→avg_hs RAW → percent values (~71/~52) would poison the fused baseline the day the (currently absent) CSV ships → Z-scores explode (skill axes peg to 0/1). (b) sanity LIMITS headshot_pct=(0,100) never catches corrupt ratio-scale values (37.0 passes); trim mode never trims; P-SAN-01 self-heals kast only — asymmetric | 09§type/unit contracts | CSV loader divides percent columns by 100 (mirror stat_fetcher P-SAN-01); sanity LIMITS headshot_pct → (0.0, 1.0) + optional percent-detect conversion like kast; unit-annotate baseline keys in one doc row | open |
| F-0020 | P2 | data-assets/completion | Programma_CS2_RENAN/data/external/ (only hltv_stats_urls.txt present) | All 7 EliteAnalytics reference CSVs + pro-baseline CSV absent from repo — elite-comparison subsystem permanently degraded on fresh installs | external_analytics._read_safe warns 7× at boot; is_healthy()=False → analyze_user_vs_elite returns empty z-scores; get_player_role → "Unknown"; pro_baseline CSV layer dead code in practice. Code degrades gracefully (R4 LOW observable) — this is a data-provisioning/completion gap, not a code bug | - | CP0 decision: ship datasets (check licensing/size), make them a documented optional download (settings path + README), or retire the elite-comparison feature; align D-B12/D-B15 notes after decision | open |
| F-0021 | P2 | analysis/deception | Programma_CS2_RENAN/backend/analysis/deception_index.py:118-152 (_detect_flash_baits) | Flash-bait detection keyed to dead CS2 event vocabulary — bait rate degenerates to 1.0 (all-bait) or 0.0 on CS2 demos | Filters `event_type == "player_blind"`; CS2 emits ZERO player_blind events (documented in round_stats_builder, which synthesizes blinds from flash_duration for exactly this reason). With any "flashbang_throw" rows and no blinds: `return 1.0` → every flash counted a bait → composite deception index inflated by W_FAKE_FLASH=0.25; if "flashbang_throw" also absent from the real vocabulary the metric silently reads 0.0. Same contract-drift class as F-0016 | 22§Funzioni come Contratti | Feed synthesized blind events (expose round_stats_builder's synthesizer or share its output) or re-key detection to flash_duration transitions; align event names with the real match_event_state vocabulary; verify caller + names at Phase K/T-DIAG | open |
| F-0022 | P2 | analysis/engagement | Programma_CS2_RENAN/backend/analysis/engagement_range.py:53-62 (_ROLE_RANGE_BASELINES) | Role-vocabulary dead key — "entry_fragger" baseline unreachable via canonical PlayerRole values; entry players silently get no range coaching | core.app_types.PlayerRole.ENTRY.value == "entry" (verified); compare_to_role normalizes input via .lower().replace(" ","_") → "entry" → baselines.get("entry") is None → returns [] silently for the entire entry role; all other keys (awper/support/lurker/igl/flex) match. Same silent-degradation class as F-0021; rating.py priors use "entry" (correct side of the split) | 22§Funzioni come Contratti | Rename baseline key to "entry" (or key the dict by PlayerRole and accept both spellings); add a test asserting every PlayerRole value has a range baseline; caller census at Phase K confirms exposure | open |
| F-0023 | P2 | nn/vl-jepa | Programma_CS2_RENAN/backend/nn/jepa_trainer.py:566-567,:595-602 + jepa_model.py:975-980,:1080 | VL-JEPA concept training objective cannot fit its labels and diverges from the inference readout — BCE-with-logits on raw cosine similarities | forward_vl exports concept_logits as RAW cosine sim ∈ [-1,1] (temperature applied only to concept_probs); train_step_vl feeds that to vl_jepa_concept_loss's binary_cross_entropy_with_logits → sigmoid outputs confined to [0.269, 0.731] while outcome-based labels span 0.0–1.0 (e.g. 0.9 economy, 0.0 zeros) → irreducible loss floor, gradients saturate toward ±1; inference reads concepts via temperature-scaled SOFTMAX over the same logits — training and readout are different functions. Ordinal ordering preserved; calibration + coherence broken. Experimental-but-active VL path (USE_JEPA_MODEL) | 33§(objective/readout coherence); L8 | Scale logits by concept_temperature before BCE (mirrors inference) or train softmax-CE against row-normalized labels; add a test asserting BCE input range covers label range; re-check top_concepts stability after fix | open |
| F-0024 | P2 | nn/orchestrator | Programma_CS2_RENAN/backend/nn/training_orchestrator.py:630-687 (_run_epoch) + jepa_trainer.py:399-409 + early_stopping.py docstring | P9-02 embedding-collapse hard-stop bypassed on the production training path — detector fed only by a method the orchestrator never calls | Orchestrator runs its own epoch loop via trainer.train_step(); embedding_collapse_detector.update() lives ONLY in JEPATrainer.train_epoch (reached solely from the drift-retrain path). Per-batch result["embedding_variance"] is returned and discarded (callbacks only) — collapse degrades to the advisory per-batch warning, the exact state the modernization report §9 / Supplement N=260 §5.1 mandated promoting to an abort. early_stopping.py docstring promises "the TrainingOrchestrator inspects" | 33§(long-running gate placement) | Orchestrator accumulates result["embedding_variance"] per train epoch and feeds the mean to trainer.embedding_collapse_detector.update() (let EmbeddingCollapseError abort); regression test: collapsed variance stream aborts run_training within 2 epochs | open |
| F-0025 | P2 | nn/rap-training | Programma_CS2_RENAN/backend/nn/training_orchestrator.py:1084-1099 (_compute_advantage call) + :1557-1568 (_classify_tactical_role) | RAP value/strategy training labels computed with a `team` attribute the monolith rows don't have — every sample treated as CT | EMPIRICAL (R0 backup): playertickstate has NO team column ("no such column: team"); shards use {'CT','TERRORIST'}. `str(getattr(item, "team", "CT"))` always yields "CT" → value head receives CT-perspective advantage as the target for T-side samples (anti-correlated for ~half the corpus); bomb_factor `player_team == "T"` unreachable; _classify_tactical_role is_ct always True → T role branches dead, CT+bomb→RETAKE stamped on T samples. LEAK-01/G-04 quality work sits on a broken side resolution. Path gated behind USE_RAP_MODEL=False (experimental) — silent poison when enabled | 22§Funzioni come Contratti; 33§measure-first | Resolve side from the shard row (knowledge.own_team, full-name 'TERRORIST' mapped to T) so both perspective and comparison vocabulary are shard-consistent; add a test with a T-side sample asserting advantage < 0.5 when CTs dominate | open |
| F-0026 | P2 | nn/rap-inference | Programma_CS2_RENAN/backend/nn/inference/ghost_engine.py:~100 (get_tensor_factory) + backend/nn/coach_manager.py:1292 (RAPStateReconstructor()) + state_reconstructor.py:49-52 | RAP train/inference tensor-resolution skew — training at TrainingTensorConfig 64×64, both inference paths at default 128/224 | Orchestrator trains with TensorFactory(TrainingTensorConfig()) (64²); ghost_engine.predict_tick uses the get_tensor_factory() singleton (default 128 map/224 view); coach_manager overlay builds RAPStateReconstructor() with tensor_config=None → same default singleton. AdaptiveAvgPool2d hides the dimension mismatch while absolute-grid-unit statistics shift (VELOCITY_FALLOFF_RADIUS=20 cells covers 31% of the 64² map vs 9% of 224²; blur sigmas likewise) → systematic distribution shift at serve time. P-SR-02 explicit-config injection exists precisely for this; neither inference site passes it. Gated behind USE_RAP_MODEL=False | 33§measure-first; L8 | Pass TrainingTensorConfig() (or persist the training config in the checkpoint sidecar and rehydrate) at both inference sites; parity test asserting train and serve factories share one config | open |
| F-0027 | P2 | knowledge/graph | Programma_CS2_RENAN/backend/knowledge/graph.py:44-56 (_connect) + call sites | Knowledge-graph connection comment claims a `_conn_lock` that does not exist — shared check_same_thread=False sqlite3 connection used unserialized across threads | R4 LOW comment at _connect: "Cross-thread access is serialized by _conn_lock at the call sites" — grep confirms no _conn_lock anywhere in the file; add_entity/add_relation/query_subgraph all use the cached connection directly (`with self._connect() as conn` = transaction scope, not a lock). sqlite3 connections are not safe for concurrent same-connection use → latent statement corruption/ProgrammingError under threaded callers (exposure = Phase K caller census; singleton factory hands one manager to all threads) | 22§Funzioni come Contratti; 10§lock discipline | Add the promised threading.Lock around every connection use (or per-thread connections); regression test hammering add_entity from 2 threads | open |
| F-0028 | P2 | coaching/hybrid | Programma_CS2_RENAN/backend/coaching/hybrid_engine.py:411-447 (_get_ml_predictions) + :635-727 (_generate_insight) | Hybrid engine computes ML predictions then discards them — the "hybrid" synthesis is RAG + baseline-Z only | _get_ml_predictions runs real inference (legacy path) and returns recommended_kills/adr/hs; the dict is threaded through _synthesize_insights into _generate_insight's parameter list and NEVER referenced in the body; _calculate_confidence's "ml_confidence" uses baseline |z|, not model output. The class docstring's synthesis strategy ("High-confidence ML: Lead with ML, support with RAG") is unimplemented. 26-HYB-01 made model LOADING honest; outputs still contribute nothing | 22§Dead code w/ behavioral risk | Either wire predictions into message content/confidence (design the delta→advice mapping) or delete the inference call and rename honestly (RAG+Z engine); decide at CP0 with the F1 adapter roadmap (adapter may supersede this seam entirely) | open |
| F-0029 | P2 | coaching/jepa-adapter | Programma_CS2_RENAN/backend/coaching/jepa_insight_adapter.py:load_jepa_for_insights + generate_jepa_insights | JEPA insight adapter arms on pretrain-only checkpoints whose coaching head is untrained — noise becomes insights when USE_JEPA_MODEL is enabled | The gate is "trained checkpoint loads" (26-HYB-01 discipline), but jepa_train pretraining saves the FULL state dict — encoders trained, LSTM/MoE/experts coaching head still random (fine-tune is disabled by 26-RANGE-01/TASKS#64). forward_coaching on such a checkpoint = pretrained encoder + random head → sigmoid outputs vary around 0.5 widely enough that |2(out−0.5)| routinely exceeds the 0.1 magnitude gate → fabricated-signal insights (hedged only by observatory maturity, which measures a different thing). Latent: flag defaults off | 33§measure-first; R3-01 anti-fabrication | Persist a head_trained marker in the checkpoint/sidecar (set by the future finetune path) and gate the adapter on it; until TASKS#64 lands, adapter should refuse pretrain-only checkpoints; part of the CP0 JEPA-readiness cluster | open |
| F-0030 | P1 | services/role-insights | Programma_CS2_RENAN/backend/services/analysis_orchestrator.py:686-737 (_analyze_role) + backend/analysis/role_classifier.py:_calculate_role_scores | Live role insight fabricates "IGL (100% confidence)" for balanced-K/D players — the classifier is fed a stats dict containing NONE of its five role-vocabulary keys | Orchestrator passes MATCH_AGGREGATE-shaped player_stats (from run_ingestion → coaching_service → advanced insights, default-on); RoleClassifier scores need awp_kills/total_kills/entry_frags/rounds_played/assists/rounds_survived/solo_kills — none are PlayerMatchStats columns → all affinity scores 0 EXCEPT the IGL kd-balance bonus (0.2 when 0.9<kd<1.2, kd_ratio IS present) → normalization makes IGL 1.0 → "Role: IGL Detected (100% confidence)" insight from a single feature; all other players return confidence 0.0 and are skipped by the cold-start guard (silent). Neural head safely absent (needs rounds_played → None). Resolves B15/B18 verify rows | 22§Funzioni come Contratti | Feed the classifier a stats dict with the real vocabulary (derive entry_frags/rounds_survived/etc. from RoundStats aggregates at the call site) or make classify() REFUSE stat dicts missing its vocabulary (return UNKNOWN, 0.0); regression test: MATCH_AGGREGATE dict with kd=1.0 must NOT yield a confident role | fixed@33d87e5 |
| F-0031 | P2 | services/phase6-contracts | Programma_CS2_RENAN/backend/services/analysis_orchestrator.py:739-798 (_analyze_utility) + game_states consumers (:451,:616,:800) | Phase-6 modules dark on the live path via input-contract mismatches — utility analysis gated on keys no producer emits | VERIFIED: _analyze_utility requires smoke_thrown/flash_thrown/molotov_thrown/he_grenade_thrown in player_stats — PlayerMatchStats carries smokes_per_round/he_damage_per_round/unused_utility_per_round instead → has_utility_data always False → module permanently skipped, silently. PENDING T-DIAG: game_states (needs game_state/action_taken/buy_type keys) producer unidentified — strategy/economy/win-probability modules likely receive [] and skip. Same aspirational-vocabulary class as F-0021/F-0022/F-0030 | 22§Dead code; L10 | Map the *_per_round columns into the analyzer's expected keys (or re-key the analyzer); T-DIAG counts insights by focus_area on a real pipeline run to size the dark-module set; align game_states producer or retire consumers at CP0 | open |
| F-0032 | P1 | control/ml-stop | Programma_CS2_RENAN/backend/control/ml_controller.py:check_state (TrainingStopRequested) + backend/nn/coach_manager.py:322,:570-605 (except StopIteration guards + broad excepts) | Operator STOP is broken — the stop exception is swallowed per phase, training continues, and final status reports a crash instead of a graceful stop | F5-16 correctly changed MLControlContext.check_state() to raise TrainingStopRequested (StopIteration is reserved for iterators), but coach_manager still guards `except StopIteration: raise` while run_full_cycle and EVERY phase wrapper (run_jepa_pretraining/run_rap_cycle/_train_role_head) catch broad Exception → pressing Stop aborts the current phase as a logged "Training Failed", proceeds to the NEXT phase, repeats until all phases "fail", then run_full_cycle's outer except sets teacher=Error; MLController._run_wrapper's `except TrainingStopRequested` graceful path is unreachable. Session_engine's context (B02) historically raised StopIteration — the stale guards' origin | 07§exception hierarchies (never blanket-catch control-flow exceptions) | Catch TrainingStopRequested explicitly (re-raise) in run_full_cycle + every phase wrapper (keep StopIteration guard for the legacy context or migrate session_engine to the same exception); regression test: context.request_stop() mid-phase-1 must abort the WHOLE cycle with teacher=Stopped | fixed@b02cdca |
| F-0033 | P2 | ui/app-state | Programma_CS2_RENAN/apps/qt_app/core/app_state.py:_bg_read (staticmethod, import-failure branch) | AppState's broken-venv diagnostic raises NameError — `self` referenced inside a @staticmethod | The R4 LOW fix added a warn-once guard `if not getattr(self, "_bg_read_error_logged", False)` inside the `except Exception` around backend imports — but _bg_read is a @staticmethod, so `self` is undefined → NameError raised from the except handler → propagates to Worker.error → "AppState poll error: name 'self' is not defined" every 10s, masking the real import failure (venv damage / mid-flight migration — the exact scenario the diagnostic targets). Latent: fires only when backend imports fail | 22§(the fix that broke the fix); 20§threading | Make _bg_read an instance method (Worker(self._bg_read) already binds) or hoist the flag to a module-level variable; regression test: monkeypatch the import to raise and assert one clean warning, no NameError | open |
| F-0011 | P2 | observability | Programma_CS2_RENAN/observability/logger_setup.py:175-210 | N per-logger RotatingFileHandlers + 2 processes share ONE rotating log file — rotation races / interleaving corruption | get_logger attaches a NEW RotatingFileHandler per named logger (propagate=False), all pointed at LOG_DIR/cs2_analyzer.log; Qt app AND session_engine daemon (separate process) both write it; rotation by any handler renames the file under the others (Windows: PermissionError — the documented LS-01 fallback symptom; POSIX: writes continue into rotated .1 via stale fd) | 07§Logging in Produzione (handler architecture; QueueHandler/Listener) | One handler owned by parent logger "cs2analyzer" + child propagate=True; per-PROCESS log file (or SocketHandler/QueueListener aggregation) to end cross-process sharing | open |
| F-0035 | P2 | ui/components | Programma_CS2_RENAN/apps/qt_app/widgets/components/empty_state.py:508-513 (+ screens/match_detail_screen.py:150,265,314) | EmptyState CTA row can never reappear once hidden — match-detail no-data/error states lose their "Back to Match History" button | _update_cta_row_visibility gates on `_cta_button.isVisible()` (EFFECTIVE visibility) — once the row is hidden, children report isVisible()=False regardless of their own flag, so every later set_cta_text(text) computes any_cta_visible=False and the row stays hidden; no public API can restore it. CONFIRMED live: load_match (:150) clears the CTA (row hides), then the no-data branch (:265) and _on_error (:314) set "Back to Match History" → button's own flag goes visible but the row check reads False → both recovery states render a dead-end EmptyState without their CTA (sidebar is the only way out). Constructor-path renders (fixtures) DO show CTAs — the gap is invisible to the render matrix. set_loading path currently has zero callers (dormant same-bug) | 20§MVP (view logic testable sans toolkit) | Check the buttons' OWN flag (`not isHidden()`) — or track intended visibility explicitly; regression: EmptyState(cta_text="X"), set_cta_text(""), set_cta_text("Y") → cta row visible | open |
| F-0040 | P1 | tools/build | tools/build_pipeline.py:execute (Stage 1) + tools/Sanitize_Project.py targets | Build pipeline unconditionally SANITIZES (deletes database.db, hltv_metadata.db, match_data/, models/, logs/) before running tests — even in --test-only mode | execute() runs Stage 1 "Project Sanitization" with --yes (no prompt) BEFORE the `if self.test_only: return` check — so `console build run --test-only` / `goliath build --test-only`, billed as "Run tests without compilation", destroy the operator's local databases, per-match shards, and every model checkpoint with zero warning. Recovery = full re-ingest + re-scrape + retrain (many hours). Design intent (clean-state distribution builds) does not cover the test-only path, and neither console nor goliath help text mentions data loss. COMPOUNDS F-0039: verify_all_safe SPECIAL-CASES build_pipeline to run with --test-only (:25-27), so the "safe" validation runner triggers this wipe deterministically and fast enough to beat its own 120s timeout | 22§principle-of-least-astonishment | Move the sanitize stage AFTER the test_only early-return (or gate it behind an explicit --sanitize flag + console-level confirmation); regression: build_pipeline --test-only against a fixture DB must leave every row intact | fixed@e5fe623 |
| F-0041 | P2 | tools/build | Programma_CS2_RENAN/tools/build_tools.py:~107 (cmd_build PyInstaller line), :~252 (cmd_debug_build shell=True), :~199-205 (cmd_verify manifest check), :~296 (cmd_manifest --verify-only) | build_tools' build/verify/manifest subcommands broken three independent ways: space-unsafe path interpolation + two schema-drift readers | (a) cmd_build interpolates the ABSOLUTE macena.spec path into a command string that run_command shlex.split()s in posix mode — Windows backslashes are eaten as escapes and this machine's space in "Renan Macena" splits the argument; cmd_debug_build interpolates the same path into shell=True (the repo's ONE B602, nosec-justified as "internal") — the unquoted space breaks it there too; PyInstaller stage cannot run from any space-containing checkout. (b) cmd_verify's "Build manifest valid" check expects the pre-R4 single-binary schema {"file","sha256"} while the R4-fixed writer in the SAME FILE emits {"binaries": [...]} — the check can never pass post-fix (same-file reader/writer drift). (c) cmd_manifest --verify-only counts data.get("files") but sync_integrity_manifest writes "hashes" — always prints "0 files". (d) build --force help says "Continue on lint failures" but lint never blocks with or without it. (e) B76: BOTH build paths target `SOURCE_ROOT/macena.spec` which DOES NOT EXIST anywhere in the repo — the real spec is packaging/cs2_analyzer_win.spec (used by build_pipeline/build_production.bat); cmd_build silently reports "macena.spec not found" and cmd_debug_build exits — the consolidated tool can never have built since the packaging move. | 22§contract drift | W3: pass argv LISTS everywhere (kill both interpolations incl. the shell=True), point verify/manifest readers at the current schemas, make the lint gate honest or drop --force | fixed@b9ee3a3 |
| F-0042 | P2 | tools/user-data | Programma_CS2_RENAN/tools/user_tools.py:cmd_manual_entry (~:117-140) | manual-entry writes PERCENT-scale KAST/HS into ratio-scale PlayerMatchStats columns as is_pro=True rows | Prompts read "KAST %:" and "Headshot %:" and store the typed values RAW (71.0 where the column convention is 0.71) with is_pro=True — the exact F-0019 scale-mixing class fixed at every OTHER write boundary (seed_hltv_top20's V-2 FIX normalizes >1.0→÷100; apply_vision likewise); one manual entry poisons pro baselines at ~100× scale. CONSUMER-SIDE CONFIRMED (advisor check, Phase T entry): pro_baseline.py:213 selects `PlayerMatchStats.is_pro == True` with NO data_quality filter and NO sample_weight use anywhere in the file — manual rows enter the baseline directly, and flag_ghost_players' zero-weighting is irrelevant for this consumer (weight never read). | F-0019 family | W2/W3 one-liner: apply the same >1.0→÷100 normalization at entry (kast, hs; leave adr raw), or prompt in ratio terms | fixed@7810bc5 |
| F-0043 | P2 | cli/training | run_full_training_cycle.py (exit path after orchestrator abort) + Programma_CS2_RENAN/backend/nn/training_orchestrator.py ("Training Aborted: Insufficient Training Data" branch) | Training entry point exits 0 on an ABORTED run — automation cannot distinguish "trained" from "refused to train" | T-DIAG reproduction (2026-08-14, this box): `run_full_training_cycle.py --model-type jepa --epochs 1` on a 2-PMS-row DB logs WARNING "Training Aborted: Insufficient Training Data", writes NO checkpoint, and exits 0. Consequences: (a) the latent guard test test_dry_run_checkpoint_integrity::test_real_run_writes_checkpoint fails on any small-data box with "must write at least one checkpoint" — misdiagnosing a data condition as a save bug (the test also lacks a data-sufficiency skip-gate, unlike test_e2e's ≥5-row gate — companion W3 test fix); (b) every automation caller is blind: ingest_pro_demos (F-0039 member #13) runs ingest→retrain bare and would report success with zero training; B47's subprocess evals likewise. Dry-run leg PASSED (Law-7 guard works); production checkpoints byte-identical after the run (BRAIN_DATA_ROOT redirect held) | 30§exit-code contracts | W2/W3: return non-zero (e.g. 3) from the abort branch, or print an explicit machine-readable ABORTED marker + non-zero; add skipif data-gate to the inverse-guard test in the same commit | fixed@75034c9 |
| F-0044 | P2 | infra/testing | Programma_CS2_RENAN/pytest.ini (SHADOW config, 6 lines — absent from the S1 ledger until B76) vs pytest.ini (root, 49 lines) | Duplicate pytest.ini makes test configuration INVOCATION-DEPENDENT — the shadow ini silently disables strict-markers and marker registration for every run scoped under Programma_CS2_RENAN/ | pytest rootdir walks UP from the args: `pytest Programma_CS2_RENAN/tests/<file>` resolves Programma_CS2_RENAN/pytest.ini (testpaths=tests, timeout=30, timeout_method=thread, ONLY the integration marker) — no strict-markers, no slow/flaky/known_fail/portability/timeout registration, different testpaths. The full gate (`pytest Programma_CS2_RENAN/tests/ tests/`) resolves the ROOT ini because the common ancestor is the repo root. Observed live at T-DIAG: PytestUnknownMarkWarning for `slow` + unknown-option warnings for timeout/timeout_method (plugin absent locally) — the Phase-T boundary note "slow mark unregistered" is SUPERSEDED by this root cause. Consequence: single-file dev runs and any CI step that cds/scopes into Programma get a DIFFERENT config than the gate; --strict-markers protection is silently absent in the most common dev invocation. LEDGER GAP: file was missing from the 617-row S1 enumeration — row added at B76 (618 total) | 30§environment resolution + 08§conftest/ini scoping | W3/W4: delete the shadow ini (root ini already covers both trees) or reduce it to a comment pointing at the root; add a meta-test asserting a single pytest config resolves for both invocation shapes | fixed@39811ae |
| F-0039 | P1 | tools/safety | tools/verify_all_safe.py:57-77 (is_safe_to_run) | "Safe" dynamic validation runner executes ≥10 data-mutating tools — including rebuild_monolith, whose default run DELETE-then-rebuilds all pro PlayerMatchStats and would be TIMEOUT-KILLED mid-rebuild (data loss) | B54 UPGRADE EVIDENCE: rebuild_monolith bare invocation runs --phase full; Phase 2 (rebuild_match_stats) DELETES every is_pro PlayerMatchStats row FIRST, accumulates replacements in memory, and bulk-inserts ONLY at the end — under verify_all_safe's 120s subprocess timeout the tick phase skips fast (incremental), Phase 2 clears ~2k pro rows in seconds, then the first multi-minute parse_demo gets SIGKILLed → pro stats left EMPTY, finally-block lock release skipped (stale d_track_running), and save_user_setting("PRO_DEMO_PATH") already mutated settings as a side effect. Recovery requires a full multi-hour rebuild or backup restore. | The filter skips only deprecated + fix_/reset_/migrate_/patch_/cleanup_/force_ prefixes + one hardcoded name. Confirmed bare-invocation MUTATING members it schedules (B51-B53 census): mine_shard_strategies (full mining run — SBERT load + mass CoachingExperience INSERTs), populate_round_stats (parses missing demos, INSERTs + UPDATEs), populate_match_results (INSERTs), repair_ratings (targeted UPDATE), repair_kast (BULK avg_kast rewrite for every pro row with roundstats), repair_equipment_value (429M-row monolith UPDATEs for 8 demos), repair_tick_features (re-parses EVERY demo, bulk UPDATE FROM), flag_ghost_players (UPDATE), purge_default_stats_rag (DELETEs knowledge rows). CENSUS CLOSED at B56 — THIRTEEN confirmed mutating members: the nine above + rebuild_monolith (delete-then-rebuild, the P1 driver), observe_training_cycle (real 1-epoch JEPA train + checkpoint writes), mine_coaching_experience (live inserts), ingest_pro_demos (incremental ingest + FULL retrain cycle). Every other tools/ script verified safe (dry-run default, required args, prompt-gated, read-only, or forced --dry-run/--apply choice). Safe counter-examples in-repo prove the pattern: dry-run defaults (d3_recover, merge_demo_pool, sync_pro_players, rescrape) | 22§TOCTOU-adjacent (convention vs enforcement) | Invert the filter to an explicit ALLOWLIST of known-read-only tools (or require each tool to expose a --check/--dry-run contract verify_all_safe passes); regression: run verify_all_safe against a fixture DB and assert zero row-count deltas | fixed@e5fe623 |
| F-0038 | P1 | ui/threading | Programma_CS2_RENAN/apps/qt_app/screens/profile_screen.py:_save (+ tactical_viewer_screen.py:_start_chronovisor_scan + wizard_screen.py:_finish) | THREE screens touch the DB on the GUI thread — doctrine breach (screens never touch DB) with real freeze risk under ingest contention | profile_screen._save opens get_db_manager().get_session() synchronously in the Save click handler (SELECT + INSERT + commit + refresh) — the code SELF-DOCUMENTS the doctrine two lines above ("screens must not open new DB read paths; DB access belongs to VMs via Worker") and grandfathers this block; tactical_viewer._start_chronovisor_scan runs a main-thread SELECT against PlayerTickState (the monolith tick DB) from the demo-loaded slot. Under an active ingest cycle (F-0037 world: daemon auto-mode default ON) SQLite write contention turns these into multi-second GUI stalls; sibling user_profile_screen does the identical save correctly through UserProfileViewModel+Worker | 20§Event Loop e Threading (never block the GUI thread); L2 lens rule (screens touching DB = auto ≥P1) | Route profile save through UserProfileViewModel (or a tiny ensure_profile VM call) and move the chronovisor match_id lookup into the existing Worker chain; regression: smoke-walk Save/demo-load with a locked DB file and assert the GUI thread never blocks >50ms | fixed@ccbdc8f |
| F-0037 | P1 | ingestion/concurrency | Programma_CS2_RENAN/run_ingestion.py:308-400 (process_new_demos / process_queued_tasks / _queue_files) + apps/qt_app/screens/home_screen.py + settings_screen.py | Zero cross-runner exclusion in the ingestion pipeline — concurrent triggers double-parse demos and write duplicate stats rows | FIVE trigger surfaces share the un-guarded queue (B47 adds batch_ingest.py — parallel ProcessPool bulk ingest whose already-ingested set is snapshotted ONCE up front; the default-ON daemon can race its whole run): session_engine auto cycle (INGEST_MODE_AUTO defaults True), Home's Analyze buttons, Settings' Start Ingestion (each UI screen guards only its OWN worker slot), and console.py `ingest start` → ingest_manager.scan_all (B46: a DIFFERENT consumer implementation over the SAME IngestionTask queue — claim-semantics parity to be reconciled at Pass 2 L3). process_queued_tasks SELECTs status=="queued" as a snapshot then marks "processing" unconditionally (no compare-and-swap) → two runners process the SAME task list; _check_duplicate_demo (:49-84) is check-then-act across the multi-minute parse window → both pass before either commits; _queue_files insert has the same window. Result: same demo parsed twice (minutes of duplicate CPU) and PlayerMatchStats/RoundStats/CoachingInsight rows inserted twice (no unique constraint on demo+player) → inflated match counts and skewed averages on every dashboard | 10§lock-ordering/graceful-concurrency; 22§TOCTOU | Atomic DB claim per task (UPDATE … WHERE status='queued', check rowcount) + a cross-process ingestion file-lock around a whole cycle; UI buttons observe a shared "ingestion running" app_state flag; regression: two threads calling process_new_demos on one queued demo → exactly one PlayerMatchStats row set | fixed@c40f970 |
| F-0036 | P2 | ui/overlay | Programma_CS2_RENAN/apps/qt_app/widgets/toast.py:118-124,157-160 | Toast auto-dismiss timers outlive the widget — RuntimeError tracebacks whenever toasts churn | QTimer.singleShot(auto_ms, self._remove) has NO receiver context, so it fires even after the toast is destroyed. Three interacting removal paths (auto-dismiss, manual ✕, eviction when >3 toasts) each schedule fade_out + singleShot(220, _cleanup); any pair on the same toast → second callback runs on the C++-deleted widget → RuntimeError in the event loop (printed traceback, app continues). Eviction GUARANTEES it: a 4th toast within 5s evicts an INFO toast whose pending 5s timer later fires on the corpse. Real exposure: error storms / batch operations that emit toast bursts | 33§weakref-finalize-over-__del__ (lifecycle ownership); 20§QTimer main-loop scheduling | Use a child QTimer(self) for auto-dismiss (dies with the toast) + a _removing flag to dedupe the three paths; regression: add 4 toasts, advance 6s, assert no RuntimeError and container empty | open |
| F-0034 | P2 | ui/animation | Programma_CS2_RENAN/apps/qt_app/core/animation.py:35-38,164,184 (+ README/README_IT/README_PT.md:333) | MACENA_UI_ANIMATIONS=0 kill-switch honored by only 2 of 10 animation helpers — "animations disabled" contract overbroad | animations_enabled() is checked ONLY by count_up (:164) and sweep_ring (:184); fade_in/fade_out/pulse/cross_fade/slide_in/slide_out/reveal_stagger/collapse_width run regardless. All three READMEs (:333) document the offscreen gate as "animations disabled", and the docstring (:37) promises "grabs capture deterministic end states" — but under the flag, fade paths still attach QGraphicsOpacityEffect (the Linux repaint-crash mitigation rests on call-site discipline alone, even in CI) and slide/fade grab determinism rests on grab timing, not the flag | 22§contract drift | Honor the flag in every factory (return a finished/no-op animation, attach no effect, jump widgets to end state); regression: with MACENA_UI_ANIMATIONS=0, fade_in attaches no QGraphicsOpacityEffect and slide_in leaves geometry at the end value synchronously | open |
