# Findings Register

Severity: **P0** crash/data-loss/wrong-output · **P1** correctness/threading/resource under real use ·
**P2** contract drift, dead code, smells with behavioral risk · **P3** style (tooling autofix or batches ONLY).
`study_ref`: course module§ when a lesson genuinely applies (any module, not only the 8 required), else `-`.
Status: open / approved / fixed@sha / wontfix / deferred.

| ID | Sev | Subsystem | Location | Title | Evidence | study_ref | Proposed fix | Status |
|---|---|---|---|---|---|---|---|---|
| F-0001 | P1 | infra/integrity | integrity_manifest.json:5 + Programma_CS2_RENAN/core/integrity_manifest.json | Integrity manifest stale — gate RED at baseline | `--verify-only` FAIL: 45 changed, 15 new, 0 removed (2026-08-14); root manifest hashes phantom `Programma_CS2_RENAN/main.py` (file absent — Kivy-era entry point); two manifest files + .bak coexist, authoritative one TBD at B58; also reds headless_validator [Integrity] sampling (3/10 mismatch) | - | Isolated resync commit opens W1; remove phantom entries; clarify single authoritative manifest | open |
| F-0002 | P2 | ui/tactical | Programma_CS2_RENAN/apps/qt_app/screens/tactical_viewer_screen.py:416 | Bare tick-rate literal 64 violates SSOT 26-NORM-01 — default gate RED | Guard test test_tick_rate_ssot.py::test_production_code_has_no_bare_64 FAILS naming exactly this line; introduced during design-atlas rebuild; escaped review because CI never ran on the branch (F-0004) | 22§Code Smells: Magic Numbers | Import DEFAULT_TICK_RATE from core.tick_rate; verify exact semantics at B42 (upgrade severity if used in time math for non-64-tick demos) | open |
| F-0003 | P2 | tests/storage | Programma_CS2_RENAN/tests/test_match_data_dangling_symlink.py (6 tests) | Symlink tests unrunnable without Windows symlink privilege — local gate RED on standard Windows | 1 FAILED + 5 ERROR, all `OSError [WinError 1314] A required privilege is not held` at fixture `link.symlink_to(...)`; no capability probe/skipif; CI runners (admin) unaffected | - | Add symlink-capability probe fixture → pytest.skip when privilege absent (CPython support.can_symlink pattern) | open |
| F-0004 | P1 | infra/ci | .github/workflows/build.yml (on.push.branches) | CI branch filter excludes the project's real branch convention — design branch had ZERO CI runs | Filter is `[main, develop, 'feature/**', 'fix/**']` but branches use `feat/*` (46-commit feat/frontend-design-atlas: no workflow runs per `gh run list`); Ubuntu deploy-truth leg never exercised; explains F-0002 escape | - | Add `feat/**`, `chore/**` to filter (keep existing); consider PR-early workflow; part of W1 | open |
| F-0005 | P2 | tools/validator | tools/headless_validator.py ([Structure] Qt app check) | Validator asserts obsolete .qss theme files deleted by design mission — validator RED | `[FAIL] ... No .qss theme files found in apps/qt_app/themes/`; only base.qss.template is rendered since design-atlas rebuild (legacy cs2/csgo/cs16.qss deliberately removed) | - | Update structure check to assert themes/base.qss.template (+ qss_generator contract) instead of *.qss | open |
| F-0006 | P1 | ingestion/parsing | Programma_CS2_RENAN/run_ingestion.py:1108 (+ sister parse_ticks/parse_events call sites) | pyo3 PanicException bypasses demo-parse crash guards — one bad demo aborts ingestion | Docstring at :1099 promises "never aborts on metadata"; guard is `except Exception`; baseline integration run: real demo under get_pro_demo_base() panics demoparser2 0.41.4 (`range end index 16 out of range for slice of length 12`, first_pass/parser.rs:82/92) and PanicException (BaseException subclass) flies through — 3 integration tests red (header meta, grenade extraction, forensic parse_ticks) | 07§(pending read: exception hierarchy) | Catch `(Exception, pyo3_runtime.PanicException)` (or BaseException with KeyboardInterrupt/SystemExit re-raise) at all demoparser2 call sites; separately root-cause the panicking demo at B07/T-DIAG (truncated file vs newer CS2 format vs parser bug — check demoparser2 changelog upstream) | open |
