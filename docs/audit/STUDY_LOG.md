# Study Log — 04-PROGRAMMAZIONE-PYTHON companion

Targeted-mastery track: 22-clean-code, 07-error-handling, 09-type-hints, 10-async,
33-profiling-memoria-gc, 20-gui, 08-testing, 30-troubleshooting — each read immediately
before the batch it informs (see BATCHES.md). Other modules consulted on demand.

| Module | Date | Key lessons (bullets) | Applied to (F-ids / B-ids) |
|---|---|---|---|
| 10-programmazione-asincrona (4,497 ln; leverage sections full — fondamenti, Runner/TaskGroup/timeout, threading-vs-multiprocessing-vs-asyncio, testing, patterns, pitfalls, best practices; §13-16/18-19 async-I/O-lib+uvloop tours SKIMMED as N/A for this thread-based codebase) | 2026-08-14 | • Concurrency decision table VALIDATES repo architecture: threading for I/O daemons ✓, no-asyncio ✓ (only async surface = remote_file_server under uvicorn's loop) • Executor semantics: course's own examples use shutdown(wait=False) — reinforces F-0013 fix (with-block join is the trap) • Graceful-shutdown pattern (signal→flag→drain→bounded join) = session_engine's design ✓ • Blocking-the-event-loop pitfall → L2 micro-check: remote_file_server async-def endpoints do sync os.scandir (small, acceptable; note) • Task-reference-loss / unawaited-coroutine warnings → N/A (no create_task in repo) • Bounded-queue backpressure: Digester's limit=1-per-cycle DB-queue consumption is an acceptable equivalent • Lock-ordering deadlock rule → L2 lens row (repo locks: _settings_lock, _TRAINING_LOCK, engine caches — verify no nested cross-order) • PEP 703 free-threading: no impact on this architecture near-term • GIL: demoparser2 (Rust) releases GIL during parse — thread-based parse timeout design is sound in principle, just wrongly implemented (F-0013) | F-0013 fix design; L2 lens rows (lock ordering, endpoint blocking); B09/B10 daemon rubric |
| 09-type-hints-e-mypy (3,933 ln, full read) | 2026-08-14 | • 5-phase strict-migration playbook = the W4c blueprint: F0 baseline error count (repo's current mypy config IS the course's F0 posture: check_untyped_defs + ignore_missing_imports, CI non-blocking) → F1 public interfaces → F2 warn_return_any/no_implicit_optional + per-module disallow_untyped_defs → F3 strict for new modules → F4 legacy module-by-module → F5 global strict • `strict=true` expands to 13 flags (know them to ratchet selectively) • warn_unused_ignores prunes stale type:ignore • `# type: ignore[code]` always with code + reason • cast() lies without runtime check — prefer isinstance narrowing or TypeGuard/TypeIs → census cast( callsites • Protocol = structural interfaces (already idiomatic candidate for backend service seams) • NewType (app_types MatchID/Tick/PlayerID ✓ validated pattern) • TypedDict for JSON shapes (✓ DemoMetadata) • TYPE_CHECKING breaks annotation-only cycles (✓ exceptions.py) • sqlalchemy mypy plugin exists; SQLModel typing story to verify at B04 • --linecount-report quantifies typing coverage per module (W4c progress metric) • abstract param types / concrete return types • @override (3.12) catches rename drift — candidate once mypy ratchets | W4c design (primary); B04 ORM typing rubric; L10 cast/type-ignore census; S6 additions |
| 07-error-handling-e-logging (4,278 ln, full read) | 2026-08-14 | • Exception vs BaseException doctrine: KeyboardInterrupt/SystemExit/GeneratorExit live OUTSIDE Exception; only legit `except BaseException` = flush-then-reraise frameworks → F-0006 fix must catch PanicException EXPLICITLY, never blanket BaseException • `raise` (bare) preserves traceback, `raise e` destroys it → S6 census `raise e` • Domain vs infrastructure exception hierarchies w/ stable Enum error codes (mai mischiare — retry semantics differ) → compare observability/error_codes+exceptions at B03 • logger.exception()/exc_info=True for tracebacks → L4 census of tracebackless error logs • contextvars do NOT propagate to threads (copy_context().run needed) → check set_correlation_id vs QThreadPool workers (L2/L4) • QueueHandler/QueueListener for non-blocking file logging (UI thread jank check at B03) • Sensitive-data masking filters in logging pipeline (repo has mask_secret — used systematically? L9) • Retry+exponential backoff+jitter, retry BUDGET (SRE cascade prevention), circuit breaker 3-state → B08 external clients rubric • Graceful degradation chains (ML→popular→static) = hybrid_engine's pattern, verify ordering+logging • suppress() for intentional ignores; else-clause to narrow try scope; EAFP preferred • Sentry: before_send PII scrub, breadcrumbs, LoggingIntegration levels → B31 telemetry_client rubric | F-0006 fix design; B03 rubric (imminent); L2/L4/L9 lens definitions; B08/B31 rubrics |
| 22-clean-code (4,051 ln, full read) | 2026-08-14 | • §19 Code-Review Checklist (7 sections: leggibilità/struttura/type-safety/sicurezza/testing/qualità/config) adopted verbatim as the pass-1 per-file rubric • Extended smells catalog: data clumps, shotgun surgery, primitive obsession, inappropriate intimacy — on top of long-methods/god-class/feature-envy/magic-numbers/deep-nesting/duplication/dead-code/flag-args • Thresholds: function <50 ln, file <800 ln, cyclomatic <10, nesting ≤4 • CQS (command vs query, `stack.pop` pragmatic exception) • Value objects (frozen dataclass slots=True) vs primitive obsession • Null Object kills scattered None-checks • mypy strict adoption ladder = global strict + per-module overrides, shrink overrides progressively (feeds W4c) • TODO/FIXME require ticket refs • Boy-Scout rule bounded by our no-drive-by-fix discipline (findings register instead) • Tech-debt quadrants: track deliberate-prudent explicitly | Rubric for ALL pass-1 batches (B01–B76); F-0002 (magic number); W4b ruff ruleset candidates (E,W,F,I,UP,B,SIM,C4,DTZ,T20,RUF,PT,ERA per §Pre-commit); W4c mypy ladder |
| 33-profiling-memoria-gc (3,887 ln; §1-8 full — CPython allocator hierarchy/pymalloc arenas, PyObject layout, refcounting, generational GC thresholds/freeze, gc module, CPU profiling deterministic-vs-statistical, memory-profiling toolchain tracemalloc/memray/fil, weakrefs incl. WeakValueDictionary caches + finalize-over-__del__; §18 memory-leak catalog full; __slots__/interning/copy/benchmarking SKIMMED as N/A — torch/numpy dominate Phase-N memory, not PyObject overhead) | 2026-08-14 | • Leak catalog (5 causes: cycles w/ external resources, unbounded global caches, closure capture, __del__ resurrection, thread-local growth) = the Phase-N rubric; Teacher daemon is the course's "applicazione long-running" archetype • Torch translation of the unbounded-cache lesson: losses accumulated WITHOUT .item() retain entire autograd graphs → check jepa_train/jepa_trainer/training_orchestrator for .item()/detach discipline, zero_grad(set_to_none), eval-under-no_grad, del-batch-refs • weakref.finalize > __del__ (S6 census __del__); WeakValueDictionary = canonical model-cache shape • gc.collect() placement after model swaps/checkpoint reloads • tracemalloc snapshot-diff = the diagnostic to prescribe in leak findings (T-DIAG bounded runs); memray for native torch allocations (Linux only) • Refcount+cycle GC mechanics ground the L5 lens vocabulary | B19-B25 rubric (primary); L5 lens definition; S6 __del__ census; T-DIAG diagnostics |
