# D-B01 — core-config (10 files, 1,295 LOC) — pass 1 read 2026-08-14

Rubric: course 22 §19 checklist. Study companion: 22-clean-code (see STUDY_LOG).

## Programma_CS2_RENAN/core/config.py (577)
- **Purpose**: settings/paths/secrets hub. Import-time side effects BY DESIGN: .env load
  (CFG-ENV-01), load_user_settings() (disk+keyring), makedirs ×5, configure_log_dir wire-up.
- **Contracts**: SETTINGS_PATH pinned to pre-reassignment get_writeable_dir() (C-02);
  CORE DB always in project folder (portability doctrine); BRAIN_DATA_ROOT only for
  regeneratable artifacts; secrets via keyring w/ "PROTECTED_BY_WINDOWS_VAULT" disk mask
  (C-05/FE-04/FE-06); $HOME refused as shard root (_resolve_match_data_path); env vars
  override path keys (Docker); thread-safety via RLock — accessors get_setting/
  get_credential/get_all_settings safe, BARE MODULE GLOBALS ARE SNAPSHOT-STALE (C-01,
  documented) → L6 census of `from config import <GLOBAL>` consumers.
- **Risks/findings**: F-0007 set_secret raises→save crash asymmetry (:168,:533);
  F-0008 get_pro_demo_base $HOME fallback (:461) contradicts :186-190 doctrine.
- Dossier-only notes: refresh_settings FONT_SIZE default 14 (int) vs canonical "Medium"
  (unreachable — defaults dict always seeds key; latent trap) :496; Russian comment
  fragment :348 (P3 batch: stray non-EN comments); _SETTING_NAME_TO_GLOBAL is a set,
  name says map :508; .env parser minimal-by-design (quoted-value edge cases documented).
- **X-refs**: logger_setup (circular-import break via configure_log_dir :386-388);
  every subsystem imports this module → its import side effects run everywhere.

## Programma_CS2_RENAN/core/tick_rate.py (64)
- **Purpose**: 26-NORM-01 SSOT — THE only sanctioned tick-rate literal.
- **Contracts**: resolve order metadata→header→None (None = honest sentinel, caller
  decides); validity [32,256] rejected LOUDLY not clamped; guard test forbids bare 64
  elsewhere (currently red via F-0002 in tactical_viewer).
- Exemplary module. No findings. Anchor for L1 lens.

## Programma_CS2_RENAN/core/constants.py (39)
- Seconds-only temporal constants; import-time tick derivations deliberately removed
  (R4 HIGH 26-TICK — tick windows computed at point of use from per-demo rate).
- TICK_RATE legacy alias of SSOT → L10: census importers, migrate to explicit SSOT import.

## Programma_CS2_RENAN/core/app_types.py (116)
- NewTypes (MatchID/Tick/PlayerID), numeric Team enum (R1-02: STRING twin lives in
  demo_frame.py; AT-01 cross-enum __eq__ raises TypeError fail-fast; bridge
  team_from_demo_frame raises on unknown). PlayerRole canonical SSOT (P3-01) —
  **display_name English-only** → L7: check UI consumers translate roles.
- IngestionStatus enum; DemoMetadata/PlayerStats TypedDicts (L1 shape anchors).

## Programma_CS2_RENAN/core/lock_files.py (243)
- **Purpose**: cross-process named locks (D-track ↔ HLTV-track), repo-root .locks/.
- **Contracts**: atomic O_CREAT|O_EXCL acquire (R4 HIGH TOCTOU fix); stale-holder
  reclaim via atomic os.replace (single winner); unreadable-lock grace window
  (50×10ms, S-R3); Windows liveness via GetExitCodeProcess (26-WIN-02 — os.kill
  unreliable); signal handlers release-then-reraise (SIG_DFL + os.kill self).
- **Risk/finding**: F-0009 release() unlinks WITHOUT ownership check — deletes a
  foreign LIVE lock if called unheld (docstring claims no-op); verify callers Phase C.
- Notes: .stale.<pid> orphans possible (cosmetic); handlers process-global (test
  leakage history per recon).

## Programma_CS2_RENAN/core/lifecycle.py (180)
- **Purpose**: single-instance (Windows named mutex) + Session Engine daemon Popen.
- **Contracts**: fail-closed on mutex anomalies (NULL handle, R4 LOW); daemon logs →
  repo root daemon_out/err.log (W5 hygiene target); CORE-12 handle cleanup on
  re-launch; shutdown terminate→3s→kill.
- **Risk/finding**: F-0010 ensure_single_instance NO-OP on non-Windows ("Windows-only
  deployments" comment) — contradicts Linux-as-deploy-target doctrine; two instances
  on Linux = concurrent DB writers. lock_files.py is the in-house cross-platform fix.
- Notes: atexit.register per launch_daemon call (duplicate registrations, idempotent-ish);
  Popen stdin=PIPE "IPC capability" — verify session_engine reads it (B02).

## Programma_CS2_RENAN/core/registry.py (52)
- **KivyMD-era ScreenRegistry** (imports kivymd → ImportError fallback MDScreen=object).
  Thread-safe (REG-01). Almost certainly DEAD in Qt era → S6/L10 caller census;
  W3 removal candidate with the other Kivy residue.

## Programma_CS2_RENAN/core/frozen_hook.py (22)
- PyInstaller freeze support + chdir(_MEIPASS); SELF-EXECUTES at import (hook() at
  bottom) — import = side effect, by design for frozen entry. Cross-check packaging
  spec imports it (B76) and nothing imports it casually (S5).

## __init__.py ×2
- P/__init__.py: version "1.0.0" only (pyproject also declares 1.0.0 — keep in sync).
  core/__init__.py: empty.
