# D-B17 — belief/game-tree (4 files, 1,428 LOC) — pass 1 read 2026-08-14

## analysis/belief_model.py (546)
- **R4 HIGH ClassVar war story**: THREAT_DECAY_LAMBDA as dataclass FIELD baked
  the default into __init__ → calibrator's class-level write was a silent no-op
  for every new BeliefState; now ClassVar ✓ (course-22 caliber Python lesson).
- **R4 MED anti-fabrication war story**: extract_death_events used to invent
  health=60/30 for force/eco rounds → "critical" bracket silently calibrated to
  ECO death rates; now DROPS non-full-buy rows, calibrates only "full" ✓ R3-01.
- R4 LOW atomic dict-REBIND for _WEAPON_LETHALITY (Teacher daemon writes vs
  analysis-thread reads; one GIL store, no half-applied .update) ✓ sophisticated.
- AC-05-01 min samples; safety bounds on every calibrated param; A-01 non-finite
  polyfit guard; A-02 singleton re-raise; F4-01 5k OOM cap; narrow exception
  catches on snapshot persist ✓.
- L8 notes: estimate_with_uncertainty seeds rng=42 PER CALL → deterministic
  quadrature, not true MC (naming nit); calibrate_weapon_lethality computes
  kill FREQUENCY ratio (usage-confounded, not per-engagement lethality —
  docstring honest, name misleading; bounds mitigate). P3/L10: literal 4000
  duplicates round_phase_full_threshold; get_session("default") dead label.

## analysis/game_tree.py (528)
- TWO documented search-bug war stories: R4 MED TT key missing node_type
  (max/chance shared slots → wrong cached values); R4 MED TT depth-direction
  (`cached <= depth` reuse rule; old `>=` reused shallow evals) ✓✓.
- Bounded computation (node budget 1000 + TT cap 10k w/ FIFO evict); md5
  usedforsecurity=False; DESIGN note on symmetric push simplification (leaf
  predictor corrects asymmetry); F4-03 public evaluate_single_action; lazy
  predictor import; fallback leaf heuristic w/ deterministic boundaries ✓.
- **L10 rows**: (1) built trees are max→chance→max only — _expand's
  is_max=False branch, evaluate's "min" branch, and _MIN_ACTIONS are DEAD
  (engine is expectiMAX w/ opponent model; class name + docstring say
  expectiminimax — doc drift). (2) GameNode.value keeps leftover opponent
  probs; the "overwritten by minimax evaluation" NOTE is false (evaluate
  never writes node.value — harmless, comment fix). (3)
  learn_from_match._infer_action_from_event keys ("first_kill", "entry_frag",
  "flash_thrown", "rotation_detected") match NO known match_event_state
  vocabulary → everything infers "hold" if fed real events; caller census
  at Phase K.
- OpponentModel: economy/side/advantage/time adjustments + EMA-blended learned
  profiles (≥10 rounds, cap 0.7) + double-floored normalization ✓ sound.

## analysis/deception_index.py (253)
- R4 MED 26-TICK: FLASH_BLIND_WINDOW in SECONDS × per-demo rate; analyze_round
  tick_rate KEYWORD-ONLY REQUIRED (same structural kill as kast.py) ✓; D-01
  two-step searchsorted OOB masking ✓; AC-04-01 extent-normalized displacement;
  P8-04 hand-tuned weights w/ validation plan ✓.
- **F-0021 (P2)**: _detect_flash_baits filters `event_type == "player_blind"`
  — CS2 emits ZERO player_blind events (the documented reason
  round_stats_builder synthesizes blinds from flash_duration). On CS2 input:
  blinds always empty → bait_rate 1.0 (every flash "a bait") whenever any
  "flashbang_throw" rows exist, else metric dead at 0.0. Composite index
  (0.25 weight) inflated/deadened accordingly. Also verify "flashbang_throw"
  vs actual event vocabulary at Phase K/T-DIAG.
- L8: _detect_sound_deception = inverted crouch ratio (crude proxy; typical
  non-crouch play scores "deceptive" — weak metric, coaching flavor only).

## analysis/__init__.py (101)
- EAGER barrel imports of all 11 engines (pulls torch via win_probability) —
  inconsistent with feature_engineering's PEP-562 lazy pattern built for the
  import-lock deadlock hazard; L2/L10 row: census who imports backend.analysis
  from daemon threads.
- Exports PlayerRole + ROLE_PROFILES from role_classifier — **B18 must verify
  role_classifier re-exports core.app_types.PlayerRole (P3-01 canonical) and
  does not define a SECOND enum** (duplicate enums → cross-module identity
  comparisons silently fail → extract_role_features would return {} forever).
