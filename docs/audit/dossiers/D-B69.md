# D-B69 — UI test suites (7 files, 1,978 LOC) — pass 1 read 2026-08-14

Zero new register findings. The named-gate suite read end-to-end;
F-0034's partial-compliance boundary is confirmed BY the tests.

## test_ui_smoke.py (126) — the named gate, read fully
- Boots the REAL composition: ThemeEngine.register_fonts + apply_theme
  → MainWindow → app._create_screens() → register_screen each →
  1440×900 show. Backend daemons/SBERT intentionally absent
  ("the UI layer must never require them to stay alive" — doctrine
  in docstring ✓).
- Four tests: walk every screen through on_enter/on_leave, ending on
  match_history WITH ui_fixtures.inject (painted paths execute);
  live theme switching across THEME_NAMES + CS2 asserting each
  theme's accent_primary lands in qapp.styleSheet(); language
  roundtrip en→it→pt→en with finally-restore; sidebar
  collapse/expand + three window resizes. QT_QPA_PLATFORM=offscreen
  + MACENA_UI_ANIMATIONS=0 set in-file (deterministic end states).

## test_qt_core.py (1,101) — the widget-layer anchor
- Screen contracts PARAMETRIZED per screen: importable, has on_enter,
  constructable — auto-covers new screens if the param list is
  maintained (cross-ref B48: headless_validator's stale screen list;
  this suite's list is the one to trust).
- i18n bridge (known key, fallback-to-key, switch, reject-unknown,
  JSON translations loaded); Worker signal contract
  (result/error/finished-always); AppState diff-apply (emit-on-
  change, skip-unchanged, None-safe, notifications); ThemeEngine
  (all themes resolvable, QPalette from tokens, rating color/label
  pins); Typography (role sizes from tokens, weight override, mono
  family, unknown-role fallback).
- Component library: ProBadge side roundtrip, StatBadge rating
  thresholds, EmptyState icon-well/ghost-link, Toast title+caption +
  container EVICTION/refit (F-0036's eviction path exercised — the
  fire-on-corpse timer itself is NOT pinned; finding stands),
  ProgressRing presets, ChatPanel (meta bubble, roles, clear,
  suggestion click, submit clears+emits, status header), shared
  primitives (DriversList, TipBox, NumberedStep, DbRecordCard,
  MonoFooter, MapTile, MetricBarRow, DeltaChip incl. hide rules).
- Pro-comparison PURE functions: radar frame15 fixture shapes,
  neutral midpoint for empty axis, h2h winner rule + fixture
  outcomes, style summaries (support/entry/anchor/damage/balanced).
- **F-0034 boundary CONFIRMED by tests**: count_up + sweep_ring pinned
  BOTH ways (disabled → immediate end state; enabled → reaches end
  value). Exactly the two compliant sites; no test claims the flag
  gates anything else. Register evidence unchanged.
- Stepper labels (geometry unchanged unlabeled, grows+paints labeled,
  count mismatch raises, navigation works).

## test_charts.py (264)
- Per-chart GEOMETRY MATH pins (radar vertex positions incl.
  bottom/left, sparkline y-mapping extremes + zero-span safety,
  utility bar w-mapping proportional/clamped, economy half-x +
  non-negative rects, momentum API) + offscreen renders.
- **TestQtChartsRetired**: source-scan guard asserting NO QtCharts
  references remain in qt_app — a residue-guard pattern (Kivy-style
  tombstone as a test).

## test_detonation_overlays.py (130)
- CS2 game-data radius constants pinned per nade type (+ positive,
  colors valid QColors, overlay wired into nade drawing).

## test_tactical_frame_widgets.py (194)
- Tactical frame widget contracts (player dots/HP/economy strips) —
  the viewer's paint-layer net.

## test_ui_harness.py (120)
- **test_i18n_key_parity_across_languages — the L7 parity GATE at
  unit tier** (en/it/pt key sets compared); design fonts bundled;
  register_fonts scans assets dir; harness renders home+history to
  tmp_path PNGs (the screenshot pipeline's own test).

## test_design_token_references.py (43)
- R4 HIGH (2026-07-16) token-reference contract: every `tokens.X`
  reference across qt_app sources must exist on the tokens object —
  kills the runtime-only-typo class; plus a scanner-sees-sources
  meta-test (guards against the scanner silently scanning nothing —
  the dead_code_detector Phase-C lesson applied to a different
  scanner ✓).
