# D-B39 — tactical widgets + overlay (10 files, ~2,215 LOC) — pass 1 read 2026-08-14

## skeleton.py (110) — pulse carry RESOLVED CLEAN
- SkeletonRect: pulse started in showEvent, **stopped + dropped in
  hideEvent** (the exact stop-discipline the B35 pulse() contract needs);
  once-guard re-arms on re-show ✓. SkeletonTable reveal_stagger
  once-guard ✓ (B35's empty-return P3 is moot here — nothing to stop).
- Residual L2/L5 lens row: pulse animates a QGraphicsOpacityEffect in a
  loop; a window RESIZE while skeletons pulse = effect-active-during-
  layout on real-display Linux (offscreen CI green; same accepted
  residual class as card shadows — CP0 may choose painted-alpha pulse
  as belt-and-braces).
- P3: painted base alpha 0.4 multiplies under the 0.3-0.8 effect pulse
  (effective 0.12-0.32 — visual nit).

## toast.py (189)
- **F-0036 (P2)**: auto-dismiss QTimer.singleShot(auto_ms, self._remove)
  holds no receiver context → fires on destroyed toasts. Three removal
  paths (auto / manual ✕ / eviction >3) each schedule fade + a second
  singleShot(220, _cleanup); any pair on one toast → callback on the
  C++-deleted widget → RuntimeError traceback. Eviction GUARANTEES the
  race (4th toast within 5s). Fix: child QTimer(self) + _removing flag.
- Frame-20 anatomy ✓; i18n severity titles ✓; container floats outside
  layouts, hidden-when-empty (SA-24 event transparency) ✓; slide_in
  geometry anim w/ doctrine comment ✓; _remove fade_out on an overlay
  child (stable context — doctrine-consistent).

## tactical/map_widget.py (907) — flagship file, zero findings
- TICK_RATE from core.tick_rate ✓ 26-NORM-01. load_map_zones pure +
  validated + clamped, degrades to [] ✓. Palette rebuilt per paint
  (theme-tracking) w/ documented baked alphas ✓.
- War stories in-file: score-layout precompute (was ~15 QFontMetrics per
  frame at 60fps), silent-blank radar warning, purple→info token analog
  for ghost overlay, one-name-only labeling rationale.
- _world_to_screen: "Qt top-left origin matches radar origin — no Y
  inversion" — consistent with the settled F-0017 orientation doctrine ✓.
  FoV cone rotate(90 − yaw) math VERIFIED correct for radar coords.
- Detonation radii converted world-units→px via world_to_screen delta
  (map-scale-aware) ✓; trajectory z-height→width/alpha encoding; smoke
  growth curve; molotov sin-pulse (advances only during playback) ✓.
- P3 notes: in-flight nade dot chords trajectory[0]→[1] while the drawn
  path shows the full curve (visual approximation, uncommented); trail
  is_ct cached at first sighting → halftime team swap keeps first-half
  color until clear_trails (B42: verify seek/round-boundary clear
  wiring); "Map: {name}" fallback text is raw EN (debug surface).

## tactical/player_sidebar.py (424)
- 60fps discipline showcase: painted _StatBar (no QSS reparse), style
  buckets applied only on (alive, selected, defuser) change, per-label
  text caches, Signal(object) for >int32 Steam IDs (documented) ✓.
- FIELD-GAP honesty: has_defuser / death_info absent from
  InterpolatedPlayerState — superset-tolerant rendering documented ✓.
- CARRY (theme-staleness family, B42): _apply_styles bucket cache keeps
  old-token instance stylesheets after a theme switch until a state
  flip; _team_color captured at construction. Same family as
  FilterChip/StatusChip (B41/B43 item).
- i18n: DEAD/ALIVE/killed_at/killed_by/util words all get_text'd ✓.

## tactical/timeline_widget.py (294) — zero findings
- Kind-differentiated chronovisor glyphs (★/◆/●) w/ forward "clutch"
  kind documented; scanner-object OR dict tolerant parse; sorted marks;
  public star_hit_test for tests ✓. Palette per paint; empty-state
  i18n'd; playhead caption clamped; drag-scrub ✓.

## coaching/chat_panel.py (271)
- VM-agnostic doctrine in docstring ✓; i18n throughout ✓; bubble
  natural-width hugging + resize re-apply + streaming update path ✓;
  selectable text; suggestion chips w/ default-arg capture ✓.
- **CARRY B43 (critical contract)**: update_last_message rewrites the
  LAST bubble — the coach screen must append a placeholder coach bubble
  BEFORE the first streaming chunk, else the user's own bubble gets
  overwritten. Verify the stream wiring order in coach_screen.
- P3: bubble._text_label dynamic attribute; _scroll_to_bottom
  singleShot(0) lambda on the scrollbar (window is one loop turn —
  negligible, unlike F-0036's multi-second timers).

## _paint_utils.py (12), tactical/__init__ (1), coaching/__init__ (5),
## widgets/__init__ (1) — trivial ✓.
