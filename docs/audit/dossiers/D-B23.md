# D-B23 — nn-support (9 files, 1,886 LOC) — pass 1 read 2026-08-14

## nn/training_callbacks.py (113) — F3-31 documented opt-in ABC (no
  @abstractmethod by design); NN-L-13 dedup; per-callback exception isolation
  in fire(); close_all ✓. Clean.

## nn/training_controller.py (164) — monthly quota + last-5 cosine diversity
  gate; NN-M-13 configurable limit; fail-safe deny on error ✓. Honest
  "prototype" note (monthly count proxies ALL PlayerMatchStats; real impl
  needs a TrainingLog table). avg_hs baseline 0.4 ratio-consistent ✓.
  L10: caller census (session_engine?) at Phase K.

## nn/training_monitor.py (165) — #27 NaN/Inf→null coercion + allow_nan=False
  loud regression guard; NN-L-12 atomic write w/ BaseException cleanup (same
  hardened pattern as persistence) ✓. L6: default log path CWD-relative.

## nn/evaluate.py (70) — SHAP optional w/ NN-EV-01 sample-mean baseline
  rationale; writes 10 weight keys of 25 MATCH_AGGREGATE_FEATURES names
  (documented; consumers must tolerate missing 15 — B24 check). Clean.

## nn/win_probability_trainer.py (143) — 9-dim offline trainer; A-12 alias
  REMOVED (cross-load prevention); P1-01/02/04/10 guards; saves BARE
  state_dict ✓ (B16 format question: format matches predictor expectation,
  but ARCHITECTURE doesn't — 9-dim vs 12-dim). **Phase-K census**: who calls
  train_win_prob_model and with which path — if it writes the canonical
  win_prob_predictor.pt, A-12 rejects it at load (documented 26-WINPROB-01
  dormant state; predictor stays heuristic until the R9 12-dim retrain).

## nn/train.py (302) — legacy entry point
- NN-JM-04 R4 MED fix ("first update_target_encoder tripped the guard —
  branch was unusable"); NN-61 R4 LOW comment-honesty fix (claimed
  vectorization that never existed here); P1-01/02/04/06/11 guards; amp
  contract honored; ML_INTENSITY throttling delay; saves as "latest" via
  hardened save_nn ✓ (factory name-consistent).
- **L10 row**: THIRD parallel JEPA training path (train_nn's 5-epoch
  "Activation prototype" + jepa_train.py CLI + orchestrator production) —
  census which are alive at B24/Phase K; consolidation candidate for W3.

## nn/tensorboard_callback.py (248) — graceful no-op w/o TB; comprehensive
  scalar/histogram coverage (RAP sparsity, JEPA losses, gates, beliefs,
  concept norms); F3-35 documented lr/group_0 layout limitation; per-epoch
  flush + close ✓. Clean.

## nn/embedding_projector.py (239) — optional UMAP/matplotlib (Agg backend,
  headless-safe); plt.close(fig) — no figure leak (module-33 ✓); narrow
  catches; interval-gated. P3/L10: _CONCEPT_NAMES duplicates jepa_model's
  canonical CONCEPT_NAMES (sync hazard — import instead).

## nn/maturity_observatory.py (442) — 5-state maturity machine
- R4 LOW state-lag fix (classify from CURRENT snapshot; epochs 1-2 were
  always "doubt"); role_stability one-epoch lag documented as STRUCTURAL
  (circularity avoidance) ✓ exemplary honesty; PRE-6 concept-temperature
  saturation alarm (modernization §8.3: 5% band, 10-epoch patience,
  one-shot latch w/ re-arm, lower=binary-collapse vs upper=uniform
  diagnosis) ✓; normalized entropies; crisis/mature/conviction thresholds
  named. P3: _classify_state param `current` shadowed by reassignment.

Zero register findings this batch.
