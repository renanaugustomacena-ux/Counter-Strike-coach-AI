# D-B66 — feature/tensor/skill tests (5 files, 2,821 LOC) — pass 1 read 2026-08-14

Zero new register findings. L1/L8 contract nets verified strong.

## test_feature_extractor_contracts.py (467)
- 25-dim contract: names count == METADATA_DIM, no duplicates; value
  ranges; no-NaN; context features; batch == individual extraction
  (element-wise equality loop).
- **26-VEC-01 pin — concurrency-correct test design**: the P3-A gate
  must count only THIS batch's clamps; test simulates a phantom
  concurrent thread bumping the GLOBAL clamp counter while a clean
  batch runs — old global-delta logic would false-raise
  DataQualityError at "100% clamp rate"; thread-local counter → no
  raise. One of the most sophisticated tests in the repo.
- D1/D2 quality gates: single-sample sliding gate + clamp-log
  throttle pinned via mock-logger with R4-14-01 / R4-14-01/D2
  message-tag assertions.

## test_feature_kast_roles.py (663)
- KAST: per-round calculation matrix (survival/kill/assist/trade
  paths), percentage aggregation, estimate_kast_from_stats — NOTE:
  estimator tests pin BOUNDS ([0,1], >0.7 qualitative) not accuracy;
  coherent with the L8 KAST-provenance ladder (the ~0.91 inflation
  documented at repair_kast is NOT contradicted by these tests).
- Role classification + role features + coaching focus; coaching
  dialogue intent/system-prompt/chat-messages/offline/constants
  (Prop 9 surface covered here rather than a dedicated file).

## test_round_stats_enrichment.py (353)
- Q1-01 blind metrics documented in the fixture builder (sum of enemy
  blind durations + DISTINCT enemies set); aggregate-round-stats-to-
  match; enrich-from-demo import path; round-boundary builder.
  Q1-02's discarded-enrichment war story (B52) has its producer side
  covered here.

## test_skill_assessment.py (353)
- Skill axes, vector calculation, sigmoid approximation bounds,
  curriculum level, skill tensor — the RAP skill_vec feed contract.

## test_tensor_factory.py (985) — the L1 anchor suite
- Every tensor path shape-pinned: map/view/motion legacy AND POV
  (3, res, res); generate_all at res 16; world_to_grid; normalize;
  FOV mask shape + **FovConeOrientation class (the no-Y-inversion
  rotate(90−yaw) doctrine's test side)**; TickYawAccessor; draw-circle
  intensity semantics; gaussian blur; resolution independence; edge
  cases.
- Uses THE SAME mirage MapMetadata constants as ui_fixtures
  (pos_x=-3230, pos_y=1713, scale=5.0) — radar SSOT coherent across
  test suites ✓ (map-geometry cluster input).
- Thread-safety test spawns 10 REAL threads on the singleton accessor
  (join timeout 5s, error collection) ✓; singleton reset discipline
  (_factory_instance = None per test — module-global mutation, safe
  under sequential pytest).
