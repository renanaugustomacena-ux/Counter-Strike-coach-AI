# D-B20 — jepa-model + role-head (2 files, 1,475 LOC) — pass 1 read 2026-08-14

## nn/jepa_model.py (1,148) — JEPA/VL-JEPA architecture
- Sound JEPA mechanics: P1-07 no_grad target encoding (EMA-only updates);
  NN-JM-04 frozen-check RAISE before EMA (corruption guard); MoCo v3 queue
  (4096, correct wraparound, buffer-registered → persisted); learned InfoNCE
  temperature (CLIP-style); VICReg regularizer w/ <2-batch guard; J-3/Phase-3A
  top-2 sparse MoE with Switch-Transformer load-balancing aux loss stored on
  self (**B21 must verify the trainer consumes _moe_aux_loss**).
- War stories in place: WR-52 sigmoid-vs-tanh underprediction; R4 LOW
  selective-decode ANY-sample rule (batch-mean averaged away the one sample
  that needed decoding); Supplement_N260 provenance on dropout 0.15 and τ 0.10.
- ConceptLabeler: NN-JM-03 label-leakage warning on the heuristic path +
  G-01 outcome-based label_from_round_stats preferred ✓ honest ML hygiene;
  feature-index table documented against METADATA_DIM=25 ✓ L1-consistent.
- **F-0016 consumer #4**: label_from_round_stats reads round_won → concepts
  3/4/5/6/11/14/15 poisoned if winner-dtype bug real (economy labels stuck
  0.3, utility_effective never fires).
- **B21 VERIFY (potential L8 finding)**: vl_jepa_concept_loss applies
  BCE-with-logits to concept_logits — forward_vl exports that key as RAW
  cosine similarity ∈ [-1,1] (temperature applied only to concept_probs) →
  sigmoid ceiling 0.73 makes labels ≥0.8 unfittable. Check which tensor the
  trainer feeds; fix = scale by temperature before BCE (or export scaled).
- Minor: is_pretrained is a PLAIN attribute (not in state_dict) — loader must
  restore it or a loaded-pretrained model runs encoder with grad enabled
  (B21/B24 verify); `enable_grad()` branch can re-enable grads inside caller
  no_grad (memory-only, L5); label_batch O(B×T) Python loop (training-time,
  fine); __main__ self-test uses print (P3, S5 row).

## nn/role_head.py (327) — Proposal 10 neural second opinion
- Correct pairings throughout: KLDivLoss(batchmean) ↔ forward_log_softmax;
  label smoothing + row renorm; F3-32 LOCAL seeded generator for the 80/20
  split (global RNG untouched); best-state CPU-clone restore; early stop;
  **amp_autocast contract HONORED** (forward+loss inside, backward/step out);
  saves via hardened persistence.save_nn (GAP-07 sidecar ✓).
- Graceful degradation ladder: no data → None; too few post-split → None;
  missing norm sidecar / checkpoint / incompatible → None (consensus falls
  back to heuristic) ✓.
- **F-0020 blast radius widened**: training table Ext_PlayerPlaystyle is fed
  by cs2_playstyle_roles_2024.csv — ABSENT from repo → role head can never
  train on fresh installs → neural opinion permanently missing (graceful).
- Notes: full-batch GD (fine at this scale); role_head_norm.json written
  non-atomically + not CTF-1-registered (P3/L9); feature vocabulary
  (rounds_survived/entry_frags/was_traded_ratio) = same Phase-K caller census
  as B18; backend.nn → backend.analysis import edge (layering census).
