# D-B67 — storage/config/knowledge tests (5 files, 2,236 LOC) — pass 1 read 2026-08-14

Zero new register findings. Two operational-disaster war stories
verified pinned; one fixture-override case study noted.

## test_config_resolution.py (335)
- **THE $HOME-shard-migration disaster pinned (2026-07-26)**: the
  docstring records the full incident — PRO_DEMO_PATH defaulted to
  expanduser("~"), _resolve_match_data_path always chose
  $HOME/match_data, and get_match_data_manager's auto-migration MOVED
  the production shard corpus there. "Observed twice: 45 shards off
  the external volume (ENOSPC mid-move), then 313 shards off /data,
  taking the root disk from 42 GB to 2 GB free." FOUR guards: default
  ≠ HOME; empty → in-project path; explicit HOME REFUSED; deliberate
  real path honoured (not a blanket ban) ✓✓ exemplary regression net.
- Config defaults / JSON overrides / save / get / refresh; REAL
  thread-safety test on the settings lock; MIN_DEMOS_FOR_COACHING==1
  and MAX_DEMOS_PER_MONTH==10 constant pins.
- Course case study: defines a LOCAL isolated_settings fixture that
  OVERRIDES the conftest one (simpler — monkeypatch-only, no global-
  mirror snapshot; monkeypatch auto-restore makes it sound).
  Deliberate per-module override per 08-testing's shadowing
  semantics; noted so nobody "deduplicates" it blindly.

## test_db_backup.py (293)
- **Free-space guard pinned (2026-07-17)**: "a 154 GB monolith on a
  54 GB-free disk filled the volume to 100% and killed the concurrent
  schema migration" — refuses when DB > free space (RuntimeError
  "Refusing backup"), allows with headroom (namedtuple-faked
  disk_usage). Backup/restore/rotation/alembic-pre-migration-hook/
  integrity/checkpoint-size-guard all covered.

## test_database_layer.py (439)
- DatabaseManager: WAL mode, session lifecycle, CRUD, upsert;
  StateManager status/notifications; StatCardAggregator. The L3
  session-contract test net for Pass 2.

## test_experience_bank_db.py (745)
- The _InMemoryDBManager pattern's ORIGIN (other suites cite it);
  _DeterministicEmbedder fake (no SBERT). AC-32-01 base64 embedding
  storage pinned at add_experience; context_hash storage + retrieval
  equality; retrieve_similar + pro-examples; synthesize_advice;
  feedback recording; stale decay; **dedup contract documented in
  test NOTE: add_experience dedups on (context_hash + action) per
  docstring** — pins the idempotency mine_shard_strategies relies on
  (B51 ✓ coherent); private helpers; extract-from-demo.
- Cross-ref: B51's `_compute_context_hash` ignores-equipment_tier
  finding is NOT contradicted here (hash inputs tested at the
  ExperienceContext level, not the miner's) — finding stands.

## test_baselines.py (424)
- Hard default baseline, get_default_pro_baseline, calculate_
  deviations, TemporalBaselineDecay curve behavior, learned
  thresholds + RoleThresholdStore (DB-backed). NOTE: pro_baseline's
  is_pro==True unfiltered SELECT (F-0042 consumer evidence) is not
  contradicted by tests — no test asserts data_quality filtering in
  baseline construction.
