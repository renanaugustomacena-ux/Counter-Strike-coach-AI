# D-B14 — feature-engineering (6 files, 1,650 LOC) — pass 1 read 2026-08-14

## feature_engineering/vectorizer.py (778) — THE training-serving contract (L1/L8 anchor)
- **Campaign-best file.** P-X-01 FEATURE_NAMES↔METADATA_DIM=25 import-time assert;
  25-slot filler decomposition with slot comments; validate_feature_parity at both
  boundaries (P-SR-01); sin/cos yaw (±180 continuity); DETERMINISTIC md5 map hash
  (built-in hash() non-reproducible — documented); retired-KAST-heuristic honesty
  (0.91-vs-0.71 war story); C-01 skew closure (slots 20-24 tick-first then context).
- Quality architecture: D2 emission-throttled logging (10 verbose → 60s aggregate,
  counters exact); 26-VEC-01 thread-local per-batch clamp counter → P3-A
  DataQualityError gate (>5% NaN/Inf refuses the training batch); D1 sliding-window
  single-sample gate w/ hysteresis + StateManager notification (Law 3: degrade
  loudly, never raise in live coaching); P-VEC-03/R4-14-03 config snapshot per
  batch (no mid-batch hot-swap, no cross-thread contamination); Bug#6 RLock on
  class config; R4-14-01 (0,0,0) throttled telemetry; H-12 weapon telemetry
  (dual-form map, 0xFFFFFF no-weapon sentinel, numeric-handle legacy detection,
  once-per-weapon unknown warning — self-healing coverage).
- **Slot-16 KAST scale VERIFIED COHERENT** (was the batch's open risk): writers
  bound by rating.compute_rating_components contract (ratio 0-1), percentage
  variant deleted F2-39 for exactly the ×100 hazard, parity test
  test_rating_components_contract.py pins demo_parser's replica. Unclamped
  vec[16] is safe by contract (L8 row records the dependency).
- Minor: extract_batch([]) → shape (0,) not (0,25) — downstream parity check
  would catch loudly (P3); literals 115.0 (round secs) / 16000.0 (econ cap) /
  4.0/5.0 alive-counts documented in docstring but unnamed (P3); teammates /4
  vs enemies /5 asymmetry is deliberate (self excluded) ✓.

## feature_engineering/kast.py (163)
- Trade-window ticks derived from KEYWORD-ONLY REQUIRED ticks_per_second ✓
  (bare-64 family structurally killed at this API). P3: docstring still claims
  "Default 64" — stale after the default was (correctly) removed.
- Logic ✓ (K excl. suicide, S = no deaths, T = attacker dies within window,
  strict > death_tick). L8 nits: team-kills count toward K (HLTV counts enemy
  kills only; rare) ; estimate_kast_from_stats F2-35 heuristic honestly labeled.

## feature_engineering/rating.py (176) — HLTV 2.0 SSOT (exemplary)
- CONTRACT blocks: kast=RATIO 0-1; rating_* columns = RAW components (baseline
  normalization ONLY inside aggregate `rating`); every PlayerMatchStats writer
  must route here; F2-40 "do NOT reconcile" note on deliberate per-component
  divergence from the regression; F2-39 deletion of the percentage variant with
  rationale. Coefficients + provenance doc pointer retained.
- P3: compute_impact_rating docstring formula names AssistPR; implementation
  proxies it with avg_adr/100 (documented behavior, mismatched symbol).

## feature_engineering/base_features.py (197)
- HeuristicConfig dataclass w/ per-field ranges, forward-compat from_dict ✓;
  extract_match_stats: NaN-vs-`or` war story ✓, F2-28 econ_rating scale fix ✓,
  R4 impact_rounds = SHARE [0,1] cross-writer lockstep ✓, kd/accuracy div guards ✓.
- **L6 row**: heuristic_config.json path = backend/storage/ INSIDE the package
  tree — frozen/PyInstaller deploy makes it read-only/ephemeral; save path
  also non-atomic write (L5 P3).

## feature_engineering/__init__.py (67)
- PEP-562 lazy __getattr__ exports (import-lock deadlock avoidance for daemon
  threads) ✓; docstring still says "Kivy UI thread" (P3 residue).

## feature_engineering/role_features.py (269)
- P3-01 canonical PlayerRole enum; P3-04 delegate→RoleClassifier w/ P-RF-02
  logged fallback; P3-09 range guard; heuristic centroid classifier w/
  exp(-2d) confidence; R4-18-01 adaptive signatures (meta-drift widening,
  observable degradation w/ exc_info) ✓.
- L10 row: get_role_coaching_focus lists aspirational keys (trade_coordination,
  info_kills, eco_round_performance, save_success_rate…) — verify consumers
  tolerate absent stats at Phase K.
