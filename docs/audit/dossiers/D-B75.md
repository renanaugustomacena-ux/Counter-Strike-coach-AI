# D-B75 — root tests/ + evals (24 files, 2,451 LOC) — pass 1 read 2026-08-14

Zero new register findings. Phase-T reading COMPLETE with this batch.

## tests/test_lock_files.py (214) — the concurrency-policy net
- Full lifecycle: acquire w/ PID+timestamp, release idempotent,
  live-PID conflict raises, DEAD-PID reclaim, is_held/holder_pid,
  never-allocated-PID liveness, context manager (normal + exception),
  path-separator SANITIZATION in lock names, signal-handler install,
  **TestAcquireAtomicity: exactly-one-winner under contention** —
  the d_track_running/hltv_schema_migration machinery (B54/B58
  consumers) is properly guarded at its SSOT.

## Root tool tests — the safe-tool nets
- test_rescrape_placeholder_pros.py (215, 10 tests) +
  test_sync_pro_players.py (96, 4): the GAP-05/GAP-06 dry-run-default
  tools have their gating tested. test_d3_rederive.py (129, 5): the
  D3 shard-metadata recovery paths. test_eval_harness.py (101, 8) +
  test_coach_answer_eval.py (68, 9): GAP-04/GAP-15 harness logic.

## tests/verify_*.py (7 scripts) + tests/forensics/ (10 scripts)
- verify_chronovisor_logic/real, verify_csv_ingestion,
  verify_map_integration, verify_reporting, verify_superposition:
  manual verification harnesses with HONEST synthetic-data
  docstrings ("SYNTHETIC DATA ... does NOT represent 'mock data' in
  the production system") — script-style, not pytest-collected
  (no test_ prefix on verify_*; forensics test_* files carry venv
  guards). These are L10/S7 candidates for tier labeling, not
  defects.
- forensics/: check_db_status, check_failed_tasks, debug_env,
  debug_nade_cols, debug_parser_fields, probe_missing_tables,
  test_forensic_parser, test_skill_logic, verify_map_dimensions,
  verify_spatial_integrity — the debugging-session toolkit.

## evals/cs2_coach_bench (2 files, 479 LOC)
- run_eval.py: 200-question benchmark runner against a model backend;
  score_responses.py: the scoring half. The GAP-15 bench referenced
  by STUDY/reporting docs — self-contained, no DB mutation.
