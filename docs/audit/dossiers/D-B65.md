# D-B65 — analysis tests (6 files, 3,441 LOC) — pass 1 read 2026-08-14

Zero new register findings. The Phase-A war-story catalogue (B16-B18)
is comprehensively pinned by tests — strongest regression net so far.

## test_game_theory.py (1,171) — the analysis regression anchor
- **R4 HIGH half-switch format-aware pins**: MR12 must NOT reset
  momentum at round 16 (only 13); legacy MR15 resets at 16; economy
  twin pins round-13-is-second-pistol (never "full-buy" on $800),
  true OT ($10k) IS full-buy, round 24 ≠ overtime.
- **TestR4MedAnalysisRegressions (batch-9 pins)**: TT key
  distinguishes max/min node types; TT depth semantics (shallow cache
  must not satisfy deeper query; deep cache valid for shallow);
  **flash-blind window scales with tick_rate** (200-tick gap = bait
  at 64 t/s, effective at 128 t/s — 26-TICK in analysis pinned);
  belief calibration DROPS eco rows instead of fabricating hp=60/30
  (anti-fabrication as an executable assertion); win-predictor
  factory wires existing checkpoint (pre-fix: production ran on
  random weights) + heuristic flag when absent — BOTH branches.
- Engine-by-engine coverage: belief model, calibrator, deception,
  momentum, entropy, expectiminimax, blind spots, engagement ranges,
  NamedPositionRegistry, factories, tactical features.

## test_game_tree.py (574)
- OpponentModel heuristic pins: economy tiers, side adjustments,
  player-advantage, time pressure, probability NORMALIZATION
  (sums to 1), online learning updates; GameNode/build-tree/apply-
  action/evaluate/best-action mechanics; factory + constants.

## test_analysis_gaps.py (563)
- Role classifier: cold-start, warm scoring, consensus, classify_team,
  team-balance AUDIT paths, role profiles. Deception: metrics,
  analyzer, compare-to-baseline, factory.
- Constants pin w/ doctrine comment: FLASH_BLIND_WINDOW_SECONDS==2.0
  ("R4 MED 26-TICK: seconds-based, converted per-demo — the old
  FLASH_BLIND_WINDOW_TICKS=128 hardcoded a 64-tick assumption");
  deception weights sum to 1.

## test_analysis_engines.py (259) + test_analysis_engines_extended.py (473)
- Base: win-probability predictor, role classifier, utility analyzer,
  economy optimizer smoke+contract tests.
- Extended: momentum state/tracker/from-round-stats, belief state +
  death estimator + calibrator, entropy, GameState, WinProbabilityNN.
- **26-WINPROB-03 PlattScaler pins** — exemplary TEST DESIGN: the
  docstring notes it validates "a numerical algorithm under test, not
  fabricated domain data" — closed-form ground truth (identity map
  A=-1, B=0 for already-calibrated data; Brier reduction for
  overconfident model; parameters stay finite — pre-fix the Hessian
  sign negation ran A,B to ~3e8 and collapsed every probability to 0).

## test_belief_model_extended.py (401)
- Empty-DB extraction, partial-column auto-calibrate, insufficient-
  samples honesty, weapon lethality bounded, threat-decay NaN guard,
  singleton factory, evidence updates, bounded estimates.
- **WR-65 R4 HIGH ClassVar propagation pin**: THREAT_DECAY_LAMBDA as
  dataclass FIELD baked 0.1 into __init__ making the calibrator's
  class-level assignment a silent no-op — now pinned BOTH behaviorally
  (assignment reaches new instances, exact threat_level math) and
  STRUCTURALLY (dataclasses.fields must not contain the name).
  Try/finally restores the class attr ✓ clean test hygiene.

## Rolling doctrine-pin additions (CP0 input)
- PINNED: MR12 half-switch (momentum + economy), TT node-type/depth,
  flash-window tick-rate scaling, eco-drop anti-fabrication,
  win-predictor checkpoint wiring, 26-WINPROB-03 Platt convergence,
  WR-65 ClassVar, deception window constants, opponent-model
  normalization.
