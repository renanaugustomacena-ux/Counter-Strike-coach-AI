# D-B64 — COPER/RAP/chronovisor/nn-infra tests (9 files, 3,740 LOC) — pass 1 read 2026-08-14

Zero new register findings. One W-wave landmine flagged (EN advice
assertions), more doctrine pins mapped for CP0.

## test_coper_pathway.py (1,167)
- Purpose-documented safety net "BEFORE we flip the feature flag" —
  the COPER chain end-to-end without Ollama/FAISS/SBERT. In-memory
  fake DB + notification-recording fake state manager (course
  taxonomy: fakes, not mocks) ✓.
- infer_round_phase BOUNDARY pins (pistol <1500 ≤ eco <3000 ≤ force
  <4000 ≤ full_buy; non-dict→full_buy; missing-key→pistol) — pins the
  round_utils SSOT; the mine_coaching_experience DUPLICATE (B55, L10)
  has no such tests — dedup evidence strengthened.
- AC-32-01 both embedding formats pinned (base64 round-trip + legacy
  JSON deserialization) ✓ coherent with eval_harness reuse (B55).
- _run_with_timeout full contract (result/timeout-None/exception
  reraise/args-kwargs); mode-priority chain COPER>Hybrid>RAG>
  Traditional asserted via Level-N notifications; C-02 non-dict
  tick_data guard; _baseline_context_note deltas; brute-force
  retrieval fallback when FAISS absent.
- **W-WAVE LANDMINE (L7)**: multiple assertions pin ENGLISH advice
  strings ("below", "20%", "similar situations", "Pro Examples") —
  if CP0 approves i18n-ifying coaching advice (the L7 EN-advice
  cluster), these tests need coordinated updates in the SAME wave or
  they red. Recorded for W2/W3 planning.

## test_chronovisor_scanner.py (296) + test_chronovisor_highlights.py (375)
- Dataclass/annotation contracts (ScanResult, CriticalMoment,
  to_highlight_annotation, context_ticks default); multi-scale
  dedup rules pinned (micro preferred over standard, higher severity
  wins same-scale, distant peaks both kept).
- TWO R4 MED pins: lag converted ticks→ENTRY units before indexing
  ("old per-tick lag returned [] on any timeline shorter than 65
  entries" — step change now detectable); timeline entry = ONE value
  per window keyed to the window's LAST tick.
- Render tests produce real matplotlib files (tmp_path), incl. a
  source-inspection test for scale marker sizes.

## test_jepa_window_fetcher.py (118) + test_rap_window_fetcher.py (127)
- **The R4 CRIT contract pinned**: the old flat _fetch_jepa_ticks fed
  randomly-subsampled unordered rows spanning players/demos — "next-
  step prediction trained on unrelated pairs". Windows now must be
  contiguous single-player, strictly-ascending, exact length;
  end-of-stream anchors DROPPED never padded; DET-01 determinism
  (same seed → identical ids; different seeds differ); B1 rotation
  preserved. RAP twin adds WR-76 legacy demo_name suffix stripping +
  short-run skips. Core F-0029/JEPA-readiness evidence: the data-feed
  correctness layer is well-guarded.

## test_rap_training_dry_run.py (259)
- B4 orchestrator gate (RAP disabled → raises; enabled constructs);
  dry-run smoke wired to the RENAMED window fetchers (R4 MED
  2026-07-17 comment in-file); RAPMemoryLite LTC ODE-solver shape
  patch presence checks.

## test_rap_coach.py (633)
- Module-wide importorskip ncps + hflayers (explains a chunk of the
  suite's 48 skips on this machine); timeout(60).
- 7-layer coverage: ResNet blocks (identity/projection shortcuts,
  stride shapes, grads); Perception 128-dim + NaN-free; Memory LTC +
  Hopfield (hidden passthrough, training-mode Hopfield bypass);
  Strategy MoE (gate weights sum to 1, top-2 sparse routing, custom
  expert counts); Pedagogy value/advantage-gap; CausalAttributor
  (diagnose shape, concept list, bounded utility_need); full
  RAPCoachModel output-key/shape contracts on the conftest seed-42
  rap_model/rap_inputs fixtures ✓.

## test_nn_infrastructure.py (381)
- EMA full lifecycle (shadow init/update/apply/restore, state_dict
  CLONES, decay effect, convergence); ModelFactory type matrix incl.
  invalid-type raise + kwargs passthrough; **persistence contracts:
  save/load roundtrip, StaleCheckpointError on architecture change,
  NN-14 FileNotFoundError for missing checkpoints** (BASE_NN_DIR
  monkeypatched to tmp_path — no real models touched ✓);
  SuperpositionLayer gates/statistics/sparsity-loss/tracing toggles.

## test_nn_extensions.py (384)
- NeuralRoleHead softmax-sums/log-softmax/single-sample/gradient;
  extract_role_features zero/missing-rounds → None guards; nn config
  device + throttling + intensity-batch + integrated-GPU keyword
  checks; datasets (float32 dtype, too-short raises); AdvancedCoachNN
  2D/3D forward, role-id out-of-bounds CLAMP, bounded outputs.

## Rolling doctrine-pin additions (CP0 input)
- PINNED: R4-CRIT window contract (JEPA+RAP), WR-76, DET-01
  determinism, B4 gate, AC-32-01 dual-format, C-02, chronovisor lag
  units + last-tick keying, StaleCheckpointError/NN-14, MoE top-2.
