# D-B71 — extended/small suites (25 files, 3,592 LOC) — pass 1 read 2026-08-14

Zero new register findings. One historic self-confession: the project
already documented a tests-that-codify-bugs case and fixed it.

## test_session_engine.py (476) — the anchor of this batch
- Zombie-task recovery: resets old processing / spares recent /
  ignores non-processing / empty-DB safe / multi-zombie; threshold
  sanity tests (positive + "reasonable") — the P4-B side that F-0015
  contrasts run_worker's stale 5-min copy against. Register evidence
  unchanged.
- **Cold-start bootstrap R4 HIGH with self-confession**: "with
  last_trained=0 the old growth test collapsed to pro_count >= 0 and
  fired on a single sample; THIS TEST USED TO CODIFY THAT BUG ('even
  pro_count=5 triggers')" — the tests-lock-bugs pattern, found and
  fixed by the project itself. 10-sample bootstrap + 10%-growth
  trigger fully pinned (5/9/10/105/111/0 cases).
- R4 MED _commit_trained_sample_count: missing CoachState row used to
  silently no-op → teacher re-entered cold-start retrain EVERY cycle;
  row-creation + persistence pinned.
- Baseline drift detection, stop/EOF shutdown events.

## Tick-rate / numeric doctrine files
- test_movement_quality_tickrate.py (168): 26-TICK-02/C4 — the lone
  _seconds_to_ticks conversion point scales linearly (3s→192@64,
  384@128); RATE-EQUIVARIANCE property (same real-time scenario
  identical at 64 and 128); orchestrator resolver honours data
  provenance. The property-test approach the course recommends ✓.
- test_playback_engine.py (194): angle wraparound; **R4 MED Inf-yaw
  pin — `while diff > 180: diff -= 360` hung the UI clock forever on
  inf; non-finite sanitized like NaN** + DF-01 extension
  (PlayerState.__post_init__ sanitizes yaw too).
- test_z_penalty.py (152, 26 tests): vertical-level classification +
  extractor wiring. test_meta_drift_attribution.py (116): R4 HIGH —
  drift ticks attributed via WR-76-stripped demo_name + recency
  filtering.

## test_security.py (152) — repo-wide hygiene scans as tests
- Source scans: no hardcoded api_key/password literals (get_setting/
  os.environ/mock_ exempt); .gitignore CONTRACTS (.env present; exact
  "database.db" or "*.db" line — docstring precision: "'*.db-wal'
  does NOT cover .db"); no .pem/.key/.crt ON DISK regardless of
  gitignore. W5-relevant: these tests ARE the hygiene preflight's
  unit tier.

## The 20 remaining small suites (mapped, read at map level)
- Analysis extended: blind_spots (5), blind_synthesis (5),
  engagement_range (6), entropy (5), momentum (5), utility_economy
  (4), spatial_and_baseline (11), spatial_engine (5) — parameter and
  bound pins over Phase-A modules.
- Infra/service: config_dotenv (6 — .env layering), config_extended
  (20), drift_and_heuristics (12 — DriftMonitor/ShouldRetrain/
  HeuristicConfig/DifferentialHeatmap), integration (4), lifecycle
  (9), map_manager (8), onboarding (8) + onboarding_training (10),
  profile_service (10), services (7), telemetry_client (3).
- No stale-suite or wrong-behavior locks surfaced at map level; the
  S7 test↔module cross-ref will catch any coverage asymmetries.
