# D-B13 — tensor-factory (3 files, 1,741 LOC) — pass 1 read 2026-08-14

## processing/tensor_factory.py (769) — POV perception tensors (L1 anchor)
- NO-WALLHACK architecture: map (teammates / decayed last-known enemies / utility+bomb),
  view (FOV / distance-weighted visible entities / utility), motion (trail / velocity
  radial / crosshair). Legacy modes kept for compat with R4 MED one-shot honesty
  warning (PlayerTickState has no `team` → legacy enemy channel structurally empty).
- Coordinate discipline: `_world_to_grid` C-03 SINGLE Y-flip (ny = pos_y − y) ✓;
  R4 HIGH FOV-mirror war story (dy = py − y_coords) ✓; fov mask + circle + view-dist
  all share one scale convention (scale·1024) ✓.
- P-X-02 shape asserts on all 3 public generators — but POV paths return EARLY
  (:170/:270) before the asserts (structurally safe np.zeros((res,res)); P3 nit).
- P-TF-01/M-10 threshold normalization (no sparse-noise amplification) ✓; P-TF-02
  circle cap res/2; P-TF-03 quadratic fade-in ramps; P-TF-04 configurable blur;
  R4-02-03 lazy scipy; thread-safe DCL singleton ✓ (course-10 pattern).
- **L8 rows**: (1) F2-03 documented — MAX_SPEED_UNITS_PER_TICK=4.0 calibrated for
  64-tick; 128-tick demos compress velocity into [0,0.5] (accepted limitation).
  (2) VELOCITY_FALLOFF_RADIUS/sigma are GRID-cell units → semantic extent differs
  between TrainingTensorConfig 64² and default 128²/224² — **train/inference config
  symmetry must be verified at B24 ghost_engine** (state_reconstructor injects
  explicit config P-SR-02; who uses get_tensor_factory() default?). (3) F2-02
  128-dim contract rests on AdaptiveAvgPool2d (assert recommended, B21).
- L4 row: inline `logging.getLogger("cs2analyzer.tensor_factory")` bypasses
  observability.get_logger (no repo handler; root-logger fallback).

## processing/player_knowledge.py (672) — sensorial model builder
- C1/26-TICK-01 ✓✓: ALL tick windows derived from per-demo tick_rate in __init__
  (memory tau/cutoff, flash/smoke/molotov, 1s audible window) with R4 HIGH
  war story (parameter-default silently beat per-demo rate). DEFAULT_TICK_RATE
  SSOT import ✓. H-10 constants from core.constants ✓.
- Anti-wallhack: visible enemies gated by enemies_visible>0 AND geometric FOV AND
  not blinded, count-capped nearest-first; H-11 Z-floor guard applied in BOTH live
  and memory paths (R4 MED: floors leak = wallhack); PROC-02 (0,0) rejection;
  P-PK-03 position_is_fallback flag (tensor_factory skips own-marker on it ✓).
- Bounded: RAP-M-10 512-tick history cap, P-PK-02 10-enemy eviction, O(1)
  pre-indexed lookups; P-PK-04 FOV/count disagreement telemetry.
- _tick_yaw shared accessor (view_x=yaw contract, R4 CRIT war story) — single
  source, re-exported by tensor_factory ✓.
- **VERIFY row (T-DIAG)**: `_build_utility_zones` handles entity_id=-1 for END
  events (R4-06-02 position match ≤100u) but the early `continue` silently DROPS
  entity_id=-1 START events → smokes/mollies with missing entity ids never become
  zones (sensorial under-report, asymmetric with the end-path). Count -1-by-type
  in match_event_state on a real shard; promote to finding if -1 starts occur.
- Note: hearing ignores Z (realistic — CS2 gunfire audible through floors) ✓;
  smoke/molotov ignition sounds not modeled (scope choice).

## processing/heatmap_engine.py (300)
- Vectorized KDE occupancy (np.add.at + gaussian_filter), activity-masked
  diverging colormap, top-5 hotspot extraction with CORRECT inverse projection
  (world coords orientation-safe) → coaching narrative path is sound.
- **F-0017 (P2)**: grid projection double-flips Y (`ny = pos_y − y` THEN
  `gy = (1−ny)·res`) — violates the C-03 single-flip doctrine; row 0 = map
  BOTTOM, i.e. the Kivy/OpenGL bottom-left-origin blit convention. Any Qt
  QImage/matplotlib consumer of rgba_bytes renders the heatmap vertically
  MIRRORED vs the radar. TODAY dead: sole production caller
  coaching_service:889 consumes only .hotspots (+ ignores .diff_matrix);
  rgba surface has zero consumers; create_texture_from_data /
  generate_heatmap_texture are Kivy residue (honest RuntimeError w/o Kivy;
  docstrings still advertise Clock.schedule_once). W3 Kivy-residue scope.
- Perf note: differential RGBA colorization computed then DISCARDED every call
  (caller uses hotspots only) — fold into F-0017 resolution.
- Caller census: coaching_service.generate_differential_insights → narrative
  insight (CoachingInsight) + overlay via reporting.visualizer (NOT rgba_bytes);
  visualizer orientation checked at B33.
