# D-B73 — NN support tests + T-DIAG target 3 (16 files, 2,946 LOC) — pass 1 read 2026-08-14

Zero new register findings. The third T-DIAG target is the
best-engineered of the three.

## test_dry_run_checkpoint_integrity.py (97) — T-DIAG target, read fully
- B4/Law-7 guard, "real, mock-free": subprocess-runs the ACTUAL
  run_full_training_cycle entry against the REAL monolith (429M-row
  PlayerTickState) with **BRAIN_DATA_ROOT redirected to tmp_path —
  production models/ NEVER touched** (contrast test_e2e, B62);
  CUDA_VISIBLE_DEVICES="" CPU-forced; 128/64 samples; 900s subprocess
  cap + timeout(1000) + slow + integration marks.
- BOTH directions asserted with the inverse-guard rationale in-file
  (dry-run → zero .pt; real --epochs 1 → at least one .pt, else "an
  edit that disabled saving would train GPU-days writing nothing").
- HONEST scope note: dry-run still commits dataset-split labels to
  the monolith via assign_dataset_splits — acknowledged DB write.
- T-DIAG design updated: this file needs only a database.db backup
  (split labels), NOT models/; expect the sampler table-scan startup
  to dominate runtime ("blows the budget on the data box" — F7 class).

## TEST-COV audit suite — test_ema_hopfield_drift_invariants.py (170)
- Docstring maps to AUDIT §3.2 silent-failure paths: **NN-16 EMA
  aliasing (apply_shadow/restore must break storage aliasing so
  clearing backup can't free memory still referenced by param.data)**;
  MEM-01 Hopfield partial state_dict load must NOT flip
  _hopfield_trained with random prototypes; MOE-01 sparse routing
  non-zero output; DRIFT-01 fix verification on the 25-dim monitor.

## test_concept_temperature_saturation.py (147)
- PRE-6 observatory alarm: learned scalar clamped [0.01, 1.0];
  saturation at EITHER bound for 10 consecutive epochs → one-shot
  WARN_CONCEPT_TEMPERATURE_SATURATED. Minimal stand-in model
  (no encoder/DB/DataLoader) — right-sized test double ✓.

## test_persistence_stale_checkpoint.py (229)
- Stale-checkpoint trilogy: detection matrix, PREVENTS-INFERENCE
  path, corrupted-checkpoint handling — extends B64's persistence
  contracts to the failure ladder.

## Remainder (mapped)
- test_amp_autocast (56), test_checkpoint_normalizer_versioning
  (181), test_embedding_collapse_detector (117), test_moe_sparse_gate
  (197), test_nn_config_reproducibility (146 — set_global_seed/
  get_device/constants), test_nn_training (190), test_model_factory_
  contracts (225), test_skill_model (204), test_training_callbacks
  (257), test_jepa_insight_adapter (244), test_knowledge_graph (239),
  test_chat_streaming (247 — incl. SessionMLContext).
