# D-B26 — experience-bank (3 files, 1,744 LOC) — pass 1 read 2026-08-14

## knowledge/experience_bank.py (1,401) — COPER experience store
- KT-01 CRUD semantics: semantic-duplicate detection (FAISS top-5 →
  same-hash brute-force fallback, 0.9 cosine threshold), EMA merge on
  same-context+action, discard+create on contradiction-with-better-outcome,
  soft DISCARD (confidence→0, row preserved for audit) ✓ coherent design.
- C-2 effectiveness gating (≥5 trials before the score influences ranking —
  EMA-flip rationale documented); CHAT-07 dedup w/ spillover backfill
  ("better one repeat than under-filling"); AC-32-01 base64-f32 embeddings
  w/ legacy-JSON read compat; AC-36-02 FAISS fast-path + mark_dirty signal;
  atomic usage_count increments (documented race prevention); P4-C atomic
  feedback UPDATE via SQL expressions (TOCTOU eliminated; single read kept
  only for action_match, documented); WR-60 phase/side-proximity feedback
  matching; F5-10 decay cap 1000; DET-01 seeded replay ("old default_rng(None)
  violated DET-01 outright" — honest); priority replay Eq.4-5 w/ REPLAY_GATE
  TrueSkill lower bound; DCL singleton; narrow deserialization catches ✓.
- **L7 census #1**: synthesize_advice narratives + pro references are EN-only
  and user-facing (coaching chat consumer at B30 confirms exposure).
- Phase-K carries: extract_experiences_from_demo / sample_for_replay caller
  census (pro_demo_miner B27? training?); delta_win_prob always 0.0 at
  extraction (honest "would need win probability model" — effectiveness
  starts neutral). P3/L8: brute-force candidate sets are LIMIT-100/50 with
  no ORDER BY (arbitrary subset scanned when FAISS absent and bank large).
- _infer_action: 4-value heuristic (scoped/crouch/pushed/held) — coarse but
  honest; _infer_position_area simplified 6-region (documented vs
  spatial_data landmarks).

## knowledge/round_utils.py (34)
- F5-20 DRY extraction; thresholds 1500/3000/4000 MATCH vectorizer/
  HeuristicConfig round-phase breakpoints ✓ (but hardcoded literals here vs
  configurable HeuristicConfig — L10 duplication row: learned-heuristic
  changes would silently diverge round_utils). Non-dict → "full_buy"
  fallback (deliberate, tested).

## tests/test_round_utils.py (309)
- Exhaustive boundary coverage (1499/1500, 2999/3000, 3999/4000), missing-key,
  non-dict, ExperienceContext hash/query-string, helper coverage ✓ quality
  test file (counts toward T-phase familiarity; no re-read needed at B62+).

Zero register findings.
