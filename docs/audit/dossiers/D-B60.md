# D-B60 — ptools: inspectors (4 files, 2,239 LOC) — pass 1 read 2026-08-14

Zero new register findings. All four census SAFE (read-only probes;
two call init_database() — an idempotent create_all, noted). The F8-xx
comment trail (a prior audit campaign) runs through all four files.

## Ultimate_ML_Coach_Debugger.py (529)
- 9-phase neural falsification tool. Default probe player MCIV_PROBE —
  named after the superseded "Clinical_Integration_Validator (MCIV
  v2.0)" (see backend_validator below). Goliath's Pathology %MCIV%
  LIKE is therefore a probe-data-leak check — cross-ref closed.
- THREE in-file R4 war stories: set_global_seed(42) belief probe +
  torch.manual_seed zoo input ("a falsification tool must be
  reproducible — pass/fail flapped on unseeded init"); max-streak
  overfitting guard (trailing streak reset on the final epoch masked
  mid-training divergence while the label claimed "max streak").
- L5 ✓: torch.load weights_only=True; NaN/Inf + dead-neuron
  (<0.001 row-max) checkpoint scans. L1 ✓: dimensional trio
  INPUT_DIM == METADATA_DIM == len(TRAINING_FEATURES), OUTPUT_DIM==10.
- 15s daemon-thread guard around run_pre_training_quality_check
  (scans per-match DBs — minutes on big datasets); RAP/RAP-lite
  instantiation failures downgraded to WARNING (optional deps) ✓.

## backend_validator.py (605) — "the single authoritative backend gate"
- Merges/supersedes Clinical_Integration_Validator (MCIV v2.0) +
  system_audit_suite (v4.0) — the tool-consolidation lineage.
- Database section calls init_database() (table-creating side effect
  inside a validator — benign/idempotent, but a write; dossier note).
  WAL-mode check, 6 required tables, CoachState smoke, backup recency
  <7d WARNING.
- **Manifest freshness check (<168h) currently FAILS as WARNING** —
  integrity_manifest.json is months stale (F-0001 adjacent evidence:
  even the sibling validator knows).
- Service Health: hltv_sync.pid + psutil alive (INFO); **Windows
  Run-key MacenaCS2Analyzer_HLTV auto-start check** (--loop/
  --hltv-service flags) — pairs with hltv_sync_service (B10);
  sqlmodel >= 0.0.14 bound.
- F8-16 honesty comment: Model Zoo randn probes are smoke tests, not
  correctness (points at test_deployment_readiness) ✓.
- P3s: "Critical dependencies" detail says 5/5 over a 4-module list;
  Coaching section asserts the PRIVATE _generate_coper_insights
  exists (validator pinned to a private API).

## context_gatherer.py (577) — census SAFE (required arg)
- Per-file relational context: structure/imports (proj|std|ext)/
  forward+reverse deps/tests/git-log-5/public API; 4-tier target
  resolution (abs → proj-rel → source-rel → dotted/package).
- Honest-limitation comments: F8-11 substring reverse-deps produce
  false positives from comments/strings (defers to dead_code_detector
  for AST truth); F8-34 subprocess list-args + timeout, no injection;
  F8-25 exit-1 on unresolved target.
- P3: _safe(lambda) logs failures as "<lambda> failed" (loses the
  collector name for the wrapped calls).

## db_inspector.py (528) — census SAFE (read-only + init_database)
- Compact DB dashboard: connectivity PRAGMAs (WAL/sync/busy_timeout),
  per-table counts (whitelist-validated names + [bracket] quoting —
  injection-safe pattern), storage tri-view (main/hltv/match_data
  size stats), ingestion status rollup (queued/processing/done/error
  + oldest_queued + last_error), CoachState daemons, alembic version,
  dataset_split + pro/user distributions, --table schema detail
  (PK/FK/index).
- In-file R4 MED war story: CoachState columns are hltv_status/
  ingest_status/ml_status (Hunter/Digester/Teacher) — the old
  *_status keys never existed, so every daemon displayed "?" ✓ fixed.
- F8-23/F8-24 comments: suppressed queries log at debug instead of
  silent except ✓.
