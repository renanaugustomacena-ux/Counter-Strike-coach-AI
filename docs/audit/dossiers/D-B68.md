# D-B68 — coaching-engine tests (6 files, 2,463 LOC) — pass 1 read 2026-08-14

Zero new register findings. The L7 EN-advice landmine reaches full
extent here; one novel documentation-as-contract pattern.

## test_coaching_engines.py (551)
- ExplanationGenerator: empty-input → "", severity buckets (|z|≥2
  High incl. NEGATIVE, 1.0 Medium, 0.3 Low), map-location phrasing
  ("B site", "A ramp"), utility ("flashbang"), elite/KAST paths,
  "Goal" line — ALL asserted against ENGLISH literals.
- PlayerCardAssimilator: ratio-scale details pinned (headshot_pct
  0.55 ✓ ratio convention), baseline mapping w/ per-round approx
  values, entry_rate/utility_damage metrics, "Star Fragger"
  archetype label (EN).

## test_coaching_service_contracts.py (347) + _fallback.py (292) + _flows.py (616)
- Mode selection matrix + health-range classification + COPER
  tick-data validation (C-02 family) + baseline-note bounds +
  singleton factory; fallback chain (COPER→Hybrid→RAG→Traditional)
  under failure injection; traditional path; get_latest_insights;
  save_corrections; longitudinal coaching windows.
- **Novel pattern — documentation-as-contract (P9-03)**:
  TestArchitecturalDocumentation ASSERTS docstring content
  (CoachingService.__doc__ must name COPER/Hybrid/Traditional/
  Fallback; generate_new_insights.__doc__ must document priority).
  Keeps the mode chain honest in prose, not just behavior ✓.

## test_hybrid_engine.py (368)
- Engine mechanics, insight priority ordering, model loading paths.
- **Direction-aware insight pins (runtime E2E war story 2026-07-17)**:
  "deaths above the pro cohort were titled 'Strong Deaths … Keep it
  up!'" — lower-is-better metrics invert Z before judging: high
  deaths → "needs work" (never "Keep it up"/"Strong"); low deaths →
  "Strong Deaths"; higher-is-better unchanged ("Strong Kast").

## test_rag_knowledge.py (289)
- Embedder (hash fallback), populator, retriever, RAG coaching path —
  the no-SBERT-in-CI discipline holds throughout ✓.

## L7 LANDMINE — full extent (CP0 planning input)
The EN-advice string assertions now span FOUR suites: coper_pathway
(B64), coaching_engines, coaching_service_flows, hybrid_engine (this
batch). If CP0 approves i18n-ifying coaching advice, the W-wave must
update ALL FOUR suites' literals in the same commits as the engine
changes, or the gate reds. Alternative the register should offer at
CP0: keep advice EN as a deliberate product decision (docs note),
which costs zero test churn. Either way the decision is now fully
scoped.
