# D-B38 — components-2 + charts (22 files, ~2,120 LOC) — pass 1 read 2026-08-14

Zero register-level findings. Two carries closed, one narrowed to a lens row.

## Carry closures
- **icons.py fallback parity — FULLY CLOSED**: repo-wide census finds ZERO
  callers of IconProvider.user/bomb/smoke/flash/molotov/he/rifle/awp/
  pistol/knife/check/warn/bolt/db — the only consumed API is NAV_ITEMS'
  7 glyphs, all covered by the QPainterPath fallback. A sprite-missing
  install cannot AttributeError anywhere. Corollary → **L10 row**: 15
  SvgIconProvider methods are dead API surface (sprite symbols
  provisioned for future frames; harmless, dossier-only).
- **momentum/economy side vocabulary — VERIFIED COHERENT**: builder
  writes side ∈ {"CT","T","unknown"} (_team_num_to_side, incl. the B12-
  verified MR12 OT alternation); both charts consume exactly that set
  with documented unknown→half-fallback branches ✓.

## components (15)
- icon_widget.py (37): callable-injection design (callers pass
  IconProvider.xxx) — no hardcoded glyph names. P3: docstring stale
  ("renders a QPainterPath icon" — renders whatever the provider gives);
  pixmap() without devicePixelRatio (HiDPI slightly soft; provider's 2×
  supersample mitigates).
- components/__init__.py (35): partial re-export surface (15 of ~20
  components; StatusChip/FilterChip/hero cards imported directly by
  screens) — P3 consistency nit only.
- db_record_card.py (95): token-name-tinted values via getattr(tokens,
  name) w/ None fallback ✓; grid rebuild deleteLater ✓. Clean.
- mini_link_card.py (58): QSS-driven clickable nav card ✓ clean.
- delta_chip.py (61): formatted-zero hide doctrine (incl. "-0.00") —
  "an honest chip never renders +0.00 against its own baseline" ✓
  research-dossier provenance in-file. Exemplary.
- status_chip.py (84): SAME theme-staleness family as FilterChip
  (instance stylesheet from tokens; hosts must call refresh_styling on
  theme_changed) — folded into the existing B41/B43 carry.
- pro_badge.py (56): QSS pill + ct/t side variants w/ repolish; "PRO"
  documented untranslatable ✓. tip_box (43), mono_footer (19),
  numbered_step (61), section_header (75), drivers_list (69 — severity
  vocabulary guard + severity_color bridge ✓), metric_bar_row (97 —
  clamped frac, graceful narrow), hero_stats_strip (98 — frozen
  dataclass, rebuild w/ deleteLater), progress_ring (95 — CCW arc w/
  frame-spec comment, duplicate-gradient-stop guard) — ALL CLEAN.

## charts (7)
- charts/__init__.py (53): token_color parses hex + rgba with BOTH
  alpha conventions (0-255 int for surface_raised_rgba, 0-1 float for
  frost tokens) ✓; documented post-definition imports (noqa E402).
- mini_sparkline.py (103): n==1 stub, flat-span guard, endpoint dot ✓.
- rating_sparkline.py (133): refs pooled into the range so threshold
  lines always visible; low-ref=error/high-ref=success ✓. Clean.
- radar_chart.py (166): quadrant-aware label anchoring; uppercase
  measurement matches caption role (war-story comment); short series
  padded with lo; span guard ✓. Clean.
- momentum_chart.py (180): true-peak axis war story in-file (old ±100
  literal implied a fake percent scale); computes K-D delta from
  kills/deaths itself → NOT in the F-0016 round_won radius; full i18n ✓.
- economy_chart.py (211): $ ladder ceil-to-step; top-rounded bars;
  rotated axis title; half-divider bounds guard; i18n ✓. CT/T legend
  literals = game vocabulary (untranslatable). Clean.
- utility_bar_chart.py (189): grouped you-vs-pro + single modes;
  auto-height; zero-max graceful; waste-row color override ✓. Clean.

## Carries forward
- B39: skeleton.py pulse() stop discipline; toast slide call sites.
- B41/B43: FilterChip/StatusChip refresh_styling wiring on theme_changed.
- B40: StatBadge delta_pct percent-scale contract at dashboard call sites.
