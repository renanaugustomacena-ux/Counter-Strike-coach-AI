# D-B25 — rap_coach both trees (22 files, ~1,690 LOC) — pass 1 read 2026-08-14

S2 sweep: DISSOLVED (see sweeps/S-2.md) — backend/nn/rap_coach/ is a clean
P9-01 shim package (10 re-export files, zero drift; skill_model correctly
points at processing.skill_assessment). W3 removal candidate pending caller
census.

## experimental/rap_coach/model.py (204)
- RAP-AUDIT-09 war story: belief head was computed but NEVER consumed →
  now fused into strategy context (25 metadata + 64 belief = 89) ✓;
  RAP-AUDIT-04 entropy sparsity (L1-on-softmax was mathematically CONSTANT
  1/N — zero gradient — documented and fixed); F3-07 gate_weights passed
  explicitly (thread-safety, no self-caching); P-X-02/NN-RM-01/02 shape
  guards; NN-39 4D/5D dual visual input; NN-40 stateful hidden exposure ✓.

## experimental/rap_coach/perception.py (98)
- F3-29 backbone rightsizing: [3,4,6,3] (224²-designed) → [1,2,2,1] for the
  64² TrainingTensorConfig inputs ("feature maps collapse after first
  stride-2") — **explicitly documents the backbone is 64²-calibrated, which
  STRENGTHENS F-0026** (inference feeds 128/224). Canonical ResNet blocks ✓.

## experimental/rap_coach/strategy.py (115)
- RAP-AUDIT-08 top-2 sparse routing with ONLY-selected-expert execution
  (real compute saving); masked scatter-gather index mapping verified
  correct (ascending-order alignment of mask_indices/isin); full gate_probs
  returned for the entropy loss ✓. SuperpositionLayer as expert layer-1.

## experimental/rap_coach/memory.py (304) — batch star
- RAP-LTC-FIX: documented MONKEY-PATCH of upstream ncps LTCCell._ode_solver
  broadcast bug (full trace + docs/rap_training_known_issue reference;
  reproduces on 1.0.1 AND 0.0.7) — L10 note: patch is version-sensitive,
  pin ncps or upstream the fix.
- NN-45/NN-MEM-02 RNG save/restore around AutoNCP wiring (deterministic,
  checkpoint-portable, global-state-clean) ✓ exemplary; RAP-AUDIT-07 0.3
  output ratio w/ neuron budget table + checkpoint-invalidation note +
  ltc_projection contract preservation; RAP-AUDIT-03 HopfieldLayer (learnable
  prototypes) vs Hopfield (self-attention) distinction documented.
- NN-MEM-01 + RAP-M-04 R4 MED war story: forward-count gate activated
  Hopfield BEFORE any optimizer step under grad accumulation → now
  notify_optimizer_step()-driven (trainer wiring VERIFIED, both step paths);
  load_state_dict override asserts trained ONLY when hopfield weights
  actually loaded (partial-load corruption guard — subtle and correct);
  R4 LOW Phase-5B lookup_weights leading-dim fix (silent no-op initializer).
- RAPMemoryLite drop-in (LSTM; timespans-ignored documented) ✓.

## experimental/rap_coach/trainer.py (163)
- Orchestrator contract FULLY VERIFIED: train_step(batch, step_optimizer=)
  → dict w/ "loss"; _optimizer_step flush; criterion_val; scheduler;
  accumulation_steps ctor ✓. _notify_memory_step after BOTH step paths ✓.
- NN-58 named loss weights; NN-TR-02b 2× Z-axis penalty (verticality
  doctrine, Task 2.17.1); val_mask three-branch handling mirrors the
  orchestrator eval ✓. Same fp16+GradScaler AMP divergence as JEPATrainer
  (B21 row applies — unify at W3).

## experimental/rap_coach/pedagogy.py (98)
- Critic + skill adapter; CausalAttributor 5 EN concepts (L7);
  _detect_utility_need = sigmoid(hidden.mean()) — honest "away from placebo
  zeros" but weak proxy; Aggression/Rotation = scaled pos_delta proxies
  (documented). P3: diagnose docstring says "dictionary", returns [B,5]
  tensor.

## experimental/rap_coach/communication.py (141)
- HONEST header: "Template values derived from model confidence — NOT from
  actual game measurements" ✓ anti-fabrication consciousness; NN-52/
  NN-COM-01 guards; <0.7 confidence suppression; relative-direction sector
  math correct.
- **RAP-quality cluster row (CP0)**: 5 attribution concepts map to 3 topics
  by `top_idx % 3` — Utility(3)→"positioning" template, Rotation(4)→
  "mechanics": wrong-topic advice for those concepts. **L7: ALL coaching
  advice EN-only** (trilingual app — Phase K/U census aggregates).

## experimental/rap_coach/chronovisor_scanner.py (440)
- ScanResult success/failure discipline (never silently empty); NN-CV-01
  stored error detail; NN-CV-02 fetch-one-extra truncation detection + loud
  warning; F3-21 50k RAM cap; NN-CV-03 bounds check; entry-stride unit
  conversion (per-window timeline, documented war story of the first-tick
  collapse); cross-scale dedup w/ finer-grain priority ✓.
- **F-0026 third consumer**: RAPStateReconstructor() default → default-res
  tensor factory. L1 sweep rows: ANALYSIS_SCALES tick constants assume
  64 t/s ("~1s" comments; scales halve in real time on 128-tick demos);
  MIN_GAP_TICKS=64 "1 second" ditto. get_session("default") dead label.

## test_arch.py (58) + conftest.py (3)
- Dual pytest-exclusion (ImportError guard + collect_ignore) — headless
  validator Phase 16 asset; F3-38 64² inputs matching TrainingTensorConfig ✓.

## Shim tree (10 files, 61 LOC) + experimental/__init__ (9) — see S-2.md.
