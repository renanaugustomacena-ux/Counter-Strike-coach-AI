# D-B12 — round-stats (4 files, 1,621 LOC) — pass 1 read 2026-08-14

## processing/round_stats_builder.py (1,025) — RoundStats production line
- **Ordinal round-numbering doctrine**: rounds numbered by round_end event ORDER,
  never the raw 'round' column (CS2 patch 14160 +1-shift war story); H-06 cross-check
  logs uniform-offset vs non-uniform mismatch ✓; H-18 non-overlap boundaries
  (start = prev_end+1) ✓; winner paired to its tick after sort ✓ (regression-tested).
- **F-0016 (P2→verify)**: `_build_round_boundaries` :87 str()-casts winner, :297-298
  compares `.upper() == side` ("CT"/"T") — but event_registry.py:49 documents
  round_end winner as **"int (team)"** and demo_parser passes the df RAW. If ints
  arrive, str(3)="3" never matches → round_won silently ALWAYS False for every
  player/round (label poisoning: RoundStats.round_won → win-rate features →
  derive_winner_from_roundstats → UI). Tests feed only strings (:310-351) so the
  gate cannot see it. T-DIAG probe: print round_end winner dtype on a real demo;
  int ⇒ escalate P1 + side-map fix, str ⇒ fix stale registry doc.
- `_team_num_to_side`: 2=T/3=CT start convention + half-swap at 13 + R4 MED MR12
  overtime 3-round alternation blocks ✓ correct MR12 math.
- **CS2 blind synthesis** (`_synthesize_blind_events_from_ticks`): CS2 emits ZERO
  player_blind events — reconstructed from per-tick flash_duration upward jumps
  (_BLIND_EPSILON=0.05) attributed to temporally-closest flashbang_detonate;
  honest skip when no detonation candidate (no fabricated attribution, R3-01 ✓).
  Full parse_ticks(["flash_duration"]) inside stats build = heavy but necessary.
- `_derive_flash_assist_window`: P-RSB-01 per-demo tick_rate, P-RSB-05 [32,256]
  validation — but fallback is bare `header.get("tick_rate", 64) or 64` (~:224)
  → **F-0002 bare-64 family** (L1 sweep list; guard test's pattern misses the
  .get-default form). Flash-assist scan rescans deaths from index 0 per blind
  event (O(blinds×deaths), window break bounds it) — perf note only.
- Q1-03 dual-form weapon sets (`hegrenade` + `weapon_hegrenade`) ✓; GAP-03 trade
  response ticks; ENRICHMENT_TO_PLAYERMATCHSTATS 14-field SSOT map ✓.
- `persist_round_stats_and_enrichment` (F6-19): never-raises contract, idempotent
  skip when RoundStats exist (enrichment UPDATE still re-applied — idempotent
  overwrite ✓), case-insensitive player match via func.lower ✓; equipment_value
  stays 0 here (documented; backfilled by tool).

## processing/rating.py (230) — KT-06 PlusMinus + role-adjusted Bayesian
- compute_plus_minus: net frags/round + 0.10×(win_rate−0.5) team bonus; max(,1)
  guards ✓. **P3: docstring doctest is STALE** — example claims 0.3543 but the
  implemented formula yields 0.3109 for (25,18,13,10) (earlier-formula leftover;
  doctests not in gate so latent). Docstring "Raises TypeError" imprecise
  (int("abc")→ValueError).
- compute_role_adjusted_rating: conjugate-normal shrinkage (weight·prior + n·obs)/
  (weight+n), 5 role priors + neutral default, composite .40/.35/.25, ADR/120
  clipped [0,1.5]. Math sound; **kast expected 0–1 scale** — caller must not pass
  percent (L8 row: verify at call sites). P3: comment "1.702 ≈ √(2π/ln2)" is wrong
  math note (√(2π/ln2)≈3.01; 1.702 is the standard logistic-CDF approx constant —
  code correct, comment wrong). L10: KT-06 caller census (UI/services?).

## processing/skill_assessment.py (162) — 5-axis skill → curriculum 1-10
- P9-01 extraction from rap_coach; lazy pro_baseline import keeps hltv_metadata.db
  off ingestion import path ✓ (documented). R4 MED "0.0 is REAL data, only None is
  missing" fix present ✓ (anti-fabrication doctrine). P-SA-01 sigmoid(1.702z)≈Φ(z)
  percentile map + P-SA-02 std≤0 skip ✓. Honest 0.5-default fallback WITH warning.
- Axis values are np.float64 (np.mean/np.clip) — fine for tensor/round() paths;
  **L8/L10 row: verify no json.dumps(skill_vec) consumer** (np.float64 not JSON
  serializable). get_skill_tensor one-hot 10-dim ✓ level-1 indexing correct.

## processing/external_analytics.py (204) — elite reference CSVs
- _read_safe: missing file → warned empty df (R4 LOW ✓); **ParserError on a corrupt
  CSV would crash __init__** (files are repo-shipped → dossier L4 row, not register).
- is_healthy R4-12-01 column-presence check ✓; degraded path returns empty z-score
  dicts with P-EA-02 warning ✓; P-EA-01 1e-8 std epsilon + R4-12-02 NaN/Inf guards
  ✓; P-EA-03 scientific-notation regex ✓; Win_Rate replace(0,1) div guard ✓.
  Clean file — no findings.
