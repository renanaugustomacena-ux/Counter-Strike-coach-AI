# D-B09 — ingestion-pkgs (14 files, 1,980 LOC) — pass 1 read 2026-08-14

Study companion: 10-async (leverage sections; STUDY_LOG).

## ingestion/demo_loader.py (753) — tactical-viewer demo pipeline
- **L9 SHOWCASE**: DS-01 restricted unpickler (module/class allowlist) + BE-12/FE-02
  HMAC-signed cache (32B random key, 0600, atomic tmp+fsync+replace; key loss =
  cache invalidation by design). Course-07/09 pickle checklist: SATISFIED.
- **validate_demo_file WIRED here** (Proposal 12) — the F-0006 remediation template.
- `#28.3 boundary catch` comments: team KNOWS PyO3 raises "bare Exception" and
  deliberately catches at boundaries — but PanicException is BaseException →
  even intended total-containment misses panics. STRENGTHENS F-0006.
- 3-pass architecture (positions → nade linking via throw-baseline → full frames),
  D-26 vectorized preprocessing, WR-40 pointer-based bomb timeline, H-05/DS-14
  heuristic-ceiling durations flagged + counted, quality flags on pass1 failure.
- Bare-64 family: :719 header fallback 64.0; :269 `30 * 64` end-window (literal 64
  beside tick_rate-derived MAX_NADE_DURATION — inconsistent); :229 32-tick fallback
  window comments "≤0.5s at 64 t/s" → L1 sweep list (F-0002 fix family).
- Cache key name+size+version (no mtime — negligible staleness risk).

## backend/ingestion/watcher.py (238)
- Watchdog event-driven enqueue: size-stability debounce (1s×2, F6-16 cap 120
  attempts), DS-02/IM-01 TOCTOU guards, read-only accessibility probe (F6-24),
  DB dedup, signal_work_available wake-up ✓. C-01 handled correctly (get_setting
  per start, not stale globals) ✓.
- **F-0008 family**: PRO_DEMO_PATH/DEFAULT_DEMO_PATH default expanduser("~") →
  unset config = watchdog observes $HOME (non-recursive; wasteful+surprising).
- P3: `from sqlalchemy import select` w/ session.exec (Row-tuple semantics; works
  for existence checks; inconsistent with sqlmodel.select elsewhere).

## backend/ingestion/resource_manager.py (201)
- CPU 10-sample moving average + 85/70 hysteresis (F6-18 separate locks ✓),
  RAM>90 immediate, HP_MODE override, worker-count policy (all-1 / 1 / cores//4).
- **KIVY GHOST**: is_gui_active() greps cmdlines for "main.py" (deleted entry
  point) + frozen exe name — cannot detect the real Qt GUI → caller census (L10);
  candidate P2 if consumed for throttling decisions.
- Priority mgmt Windows-only (os.name=='nt') — silent no-op on Linux deploy
  target (the "don't impact UX" goal absent there) → dossier/CP0 note.

## backend/ingestion/csv_migrator.py (213)
- CSV→Ext tables; F6-17 module-level safe parsers; R3-H05 idempotency; 1000-row
  batch commits; R4 HIGH __main__ uses singleton accessor (invariant). Documented
  gap: external match_id=0 internal link.

## ingestion/steam_locator.py (135) — PRIMARY Steam authority (F6-11 pair)
- Windows registry → Linux paths incl. Flatpak ✓ deploy-aware; psutil partition
  discovery + hardcoded fallback; recursive+flat glob (double scan, DB-dedup
  harmless); queue-if-new via IngestionTask.

## ingestion/integrity.py (47)
- Thin delegate to demo_format_adapter ✓ consolidated; legacy constants documented unused.

## ingestion/pipelines/user_ingest.py (79)
- User-demo pipeline: parse → PlayerMatchStats upsert → F6-19 round-stats
  enrichment (anti-fabricated-coaching-signal) → ML pipeline → R3-H03 archive only
  after full success. Broad except per demo (batch resilience, logged).

## ingestion/pipelines/json_tournament_ingestor.py (167)
- Tournament JSON→CSV: R3-M17 structure validation, DS-04 numeric coercion,
  cleanly decomposed extraction, progress logging.

## ingestion/registry/registry.py (122)
- Processed-demo registry: thread lock + FileLock in CONSISTENT order (DS-08),
  R3-H04 write-ahead atomic save, backup + verified recovery chain, loud
  "all demo history lost" critical on double failure, F6-20 O(1) set. EXCELLENT.

## ingestion/registry/lifecycle.py (25) — 30d expiry purge of processed demos.
## __init__.py ×4 (0) — empty.
