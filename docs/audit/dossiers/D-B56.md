# D-B56 — seed + supply-chain tools (8 files, 2,506 LOC) — pass 1 read 2026-08-14

**F-0039 census CLOSED: 13 confirmed mutating members** (register
updated). This batch adds ingest_pro_demos (#13) and clears the rest.

## seed_hltv_top_n.py (630) — census SAFE (forced choice)
- THE best gating pattern in the repo: bare invocation errors with
  "Pass either --dry-run or --apply" — an explicit-choice parser.error.
- Doctrine §111-121 mapping in-docstring; SCOPE banner (hltv_metadata
  ONLY — cites the hltv-db-separate memory); robots preflight on apply;
  selector-drift fallback ladders w/ the 2026-04-29 ".bodyshot-team"
  verification note (unfiltered scan leaked ~36 players/team → capped);
  roster 5-7 sanity warnings; fresh-cache short-circuit overridden for
  default-sentinel cards; retry+backoff ON TOP of the fetcher's 2-7s
  throttle; team_id backfill war story (jL/19206 landed team_id=NULL,
  unrouteable from the baseline merger); atomic report writes
  (tmp+os.replace); pending_vision.json queue handoff.

## seed_hltv_apply_vision.py (324) — census SAFE (required args)
- Phase-2 vision-fallback writer designed to be driven by an
  INTERACTIVE CLAUDE CODE SESSION via puppeteer-mcp screenshots
  (documented three-step flow). hltv-db-only scope; §117 quality gates:
  rejects all-zero stats ("would recreate the default sentinel");
  plausibility warnings; **F-0019-aware KAST percent→ratio
  normalization**; batch mode w/ per-entry error isolation.

## ingest_pro_demos.py (230) — **F-0039 member #13**
- Bare invocation (incremental) = queue + process_queued_tasks
  (high_priority) + CoachTrainingManager.run_full_cycle — ingestion
  AND a full 5-phase retrain. --full adds the big scoped clear with TWO
  war stories: pro-scoped tick DELETE ("a blanket DELETE also destroyed
  the USER's tick data, which a pro re-ingest never recreates") and the
  removed module-level dedup monkeypatch ("disabled dedup for
  EVERYTHING... user demos included"). Download-duplicate "(1)" folder
  rejection; --retrain-only mode; prerequisites gate before training ✓.
  Another save_user_setting side effect (PRO_DEMO_PATH).

## Supply-chain quartet (all census SAFE)
- refresh_model_pins.py (194): C-MOD-01 — HF revision pins + artifact
  SHA-256s into integrity_manifest; bare = --show; refresh requires
  --model + 40-hex --revision; --check gate. Phase-2 rag_knowledge
  enforcement flagged honestly.
- refresh_compose_digests.py (229): C-DOCK-01 — tag→digest pinning via
  docker/skopeo manifest inspect; DRY-RUN DEFAULT; traceability
  comments ("# was: repo:tag"); --check exits 1 on remaining tag pins.
- sbom_generator.py (214): C-SC-06 — CycloneDX 1.6 from env or hashed
  lockfile; purl components; git-commit property; stdout default.
- audit_binaries.py (218): post-build dist binary hash-lock →
  binary_integrity.json; graceful no-build skip ("dev mode") ✓.

## audit_scanner.py (467) — census SAFE (required arg)
- The in-house mechanical audit engine: AST inventory (public API +
  docstring presence), import graph, caller map, per-function
  cyclomatic complexity, pattern violations, test-file mapping,
  markdown/JSON reports. The automated counterpart of this campaign's
  manual pass — useful cross-check input for Pass 2 L10/L-COV.
