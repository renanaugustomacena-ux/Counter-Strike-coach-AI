# D-B72 — data-layer + tick-rate tests (33 files, 5,033 LOC) — pass 1 read 2026-08-14

BOTH known-red gate members live here and are now fully decoded.
F-0002's register row amended with the live enumeration. One new
coverage gap noted (rating-constants lockstep). User directive logged
this session: END STATE = ONE BRANCH (merge audit → design-atlas →
main after W6; task #20).

## test_tick_rate_ssot.py (161) — F-0002's guard, read fully
- 26-NORM-01/C11 owner decision (2026-07-17) as a test: resolve_tick_
  rate ladder (metadata → header → None SENTINEL, never fabricated;
  [32,256] window per DS-07); DEFAULT_TICK_RATE==64 "the one
  sanctioned literal".
- AST-based bare-64 hunter over Programma_CS2_RENAN + root tools/
  (assignments, AnnAssign incl. Field(default=), positional/kw-only
  defaults, call kwargs, getattr 3-arg, dict keys) + a SEEDED-
  VIOLATION meta-test (scanner must bite — 4 synthetic offenders
  detected) — same meta-guard family as design-token references.
- **LIVE RE-RUN: exactly ONE offender — tactical_viewer_screen.py:416
  `self._demo_tick_rate = 64`** (B42's init placeholder; the loud
  resolver is already correct). W2 fix = one-line SSOT import.
  Register F-0002 amended.

## test_match_data_dangling_symlink.py (100) — F-0003 decoded
- The 2026-07-26 war story (same day as the $HOME disaster): the old
  self-repair unlinked a dangling symlink and invented an empty dir —
  destroying the only pointer to unplugged-drive data. Suite pins:
  construction RAISES MatchDataUnavailableError, symlink SURVIVES
  un-repointed, no empty dir invented, error names link+target,
  singleton propagates w/o caching; absent-path fresh-install still
  creates.
- Windows-local red mechanism: the FIXTURE's `link.symlink_to(...)`
  needs SeCreateSymbolicLink (WinError 1314) → 5 setup ERRORS + the
  live-symlink test fail. Linux-correct by design. W2 disposition:
  skipif-on-unprivileged-Windows keeps the local gate honest while
  Linux CI enforces.

## test_match_data_no_implicit_migration.py (130)
- The $HOME disaster's THIRD guard (with config-resolution refusals
  B67 + dangling-symlink): construction NEVER relocates shards; the
  full incident narrative in the docstring ("relocated the production
  shard corpus twice in one afternoon: 45 shards... then 313 shards,
  42 GB → 2 GB free"); explicit migrate_match_data() still works.

## test_rating_components_contract.py (205)
- Pins THREE writers to compute_rating_components (SSOT semantics,
  demo_parser parity, aggregate-builder parity) — the F-0016-family
  raw-scale contract net. **GAP (advisor check): repair_rating_scale's
  stdlib-only constant MIRROR is NOT in the parity net** — its
  "lockstep" pointer is aspirational; if BASELINE_* change, the
  repair tool silently diverges. W3 one-liner: 4th parity class
  importing the tool's constants (ptools is importable). P3.

## Tick-rate propagation cluster
- test_ingestion_tickrate.py (188): GAP-01 header-meta parsing w/
  loud out-of-range warnings (caplog-asserted); is_blinded flash-
  duration derivation. test_tick_rate_propagation.py (75): C1.2 /
  26-TICK-01/03 RAP-path propagation + [32,256] rejects.
  test_trade_kill_detector.py (367): result dataclass, round-number
  assignment, detection matrix, per-player stats; R4 LOW pin — the
  64-baked TRADE_WINDOW_TICKS export retired for seconds×rate.
  test_trade_timing.py (192): GAP-03 response-timing regressions.

## test_hltv_table_registry.py (119)
- R4 CRIT contract: _HLTV_TABLES registry complete (the separation
  SSOT the conftest fixture reuses ✓); **R4 MED SQLite lesson pinned:
  NULLs are DISTINCT in UNIQUE indexes — the old 3-column constraint
  never fired (XOR guarantees a NULL); partial unique indexes now
  reject duplicate (subject, map)** while allowing same map across
  subjects.

## Remainder (mapped)
- Parser/format: demo_parser (325), demo_format_adapter (252),
  dem_validator (130), grenade_thrown_extraction (243),
  hltv_stat_parsing (71), faceit_integration (74).
- Pipeline: ingestion_pipeline (268), ingestion_boundary (137),
  auto_enqueue (146), debug_ingestion (80), data_pipeline_contracts
  (190), label_source_monitor (144), strategy_label_migration (142),
  pro_demo_miner (227).
- Contracts: metadata_dim_contract (121), match_shard_schema_contract
  (87), database_wal_enforcement (170 — the match_data_dir fixture's
  documented consumer), db_governor_integration (224), models (74),
  features (74), tactical_features (83), dimension_chain_integration
  (124), vector_index_contracts (54), storage_manager_archive (55).
