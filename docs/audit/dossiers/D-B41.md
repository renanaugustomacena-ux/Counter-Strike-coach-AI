# D-B41 — screens: detail + performance (2 files, 2,055 LOC) — pass 1 read 2026-08-14

## match_detail_screen.py (1,227) — the strongest screen so far
- **F-0035 live site fully confirmed**: _empty_state constructed with NO
  cta (row starts hidden); load_demo clears cta; no-data (:265) and
  _on_error (:314) set "Back to Match History" → the row-visibility bug
  keeps both recovery CTAs invisible. (Registered at B37.)
- **hltv-aggregate question RESOLVED**: _on_data documents that the
  cross-match get_hltv2_breakdown payload "stays in the VM contract;
  frame 09 renders the per-match component fields in stats instead" —
  no mislabeled rendering. P3/W3: the VM still FETCHES that aggregate
  every load (analytics round-trip whose result is never rendered) —
  drop it or render it.
- Discipline showcase: full i18n via md_* keys (best coverage of any
  screen); FE-01 Qt.PlainText on ALL DB/scanner-sourced text (insight
  titles/messages, moment descriptions, round notes) — prompt-injection
  / rich-text hygiene ✓; R4 LOW connect-once war story (duplicate slots
  → N navigations); name-based tab restore across rebuilds (index
  positions shift when rounds tabs vanish); deleteLater on removed tab
  pages (QTabWidget.clear only detaches); on_enter skip-reload comment.
- FIELD-GAP honesty ×5: demo size/duration, hero DeltaChip baseline
  (belongs to History where the list IS the baseline), bomb outcome,
  enemies_left, notes (falls back to duel flags). set_critical_moments
  documents the missing match_id/tick plumbing (CP0 RAP cluster
  adjacency); deep-link emits start_tick consistent with
  TacticalChronovisorVM.navigate_to ✓.
- F-0016 radius confirmed rendering: round dot strip, W/L column,
  totals row, side splits — all read persisted round_won.
- Buy-classification thresholds as named constants; _half_round side-
  change detection w/ MR12 fallback ✓ consistent with charts.

## performance_screen.py (828)
- Per-section try/except isolation (a failed section logs + drops, the
  rest render) ✓; R4 stories in-file: context-before-data contract,
  "h3 is not a role" silent-fallback fix (:382), ±5% pro band
  tightening, waste-row sentiment-sign doctrine ("▼ -31% vs pro
  (waste)"), sparkline ref-caption squeeze fix.
- Anti-fabrication ✓: pro baseline absent → "—" placeholder, never a
  fake comparison (documented FIELD-GAP); pro-overview mode suppresses
  deltas entirely.
- EmptyState = constructor-CTA path → F-0035-safe ✓.
- P3: _fmt_pct renders "63th %" — hardcoded "th" ordinal for every
  number (1th/22th/63th); fold into the L7 localization work (IT wants
  "63°"). P3: ADR sentiment bands 70/50 here vs match_detail's 75/55
  (hand-tuned drift between sibling surfaces). P3: _build_context_strip
  docstring first claims live rebinding then correctly says snapshot —
  self-contradicting; snapshot is the truth.
- L7 ledger additions (constructor-EN): 3-sentence pro banner, hero
  labels (Avg rating/Matches/K / D/ADR/KAST), "Versus pro cohort",
  context/trend/utility empty texts, " — pro reference" suffixes,
  empty/error state texts. Theme-staleness ledger: pro banner + empty
  states (instance styles).
- on_leave no-op (no cancel API on PerformanceVM — loads idempotent) ✓.

## Ledgers after B41
- fade_in census so far: match_history._render_filtered (B40) only.
- Theme-staleness hosts: match_history, home, performance, roster cards.
- L7: heavy EN residue on home/history/performance; match_detail nearly
  clean (md_* keys) — proves the pattern the L7 fix should generalize.
