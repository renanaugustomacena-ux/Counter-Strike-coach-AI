# D-B15 — baselines + validation (12 files, 2,090 LOC) — pass 1 read 2026-08-14

## baselines/pro_baseline.py (665) — 4-layer baseline fusion
- Layered fusion hard→CSV→demo→HLTV with _provenance + degraded warning; per-player-
  first averaging (volume-bias kill) in BOTH DB and demo loaders ✓; P-PB-01 dpr guard,
  P-PB-02 clamped survival approx, P-PB-03 dynamic CSV map; R4-20-01 yield_per(500);
  get_pro_positions tick-rate-aware stride w/ [32,256] validation (R4 LOW fix) ✓;
  TemporalBaselineDecay w/ THE naive/aware-datetime war story (SQLite tz drop) ✓.
- **F-0018 (P2)**: layers 2/3 merge HARD defaults INTO their returns; fusion loop
  then copies every key → later layer's merged-in default CLOBBERS earlier layer's
  empirical value (concrete: demo layer lacks rating_impact → its hard-default
  1.10 overwrites CSV's empirical Impact when ≥10 pro demos && HLTV empty).
  Violates the documented "stronger overrides weaker" contract.
- **Scale verification (empirical, R0 DB backups)**: PlayerMatchStats avg_hs/
  avg_kast/accuracy are RATIO (0.318/0.944/0.347 sampled); stat_fetcher.py:588
  stores kast/100 → HLTV layer RATIO ✓; HARD defaults ratio ✓. CSV layer maps
  "KAST"/"Headshot %" RAW (percent) → **F-0019a latent poisoning** — armed the
  day the (currently absent) CSV ships. ProPlayerStatCard COUNT=0, pro demo
  rows=0 → only hard_default layer live today.
- L10: _get_default_pro_baseline() zero callers in-file (census at L10);
  TemporalBaselineDecay callers → Phase N/K. P3: second import block mid-file.

## baselines/role_thresholds.py (319) — anti-mock threshold store
- Cold-start None doctrine, MIN_SAMPLES=30 (P-PB-04 bootstrap CI), P-RT-02
  unique-player counting, P-RT-03 validate-before-persist, P3-06 DCL singleton ✓.
- VERIFY (Phase A): learn_from_pro_data learns only 5/9 declared thresholds
  (first_death/utility_damage/clutch/trade never learned → None forever);
  records MISSING awp_kills/entry_frags keys contribute 0.0 ratios via
  .get(,0) → 75th percentile dragged toward 0 → degenerate low thresholds
  possible. Resolve when reading RoleClassifier + the learn call site.

## baselines/meta_drift.py (168)
- R4 HIGH war story (unrelated-ID join + no map filter → fixed via demo_name
  attribution + map_name filter) ✓; tz normalization; hist−=recent dedup; F2-44
  tuple guards; P-MD-01 SSOT map extent; P-MD-02/03 guards+named weights ✓.
- L1 sweep row: `PlayerTickState.tick % 128 == 0` HARDCODED stride (26-TICK
  family; centroid math tolerant, but inconsistent with pro_baseline's
  rate-aware stride). Stat drift baseline includes recent rows (dampened
  measure — note only).

## baselines/nickname_resolver.py (134) + pro_player_linker.py (107)
- Cascade exact→cleaned→substring→fuzzy(0.8, SequenceMatcher); PROC-03
  both-side cleaning; F2-41 O(n) bound documented. P3: full ProPlayer table
  loaded 2-3× per unresolved name; substring has no min-length guard
  (2-char nicknames over-match; first-wins order).
- Linker: idempotent backfill (is_(None) guard), single commit, unresolved
  telemetry ✓ clean.

## validation/drift.py (261)
- DRIFT-01 two-level architecture: match-aggregate DriftMonitor + 25-dim
  TickFeatureDriftMonitor (model-INPUT space) w/ shared _STD_EPSILON semantics,
  loud unfitted RuntimeError, dim-mismatch guards; should_retrain 3-of-5 ✓.
  Callers census at Phase N (training_orchestrator).

## validation/dem_validator.py (217)
- Fail-fast: filename injection/double-extension guards (F2-26), symlink
  refusal, size band, magic numbers, truncation probes; R4 LOW "intentional
  layering — do NOT reconcile to DS-12" doc ✓ exemplary. __main__ self-test
  (S5 census row). Caller census vs demo_format_adapter.validate_demo_file
  at Phase C/T.

## validation/sanity.py (127)
- P-SAN-01 kast percent→ratio self-heal in trim mode ✓; strict/trim split ✓.
- **F-0019b**: LIMITS headshot_pct = (0, 100) but the parser emits RATIO
  (empirical: avg_hs 0.318 = mean of per-round values) → band is 100× too
  loose; corrupt ratio-scale values (e.g. 37.0) pass strict validation and
  trim mode never trims; P-SAN-01 heals kast ONLY (asymmetric handling of
  the same scale-ambiguity class).

## validation/schema.py (92)
- Versioned registry (v1 core + v2 accuracy), F2-48 fractional-int detection
  (no silent truncation), numeric-dtype gate ✓. P3: `version: int = None`
  should be Optional[int].

## Cross-batch correction + F-0020
- D-B12 said external_analytics CSVs "repo-shipped" — FALSIFIED: data/external/
  contains ONLY hltv_stats_urls.txt. All 7 EliteAnalytics datasets + the
  pro-baseline CSV are ABSENT → **F-0020 (P2)**: elite-comparison subsystem
  permanently degraded on fresh installs (empty z-scores, role "Unknown");
  pro_baseline CSV layer dead. Graceful in code, but a project-completion
  decision (ship data / optional download / retire feature) → CP0.

## __init__.py ×3 (0) — empty.
