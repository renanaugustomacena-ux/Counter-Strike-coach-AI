# D-B48 — tools/headless_validator.py solo (2,910 LOC) — pass 1 read 2026-08-14

Zero register findings. The gate itself audits CLEAN — 26 phases, and
the checks encode most of the repo's doctrines as executable guards.

## Architecture
- check()/warn() severity split; CheckResult dataclass; per-check
  timing in --verbose; exit 0 with warnings allowed, 1 on any fail.
- Deliberately NON-importable (`if __name__ != "__main__": raise
  ImportError`) — prevents accidental test collection ✓.
- Optional-dep downgrades: kivy / ncps+hflayers absent → affected
  checks become warns (_check_kivy/_check_rap) ✓.

## Coverage map (verified against my pass-1 reads)
- Phases 2–3l: ~140 import checks across every backend package —
  matches the module inventory from Phases F–K ✓.
- Phase 4/11: in-memory schema + CRUD round-trips; enum value sets;
  MATCH_AGGREGATE_FEATURES→PlayerMatchStats attribute check (the B22
  two-feature-spaces doctrine as a guard) ✓.
- Phase 9: 14 class contracts (DatabaseManager, Console D1-D10 surface,
  ModelFactory, JEPA/VL-JEPA, datasets…) ✓ consistent with dossiers.
- Phase 10: dims (INPUT==METADATA==25, OUTPUT==10, 16 concepts), EMA
  cycle, dataset windowing, checkpoint-name table, ConceptLabeler
  range — the L1/L8 lens rows as executable checks ✓.
- Phase 12/21/23: AST scans — bare except, eval/exec, secrets regex,
  torch.load weights_only=True, shell=True (prod), bare-assert budget,
  print budget, oversized-func budget, mutable-global budget in nn/,
  type-hint sampling (seeded), circular-import DFS with TOP-LEVEL-only
  edges and the deferred-import rationale documented ✓ excellent.
- Phase 18: F5-16's TrainingStopRequested contract as a guard (raises
  after request_stop; is Exception, NOT KeyboardInterrupt) — the
  F-0032 fix will extend naturally here.
- Phase 21 manifest sampling = the KNOWN RED at baseline (F-0001,
  3/10 mismatch) — CRLF→LF normalization matches the manifest hasher.
- Phase 25: token freshness via gen_design_tokens --check for BOTH the
  Python dataclass and the TS mirror (one-way flow doctrine as a gate).
- R4 MED war story in-file: the drive-letter check was a NO-OP (never
  raised) — now accumulates violations.

## P3s (dossier-only)
- **UI coverage drift**: Phase 7/24 screen lists lack
  pro_comparison_screen + pro_player_detail_screen; the VM list lacks
  focus_insight_vm, pro_comparison_vm, pro_player_detail_vm (added
  after the lists were written; the "All 14" comment was accurate
  then). A pro-screen import break would pass this gate (pytest still
  catches it). W4 one-liner: extend both lists.
- Phase numbering skips 19 (18 → 20); header docstring says "Phases
  16-23" but phases run to 26 (doc drift).
- "table count >= 20" check name vs EXPECTED_TABLES == 19 entries
  (label drift). settings-defaults check parses config.py textually
  (brace counting — brittle, but warn-level).
