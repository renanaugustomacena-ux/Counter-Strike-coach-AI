# D-B62 — test infra + conftest + automated_suite (9 files, 1,166 LOC) — pass 1 read 2026-08-14

Module 08-testing read first (STUDY_LOG row in this commit). Zero new
register findings; one fixture-scale lens flag opened for B63+; T-DIAG
run design derived from READING the targets (advisor discipline).

## Programma_CS2_RENAN/tests/conftest.py (653) — the fixture SSOT
- Course-pattern compliant: venv guard w/ P6-03 CI bypass (exit 2);
  integration marker registered + collection_modifyitems auto-skip
  unless CS2_INTEGRATION_TESTS=1; centralized path stabilization;
  KIVY_NO_ARGS.
- Fixture ladder documented in-file: in_memory_db (schema-only, w/
  PT2-18 honesty comment on init_database divergence risk) →
  seeded_db_session (CI-portable realistic data) → real_db_session
  (integration, double skip-gate). mock_db_manager replicates the
  real manager surface incl. expire_on_commit=False parity ✓.
- isolated_settings: snapshots config._settings AND the module-global
  mirror, monkeypatches SETTINGS_PATH, restores both at teardown —
  the process-pollution guard done right ✓.
- seeded_hltv_session uses database._HLTV_TABLES as the create_all
  filter — the separation SSOT reused in tests ✓ (coherent with the
  H1 migration doctrine).
- match_data_dir: honest NOT-full-schema contract comment (no
  match_metadata → no 26-NORM-01 tick_rate; "extend before reusing —
  today's only consumer checks WAL enforcement") ✓.
- P3: seeded_db_session docstring claims "12 RoundStats" but seeds
  4 rounds × 2 players = 8.
- **LENS FLAG (tests-that-lock-bugs, opened for B63+)**:
  seeded_hltv_session seeds PERCENT-scale kast (73.8) and
  headshot_pct (38.5) with time_span "last_3_months"/"2024" — the
  PRE-F-0019 convention (production writers normalize to ratio;
  spans are "all_time"/"2025"). Any consumer test asserting against
  these fixtures validates the dual-scale tolerance path, not the
  canonical ratio path. Also fictional hltv_ids that collide with
  real ones (fixture donk=18987; production 18987=b1t) — harmless
  in-memory, noted. If W2 touches the F-0019 family, this fixture
  must be normalized in the same wave.

## tests/conftest.py (10) — root path bootstrap only ✓

## tests/setup_golden_data.py (175) — census SAFE (main-only)
- `raise ImportError` unless __main__ — makes the utility
  collection-proof under pytest ✓ clever guard; venv guard.
- golden.dem → golden.db regenerable fixture: full TICK_FIELDS (incl.
  the repaired has_helmet/has_defuser columns ✓), 12 common events →
  evt_* tables, parse_header → metadata table; raw→internal column
  rename map (X→pos_x etc.). Fallback core-field list on parse error.
  Consumer census due at S7.

## automated_suite/ — the pyramid's top tier, marker-discipline ✓
- **test_e2e.py (72) — T-DIAG TARGET, profiled before running**:
  integration + slow + timeout(120) vs an in-file measured scale of
  "340 MB/s reads, 45+ min on the data box". Body: init_database →
  isolated_settings config write ✓ → skip-gate (<5 real PMS rows) →
  **CoachTrainingManager.run_full_cycle() — REAL training: model
  checkpoint writes to models/, CoachState updates, possible
  dataset_split reassignment** → post-assertions on CoachState.
  T-DIAG design (recorded now, executed after B75): back up
  database.db + models/ + training_progress.json FIRST; run with the
  suite's own 120s timeout ACTIVE — the timeout-kill behavior IS the
  diagnostic (can this suite ever pass as bounded?); record
  before/after row counts + checkpoint hashes; restore models/ if
  changed. The rebuild_monolith lesson (F-0039) applies verbatim:
  kill-mid-checkpoint-write is the risk to instrument, not avoid.
- test_system_regression.py (58): SAFE — pure-model schema pin (no
  DB) + read-only skip-gated integration probe. Cheap T-DIAG member.
- test_smoke.py (84): runs in the DEFAULT gate; pins METADATA_DIM==25
  and ModelFactory type constants — real regression pins ✓.
- test_functional.py (23): isolated_settings save/load roundtrip ✓.
- test_unit.py (90): extract_match_stats formula checks (accuracy =
  Σhits/Σshots; opening_duel {-1,0,1} none-vs-loss vocabulary pinned
  by assertion); trilingual i18n spot checks (en/pt/it dashboard +
  it coaching) + LOC-02 missing-key fallback — L7 evidence that a
  parity test EXISTS at the unit tier ✓.
- __init__.py (1): banner comment only.
