# D-B37 — widgets components-1 (12 files, 2,128 LOC) — pass 1 read 2026-08-14

## card.py (179) — S3 item-1 FINAL VERDICT
- floating/frosted depths attach QGraphicsDropShadowEffect in showEvent;
  static (never animated), single live call site (wizard_screen:212
  frosted; floating has ZERO users — its QSS rule at base.qss:620 is
  dormant). Per the refined doctrine (crash = effect active during
  animated repaint; three in-repo sources), static card shadows are
  OUTSIDE the crash context; CI's offscreen Linux screen walk exercises
  the wizard frosted card GREEN. **S3 item 1 CLOSED: no effects-doctrine
  violation anywhere in qt_app.**
- P3: showEvent placement rationale is stale ("findChildren sees the
  final subtree" — the chart-view guard it served was removed; both
  docstrings say so). P3: frosted inline QSS hardcodes border-radius
  12px (matches no radius token: sm4/md8/lg16). P3: _parse_rgba does
  `import re` in-function. Repeated shows re-allocate the effect
  (setGraphicsEffect deletes the prior — leak-free, minor churn).

## nav_sidebar.py (199) — icons.py carry RESOLVED (this half)
- NAV_ITEMS references EXACTLY the 7 glyphs the QPainterPath fallback
  implements (home/brain/list_icon/chart/crosshair/gear/help_circle) —
  resolved at import time, so a sprite-missing install boots with intact
  nav. `user` + game glyphs remain SVG-only → residual check moves to
  icon_widget.py (B38).
- i18n labels + retranslate ✓; tooltip "label — shortcut" from NAV_ITEMS
  SSOT ✓; collapse = min/max-width GEOMETRY animation (doctrine-safe ✓).
- P3: toggle_collapse replaces _anim_group without stopping the old one —
  rapid toggling runs two groups on the same properties (jitter); end
  state self-corrects (_on_anim_finished reads current _is_collapsed).
- Version via importlib.metadata w/ "dev" fallback ✓. Brand/version
  literals sanctioned (non-translatable).

## empty_state.py (256)
- **F-0035 (P2)**: _update_cta_row_visibility gates on children's
  EFFECTIVE visibility (`isVisible()`) — once the CTA row hides, every
  later set_cta_text() computes False and the row can never return.
  CONFIRMED live at match_detail_screen: load_match clears the CTA →
  no-data (:265) and error (:314) branches set "Back to Match History" →
  row stays hidden → both recovery states are CTA-less dead ends
  (constructor-path fixture renders DO show CTAs — invisible to the
  render matrix). set_loading has zero callers (dormant same-bug).
- PyInstaller resource note documented in-file (L6 lens row);
  lazy skeleton insert-index logic correct (order-dependent, verified).

## stepper.py (284)
- Two paint modes (plain/labeled); token state colors; ring-clip war
  story (_V_MARGIN 6); QFontMetrics pitch; set_labels for retranslate ✓;
  validation raises; dedupe Property emit ✓. Labels are caller-supplied
  (i18n upstream). Clean.

## match_row_card.py (228)
- Frame-08 row; i18n get_text w/ defaults for stat words ✓; _pct 0..1→%
  coherent with the F-0019 ratio world ✓; FIELD-GAP fallbacks ("—") match
  the VM's documented gaps ✓; DeltaChip gated to personal rows w/ honest
  baseline ✓; rating_label EN (L7). None-rating renders "0.00 / Below
  Avg" (measured-looking zero — dossier note; VM filters usually prevent).

## stat_badge.py (168)
- Unicode trend glyphs, semantic colors (up=success/down=error/flat=
  tertiary) ✓; set_rating frame-33 variant w/ caller-composed caption
  (anti-hardcode) ✓; negative delta sign handled by float format ✓.
  Contract note: delta_pct is PERCENT-scale (callers beware — B40).

## last_match_hero.py (166)
- Stacked data/empty; EmptyState via CONSTRUCTOR path (F-0035-safe);
  rating helpers; sparkline cross-ref (B38). P3: `view.mousePressEvent =`
  monkeypatch assignment (works, bypasses subclassing); private
  `_relative_time` imported cross-component from match_mini_card.
- L7 evidence: "LAST MATCH", "No matches analyzed yet", "Analyze a
  demo", "Point the analyzer…" — dashboard hero EN literals.

## toggle_switch.py (154)
- Doctrine-aware: knob animates GEOMETRY-adjacent custom Property with an
  explicit "NOT an opacity effect" comment ✓. Keyboard accessible
  (Space/Enter + focus ring) ✓ WCAG. setChecked emits toggled (Qt
  QAbstractButton parity). F-0034 family evidence: knob anim ignores
  animations_enabled too. Clean.

## focus_insight.py (132)
- Stacked data/empty pair-card; ghost CTA gated on navigate_to ✓.
- L7 evidence: "FOCUS THIS WEEK", "Coming into focus", "Open analysis →",
  empty-state body — all EN in code.

## map_tile.py (129)
- Fully-painted tile re-reading get_tokens() per paint (theme-switch
  correct by construction ✓); i18n words w/ defaults; min-height
  collision war story; rating-colored 4px bar capped at 1.5. Clean.

## match_mini_card.py (119)
- CARRY (S6/L8): _relative_time assumes NAIVE match_date is UTC
  (`replace(tzinfo=utc)`) — if the ingest producer stores local-naive
  datetimes (datetime.now()), every relative timestamp is offset-shifted
  (+2h for the user's TZ → fresh match shows "2h ago"). Resolve with the
  S6 datetime.now census against the match_date producer.
- L7 evidence: "just now"/"Xm ago"/"Xd ago" EN strings. Else clean.

## filter_chip.py (114)
- Instance-QSS pill w/ hover; stretch-fill war story (Fixed vertical
  policy); set_checked programmatic does NOT emit (asymmetric with
  ToggleSwitch — correct for filter groups, no loops).
- CARRY B41/B43: refresh_styling() must be called on theme_changed by
  hosts — instance stylesheet keeps STALE token colors after a theme
  switch otherwise (global QSS re-polish doesn't touch it).

## Cross-refs into B38
DeltaChip, ProBadge, StatusChip, MiniSparkline, icon_widget (fallback
residual), skeleton (pulse stop call-site → B39).
