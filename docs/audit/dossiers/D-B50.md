# D-B50 — UI tooling (5 files, 2,047 LOC) — pass 1 read 2026-08-14

Zero new register findings. F-0034 mechanics confirmed from the harness
side; gemma3:e2b census grows to FOUR sites.

## gen_design_tokens.py (445)
- The one-way-flow generator: EXPLICIT field mapping (rename = conscious
  change, documented), byte-identical output for --check diffing, DTCG
  $value support, Python + TS (--web) mirrors, --stdout, exit 0/1/2.
  Output template matches the B35-read generated file exactly ✓.
- P3s: SECTION_HEADERS/GLOBAL_SECTIONS use manual indices (one comment
  already notes a shift) — adding a field silently misplaces cosmetic
  section headers; --check hint prints the Linux venv path on Windows.

## build_web.py (141)
- pnpm workspace build w/ mtime freshness check; radar PNGs copied from
  PHOTO_GUI/maps (same source the Qt viewer uses ✓ consistent space);
  actionable pnpm-missing error; exit 0/1/2. P3: docstring claims
  install re-runs "when lockfile changes" — not implemented (node_modules
  presence only).

## ui_screenshot.py (194)
- Boot doctrine honored and documented (presentation-only; no daemons/
  SBERT/AppState polling). offscreen + MACENA_UI_ANIMATIONS=0 defaults;
  _settle (pool drain) + _wait(400ms) with rationale comments —
  **CONFIRMS F-0034 from the harness side: determinism is achieved by
  WAITING for animations, not by the kill-switch** (the fade paths it
  waits on are exactly the unguarded ones). Per-tab (--md-tab) and
  per-variant suffixed filenames prevent overwrites ✓.
- P3: uses qt_app_module._create_screens and window._nav_sidebar
  (private seams — deliberate harness coupling).

## ui_gallery.py (290)
- Component gallery (frame 33/20 + chat page) with frame-realistic
  sample copy, documented as dev-tool fixture data ✓; reuses
  ui_screenshot's settle/wait helpers. gemma3:e2b site #3.

## ui_fixtures.py (977) — the strongest tooling file in the repo
- Injection philosophy documented and held: fixtures call the screens'
  OWN signal-handler slots; the only unhooks are documented per-case
  (pro_comparison VM disconnect vs late-worker clobber; ghost-check
  disconnect vs lazy torch GhostEngine; chronovisor stem-null trick
  with the exact settle-race explained).
- Frame-exact discipline: SUM-CHECKED history (47 rows avg exactly
  1.08, last-5 exactly 1.17 — arithmetic in the comment); honest note
  where the FRAME itself is internally inconsistent (±1pt rounding);
  MR12 24-round table; curated+filler corpus reaching every frame
  counter (47/2,148); real inverse radar transform (_mirage_world
  matching map_config pos_x/pos_y/scale); trails filled through 30
  REAL frame_updated emissions; context-before-data injection order
  respecting the VM's R4 MED contract ✓.
- Every FIELD-GAP superset key cross-references the matching VM/screen
  comment — all verified consistent with my B36–B42 reads ✓✓.
- gemma3:e2b site #4 (inject_coach). P3: inject_wizard mkdtemp leaves
  macena_brain_* temp dirs behind (harness-only hygiene).

## gemma3:e2b census (W2/W3 fix targets)
help_screen.py:188 · ui_gallery.py (set_status) · ui_fixtures.py
(inject_coach) · tests/test_qt_core.py:636-641. llm_service default is
gemma4:e2b; chat VM + help fallback topics already correct.
