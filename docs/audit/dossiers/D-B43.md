# D-B43 — screens: coach + pro pair (3 files, 2,048 LOC) — pass 1 read 2026-08-14

Zero new register findings. Three carries closed.

## coach_screen.py (862)
- **Streaming carry (B39) CLOSED CLEAN**: _on_stream_progress creates the
  coach bubble on the FIRST chunk (add_message) and only then routes
  later chunks through update_last_message; _streaming_active resets on
  full re-render, clear, and loading-end (cancelled streams). The
  user-bubble-overwrite hazard is explicitly handled.
- Research patterns implemented w/ FIELD-GAP honesty: top-3 severity-
  ranked shortlist w/ collapsed overflow (29.4), provenance mono line
  composed ONLY from real payload fields (29.3), belief n-chip hidden
  until a count exists ("never renders n=— demos"), driver rows em-dash
  for sourceless stats. FE-01 PlainText on all DB-sourced text ✓.
- Belief: 0..1-or-0..100 tolerant normalization + sweep-only-on-change ✓.
- LLM picker: main-thread /api/tags w/ documented rationale; persisted-
  pick-not-installed honesty ("saved (not installed)" instead of silent
  swap); pick persists via userData; _set_llm_model harness seam ✓.
- **Map-SSOT cluster member #7**: _MAP_RE hardcodes 9 maps (no cache/
  office — diverges from match_utils' 11) for pro-context captions.
- P3: retranslate calls _update_drivers() twice; ChatPanel private
  _clear_btn/_collapse_btn rewiring (documented same-package); odd
  double-# comment style on the FIELD-GAP blocks.
- i18n coverage excellent (coach.* keys).

## pro_comparison_screen.py (845)
- **Ratio-display carry (B36) CLOSED**: _fmt_metric pct = value×100 on
  the verified ratio-scale payloads (kast/hs/opening/clutch) ✓.
- Anti-fabrication showcase: _metric_value returns None for ABSENT AND
  ZERO values (documented: VM zero-backfill is not a real score) → "—"
  cells, no winner, tertiary tint; radar uses pairwise normalization
  with a neutral 50/50 midpoint for axes with no usable pair keys
  ("never a broken spike-to-center polygon"); per-axis FIELD-GAP
  proxies documented (Economy = consistency proxy until eco stats
  scraped).
- Me-vs-Pro belief gate: <10 personal matches → frame-20 gate
  EmptyState (constructor cta+link → F-0035-safe) instead of a
  half-empty radar ✓ statistically honest.
- Contains-completer war story (HObbit/m0NESY canonical casing) ✓;
  style-summary rule ladder fully i18n'd; H2H eps tie-bands per kind;
  "even"/"—" winner semantics ✓. Theme-staleness ledger: mode
  FilterChips (same family).
- i18n coverage excellent (procomp.* keys).

## pro_player_detail_screen.py (341)
- **Scale carry CLOSED**: _format_metric percent kind is DUAL-SCALE
  tolerant (×100 if v ≤ 1.5 else as-is) — renders correctly for the
  ratio world and any legacy percent rows.
- Status EmptyState swap (no CTA ever → F-0035-safe); 999-rank sentinel
  → "Unranked" consistent with VM default; JSON-sourced recent matches
  w/ defensive float parse ✓.
- **L7 ledger — heaviest single-screen residue**: effectively NO i18n
  (constructor EN everywhere: title, Back, Country/Team/Age/Time span,
  Performance Stats, Recent Matches, all status texts; retranslate()
  sets the EN literal "Pro Player"). The S3 setter-census counted only
  ×1 here — constructor-args hid the rest (same correction as B40).
- P3: hardcoded 6px radius + px paddings in match rows (token drift).

## Cross-checks
- B36's latent time-span note stands (VM .first() vs detail's all_time
  preference) — W3 one-liner unchanged.
- fade_in census: still only match_history (B40). Effects tension list
  unchanged.
