# D-B31 — analysis-orchestrator + LLM services (4 files, 1,731 LOC) — 2026-08-14

## services/analysis_orchestrator.py (1,097) — Phase 6 integration layer
- Quality: F5-14 per-module failure counters w/ suppression + O-01 per-match
  reset + 26-ORCH-01 never-silent-pass notification failures; R4 HIGH
  26-TICK war story (hardcoded tick+128 post-window + EXACT tick match
  silently found zero rows → per-demo 2s + nearest-within-±1s);
  _resolve_tick_rate = proper 3-tier SSOT (data column → MatchMetadata →
  loud canonical fallback) ✓; win-prob heuristic-note honesty; per-module
  isolation (one failure never kills the suite) ✓.
- **CENSUS CLOSURES**: F-0016 consumer #7 (momentum round_outcomes);
  F-0021 caller CONFIRMED LIVE (deception w/ SSOT rate; exposure = tick_rows
  event vocabulary at T-DIAG); F-0022 exposure CONFIRMED (role column →
  analyze_match_engagements; "entry" hits the dead key); entropy consumer =
  ONE Info flavor insight (B16 "no promotion" assessment holds); movement
  tick-rate threading VERIFIED (B18 carry closed); eager analysis barrel
  import at ctor (B17 row — singleton mitigates).
- **F-0030 (P1)**: _analyze_role feeds MATCH_AGGREGATE player_stats to
  RoleClassifier — ZERO of the five role-vocabulary keys exist in that dict
  → all scores 0 except the IGL kd-balance bonus → players with K/D
  0.9–1.2 get "Role: IGL Detected (100% confidence)" from one feature;
  everyone else silently skipped by the 0.0-confidence guard. Live
  default-on path (advanced insights). Resolves the B15/B18 verify rows.
- **F-0031 (P2)**: Phase-6 input-contract mismatches leave modules dark —
  VERIFIED: _analyze_utility gates on smoke/flash/molotov/he_grenade
  _thrown keys that NO producer emits (PMS carries *_per_round) → utility
  module permanently skipped; UNVERIFIED-pending-T-DIAG: game_states
  producers (action_taken/buy_type/game_state keys) for strategy/economy/
  win-prob — likely empty on the live path. The B16-18 "aspirational
  vocabulary" cluster, matured.

## services/llm_service.py (462)
- CFG-ENV-01 config-before-env; R4 LOW lazy model resolution (restart-
  required selector bug fixed); TTL availability cache w/
  timeout-does-NOT-poison discipline ✓; family-preference model fallback
  w/ pull instructions; tools probe keyed to model (400 → cached False);
  chat_stream RAISES by documented contract (partial-render decision
  belongs to caller) w/ stall-as-read-timeout ✓ clean. P3: singleton
  unlocked; num_ctx/temperature literals.

## services/ollama_writer.py (108)
- USE_OLLAMA_COACHING default FALSE → polish() is identity by default
  (all polished paths byte-identical off) ✓; graceful error-marker/exception
  fallbacks to original text; F5-24 prompt-configurability note ✓ clean.

## services/telemetry_client.py (64)
- R4 HIGH war story: player_id-vs-player_name schema mismatch (every
  request 422'd) + 200-vs-202 success check — "success was unreachable
  end-to-end" ✓ fixed via is_success; anti-fabrication __main__ note
  (refuses synthetic test data) ✓; env-configurable URL; httpx optional.
  Consumer census → server.py at B32.
