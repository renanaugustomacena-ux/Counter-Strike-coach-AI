# D-B70 — regression-tier suites (7 files, 2,751 LOC) — pass 1 read 2026-08-14

Zero new register findings. This batch is the campaign-history ledger
in test form — every prior audit wave (P0-P3, WR, R4 batches, DS, F7)
has its regression net here.

## test_phase0_3_regressions.py (574) — P6-02 charter
- P0_04 CoachState singleton + status-enum validation; P0_05 backup
  ATOMICITY; P0_06 connection leak on checkpoint; P1_01 early
  stopping wired; P1_03 JEPA collapse detection; P1_04 insufficient-
  data REFUSAL; P1_05 negative-sampling exclusion; P3_01 player-role
  unification; P3_02 avg_kills SCALE consistency (per-round vs
  per-match ambiguity pinned); P3_07 tuple/list ambiguity.

## test_tools_regressions.py (361) — R4 MED batch-11 pins for Phase-C tools
- register_orphan MAX(*_total)-vs-SUM undercount (the exact war story
  cross-read at B59/B61 — now confirmed test-pinned from the tools
  side); dead_code_detector Phase-C `alias not in content` always-
  False bug; tooling-compiles sweep (every tools/*.py AST-parses —
  cheap drift net); **derive_winner_from_roundstats (the
  F-0016-family winner-derivation logic has direct tests)**;
  repair_tick_features LOWER(TRIM) join; wipe-restore clears stale
  WAL sidecars (B52's rationale, pinned); mine_shard tick-rate
  windows (the CONVERTED windows are pinned — B51's two surviving
  bare literals `<=32`/`<=128` remain UNPINNED, consistent with the
  register's F-0002-family state).

## test_handoff_regressions.py (366) — WR-series pins
- WR-31 restore-backup WAL handling; WR-57 Traditional coaching never
  returns zero insights; WR-58 COPER timeout falls back (not dies);
  WR-56 dialogue context; **WR-44→R4 MED honest time_in_round (raw
  value NOT clamped at 115s; the clamp lives in the vectorizer —
  anti-fabrication division of responsibility pinned)**; CORE-10 NaN
  yaw guard; WR-21 refresh settings; **DS-08 SSRF prevention** (the
  remote-file-server boundary has tests).

## test_security_hardening.py (267)
- **sanitize_llm_context — prompt-injection guard tests** (the L9
  LLM boundary is test-covered); safe_identifier / safe_col_type /
  safe_default_literal (schema.py's injection validators, B47 ✓
  pinned); backup-label sanitization.

## test_v1_blockers.py (460)
- Coaching Level notifications, analysis-orchestrator notifications,
  toast system, service utilities, exceptions hierarchy, design
  tokens, splash screen, error codes — the v1 release-blocker net.

## test_observability.py (333)
- JSON formatter shape; **correlation-ID tests** (the contextvars
  thread-propagation question from module 07 has its behavior pinned
  — L2/L4 lens input); log-level resolution + configure; idempotent
  get_logger; tool logger; error-code registry; retention config.

## test_deployment_readiness.py (390)
- The "deeper checks" target that backend_validator's F8-16 comment
  points at: forward-pass reliability, inference latency budget,
  batch-size invariance, deterministic reproducibility, OOD graceful
  handling, deployment verdict aggregation (_SubCheck harness).
