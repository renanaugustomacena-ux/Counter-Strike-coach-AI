# D-B61 — ptools: infra + consolidated utilities (9 files, 2,878 LOC) — pass 1 read 2026-08-14

Phase C's final batch. **F-0041 (P2)** and **F-0042 (P2)** registered.
The S5 shell=True carry is CLOSED (it's real, nosec-justified, and
broken anyway — see F-0041). Both tools/ dirs now fully read.

## _infra.py (441) — the shared validator frame
- Venv guard runs AT IMPORT (module-level path_stabilize()) — exit 2
  outside venv, CI bypass; KIVY_NO_ARGS setdefault; win32 stdout UTF-8.
- ToolReport exit semantics: `failed` counts ONLY CRITICAL/ERROR —
  WARNING/INFO check failures never flip the exit code (explains why
  backend_validator's stale-manifest WARN doesn't redden the gate).
- In-file R4 LOW war story: win32 _detect_color returned truthy
  str/literal True → raw ANSI codes in redirected/CI output; both
  platforms now require a TTY and return real bool ✓.
- to_json Enum-serialization portability handled (<3.12 vs >=3.12
  asdict behavior + legacy _value_ dict). P3: docstring calls the root
  "Macena_cs2_analyzer/" (stale project name); validate_tool_contract
  helper — L10 zero-caller candidate.

## build_tools.py (374) — **F-0041 discovery**
- Docstring says it "Merges: build_pipeline, ..." yet root
  tools/build_pipeline.py (F-0040) still exists separately — the
  consolidation never retired the sibling (L10).
- F-0041 cluster: (a) absolute spec-path interpolated into a
  shlex.split(posix) string AND into the repo's one shell=True
  (:252, nosec B602) — backslash-eating + the space in this
  checkout's "Renan Macena" path break BOTH PyInstaller paths;
  (b) cmd_verify still reads the pre-R4 manifest schema
  {"file","sha256"} that the SAME FILE's R4-fixed writer
  ({"binaries":[...]}) no longer produces; (c) cmd_manifest
  --verify-only counts "files" but the sync tool writes "hashes" →
  "0 files"; (d) --force lint-continue is a no-op.
- In-file R4 LOW war story ✓: manifest rewritten inside the loop —
  only the last artifact was attested; now accumulates entries.
- cmd_build runs alembic upgrade head against the live DB as a build
  stage (documented, note); cmd_verify's forbidden-patterns dist scan
  (db/dem/pt/env/pem/key/log) is solid ✓.

## register_orphan_matches.py (423) — exemplary gating
- DRY-RUN default + --commit + --force ladder; ro sources; UPSERT
  idempotent; registered_only tag; exit 2 on any file failure.
- In-file R4 MED war story: MAX(*_total) truth vs SUM(MAX(per-round))
  undercount — cross-referenced BOTH WAYS with aggregate_match_stats_
  sql (each cites the 910 m1-mirage evidence) ✓ coherent pair.
- DatasetSplit.UNASSIGNED at insert — completes the B59 L8 contract:
  registered rows enter UNASSIGNED; data_pipeline owns the column
  (observe_training_cycle's UNASSIGNED-gap probe watches exactly this).
- logging.basicConfig(force=True) w/ rationale comment (transitive
  imports install handlers that silently dropped WARNINGs) ✓.

## repair_rating_scale.py (226) — the F-0016-family data repair
- The OTHER side of B59's raw-vs-normalized war story: R4 MED
  2026-07-17, 1571/2501 full_sql rows carried baseline-normalized
  ratios; writer fixed via SSOT, THIS tool repairs the data.
- Dry-run default with mode=ro CONNECTION (even the handle is
  read-only in dry-run — elegant); CSV full-table backup; single
  transaction; post-repair verification loop (exit 1 on residue);
  Phase 2 repairs 'complete' rows' impact_rounds (the legacy
  impact-RATING-aliased-into-share bug, matching B59's conflation
  war story from the writer side).
- DELIBERATE stdlib-only formula duplication w/ in-file justification
  (WSL SSD, no venv) + lockstep pointer to
  test_rating_components_contract.py — L8: accepted-with-test-guard.

## project_snapshot.py (438) — census SAFE (read-only + init_database)
- In-file R4 MED war story: ingest statuses are 'completed'/'failed'
  — 'done'/'error' never exist and always displayed 0. **db_inspector
  (B60) STILL carries the unfixed variant** in its formatter (reads
  done/error → failure count permanently 0) — P3, W3 one-liner,
  regression-by-omission across siblings.
- P3: collect_manifest re-hashes with RAW read_bytes() — no CRLF
  normalization (sync_integrity_manifest normalizes) → on a Windows
  autocrlf checkout the manif drift number is wildly inflated.
  Display-only, but the number is unreliable.
- Rest: git porcelain counts, runtime/CUDA, WAL/timeout, KEY_TABLES
  whitelist counts, checkpoint ages, deps versions, config summary.

## ui_diagnostic.py (316) — census SAFE
- Merges 4 retired UI tools; **KV Validation section is an honest
  tombstone** ("Kivy UI retired — migrated to Qt", section kept so
  console `test ui` and section counts stay stable) — the right
  Kivy-residue pattern (W3 cross-ref).
- Localization: en/it/pt key-parity check EXISTS here (base = first
  lang; WARNING) — L7 cross-ref; `extra` keys computed but never
  checked (P3 dead var).
- Qt section: threshold checks (screens ≥10, VMs ≥5, QSS ≥1 w/ rules,
  AST-parse each screen) — drift-tolerant vs headless_validator's
  stale exact lists (B48). Spatial: registry roundtrip corners +
  landmark bounds; SpatialEngine instantiated but unused (P3).

## user_tools.py (315) — census SAFE (all subcommands interactive)
- Merges 7 retired interactive utilities. personalize: API keys route
  through save_user_setting → OS keyring (FE-04/FE-06 verified in
  config.py:524-534 w/ the STORAGE_API_KEY war story; disk gets
  "PROTECTED_BY_WINDOWS_VAULT") — NO plaintext-secrets finding ✓;
  F8-10 no-key-fragment printing ✓.
- **F-0042**: cmd_manual_entry prompts "KAST %" / "Headshot %" and
  stores raw percents into ratio-scale columns as is_pro=True (see
  register). customize accepts any non-empty theme string (P3).
- heartbeat: CORRECT completed/failed vocabulary (third sibling
  confirms db_inspector as the straggler); F8-35 stale-PID handling
  (the specific except arm is mostly unreachable — pid_exists returns
  bool; generic arm covers it; cosmetic).

## __init__.py (0) — empty namespace marker ✓
