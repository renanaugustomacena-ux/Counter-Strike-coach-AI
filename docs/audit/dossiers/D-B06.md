# D-B06 — storage-aux + migrations (27 files, ~2,540 LOC) — pass 1 read 2026-08-14

## backend/storage/backup_manager.py (309)
- Session-time auto-backup of the monolith (used by session_engine daily startup).
  Native sqlite3.backup API (BE-01/DB-03: no SQL string → no injection surface),
  label allowlist validated BEFORE path construction, is_relative_to escape guard
  (BE-06 startswith war story), ST-BK-01 size ceiling 50GiB default (271GB-symlink
  monolith story) + 1.2× free-space margin, quick_check integrity (DG-01),
  7-daily+4-weekly retention with ISO-(year,week) fix (R4). EXEMPLARY.

## backend/storage/db_backup.py (296)
- SECOND backup system (Task 5.4 governance): backup_monolith (migration-time,
  P0-05 atomic API + integrity_check + free-space DB+5GB headroom — 2026-07-17
  154GB disk-fill incident), backup_match_data (tar.gz of shards, WR-33 atomic
  per-shard snapshot, R4: partial archive RAISES — never masquerades complete),
  restore_backup (STOR-01 stale-WAL removal + R4 MED pre-rollback checkpoint +
  rollback file), rotate_backups (keep 5 per subdir).
- **L10 register candidate (P2-batch)**: twin backup systems w/ inconsistent
  guards — size CEILING only in BackupManager, headroom-only here; different
  naming (backup_LABEL_ts vs database_ts) and retention (7d+4w vs keep-5) in the
  same USER_DATA_ROOT/backups root. Roles ARE distinct (session vs migration/full-
  tier) — harmonize guards + document split, or unify.

## backend/storage/db_migrate.py (120)
- Startup alembic auto-upgrade (TASK 2.20.1): compares current vs head, upgrades
  canonical root chain; tolerant of missing alembic (frozen builds); R4 engine
  dispose fix. Caller census → B34/Phase C.

## backend/storage/remote_file_server.py (257)
- Personal LAN demo server (explains fastapi/uvicorn deps). SOLID: hmac.compare_digest,
  P7-03 per-IP rate limit (10/min, idle-key eviction), P2-04 is_relative_to download
  guard, BE-07 TLS-or-refuse on non-localhost bind (explicit CS2_ALLOW_INSECURE_BIND
  override), 503 on unconfigured key, per-request key resolution (rotation without
  restart), /health exempt. No findings.

## Migration trees — AUTHORITY RESOLVED (recon concern DISSOLVED)
- **Canonical: root alembic/** (alembic.ini script_location; 18 versions,
  f769fbe67229 2025-12-26 → f6a7b8c9d0e1 2026-07-17, clean linear chain).
  env.py: pre-migration backup hook (via db_backup — the twins' role split),
  CS2_ALEMBIC_URL override (silent-prod-redirect war story), noqa'd model imports
  with no-dead-code rationale.
- **Tombstone 1: Programma_CS2_RENAN/migrations/** — env.py raises RuntimeError
  unconditionally, self-documenting. ✓
- **Tombstone 2: backend/storage/migrations/** — R2-01 README + env.py raising in
  both modes; 2 historical versions retained (baseline b609a11e13cc forward-only
  w/ R2-06 destructive-downgrade refusal; add_rating_components w/ except-pass
  retry tolerance — acceptable in tombstoned files). ✓
- Belt-and-braces model: alembic (canonical) + database._add_missing_columns
  (additive net) + HLTV reconcile-by-drop + per-shard _MATCH_DB_MIGRATIONS —
  four mechanisms, each scoped to its own store, all documented. COHERENT.
- Notes: (1) env.py metadata includes HLTV-db models (ProEvent etc.) → a future
  `alembic revision --autogenerate` vs the monolith would draft SPURIOUS Pro*
  tables — latent footgun, P3 note (document or exclude via include_object).
  (2) d4e5f6a7b8c9 defers the steamid index "to a post-D1 migration" — NEVER
  LANDED (chain ends without it; db_models comments agree) → D1-completion status
  check at B52/T-DIAG; if D1 done, the index is overdue (query perf).
- Version-file quality arc visible: early autogen (nullable-default gaps) → late
  idempotent _column_exists/_index_exists guards, ADD-only doctrine headers,
  owner-decision drops (26-SCHEMA-02 dead connect flags).
