# D-B18 — movement/roles/economy (3 files, 1,552 LOC) — pass 1 read 2026-08-14

## analysis/role_classifier.py (562)
- **B17 duplicate-enum question RESOLVED CLEAN**: imports core.app_types.PlayerRole
  (P3-01 canonical, str-Enum → plain-string comparisons also safe); the barrel
  re-export is the canonical enum. No identity hazard.
- Anti-mock doctrine end-to-end: cold-start FLEX @ 0.0 confidence (loud warn),
  learned thresholds via RoleThresholdStore, threshold-gated scoring with
  linear fallbacks (graceful when 4/9 thresholds unlearned — B15 note holds);
  R-03 neural output shape validation; R-01 named consensus constants
  (agree→boost / neural wins on margin / heuristic breaks ties); RAG coaching
  w/ _FALLBACK_TIPS; classify_team max-1-AWPer constraint; audit_team_balance
  structural checks ✓ solid file.
- **VERIFY (Phase K)**: scoring vocabulary awp_kills/total_kills/entry_frags/
  rounds_played/assists/rounds_survived/solo_kills/first_deaths/
  utility_damage_avg — matches role_thresholds ✓ internally, but the CALLER
  must actually supply these keys (PlayerMatchStats has no awp_kills/
  entry_frags/solo_kills columns) → if callers feed PMS-derived dicts, all
  role scores collapse toward 0 + IGL balance bonus → degenerate IGL/FLEX
  classification. Same census as B15 role_thresholds row. L7: audit strings EN.

## analysis/movement_quality.py (574)
- **Unit-discipline exemplar**: 26-TICK-02 war story (hardcoded 128 t/s doubled
  every window on 64-tick demos) → canonical SECONDS constants + single
  _seconds_to_ticks conversion (min-1 clamp); distance thresholds documented
  tick-rate-independent; MLMove (SIGGRAPH 2024) grounding; ADDITIVE doctrine
  (never touches METADATA_DIM) ✓.
- 4 detectors with context exemptions (combat-justified descents, man-
  disadvantage passivity, coordinated-push teammate proxy — proxy limits
  documented honestly). analyze_match_ticks: callout coverage, elevation
  utilization, position stability.
- **L1 verify row**: hold durations measured in row-INDEX counts (i −
  hold_start_idx) and divided by tick_rate — correct only if tick rows are
  1:1 per tick (full-density PlayerTickState per B10 dual-write; confirm no
  sampled/strided caller). L8 minor: `z > 50` absolute elevation heuristic is
  map-dependent (Nuke negative-Z levels). P3: module singleton without the
  DCL lock pattern used by siblings (stateless — benign).

## analysis/utility_economy.py (416)
- **R4 HIGH war story**: half-switch round used to route to _overtime_decision
  → impossible "full-buy regardless" @ 0.95 confidence at EVERY halftime
  (money resets at second pistol); now HALF_ROUND[mr_format] second-pistol
  routing + regulation length derived 2·(half_round−1), true OT (>24 MR12,
  $10k) full-buy ✓. P3-12 configurable MR format.
- UtilityAnalyzer: prefix-keyed stats vocabulary (smoke/flash/molotov/
  he_grenade _thrown/_damage/_affected — enum-value-derived ✓ self-test
  consistent); P8-06 hand-estimated baselines w/ validation plan; zero-thrown
  → effectiveness 0 → recommendation fires (reasonable coaching).
- Notes: `rounds_played` default 20 in smoke usage rate (silent bias when key
  absent — P3); WEAPON_COSTS static data ✓; loss_bonus default 1900 (2nd
  tier) documented via param; EN-only coaching strings (L7 family); __main__
  self-test (S5 row). Caller census for the *_thrown vocabulary at Phase K
  (who aggregates these keys from events?).
