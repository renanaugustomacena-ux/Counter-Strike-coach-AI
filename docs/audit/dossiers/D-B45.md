# D-B45 — screens: wizard + help (2 files, 1,465 LOC) — pass 1 read 2026-08-14

## wizard_screen.py (856)
- **F-0038 site 3 (register widened)**: _finish() opens a main-thread
  DB session (PlayerProfile ensure-exists) — same pattern and same fix
  template as profile_screen._save.
- The single live DropShadow site (frosted content panel, :212) carries
  its P1-UX rationale in-file (legibility over wallpaper; frost_bg
  theme-driven) — cleared at B37, doctrine-consistent ✓.
- War stories: WZ-01 normalize, WZ-03 non-blocking demo-dir creation
  ("Path saved anyway"), WZ-04 writable-fallback ladder (Documents/
  DataCoach → ~/DataCoach); TWO height-for-width layout stories (fixed
  width for wrapped steps; center-via-stretches because aligned items
  drop height-for-width) — deep Qt layout knowledge in comments.
- Frame-18 validation row: nearest-existing-ancestor probe + os.access
  + shutil.disk_usage per keystroke (cheap, guarded); existing-data
  warning; tree preview matches the REAL _BRAIN_SUBDIRS it creates ✓.
- Research 29.6 (onboarding ends in proof; calibration visible before
  the first number) implemented as copy-only ✓. Skippable-steps
  frozenset documents the only optional step ✓.
- L7 ledger (the S3 ×5 verdict, widened): intro/name/demo/finish page
  bodies, placeholders ("e.g. s1mple"), validation errors, picker
  dialog titles, "Selected:" prefixes, initial "Step 1 of 5" — while
  brain page, stepper labels, and what-happens-next ARE keyed (split
  coverage, same generalization target).

## help_screen.py (609)
- **gemma3:e2b carry CONFIRMED**: _GETTING_STARTED_STEPS help_step5
  fallback desc says "gemma3:e2b" while both fallback-topic mentions
  (:83, :130) correctly say gemma4:e2b (llm_service default). Wrong
  user-facing install instruction — W2/W3 one-liner + i18n key fix.
- Topic loading: triple fallback (ImportError / empty / exception →
  _FALLBACK_TOPICS) ✓; search filters title+content; selection
  preserved across repopulates w/ getting_started default ✓; RELATED
  top-3 rebuild; external links = RichText anchors w/
  setOpenExternalLinks (repo/ollama/steamid/steamkey — real URLs) ✓.
- Honest-docs details: DEMO FOLDER callout's ≥10MB caption cites the
  REAL DS-12 guard; keyboard hints comment says "verified against the
  codebase" (hamburger collapse 220→60, returnPressed) ✓; docs-source
  TipBox names the real load path.
- P3 (FE-01 tail): _content_label renders doc content without
  Qt.PlainText — repo-owned .md content, low risk, but the discipline
  applied at match_detail/coach belongs here too.
- L7: fallback topic CONTENT is EN-only by design (documented); the
  data/docs/*.md corpus is single-language — CP0 scope decision for L7.

## Phase U read COMPLETE (B34–B45)
All 12 batches read personally: app shell, theme/tokens core, 11 VMs,
~44 widgets, 7 charts, tactical widgets, 17 screens. Boundary gate runs
next; WAVES.md entry records the phase evidence.
