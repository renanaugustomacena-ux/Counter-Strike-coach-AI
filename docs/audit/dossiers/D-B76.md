# D-B76 — infra/config (39 files incl. the shadow ini, ~3,090 LOC) — pass 1 read 2026-08-14

Phase I = Pass 1 COMPLETE. **F-0044 (P2) registered** (shadow
pytest.ini); F-0041 gained member (e) phantom macena.spec; F-0001
root-manifest question RESOLVED. Module 30 read first (STUDY_LOG).

## The four findings-grade discoveries
- **F-0044 — Programma_CS2_RENAN/pytest.ini (6 ln, shadow config,
  MISSED BY S1 — ledger now 618 rows)**: any pytest invocation scoped
  under Programma resolves IT (no strict-markers, only integration
  marker, timeout_method=thread) while the full gate resolves the
  root ini — invocation-dependent config; retroactively explains the
  T-DIAG "unknown mark slow" warnings.
- **F-0041(e)**: macena.spec exists NOWHERE — build_tools' two build
  paths target a phantom; real spec = packaging/cs2_analyzer_win.spec.
- **F-0001 resolution**: root integrity_manifest.json (v2.0,
  2026-02-04, 18 hashes incl. deleted main.py + observability/rasp.py)
  is the phantom-main.py carrier; core/ manifest is authoritative.
- **F-0014 evidence #4**: scripts/build_exe.bat pyinstalls the
  DELETED main.py with --collect-all kivy/kivymd via venv_win — the
  Kivy-era corpse the plan already slates for W3 deletion.

## CI — .github/workflows/build.yml (471) read in full
- F-0004's exact mechanism: on.push.branches [main, develop,
  feature/**, fix/**] — `feat/**`/`chore/**` never match; plus
  docs/** paths-ignore (audit commits wouldn't trigger anyway).
- Stage map: lint (pre-commit, SKIPs the two system hooks needing
  full deps) → test matrix (Ubuntu = coverage gate 33%, Windows =
  BLOCKING with the 26-WIN-01/02 re-arm history in comments; CI venv
  guard bypass; QT offscreen) → integration matrix (headless_validator
  2-min cap; **F5 war story in-comment: "the integration-marked tier
  had NO executor anywhere — phantom imports rotted for months"**;
  `-m "integration and not slow"` w/ timeout 300; cross-module
  METADATA_DIM==INPUT_DIM check; portability; manifest verify) →
  security (bandit MEDIUM+ fail, detect-secrets w/ honest exclusion
  rationale, pip-audit) → mypy INFORMATIONAL (W4c F0 posture
  confirmed) → web apps (pnpm tsc+vite, W6.3a) → dist build
  (main-only, needs integration+security, locked CPU pins,
  audit_binaries post-gate). All actions SHA-pinned (POL-CI-01 ✓).
- fuzz-nightly.yml (101): §64/C-SBX-02, develop-only informational
  Phase 1, cron 03:23, auto-opens issues on crashes, honest Phase-2
  promotion note. notify-failure.yml (99): rolling CI-red issue
  open/close automation. threat-model-gate.yml (159): PR gate over
  SECURITY/BOUNDARY_FILES.txt changes.

## Test/tool config
- Root pytest.ini (49): strict-markers, full marker set (slow/
  integration/unit/portability/known_fail/flaky/timeout), F9-12
  timeout=30 w/ honest Windows-leg -p no:timeout rationale,
  norecursedirs incl. D:* artifact. pyproject.toml (81): P5-04
  packaging; black/isort 100 cols; mypy F0 posture; coverage config
  w/ P6-01 roadmap comment (33% floor "raise BOTH together with
  build.yml"); tools/ + entry scripts honestly omitted from the
  denominator. .pre-commit-config.yaml (97): 4 local hooks (headless
  + dead-code pre-push; manifest verify + dev_health pre-commit),
  standard hooks (large-files w/ asset exclusions, detect-private-
  key), black 24.1.1 / isort 5.13.2 pinned (W4a alignment target:
  plan says 24.10.0).
- alembic.ini (147): single tree, script_location=alembic ✓ (L3 map
  confirmed at the config layer).

## Packaging + scripts
- packaging/cs2_analyzer_win.spec (168): the CURRENT PySide6 spec;
  windows_installer.iss (78): InnoSetup. scripts/build_production.bat
  (94): venv_win + real spec chain. setup_new_pc.bat / export_env.bat
  / Setup_Macena_CS2.ps1: bootstrap trio. launch.sh (29): venv check,
  py>=3.10, pycache clear, QT_QUICK_BACKEND=software +
  --disable-gpu (Linux GPU-driver war story), exec module ✓.
- train.sh (71): EPOCHS override, JEPA always + RAP maturity-gated
  (50 demos); train_docker.sh (52); ingest.sh (79): ingest WITHOUT
  training chain (honest separation ✓); reaggregate.sh (71);
  _rocm_smoke.sh (37).

## Requirements family (8 files) + supply chain
- requirements.txt exact pins (DEP-1; PySide6 6.11.0, demoparser2
  0.41.4 — the B57 fuzzer help text says ==0.41.1, trivial drift);
  requirements-ci = ranges + CPU index w/ rationale; -lock/-lock-cpu
  (132/134) pip-tools chains; -dev.in/-dist/-rap(.in);
  .cs2_req_no_torch (54); bindep.txt (17); MANIFEST.in (19).
- docker-compose.yml (16): flaresolverr v3.4.6 TAG-pinned — the
  C-DOCK-01 refresh tool (B56) exists precisely for this; --check
  would flag it (W4/W5 item).
- **.gitattributes (29)**: *.db and *.pt declared LFS; line-ending
  POLICY comment (2026-07-02): index historically mixed 478 CRLF /
  623 LF — "do NOT renormalize globally without an owner-approved
  plan" — a BINDING W4/W5 constraint (explains the per-commit CRLF
  warnings; coherent with the manifest's CRLF-normalized hashing).
- .gitignore (217): *.db/*.db-wal/-shm, /models/, logs/ (README
  exceptions), .env (+.env.example exception) — matches
  test_security's gitignore-contract tests ✓. .env.example (41):
  honest var docs (Sentry DOUBLE opt-in; CS2_MANIFEST_KEY HMAC;
  local-default telemetry).
- SECURITY/ (skimmed per plan): THREAT_MODEL, CONTROL_CATALOG,
  INCIDENT_RESPONSE, SLSA, WIPE_RUNBOOK, CVE_LOG, BOUNDARY_FILES,
  waivers.yaml + policies/POL-*.yaml (sampled POL-CI-01 SHA-pinning —
  enforced in build.yml ✓; policy_runner B54 executes these).
  Trilingual READMEs present here too.
