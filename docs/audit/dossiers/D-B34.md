# D-B34 — app shell (7 files, 1,258 LOC) — pass 1 read 2026-08-14

Study companion: 20-gui (STUDY_LOG row); S3 sweeps recorded (sweeps/S-3.md).

## core/worker.py (73) — THE L2 foundation
- Canonical Worker(QRunnable)+WorkerSignals: RuntimeError-guarded emits on
  ALL four signals (receiver-GC'd race), setAutoDelete, F2 opt-in progress
  injection with the same guard ✓ textbook cross-thread marshalling. Clean.

## core/i18n_bridge.py (117)
- JSON-first → hardcoded(lang) → hardcoded(en) → caller default → raw key
  priority chain (documented); {home_dir} substitution; language_changed
  Signal. **Stale-comment note**: claims core/localization "imports Kivy at
  the top" — VERIFIED FALSE (localization.py head is json/os/config only;
  Kivy import already removed) → the ImportError fallback never fires and
  full trilingual dicts load ✓ concern dissolved (P3 comment fix). The
  documented `default=` parameter sanctions inline-EN-literal screens
  (relevant to the 32 S3 candidates).

## core/app_state.py (300)
- 10s QTimer → Worker bg read → main-thread _apply diff-emit ✓ textbook;
  sanctioned-single-write honesty note (notification is_read marking, "no
  other write path may be added"); heartbeat staleness 300s; P3/P4 toggle
  accessors w/ malformed-settings-safe bool coercion + persist-through-
  config ✓; webengine/pyqtgraph fallback doctrine documented.
- **F-0033 (P2)**: _bg_read is a @staticmethod whose R4 LOW warn-once
  branch references `self` (`getattr(self, "_bg_read_error_logged", ...)`)
  — NameError when the import-failure path fires (broken venv / migration
  mid-flight: the EXACT scenario the diagnostic was added for). The
  NameError propagates to Worker's error signal, masking the real import
  failure the fix intended to surface.

## app.py (390)
- Token-driven splash (fixed pre-atlas hardcoded hexes — remaining hex hit
  is the splash STATUS-TEXT color "#a0a0b0" at _splash_status, P3 token
  candidate); ordered quit handler (polling → lifecycle → console,
  rationale documented); deferred screen imports (focused ImportError);
  R4 MED boot-modal war story (probe re-called get_console, modal never
  showed → boot outcome threaded explicitly); DOCK-01 raise/activate;
  WR-10 responsive splash download loop; chained excepthook; boot side
  effects confined to main() per doctrine ✓.

## main_window.py (377)
- on_enter/on_leave lifecycle + retranslate propagation to sidebar and all
  screens; R4 MED SoundManager war story ("never instantiated — the
  SOUNDS_ENABLED toggle had no effect at all") w/ NullSound fallback;
  NAV_ITEMS as SSOT for buttons + shortcuts; StackAll wallpaper layering
  w/ scaled-crop cache invalidation; motif tile rendered once + tiled
  (O(viewport)); toast overlay outside the stack w/ resize refit ✓.
- **Effects-doctrine refinement (S3 item 1)**: :349 documents the fade
  disable — "QGraphicsOpacityEffect causes QPainter errors on Linux when
  applied DURING widget layout/repaint" — the ban is repaint-context-
  scoped, not absolute. B35 animation.py safety note completes the ruling.
- P3: _CustomTitleBar tooltips are 3 raw EN literals (frameless mode only
  — add to the S3 candidate ledger).

## __init__ ×2 (1 LOC) — trivial.
