# D-B07 — demo-parsing (5 files, 1,902 LOC) — pass 1 read 2026-08-14

## backend/data_sources/demo_parser.py (653)
- Core demoparser2 wrapper: parse_demo (round stats), parse_sequential_ticks (full
  1:1 tick stream — R4 MED: the LAST tick-decimation mechanism removed per supreme
  invariant), _add_event_stats_safe (no-fabrication doctrine R3-01), event-driven
  KAST w/ saturation war story (closed-form estimate stamps 1.0 on ≥1 kill/round),
  _final_scoreboard_totals last-not-max (PGL Astana +146 phantom kills story),
  H-01 column-name resilience (Source1/2 field variance), HLTV2.0 vectorized
  columns pinned to rating.py by test_rating_components_contract.
- **F-0006 WIDENED**: both entry points catch (OSError, ValueError, RuntimeError,
  KeyError) at :60/:651 — PanicException bypasses; panic inside worker thread
  propagates through future.result() the same way.
- **F-0013 (P2, new)**: timeout is ILLUSORY — future.result(timeout) fires, but
  `with ThreadPoolExecutor` exit calls shutdown(wait=True) which JOINS the still-
  running parse: a hung demoparser2 blocks parse_demo/parse_sequential_ticks
  forever (Digester daemon hang); a slow-but-finishing parse still pays full cost
  after "timing out". Docstring promises "prevent indefinite hangs" — violated.
- :313 `header.get("tick_rate", 64) or 64` — bare-64 pair (except-branch uses
  DEFAULT_TICK_RATE; inconsistent) → F-0002 fix family; guard-test scanner scope
  question for B62.
- H-02 size-scaled timeout (max(300s, 3s/MB)); rich timing/memory telemetry.

## backend/data_sources/demo_format_adapter.py (282)
- Pre-parse validation: PBDEMS2 magic, 10MB–5GB bounds (DS-12), truncation
  heuristics (4-byte alignment, <50MB warning), version registry + protobuf
  changelog, canonical→version field mapping. **F-0006 remediation asset**: this
  layer would likely reject the panicking home-dir demo — route _parse_demo_header_meta
  (and all direct DemoParser constructions) through validate_demo_file first.

## backend/data_sources/round_context.py (236)
- Round boundaries via round_freeze_end+round_end pairing; bomb events (H-07 incl.
  exploded); assign_round_to_ticks via merge_asof O(n log m), tick_rate REQUIRED
  kwarg (26-TICK ✓), R4 MED no-upper-clamp + warmup-0.0 war stories, int64 dtype
  normalization. Per-event broad except isolation (acceptable L4 pattern; panic
  bypass caveat applies).

## backend/data_sources/event_registry.py (358)
- Canonical CS2 event vocabulary registry: 23 events specced, 12 implemented,
  handler paths + notes, coverage_report by priority/category. F6-33 documented:
  handler_path strings not validated (stale-on-move risk). Honest expansion map —
  the unimplemented "standard" utility detonation events are FIELD-GAP context
  for entropy/deception analyses (B16-B18 x-ref).

## backend/data_sources/trade_kill_detector.py (373)
- Trade detection: roster from early-tick team_num mode; searchsorted round
  assignment; backward scan bounded by window AND round (M-05 inclusive boundary);
  DS-07 per-demo tick_rate from header w/ R4 MED loud fallback using
  DEFAULT_TICK_RATE ✓ (SSOT-correct — contrast demo_parser :313). Per-player
  aggregation. Same-round break on prior.round mismatch assumes tick-sorted
  single-round contiguity ✓ (sorted upstream).
