# D-B53 — repair tools (11 files, 1,828 LOC) — pass 1 read 2026-08-14

**F-0039 registered (P2)**: the B49 carry resolved — verify_all_safe's
prefix filter schedules ≥9 confirmed bare-invocation mutating tools (6
from this batch). Census continues at B54-B56.

## Mutating on bare invocation (F-0039 members)
- repair_ratings.py (100): targeted UPDATE of rating==0 rows via the
  SSOT compute_rating_components (RAW-components contract documented);
  honest data_quality complete→partial downgrade; ghost-player skip;
  DL-1 lineage ✓ well-built, just un-gated.
- repair_kast.py (133): has --dry-run but LIVE default — BULK rewrite
  of avg_kast for every pro row with roundstats (replacing the inflated
  ~0.91 estimator with event-accurate ~0.70-0.75); before/after +
  distribution report; LOWER() join ✓.
- repair_equipment_value.py (189): one-shot campaign tool over a
  hardcoded 8-demo list (sourced from tick_census evidence — loop
  closed ✓); chunked rowid UPDATEs w/ 200MB cache; verification pass.
- repair_tick_features.py (198): the heaviest — re-parses EVERY demo,
  temp-table UPDATE FROM against the 429M-row monolith. THREE war
  stories in-file: LOWER(TRIM) join ("exact join silently skipped
  every mixed-case player — half the monolith"); the measured O(N×M)
  runaway (">40 min, days-scale projection") fixed by the temp index;
  ANTI-FABRICATION missing-field exclusion ("would overwrite the whole
  demo with fabricated zeros") ✓✓.
- flag_ghost_players.py (67): sample_weight=0.0 for zero-engagement pro
  rows; already-flagged filter (idempotent); rows preserved for
  lineage ✓.
- purge_default_stats_rag.py (122): CHAT-06/TASKS#34 cleanup; reuses
  _is_default_stats_card (predicate SSOT); --dry-run available, live
  default; title-prefix matching w/ documented edge case.

## Safe by design (the pattern F-0039's fix should generalize)
- rescrape_placeholder_pros.py (191): GAP-06; DRY-RUN DEFAULT; --apply
  gated behind robots.txt preflight + owner-authorization note (L9
  showcase); --limit resume-friendly; post-scrape _still_default
  verification; acceptance gate documented (purge tool dry-run → 0).
- sync_pro_players.py (144): GAP-05; DRY-RUN DEFAULT; timestamped
  file-copy backup before apply; post-purge asserts; clarifies the
  cross-DB doctrine (canonical pros live in hltv_metadata.db; main-DB
  copies were stale seed rows) — coherent with the pro screens' use of
  get_hltv_db_manager ✓.
- tick_census.py (239): READ-ONLY chunked corpus census; dead-dimension
  detection (>90% zero, low-frequency allowlist); repair
  recommendations — the evidence source for repair_equipment_value's
  demo list AND the has_helmet/defuser backfill timeline note ties to
  repair_tick_features (2026-07-17) ✓ closed loop.
- migrate_db.py (254): R2-11 tombstone done RIGHT — DeprecationWarning
  on import + `__main__` guard that prints the alembic instruction and
  exits 0 BEFORE any logic (explains the S5 double-__main__); archived
  body unreachable by CLI. Model for the L3 migration-plurality cleanup.

## merge_demo_pool.py (191)
- DRY-RUN default + --execute; stem-dedup semantics cross-referenced to
  run_ingestion's canonical three-tier dedup; DS-12 min-size skip;
  case-insensitive keys; per-file error isolation; known-set updated
  mid-run ✓ clean.
