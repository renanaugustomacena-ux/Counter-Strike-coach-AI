# D-B27 — knowledge/RAG (6 files, 1,909 LOC) — pass 1 read 2026-08-14

## knowledge/rag_knowledge.py (741)
- KnowledgeEmbedder: SBERT w/ WR-10 download-toast UX + cache detection across
  HF hub layouts; H-01 non-import failure logging + SA-27 is_fallback
  accessor; R-02 bag-of-words hash fallback (shared-word cosine > random —
  documented improvement); v3 embedding versioning + trigger_reembedding
  (F5-03 5k cap) + FAISS invalidation ✓.
- KnowledgeRetriever: H-02 dim validation; FAISS fast-path + brute-force
  (limit 500); WR-61 batch usage updates (auto-commit session ✓).
- KnowledgePopulator: Coach Book index format (versioned, per-map files) +
  legacy fallback; _ALLOWED_ENTRY_KEYS forward-compat filter ✓.
- **Seed data EXISTS** (tactical_knowledge.json + book/{index,general,4 maps}
  .json) — knowledge subsystem data-complete, unlike F-0020 CSVs.
- **L7 census #2**: generate_rag/unified_coaching_insight strings EN-only,
  user-facing. P3: _get_retriever/_seed_checked module caches unlocked
  (benign races); seeding not idempotent per-category (partial-failure edge);
  ensure_seed flag set before work (no retry on failure).

## knowledge/vector_index.py (365) — FAISS manager, concurrency exemplar
- THREE R4 MED war stories in one file: (1) rebuild-lock + double-check
  (two threads observing dirty both rebuilt); (2) atomic (index, id_map)
  PAIR snapshot (torn reads mapped FAISS rows onto wrong DB ids); (3)
  ragged-dimension modal filtering (100-dim fallback vs 384-dim SBERT rows
  crashed np.stack → now loud skip w/ ids). Zero-norm guard; persisted
  indexes; OVERFETCH multipliers documented; batched experience extraction;
  stale persisted ids self-heal via post-fetch filtering ✓.

## knowledge/graph.py (242)
- WAL-mode cached connection, F5-08 config-based path, AC-33-01 BFS w/
  depth cap, upsert entities / INSERT-OR-IGNORE relations ✓.
- **F-0027 (P2)**: the connection-cache comment claims "Cross-thread access
  is serialized by _conn_lock at the call sites" — NO SUCH LOCK EXISTS in
  the file. One shared check_same_thread=False sqlite3 connection used
  unserialized by add_entity/add_relation/query_subgraph — sqlite3
  connections are not safe for concurrent same-connection use. Exposure =
  caller census (B28+ coaching engines / UI workers). Also query_subgraph
  mutates the shared connection's row_factory permanently (harmless today).

## knowledge/init_knowledge_base.py (135)
- Coach Book preferred, legacy fallback w/ loud warnings; AC-34-01 sys.path
  hack removed; F5-03 aggregate stats; FAISS build step ✓. P3/W3 candidate:
  populate has NO dedup — rerunning the "run once" script duplicates the
  whole book (knowledge side has no CHAT-07 equivalent).

## knowledge/pro_demo_miner.py (426)
- CHAT-06 DEFAULT_STATS sentinel filter (identical-fabricated-baseline
  pollution; ratio-form on-disk documentation) ✓ anti-fabrication; C-3
  ratio/percent 1.0 discriminator (dead-zone rationale); R4 MED
  opening_duel ratio war story ("every player read 'Disciplined opener.',
  printed 0.5%"); R4 MED raw-sqlite3 war story (silently CREATED empty
  database.db in the package tree, mined zero rows, leaked conn → now
  get_db_manager + LIMIT) ✓✓. Archetype thresholds named; RoundStats mining
  uses kast/round_rating, NOT round_won (F-0016-unaffected ✓).
- L10 row: _KNOWN_MAPS (8, no train) vs orchestrator known_maps (8, no
  train) vs entropy _MAP_GRID_RESOLUTION (9, with train) — map-list SSOT
  candidate.

## knowledge/__init__.py (0) — empty.
