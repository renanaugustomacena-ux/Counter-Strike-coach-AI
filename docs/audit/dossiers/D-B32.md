# D-B32 — control plane + server (10 files, 2,644 LOC) — pass 1 read 2026-08-14

## control/console.py (739)
- Documented lock-ordering doctrine (Console > Supervisor, never inverted);
  D1 __new__-under-lock singleton w/ clean retry-on-init-failure; D2/D3
  computed-live state w/ documented priority; D4 monitor timeout; D5 timer
  cancellation; R3-H06 duplicate-restart guard; NN-85 manual-start retry
  reset; D7 idempotent shutdown; ordered teardown w/ per-step isolation +
  drain loop + CO-02 timeout; D8 success-only caching; D9/D10 correlation
  + global state; WR-13 Docker notifications; demo-count cap ✓ exemplary.
- **F-0014 fix context**: the supervisor IS the live hltv_sync launcher
  (subprocess python hltv_sync_service.py) — start_detached's dead main.py
  path is internal-only. PRO_DEMO_PATH via get_setting("",)+exists — safe
  from the F-0008 $HOME trap ✓. Zero findings.

## control/ingest_manager.py (291)
- WR-07 batch cap; F5-35 Event waits + NN-80 clear-before-check race fix;
  R3-06 disappeared-file guard; claim-then-process-outside-session ✓.
- **F-0015 WIDENED**: _recover_stuck_tasks uses the SAME 5-minute
  _STALE_THRESHOLD (P4-B documented as THE duplicate-processing bug;
  session_engine uses 30min). Serial within this manager, but the queue is
  SHARED with run_worker/session_engine Digester — this recovery can
  requeue a sibling daemon's in-flight >5min parse. Second site added to
  F-0015. L10: imports run_ingestion PRIVATE _ingest_single_demo/_queue_files.

## control/ml_controller.py (193)
- **F-0032 (P1)**: operator STOP is broken end-to-end. F5-16 changed
  check_state() to raise TrainingStopRequested (correct — StopIteration is
  reserved), but coach_manager still guards `except StopIteration: raise`
  while run_full_cycle AND every phase method catch broad Exception →
  TrainingStopRequested is swallowed PER PHASE: stop aborts one phase as a
  logged "failure", training CONTINUES to the next phase, cycle "completes"
  with phase-failure logs, and run_full_cycle's outer catch reports
  set_error("teacher") — _run_wrapper's graceful-stop handler is
  unreachable. Operator control defeated + status lies (Error, not
  Stopped). Fix must also reconcile session_engine's context (B02 —
  historically StopIteration, hence the stale guards).
- Also: cross-process training_file_lock (flock/msvcrt) exists with NO
  caller in this module — NN-02's in-process Lock does not protect against
  the session_engine Teacher in another process (census: does session_engine
  use the file lock? B02 re-check at Pass 2). NN-CTRL-01/02 lock hygiene ✓.

## control/db_governor.py (169)
- DG-02 async-only full integrity checks (120s timeout; 16GB-monolith
  awareness); R3-H07 indeterminate-result honesty; hltv auto-restore from
  .bak → schema-create fallback ✓ clean.

## backend/server.py (358)
- Header: "NOT WIRED — standalone utility server" ✓ documented status;
  S-R3 sys.path-pollution war story (tools/ shadowing in test suite);
  TASK 2.5.1 rate limiter w/ R4 LOW idle-IP eviction (memory-leak fix);
  TASK 6.2 background disk writes; filename alnum-sanitization (L9 ✓);
  #28.1 documented broad-except policy for top-level handlers; telemetry
  schema+202 matches client (R4 HIGH fix's server half ✓). L9 note:
  /api/training/* endpoints unauthenticated — loopback-only by default,
  NOT-WIRED status mitigates; revisit if ever integrated.

## services/lesson_generator.py (380)
- F5-18 named thresholds; template lessons + optional LLM narrative;
  R4 LOW diagnostic-shows-why fix ✓. P3 rows: over-dying warning compares
  per-round avg_deaths (~0.65) against match-total threshold 15 — branch
  UNREACHABLE (F-0019 scale-mismatch family, one lesson section falls back
  to defaults); FOURTH divergent known-map list (map SSOT row); hardcoded
  pro-tip claims w/ unsourced "90%" stat (content-quality note); EN (L7).

## services/player_lookup.py (557)
- Anti-hallucination: CHAT-06 sentinel detection → "stats not yet scraped"
  block (fabrication suppression); n/a-for-zero ("no fabricated
  zero-percent stats"); R4 MED odw ratio fix ("0.5% instead of 50.0%");
  VERIFIED-DATA delimited blocks; TTL nickname cache; stop-word filter;
  demo-only fallback profiles ✓ clean. L10: sentinel tuple duplicated from
  pro_demo_miner (documented circular-import rationale — sync hazard).

## 3 empty __init__.py.
