# D-B03 — core-spatial + observability (9 files, ~2,020 LOC) — pass 1 read 2026-08-14

Study companion: 07-error-handling (read immediately before this batch; see STUDY_LOG).

## core/spatial_data.py (425)
- MapMetadata frozen dataclass; world_to_radar/radar_to_world EXACT inverses of
  spatial_engine.pixel_to_world — **L1 CONTRACT VERIFIED** (radar_width default 1024 ==
  RADAR_REFERENCE_SIZE). Two inverse impls exist (here + spatial_engine) → L10 dedup
  candidate (engine should call meta.radar_to_world).
- Registry: 11 entries (9 maps + nuke_lower/vertigo_lower). JSON config
  (data/map_config.json, SD-02 numeric validation) → fallback hardcoded registry.
  Landmarks DERIVED from map_callouts (R4 MED — divergence fix). Thread-safe singleton
  (double-checked lock, SD-01).
- get_map_metadata partial match: SD-03 collects candidates + warns on ambiguity,
  skips _lower — the MATURE pattern; asset_manager's loose matcher should mirror it
  (harmonization note; mismap risk demoted — no prefix-similar pairs in today's registry).
- Z-level API (get_map_metadata_for_z, classify_vertical_level, compute_z_penalty
  [0..1, 500u saturation]) EXISTS but SpatialEngine never consults it (F6-26 gap —
  lower-Nuke players project on upper radar). FIELD-GAP note; L1 row.

## core/map_callouts.py (374)
- WR-77 callout SSOT: ~160 NamedPositions across 9 maps, 3D nearest-neighbor
  (Z INCLUDED — better than radar projection), radius metadata, JSON extension
  loading (data/map_callouts.json), singleton registry.
- Notes: get_callout returns English names + "unknown area" literal (callouts are
  standard EN jargon; the prose fallback → L7 minor); lazy singleton not thread-safe
  (benign double-init); imported by engagement_range, round_reconstructor,
  spatial_data landmark derivation.

## observability/logger_setup.py (326)
- JSON structured logs (ts/lvl/mod/thread/msg + cid/code/exc*), thread-local
  correlation IDs (appropriate for thread architecture; contextvars trap N/A),
  CS2_LOG_LEVEL override, get_tool_logger (timestamped per-run files under
  logs/tools/ — retention via configure_retention(30d), caller census at B34),
  lazy app_logger (WR-26), configure_log_dir breaks config circular import.
- **F-0011**: every named logger gets its OWN RotatingFileHandler on the SAME
  cs2_analyzer.log (propagate=False) AND app + daemon PROCESSES share that file —
  rotation races / cross-process corruption; LS-01 fallback comment documents the
  Windows symptom ("competing handles") without naming the cause.
- No masking filter in pipeline (module-07 §masking) — secrets depend on caller
  discipline; config.py deliberately never logs .env values; L9 census.

## observability/error_codes.py (307)
- Formal registry (code/severity/module/description/remediation) — module-07 DDD
  error-code pattern. BUT: contract "every inline code registered here" violated —
  ~28 registered vs far more inline (C-*, FE-*, R4, DF, MM, SD, LOC, WR, DP, GAP,
  26-*); several entries reference deleted Kivy main.py ("main" module, F7_19
  KivyMD); log_with_code() helper exists but callsites hand-write prefixes (even
  logger_setup's own LS-01). → L10 census with counts; P3 batch for main.py rot.

## observability/exceptions.py (49)
- Clean hierarchy: CS2AnalyzerError(+error_code) → Configuration/Database/Ingestion/
  Training/Integration/UI. Sentry fingerprinting rationale. ADOPTION unknown —
  S6 census of raisers/catchers (aspirational-hierarchy risk).

## observability/rasp.py (201)
- RASP guard: HMAC-signed manifest (R1-12), fail-CLOSED in frozen builds without
  CS2_MANIFEST_KEY (R4), dev fallback key + RP-01 warning; multi-location manifest
  resolution for PyInstaller; SHA-256 per file; broad-except → violation entry.
- **Manifest triangle clarified**: rasp reads core/integrity_manifest.json (live,
  32KB); ROOT integrity_manifest.json (2.3KB, hashes phantom main.py) is a separate
  stale artifact; + core/.bak. Feeds F-0001 (authoritative file pinned at B58).

## observability/sentry_setup.py (151)
- Exemplary vs module-07 checklist: double opt-in (enabled AND dsn), pytest gate,
  PII scrub (home paths in frames+breadcrumbs, server_name), send_default_pii=False,
  LoggingIntegration (WARNING crumbs / ERROR events), release tag, graceful init
  failure, breadcrumb helper no-op when uninitialized.
- Minor L9: before_send doesn't scrub top-level extra/contexts/message (partially
  covered via breadcrumbs path).

## observability/label_source_monitor.py (161)
- G-01 telemetry: sliding 5-min window, 1% skip-rate alarm latch, min_samples
  warm-up guard, loud ValueError on contract violation, thread-safe, run-long
  aggregates. Spec citations inline. Flawless — no findings.

## observability/__init__.py (0) — empty.
