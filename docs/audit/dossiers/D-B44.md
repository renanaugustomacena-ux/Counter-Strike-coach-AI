# D-B44 — screens: settings + config + profile (5 files, 1,971 LOC) — pass 1 read 2026-08-14

Two P1s registered out of this batch's call-site verification work:
F-0037 (ingestion concurrency — verified from the settings/home trigger
census down into run_ingestion) and F-0038 (main-thread DB, two sites).

## settings_screen.py (1,031)
- **ToggleSwitch carry (B37) CLOSED**: flagship toggles pass initial
  state via CONSTRUCTOR (no setChecked → no toggled emission → no boot
  config write) ✓.
- **F-0037 evidence site**: Start Ingestion runs process_new_demos in
  its own worker slot; Home holds a SEPARATE slot; daemon auto-cycle is
  a third trigger (INGEST_MODE_AUTO defaults True) — zero cross-runner
  exclusion downstream (see register).
- Showcases: wipe flow = double-confirm then HONEST CLI-only pointer
  ("UI must not invoke destructive backend paths"); FE-04 footer names
  the real persistence target + chmod; wallpaper cards read image
  HEADERS only (QImageReader.size) for captions; per-theme swatches
  from get_tokens(theme_key) so each card previews its own palette;
  1px-border-in-both-states pill styling (no 1px reflow — P4 UX audit
  note in-file); wallpaper grid wrap rationale.
- Theme/font/language/wallpaper handlers: apply + persist + repolish
  own toggles ✓ (this screen theme-tracks ITSELF; the systemic
  staleness ledger concerns the others).
- L7 ledger: "Demo Path:"/"Pro Path:"/"Change"/"Set"/"Scan Interval"/
  ingestion status strings/interval error/"Select … Folder" dialogs/
  flagship card title+3 of 4 rows (the marquee row IS i18n'd —
  inconsistent siblings).

## steam_config_screen.py (268)
- Keyring-unavailable plaintext warning (L9-aware) ✓; vault sentinel
  handled; Password echo; R4 LOW SteamID64 17-digit validation war
  story ✓; sync via Worker w/ status transitions ✓.
- P3/L7: "Sync Now" restore string is hardcoded EN — after one sync the
  button label diverges from the i18n'd sync_steam text; status
  messages + keyring warning + placeholders EN. Status auto-hide
  singleShot(8s) is safe (screen is app-lifetime — not F-0036 class).

## faceit_config_screen.py (147)
- retranslate() is an HONEST documented no-op ("English-only for now")
  — self-declared L7 debt; otherwise mirrors steam (keyring warning,
  Password echo) ✓. S3's ×1 undercount corrected: whole screen is EN.

## profile_screen.py (297)
- **F-0038 site 1**: _save opens a main-thread DB session (ensure
  PlayerProfile exists) directly under a docstring that states the
  doctrine and grandfathers the block. Sibling user_profile does the
  same operation correctly via VM+Worker — the fix template is one
  screen over.
- FIELD-GAP honesty: record card renders "—" for id/created_at/
  matches_analyzed rather than opening read paths; frame-17 TipBox
  documents FE-04 storage facts; Related mini-cards i18n'd ✓.
- P3 (FE-01 gap): DbRecordCard SQL line interpolates the user-typed
  name into a QLabel WITHOUT PlainText format — QLabel auto-rich-text
  can render markup from a hostile nickname (display corruption only;
  L9 tail).

## user_profile_screen.py (228)
- **Double-save carry (B36) CLOSED**: edit flow is a MODAL dialog →
  save via VM Worker → re-entry impossible through the UI; DB unique
  constraint remains the backstop ✓.
- Role→token color map w/ tolerant fallback; avatar first-letter
  fallback ✓. L7: edit dialog fully EN ("Edit Profile"/"Bio:"/"Role:"/
  placeholders), status strings EN, back button EN static.
