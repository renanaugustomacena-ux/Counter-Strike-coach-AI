# D-B28 — coaching engines (16 files, ~1,850 LOC) — pass 1 read 2026-08-14

## coaching/hybrid_engine.py (787)
- 26-HYB-01 load-or-degrade ✓ (untrained-inference war story: "untrained
  outputs reached the coaching chain" → None → RAG-only); R4 MED JEPA
  positional-mapping guard (health delta ≠ "recommended_kills"; JEPA
  contributes via adapter); the two-25-dim-spaces gap DOCUMENTED IN CODE
  (F1/W5.1 note) — and the legacy path is actually semantically consistent
  (aggregate model + aggregate features both sides ✓, B24 L1 row refined).
- _LOWER_IS_BETTER direction-aware evaluation (E2E war story: "Strong
  Deaths … Keep it up!" praised dying +7σ) ✓; C-2 baseline TTL; C-41 3-tier
  fallback ladder (get_pro_baseline → HARD_DEFAULT → versioned local w/
  staleness CRITICAL) ✓ course-07 chain; F4-02 degraded tag via i18n (the
  ONE localized fragment); F4-04 ctor side-effect removal; AC-15-01 lazy
  SBERT. calculate_deviations + MetaDriftEngine consumers FOUND (B15 rows
  closed).
- **F-0028 (P2)**: _get_ml_predictions output is threaded into
  _generate_insight and NEVER USED — the "hybrid" engine's ML half
  contributes nothing (confidence uses baseline |z|, content uses z +
  RAG). The documented "High-confidence ML: Lead with ML" strategy is
  unimplemented. 26-HYB-01 made loading honest; outputs still go nowhere.
- **L7 FLAGSHIP**: insight titles/messages built in EN and PERSISTED to
  CoachingInsight (only the degraded-warning suffix localized) — the
  aggregation evidence for the Pass-2 systemic L7 finding. P3: usage_count
  as effectiveness proxy; save_insights_to_db has no dedup (UI-path census).

## coaching/jepa_insight_adapter.py (242) — F1.1 seam
- Quality: NO-WALLHACK contract; sigmoid→signed-delta remap w/ documented
  LOSS-02/WR-52 historical ambiguity; _MIN_MAGNITUDE noise gate;
  semantically-void direction dropped ("be blinded more"); maturity ladder
  (doubt=hedged 0.5×/1 → mature 1.0×/3); pure-function core; 26-HYB-01
  load discipline; flag-off byte-identical ✓.
- **F-0029 (P2)**: the trained-checkpoint gate cannot distinguish
  pretrain-only checkpoints (encoders trained, coaching head RANDOM —
  fine-tune disabled by 26-RANGE-01/TASKS#64) from finetuned ones → with
  USE_JEPA_MODEL=True + a pretrain checkpoint, head noise becomes insights
  (random sigmoid deltas routinely exceed the 0.1 gate). Latent (flag
  default off); CP0 JEPA-readiness cluster with the TASKS#64 status item.

## coaching/pro_bridge.py (128)
- P3-02 scale war story (24× match-totals invalidated every z-score); V-2
  percent-defense on kast/hs incl. R4 LOW archetype fix ("every non-star →
  Support Anchor"); R4 impact_rounds "Absent beats fabricated" — key
  OMITTED ✓ anti-fabrication. Notes: hs default 0.45 when missing;
  entry_rate ASSUMED_ROUNDS_SAMPLE=100 heuristic (documented).

## coaching/token_resolver.py (107) — clean token assembly, malformed-JSON
  guard, ratio-consistent comparisons ✓.

## coaching/explainability.py (92) — template narratives w/ SILENCE_THRESHOLD
  ("silence is a valid action"); placeholder/format-key coverage VERIFIED
  complete; named severity bounds ✓. EN-only templates (L7).

## coaching/correction_engine.py (64) + nn_refinement.py (34)
- P3-07 float/tuple/list input tolerance; COACH_WEIGHT_OVERRIDES setting;
  top-3 weighted. DA-03 honest rename doc ("does NOT invoke any neural
  network — pure arithmetic scaling") ✓.

## coaching/longitudinal_engine.py (48) + progress/{longitudinal 9,
  trend_analysis 19} — trend→insight (EN, L7); AC-39-01 <2-sample guard ✓.

## knowledge_base/help_system.py (81) — markdown docs index from data/docs
  (dir EXISTS ✓); C-54 lazy singleton. P3: `print()` on read error (S6
  census hit — logger expected).

## onboarding/new_user_flow.py (140) — TASK 2.16.1 TTL cache + invalidation,
  DA-16-01 user/pro filter ✓ clean. P3: EMOJI in user-facing messages
  (violates the emoji-stripped convention enforced elsewhere) + EN (L7).

## coaching/__init__.py (25) — eager barrel importing hybrid_engine (torch
  chain) — same L2/L10 eager-import family as analysis/__init__.
## knowledge_base/__init__.py (1) + 2 empty inits.
