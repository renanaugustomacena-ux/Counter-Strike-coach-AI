# D-B58 — ptools: HLTV group (3 files, 1,784 LOC) — pass 1 read 2026-08-14

Scope note that reframes the census: verify_all_safe scans ROOT tools/
only (`_project_root / "tools"`, rglob) — Programma_CS2_RENAN/tools/
(B58-B61) is entirely OUTSIDE its blast radius. F-0039 membership
stays closed at 13; ptools gating notes below are convention-only.

## sync_integrity_manifest.py (171) — the F-0001 gate
- The TOOL is clean; F-0001 is data drift, not tool defect. CRLF→LF
  normalization before SHA-256 (cross-OS hash parity ✓); _HASH_EXCLUDES
  incl. node_modules w/ the flatted-.py phantom-drift war story;
  newline="\n" write + 26-ENV-01 anti-churn comment; sorted keys;
  BaseValidator frame (_infra → B61).
- **LIVE re-run today: 45 changed, 15 new, 0 removed, exit 1 —
  IDENTICAL to the R0 baseline.** The campaign added no drift
  (docs/audit isn't hashed). Precision fix: the manifest has NO
  main.py key — the "phantom main.py" detail belongs to F-0014
  (dead_code_detector ENTRY_POINTS), not F-0001.
- Bare invocation = REGENERATE mode (writes the manifest). Intentional
  (pre-commit hook by design) and out of verify_all_safe scope, but
  one casual bare run would self-heal the gate and erase the drift
  evidence — W1's resync does exactly this DELIBERATELY, after
  reviewing the 45/15 diff.

## migrate_hltv_schema_2026_05.py (159) — exemplary one-off
- The global-SQLModel.metadata trap documented AND defused: explicit
  `tables=` filter on create_all ("conflating them = trust below
  zero" separation war story); checkfirst idempotent re-run;
  hltv_schema_migration lock + signal handlers; explicitly NOT
  d_track_running w/ rationale; post-verify: expected-new missing →
  exit 2, forbidden main-DB leak (18-table blocklist) → exit 3.
- P3: the forbidden blocklist is a static snapshot — main-DB tables
  added later aren't guarded (defense-in-depth only; `tables=` is the
  real guard).
- L3 input: hltv_metadata.db deliberately OUTSIDE alembic (alembic.ini
  binds database.db only); schema evolves via these idempotent
  one-offs. Completes the migration-plurality map.

## seed_hltv_top20.py (1,454) — the March-2026 static seed
- Predecessor of B56's scraping seed_hltv_top_n: 20 teams / 100
  players hand-transcribed; ~20 verified PLAYER_STATS; hltv_id-keyed
  upserts, idempotent. Bare = LIVE writes (no dry-run) — ptools scope
  means no runner fires it; convention gap only.
- **DEFAULT_STATS = the ORIGIN of the default-sentinel cards** (rating
  1.02 / kpr 0.67 / adr 72 / kast 71 / maps_played 150): the
  fabricated-estimate rows that GAP-06 rescrape, purge_default_stats_
  rag, and seed_hltv_top_n's sentinel-override were all built to
  clean. Loop closed at the source. W3 candidate: delete the fallback
  outright post-GAP-06.
- P3 provenance bug: detailed_stats_json stamps "source": "hltv.org"
  UNCONDITIONALLY — estimated players carry hltv.org provenance; the
  "est." distinction lives only in stdout. Sentinel detectors match
  values, not provenance, so the cleanup machinery still works.
- V-2 FIX percent→ratio normalization at the seed boundary (kast /
  headshot_pct / opening_duel_win_pct ÷100 when >1.0) — coherent with
  apply_vision's F-0019-aware normalization ✓.
- **StatCard identity contract (CONTRACTS.md candidate, L3/L8)**:
  player_id = FK proplayer.hltv_id, indexed NOT unique; composite
  (player_id, time_span) uniqueness explicitly DEFERRED by the H1
  migration. Every writer upserts via player_id-only .first() → one
  card per player, LAST WRITER WINS including the time_span relabel
  ("2025" seed vs "all_time" stat_fetcher:597). Readers select
  player_id-only, unordered .first() — safe ONLY under the one-card
  invariant. Any future writer that breaks the convention makes reads
  nondeterministic.
- _TOP20_MAP holds 19 of 20 ranks (#13's player is on a team outside
  the seeded rosters); helper returns None → honest JSON null ✓.
- sys.path bootstrap inserts BOTH repo root and Programma_CS2_RENAN
  (S5 dual-import hazard pattern, known).
