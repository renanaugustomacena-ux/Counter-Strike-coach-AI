# D-B63 — NN/training tests (6 files, 4,020 LOC) — pass 1 read 2026-08-14

Zero new register findings. One tests-lens catch (stale demonstration
suite), one L8 contract amendment (FOURTH dataset_split writer), and a
doctrine-pin coverage map for CP0.

## test_coach_manager_flows.py (1,178)
- High quality: real in-memory FAKE DB (course taxonomy — not mocks),
  autouse _isolate_shard_completeness w/ P4-A rationale, split
  boundary math (1 match→test; 2→train+test), temporal-order
  invariant, radar clip bounds, run_full_cycle Idle guard.
- Tests ENCODE owner decisions: 26-SCHEMA-02 docstrings pin
  "prerequisites are pro-only; Steam/FACEIT never in the message"
  (2026-07-17 decision); the narrowed-except taxonomy is asserted BY
  DESIGN (OSError caught, RuntimeError propagates — docstring says so).
- R4 MED note in-file: TestModuleLevelFunctions deleted WITH its dead
  module-level RAP helpers — test-suite dead-code discipline ✓.
- **L8 AMENDMENT — FOURTH dataset_split writer**: coach_manager has
  its own assign_dataset_splits (chronological 70/15/15, pro/user
  cohorts INDEPENDENT). Full writer enumeration now: (1)
  coach_manager.assign_dataset_splits, (2) data_pipeline P-DP-02
  temporal+decontamination bulk UPDATE, (3) D2A MD5 insert-time,
  (4) register_orphan UNASSIGNED insert. Pass-2 L8 must map which
  writer runs when in run_full_cycle and whether (1) and (2) can
  disagree. CONTRACTS.md row updated at L8.
- Patch discipline: definition-site patches are CORRECT here because
  the production code lazy-imports inside functions (resolves at call
  time) — patch-where-used rule satisfied in effect ✓.

## test_coach_manager_tensors.py (242) — **tests-lens catch**
- TestFeatureListIntegrity: 25-dim contract pins (lists == METADATA_
  DIM, no dupes, TARGET_INDICES bounds) ✓ mirrors ML-debugger phase 5.
- **STALE DEMONSTRATION SUITE**: the "Bug #4" None-poisoning tests
  re-implement the OLD extraction expression INLINE (`d.get(f, 0.0)`)
  and never call the production _prepare_tensors — which HAS the fix
  (verified: walrus None-guard + rationale comment in
  coach_manager._prepare_tensors). The three tests pass regardless of
  production behavior — zero regression value; the file docstring
  still claims the bug is live. No test anywhere calls
  _prepare_tensors WITH None fields. W3 test-hygiene: replace the
  demos with a production-path regression test (None-laden fake →
  assert no NaN). P3.
- TestProBaselineVector duplicates the production defaults dict as a
  hardcoded copy (key-coverage check only) — drift-prone mirror, noted.

## test_jepa_model.py (536)
- Architecture pins: EMA target-encoder excluded from grad assertions
  (documented); freeze cycle; save/load w/ weights_only=True ✓ L5;
  VL-JEPA concept_probs sum-to-1, diversity-loss collapse ordering,
  ConceptLabeler heuristic activations (exposed/wasteful/isolated) +
  2D/3D batch shapes; **checkpoint MIGRATION test** (JEPA state_dict →
  VL-JEPA strict=False; missing keys asserted to be exactly the
  concept layers, unexpected == 0) — upgrade-path pinned ✓.
- P3: NamedTemporaryFile(delete=False) in test_model_save_load never
  unlinked (temp .pt leak per run); randn without seed but assertions
  are shape/finite-only (seed-independent) ✓.

## test_jepa_training_pipeline.py (704)
- "Target: 100% function-level coverage of jepa_train.py"; timeout(60)
  file-wide. Dataset windowing (context/target no-overlap, pad rules,
  empty/single-tick edges); EMA momentum schedule monotone → 1.0;
  negative sampling excludes self, batch-1 fallback; loss-decreases
  probe; collapse detection (variance + cosine < 0.99); checkpoint
  roundtrip; REPR-01/Law-17 rehydration warning trio (warns without
  counters, silent when fresh, rehydrates when present) via
  mock-logger idiom.
- **26-RANGE-01 guard tests (R4 HIGH, TASKS#64)**: the OPEN 25-dim
  CLI target vs OUTPUT_DIM=10 head contract — guard raises a NAMED
  ValueError; test pins guard-not-resolution. Direct CP0 evidence for
  the JEPA-readiness cluster (F-0029 family): the mismatch is loud,
  not fixed.

## test_training_orchestrator_flows.py (1,055)
- Doctrine-pin density is the highest in the repo: B1 per-epoch seed
  rotation (train=GLOBAL_SEED+epoch asserted call-by-call; val fixed
  42); **R4 HIGH epoch-loss denominator** (skipped batches must not
  dilute — best-checkpoint/early-stop corruption war story pinned,
  all-skipped→0.0); **TASKS#59 scheduler zero-step guard** + loud
  warning asserted; B3.2 best_val_loss sidecar resume (restore/
  missing/no-extra/save-persists); B3.3 EMA total_steps from planned
  steps; V-1 target-follows-context + exactly-10-ticks→None (overlap
  would teach the model to predict what it sees); J-5 short-batch
  skip-not-pad (zero vectors = impossible game states); NN-H-03
  pool-warmup logs once; W2.5/DR-17 drift wiring (off-by-default,
  alert-once escalation, NEVER-raises); advantage weights sum to 1.0;
  role indices 0-9 contiguous; map-name resolution ladder incl.
  warn-once-per-demo.
- P3: two test classes placed AFTER the `if __name__` block (collected
  fine; odd placement).

## test_training_orchestrator_logic.py (305)
- Init validation (unknown type raises — "unlike ModelFactory" noted);
  early-stopping counter/reset/trigger; empty-batch refusals;
  deterministic negative-sampling RNG; **26-ORCH-02 loud tick-rate
  fallback tests** (metadata failure + evil-cache outer guard both
  warn with the doctrine ID and fall back to default) — F-0002-family
  behavior pinned at the orchestrator layer; **G-01/26-TICK: VL
  concept labels use the item's REAL round_number** (200k-tick item
  must NOT estimate round ~28; missing round yields None, NEVER
  borrows round-1 stats) — the anti-fabrication doctrine as tests ✓✓.

## Doctrine-pin coverage map (CP0 input)
- PINNED by tests: V-1, J-5, B1.x, B3.2/B3.3, TASKS#59, R4-loss-
  denominator, 26-ORCH-02, G-01 round-truth, REPR-01, NN-H-03, DR-17,
  26-RANGE-01 (guard), 26-SCHEMA-02, split boundary math.
- NOT yet found pinned: the F-0002 bare-64 literal enumeration itself
  (test_tick_rate_ssot is RED by design — its failing list IS the W2
  worklist; enumerate at B64+ when reading that file).
