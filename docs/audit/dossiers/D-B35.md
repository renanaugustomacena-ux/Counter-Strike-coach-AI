# D-B35 — theme/tokens core (14 files, 2,020 LOC) — pass 1 read 2026-08-14

## core/animation.py (322)
- SAFETY NOTE header (:8-16): QGraphicsOpacityEffect attached during widget
  layout/repaint → QPainter errors on Linux. Geometry helpers (slide_in/
  slide_out/reveal_stagger/collapse_width) documented safe (animate
  `geometry`, no effect attached); fade_in/fade_out/pulse/cross_fade DO
  attach opacity effects via _ensure_opacity_effect → safe only on stable
  widgets. All anims DeleteWhenStopped; pulse() returns the group so
  callers can stop() (skeleton loaders — call-site check at B39).
- **F-0034 (P2)**: animations_enabled() (MACENA_UI_ANIMATIONS) checked only
  by count_up (:164) / sweep_ring (:184) — the other 8 helpers ignore the
  kill-switch while all three READMEs (:333) sell the offscreen gate as
  "animations disabled". Fade paths attach opacity effects even under the
  flag (CI included).
- P3: reveal_stagger returns an always-empty `anims` list (animations are
  created inside QTimer closures, never collected) — docstring promises
  them; callers cannot stop a stagger in flight.

## core/design_tokens.py (316) — GENERATED
- Header: generated by tools/gen_design_tokens.py from design-tokens.json,
  do-not-hand-edit ✓ one-way token flow honored. Frozen dataclass ×3 themes
  (CS2/CSGO/CS1.6); hex literals SANCTIONED here (this IS the token SSOT).
  Module-global _active_theme mutated only via set_active_theme
  (ThemeEngine the sole writer); get_tokens falls back to CS2 on unknown
  names. Clean.

## core/theme_engine.py (283)
- apply_theme: render_qss + font rule appended after QSS (cascade-wins
  note) + QPalette from the SAME tokens + wallpaper resolve + theme_changed
  emit ✓ no second palette. severity_bucket/severity_color = severity SSOT
  (collapses DB/insight vocabulary); rating_color/rating_label WCAG 1.4.1.
- L7 evidence: rating_label returns EN literals ("Excellent"/"Good"/
  "Average"/"Below Avg") — user-visible via rating badges (callers B37+).
- P3: set_font invalidates the QSS cache although the font rule is never
  cached (harmless extra template read). register_fonts warns per missing
  PHOTO_GUI font; assets/fonts auto-scan debug-silent by design.
- _update_wallpaper: lazy config import; design-atlas default = flat
  surface_base; persisted choice missing on theme switch → flat ✓.

## core/qss_generator.py (41)
- Template.safe_substitute(asdict(tokens)) + per-theme cache +
  invalidate_cache. P3: safe_substitute leaves `$typo` literals in the QSS
  on a missing/misspelled token — Qt silently drops the malformed rule, so
  template↔tokens drift is invisible at runtime (test coverage check at
  B62+: does test_qt_core assert no `$` survives rendering?).

## core/svg_icon_provider.py (243)
- Sprite (design/assets/icons/sprite.svg) read ONCE at import; warn +
  QPainterPath fallback if absent; regex <symbol> extraction; currentColor
  substitution per render; 2× supersampled QSvgRenderer → smooth downscale;
  (symbol,size,color)-keyed QIcon cache (static sprite → effectively
  permanent). sprite_is_available() feeds the icons.py flag-flip.
- S3 hex census CLOSED here: the 22 hits are `color: str = "#ffffff"`
  parameter DEFAULTS (neutral tint overridden by callers passing token
  colors) — sanctioned, not theme drift.

## core/icons.py (171)
- USE_SVG_ICONS flag + sprite_is_available() choose the IconProvider
  facade ONCE at import. QPainterPath fallback implements 7 nav glyphs
  (home/brain/list_icon/chart/crosshair/gear/help_circle).
- **CARRY (P2 candidate)**: SvgIconProvider's nav/chrome section includes
  `user` (plus game/status glyphs) that the fallback LACKS — if
  nav_sidebar calls IconProvider.user() on a sprite-missing install, the
  fallback dies with AttributeError, defeating its stated mission ("never
  ships with broken nav icons"). Verify nav_sidebar's exact method set at
  B37/B38 before registering.
- P3: gear() re-runs `import math` inside both tooth loops; the 7 hex hits
  are the same param-default pattern (census closed).

## core/web_bridge.py (236)
- MarqueeBridge(QObject) for QWebEngine marquee screens: publish_*
  serialize-once with warn-on-fail (TypeError/ValueError); Properties with
  notify signals (tick/frame/coach/ready/map/segments/events/ghost); JS
  slots emit seek_requested/player_selected/ghost_requested for the host
  VM; web log() routed into the Python logger. Textbook QWebChannel
  bridge; publish_tick dedupes; ready latches on first frame ✓.
- P3: class docstring claims `request_ghost(tick) -> str (JSON)` — actual
  contract is async (ghost_requested out, ghost_ready back); drift from a
  synchronous draft.

## core/typography.py (105)
- Roles via QSS variant property (unpolish/polish) for QLabel variants,
  QFont builder otherwise; sizes always read get_tokens() ✓ theme-tracking;
  mono_caption = shared replacement for per-module _caption_font copies.
  Space Grotesk display stack with Qt family-matching fallback. Clean.

## core/sound.py (84)
- 4 preloaded QSoundEffect; play() gated on app_state.sounds_enabled
  (default False); warn-once per missing WAV (source().isEmpty() check is
  correct — source set only when the file existed). B34's R4 war story
  closed: main_window instantiates it. Clean.

## core/match_utils.py (72)
- extract_map_name: prefix regex (second-underscore stop documented) +
  contains-scan fallback. **_KNOWN_MAPS (11 entries incl. cache/office) =
  another divergent known-map list → map-geometry SSOT CP0 cluster.**
- count_personal_and_pro: DISTINCT pro demo counting (10×-per-demo row
  inflation documented) shared by Home chip + History caption ✓.

## core/widgets_helpers.py (56)
- make_button (variant property + repolish + pointing cursor);
  navigate_to (window().switch_screen, hasattr-guarded → harness-safe).
  Clean.

## core/easing.py (51)
- Named QEasingCurve aliases built at import (QtCore — safe before
  QApplication); cubic_bezier helper for Remotion/CSS parity. Clean.

## core/qt_playback_engine.py (39)
- PlaybackEngine subclass: QTimer(16ms) + time.monotonic dt → _tick;
  overrides play/pause; _clock_event=None blocks the parent Kivy path.
  Cross-ref core/playback_engine (B02). Clean.

## core/__init__.py (1) — docstring only.

## S3 item-1 verdict (effects doctrine) — SETTLED for B35 scope
- base.qss.template:432: button gradients chosen to give depth "without
  QGraphicsDropShadowEffect (safe on mid-repaint widgets)" — the SAFE
  alternative, anti-effect reading. Together with animation.py's safety
  note and main_window:349, the repo doctrine = opacity/shadow effects
  banned in layout/repaint contexts; geometry animations and gradient
  cues preferred; consistently applied across B34/B35 scope. Sole
  remaining scrutiny: card.py floating/frosted DropShadow variants (B37).
