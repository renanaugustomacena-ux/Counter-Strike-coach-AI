# D-B04 — storage-core (5 files, 1,752 LOC) — pass 1 read 2026-08-14

Study companion: 09-type-hints (read before batch; W4c blueprint captured in STUDY_LOG).

## backend/storage/database.py (551)
- **DatabaseManager (monolith database.db)**: single engine, pool_size=1 + max_overflow=4,
  check_same_thread=False, timeout 30s; per-connection pragmas WAL + synchronous=NORMAL +
  busy_timeout=30000 + foreign_keys=ON (DB-06) + wal_autocheckpoint=512 (DB-07).
- **Session contract (L3 ANCHOR)**: get_session() ctx-mgr AUTO-COMMITS on clean exit,
  rollback + expire_all on exception (DB-02); expire_on_commit=False. Deprecated
  engine_key param retained (L10 cleanup). NOTE: session_engine F6-03 comment claims
  "context manager does not auto-commit" — STALE, it does (P3 doc-rot).
- **Explicit table registries**: _MONOLITH_TABLES (18) vs _HLTV_TABLES (7) prevent
  per-match tables leaking into monolith; HLTV list is DROP-enforcing (R4 CRIT
  war story: Phase-H1 tables destroyed by omission; contract-tested).
- **Self-migration at boot**: _add_missing_columns() additive ALTERs with DB-04 type
  allowlist + proper quoting (R4) + duplicate-column race tolerance. → B06 QUESTION:
  authority vs the 3 alembic trees (build_production runs alembic) — dual-migration
  drift risk, judge at B06.
- _upsert_player_stats: native ON CONFLICT (demo_name, player_name) — TOCTOU-free,
  empty-set_ edge (R4 MED); UNIQUE constraint confirmed in db_models ✓.
- delete_match_cascade (children→parent; file delete after commit, compensated by
  detect_orphans P5-B); record_lineage (DL-1). HLTVDatabaseManager mirrors pragmas;
  _reconcile_stale_schema drops mismatched/orphan tables (P7-04 name validation) —
  deliberate migration-by-drop for recoverable scraped data.
- CFG-1 chmod 600 POSIX best-effort. Lazy double-checked singletons ×2.

## backend/storage/db_models.py (906) — 24 models
- Constraint discipline: CheckConstraints (rating 0–5, CoachState id=1 singleton P2-01,
  ProMapRecord XOR subject), UniqueConstraints (ux_playermatchstats_demo_player — the
  upsert anchor; H-15 round dedup; ux_h2h_teams), **partial unique indexes** with R4 MED
  SQLite-NULLs-are-distinct war story; FK ondelete SEMANTIC (R2-07: ticks SET NULL,
  vetoes/statcards CASCADE); onupdate auto-refresh on IngestionTask.updated_at (R2-05).
- JSON hygiene: size caps (16K game_state / 8K aux / 32K statcard) + well-formedness
  validation (DM-01); legacy float→label coercion on data_quality.
- WR-76 MATCH_STATS_DEMO_SUFFIX_RE shared (demo_name stem mismatch between tables —
  L1 contract row). All timestamps tz-aware UTC ✓.
- Documented debts: DM-02 Ext_PlayerPlaystyle conflates playstyle+profile (split
  candidate); 26-SCHEMA-02 dead columns removed by owner decision.
- **L1 candidate**: steamid TYPE INCONSISTENCY — PlayerMatchStats.steamid int,
  Ext_PlayerPlaystyle.steamid str + steam_id str(unique); join-contract check at L1.
- PlayerProfile.role default "All-Rounder" vs app_types.PlayerRole vocabulary (flex) —
  two role vocabularies (UI-profile vs canonical) → L10.
- MatchResult.match_id PK "external or auto" — assignment discipline at B10/B52.
- TacticalKnowledge/CoachingExperience embeddings: JSON 384-dim (SBERT MiniLM) — L1 row.

## backend/storage/storage_manager.py (295)
- Ingest/archive path resolver: BRAIN_DATA_ROOT → brain datasets dirs, else project
  data/; DEFAULT_DEMO_PATH (fallback $HOME — F-0008 family evidence); PRO_DEMO_PATH
  respected only if exists; pro_ingest_dir excluded from mkdir (user-managed).
- **Settings snapshot at CONSTRUCTION** — long-lived instances (Digester creates one
  at daemon start) hold stale paths after settings change → L6 row.
- enforce_quota: .dem-only measurement (R4 MED war story: whole-tree vs $HOME always
  over quota); archive moves oldest top-level *.dem (non-recursive ✓ bounded).
- list_new_demos: rglob minus symlinks minus ingested/ (R4 reparse-forever fix);
  dedup vs IngestionTask.demo_path + PlayerMatchStats stems; **10k query caps WITH
  loud warnings** (22§no-silent-caps honored ✓); STOR-07 suffix hygiene.
- get_demo_path: P2-03 path-traversal guard ✓. can_user_upload: per-user quota
  (R4 arbitrary-row fix), no identity → unrestricted; pro always allowed.
- archive_demo: best-effort by contract (documented).

## __init__.py ×2 (0) — empty (storage/, storage/models/). models/ package is EMPTY —
  placeholder dir; L10: either intended future home (db_models split?) or dead.
