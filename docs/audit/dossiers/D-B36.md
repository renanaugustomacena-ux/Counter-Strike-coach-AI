# D-B36 — viewmodels (11 files, 1,586 LOC) — pass 1 read 2026-08-14

**Module-20 MVP rubric verdict: PASS across all 11 VMs.** Zero widget
imports anywhere in viewmodels/; QObject+Signal surface only; every DB
touch runs in Worker(_bg_*) with deferred backend imports; signals
auto-marshal to the main thread. The layer discipline the design-atlas
rebuild promised is real.

## coach_vm.py (84)
- Worker+Signals ✓ loading guard ✓ error resets loading ✓. Player insights
  first, fall back to ALL insights (labeled pro via `player_name != player`
  heuristic). Insight title/message = DB EN text (L7 systemic, known).

## user_profile_vm.py (108)
- Load/save via Worker; `_bg_save` returns attribute access after the
  session context — VERIFIED SAFE: database.py:230/498 build sessions with
  `expire_on_commit=False` (both managers), so detached reads keep loaded
  values. save_profile has no re-entry guard, but PlayerProfile.player_name
  is `unique=True` (db_models:219) → double-click worst case IntegrityError
  → error toast, no duplicate rows. P3: check save-button disable at B44.
- L7 evidence: "No description yet." / "All-Rounder" / "Player" defaults.

## pro_player_detail_vm.py (114)
- Prefers all_time stat card, falls back to any; JSON-decode guard →
  empty dict; zeros when no card (F-0020 world renders "n/a"/0). Clean.

## match_history_vm.py (118)
- data_quality NULL three-valued-logic R4 war story in-file ✓; FIELD-GAP
  comments honest (clutch counts / demo size / pro event metadata absent
  from schema). P3: cancel() semantics — the flag is only checked AFTER
  the query; a cancelled load still emits matches_changed([]) which can
  wipe a fresher load's rows if cancel is used as "supersede" (B41: check
  who calls cancel and when).

## focus_insight_vm.py (120)
- R4 MED anti-fabrication story in-file: card claims no measurement, just
  an honest navigation hint ✓. Broad excepts degrade to has_data=False
  (logged debug; dashboard-card doctrine). L7: hint body is EN in code.

## match_detail_vm.py (183)
- Effective-player resolution R4 story ✓ (stats/rounds/breakdown all keyed
  to the demo's actual player). Confirmed reader of persisted round_won →
  same F-0016 radius (source-side dtype bug; no widening). hltv2 breakdown
  is a CROSS-MATCH aggregate rendered on a per-match screen — B40/B41:
  verify the card labels it as aggregate. Broad except around breakdown
  degrades w/ warning ✓.

## performance_vm.py (187)
- Dense with verified war stories: context-emitted-BEFORE-data (direct-
  connection rebuild order), NULL NOT IN, metric-omission-not-zero
  percentiles (R4 HIGH). 6-tuple return coherent at both ends (ImportError
  early-return included). Percentile falsy-filter drops 0.0 rows =
  consistent with omission doctrine. Pro cohort quality filter matches
  match_history's. Clean.

## coaching_chat_vm.py (205)
- _ensure_engine degrade-to-offline doctrine documented ✓; F2 streaming
  (progress-injection Worker, DR-14 full-accumulated chunks, ≥30ms
  coalesce state) ✓; F2.3 cancel → "" → no bubble ✓; QT-02 clear_session
  in Worker ✓ (finished-only connect: an engine raise is silent but state
  still resets — acceptable).
- VERIFIED: offline hint "ollama pull gemma4:e2b" MATCHES the
  llm_service.py:49 hard default ✓. CARRY B45: help_screen.py:188 says
  "gemma3:e2b" — stale one-char drift in one of its three mentions (P3).
- P3: messages mutated under threading.Lock but all mutation sites are
  main-thread slots (lock redundant); emit-while-holding-lock would
  deadlock on reentrant send (non-reentrant Lock) — low risk, note only.
  _pending_player/_pending_demo created outside __init__ (getattr-guarded).
- L7 evidence: offline hint + "Error: {msg}" system bubbles are EN.

## pro_comparison_vm.py (216)
- R4 HIGH units war story in-file (clutch fraction vs count — absent
  metrics render "—") ✓. User vs pro units VERIFIED coherent: user
  avg_kast/avg_hs are ratios (F-0019 empirical) and stat_fetcher.py:588-593
  normalizes pro kast/headshot_pct/opening_duel_win_pct ÷100 → ratios too.
- P3 (latent, W3 candidate): _get_pro_stats takes `.first()` stat card
  with NO time_span preference — sibling pro_player_detail_vm explicitly
  prefers all_time. Today the only producer writes exactly one card
  ("all_time", stat_fetcher:597) so behavior is correct; a multi-span
  producer would make A-vs-B compare different spans silently. Copy the
  sibling's preference.
- CARRY B43: screen must render ratio-scale kast/hs/duel ×100 under its
  "%" labels (latent until pro cards populate — F-0020). L7: metric
  display names are EN constants.

## tactical_vm.py (250) — three VMs
- TacticalPlaybackVM: engine frame callback runs on the QTimer (main)
  thread; dedupe-emit tick/playing; load_frames threads tick_rate through
  explicitly ✓ (26-NORM-01 respected at the VM seam).
- TacticalGhostVM: lazy GhostEngine w/ warning fallback; R4 LOW
  no-ghost-beats-fabricated-origin-ghost ✓; F-0026 containment confirmed
  at VM layer (engine constructed with defaults — the skew finding's
  third inference site, already registered).
- TacticalChronovisorVM: honestly gated — raises when USE_RAP_MODEL=False
  → scan_error_changed (CP0 RAP-cluster containment evidence ✓). P3:
  _scan_cancel checked only BEFORE the scan starts; a completed scan
  emits results even after cancel_scan() (B42: check on_leave usage).
  jump_to_prev monotonicity rests on start_tick sort (peak within moment
  bounds — sound).

## __init__.py (1) — docstring only.
