# D-B11 — pipeline-core (5 files, 1,621 LOC) — pass 1 read 2026-08-14

## processing/round_reconstructor.py (614)
- WR-76 anti-hallucination grounding: PlayerTickState → RoundTimeline with callouts,
  weapon/health/engagement events, EXPLICIT _DATA_LIMITATIONS injected into LLM
  context, R4 LOW team-wide-kill labeling fix (self-contradiction hallucination
  source). Batch reconstruct_rounds avoids N+1 ✓. format_for_llm structured block.
- _POSITION_SAMPLE_INTERVAL=256 fixed stride: DOCUMENTED 26-TICK-02 exemption
  (display-only granularity; correctness from persisted time_in_round) ✓ sound.
- Notes: direct Session(engine) read path (bypasses get_session contract — fine
  for reads; L3 census row); _MAX_TICKS_PER_ROUND 20k cap truncates SILENTLY
  (no-silent-caps: add warning; P3); weapon display map EN (LLM context, OK).

## processing/data_pipeline.py (407) — ML data prep, PUBLICATION-GRADE
- Anti-leakage: temporal split FIRST (P-DP-01), outlier fences from TRAIN only
  (Tukey 3.0×IQR named constant P-DP-03), scaler fit train-only then transform all;
  C-06/P-DP-02 player decontamination to EARLIEST split (temporal-correct, rows
  dropped not moved — documented rationale); H-09 deterministic ordering;
  P-DP-04 idempotency guard; 50k cap WITH warning ✓; F2-22 chunked UPDATE ≤999 vars;
  P-DP-05 scaler persisted w/ sklearn major.minor compat check; growing-window
  generator (paper §5.1.4). Notes: stratify_col built but unused on temporal path
  (P3); joblib scaler = local trusted artifact (L9 OK).

## processing/tick_enrichment.py (354) — METADATA_DIM 20-24 skew closure
- Round ctx + bomb state (R4 MED between-sample transition semantics, pointer scan)
  + alive counts + team economy + FOV enemies_visible (C-02 vectorized O(P²)/tick,
  no occlusion — documented, PlayerKnowledge owns occlusion downstream).
- **Fragility rows (dossier→L1)**: (1) alive counts = total-minus-own — spectator
  rows in tick data (team ≠ CT/T) would inflate enemies_alive; verify on real demo
  at T-DIAG. (2) enemies_visible writes via `array[df_index_labels] = counts` —
  SAFE today only because the merge chain resets to RangeIndex; any upstream filter
  without reset_index silently misattributes counts → recommend defensive
  reset_index(drop=True). fov_degrees=90 param duplicates constants.FOV_DEGREES (L10).

## processing/state_reconstructor.py (134) — parity discipline EXCEPTIONAL
- Ticks → RAP tensors via UNIFIED FeatureExtractor + P-SR-01 parity validation at
  the training boundary; C-01 context dicts populate features 20-24 (matches
  ghost_engine inference); R4-04-01 require_pov raises on POV/legacy skew (else
  loud warning); R4 MED map_name=None default (per-tick resolution; mirage-stamp
  war story); P-SR-02 explicit TensorConfig injection; 50%-overlap windowing w/
  documented tail-drop.

## processing/connect_map_context.py (112)
- Z-penalty distance (R4-11-01: Z_LEVEL_THRESHOLD/Z_PENALTY_FACTOR from
  spatial_data SSOT ✓); 2D fallback; R4-11-02 per-map max_distance; 6-dim
  objective-distance features (bombsites/spawns/mid) clipped [0,1], pad/truncate.
  No findings.
