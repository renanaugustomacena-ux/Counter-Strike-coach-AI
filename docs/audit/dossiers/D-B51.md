# D-B51 — shard tools (2 files, 1,994 LOC) — pass 1 read 2026-08-14

Zero new register findings; the verify_all_safe carry gains its FIRST
confirmed live-mutation member (census completes at B52–B55).

## d3_recover_shard_metadata.py (543) — exemplary recovery tool
- Dry-run DEFAULT (bare invocation writes nothing → verify_all_safe-safe
  ✓); §129 5-question doctrine embedded in the report; SHA-256 forward
  map + match-id integrity re-verification + map cross-validation;
  backup-before-write; idempotent skip; post-write verify; per-shard
  error isolation.
- 26-NORM-01 discipline: header-derived tick rate w/ [32,256] band;
  REFUSES to fabricate a rate (None, not default); honest sentinels
  ("unknown" teams; parser_version v2-d3-recovered vs
  v2-d3-recovered-default-rate); --rederive-v1 flow repairs the legacy
  2026-05-06 run's HARDCODED 64.0 rows, never silently blessing them.
- DP-06 SSOT resolution (dead "New Volume" mount war story).
- **Map-SSOT member #8**: KNOWN_MAPS set (9 maps incl. train).

## mine_shard_strategies.py (1,451)
- Rule-based labeler: ~150 labels across 5 families over shard
  matchtickstate + match_event_state; broken event round_number →
  tick-bisection mapping (documented); MR12+OT _same_half; K-means
  bomb-site centroids w/ methodology note; PRAGMA query_only on shard
  reads; batch SBERT embedding w/ hash fallback; md5 dedup keys incl.
  shard+round; per-1000 commits; scoped --fresh DELETE with the
  blanket-DELETE war story ("also destroyed mine_coaching_experience's
  records" — now WHERE strategy_label IS NOT NULL) ✓.
- 26-TICK: seconds × per-shard rate windows w/ TWO in-file war stories
  (baked *_TICKS constants; "an inline 64 survived the sweep here").
- P3 (W3 one-liners): TWO more bare tick literals survived that same
  sweep — HE nade-kill window `<= 32` and double_utility_lineup window
  `<= 128` — both unscaled by rd.tick_rate (windows halve on 128-tick
  shards; effect bounded to mined-label counts). P3:
  _compute_context_hash IGNORES its equipment_tier parameter (hashes
  the literal "unknown") — stored context_hash is tier-blind; embedding
  text keeps the tier so similarity retrieval is unaffected; check
  context_hash consumers at Pass 2 L8. P3: heuristic winner derivation
  (timeout→CT) is honest-but-approximate for label outcomes.
- **Map-SSOT member #9**: KNOWN_MAPS dict (8 maps, no train).
- **verify_all_safe carry — CONFIRMED LIVE MUTATION**: bare invocation
  (no args) runs the FULL pipeline — scans every shard, loads SBERT,
  and INSERTS thousands of CoachingExperience rows. Prefix "mine_" is
  not in the skip list → a verify_all_safe "validation" run would
  execute it. Register decision after B52–B55 completes the no-arg
  census (one finding listing all mutating members).
