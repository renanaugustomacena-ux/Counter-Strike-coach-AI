# D-B29 — services core (4 files, 1,460 LOC) — pass 1 read 2026-08-14

## services/coaching_service.py (1,068) — the coaching orchestrator
- P9-03 4-tier mode chain (COPER → Hybrid → Traditional+RAG → Traditional)
  with DOCUMENTED fallback transitions + mode-quality UI notification ✓;
  C-01 never-zero-coaching discipline (generic insights on every failure
  path); G-08 deviation-based fallback; R4 MED war story (SQLAlchemyError
  missing from the longitudinal catch tuple KILLED generate_new_insights
  mid-flight — "non-fatal" contract violated) ✓; Bug #8 tick_data dict
  guard; AC-23-01/03 lazy cached engine + DCL singleton.
- _run_with_timeout: 30s daemon-thread guard on COPER (SBERT/FAISS/Ollama
  stalls). BETTER than F-0013's design (no join-on-exit trap) — on timeout
  the worker leaks as a daemon and its work completes in background (L5
  note: orphaned DB writes may still land after fallback insights — dossier
  duplicate-insight edge). Hybrid/Traditional paths unwrapped (accepted).
- Carries CLOSED: TemporalBaselineDecay consumer FOUND (:137);
  ExplanationGenerator + generate_corrections consumers found;
  collect_feedback_from_match wired (experience feedback loop live);
  F1.2 JEPA block properly setting-gated + bounded (rows[-256:]) with
  CONSERVATIVE maturity mapping (MATURE→conviction, documented rationale).
- L7: all titles/messages EN; Ollama polish output → CoachingInsight.message.
  P3: _create_insight_obj title always "Improve your X" even when the
  narrative is the positive template; _find_best_match_pro full-table scan.

## services/profile_service.py (165)
- F5-22 credentials via keyring/env; P-01 bounded retry + exponential
  backoff (course-07 ✓); AC-28-01 both-failed skip; AC-28-02 URL-encoded
  nickname; P-02 response-shape validation; P-03 populated bio fields;
  10s timeouts; auto-commit session documented (F5-21) ✓ clean.

## services/visualization_service.py (136)
- R4 MED war story ×2: fig pre-exists try (UnboundLocalError in finally
  masked the real error); plt.close(fig) in finally ✓ figure hygiene.
- **L2 row**: uses pyplot GLOBAL-state API (plt.xticks/legend/savefig)
  without Agg pin here — pyplot is not thread-safe; concurrent worker
  calls interleave figure state. Recommend Figure-object API or a lock.

## services/analysis_service.py (91)
- **F-0020 exposure point FOUND**: AnalysisService() constructs
  EliteAnalytics → on fresh installs 7 missing-CSV warnings at init and
  all elite z-scores empty (census row closed). detect_feature_drift
  consumer FOUND (functional drift path ✓ closes B15 row).
- get_pro_comparison requires the pro's own PlayerMatchStats row (ingested
  pro demo), distinct from HLTV cards — naming note. Clean file.
