# D-B49 — portability + verify tools (6 files, 2,412 LOC) — pass 1 read 2026-08-14

Zero new register findings. One P2-candidate CARRY opened (verify_all_safe
filter gap — verdict depends on B52-B54 no-arg behavior census).

## verify_lock_hashes.py (248)
- Supply-chain gate (doctrine §53 / C-SC-03 / POL-DEPS-01): exact-pin +
  sha256-hash verification per lock line; permitted-directive allowlist;
  dataclass findings + JSON mode; exit 0/1/2. HONEST Phase-1 status
  in-docstring: hashed lockfiles don't exist yet (--allow-empty until
  `uv pip compile --generate-hashes` lands) — W4/W5 maturity item.

## verify_main_boot.py (176)
- **The anti-drift pattern**: screens AUTO-DISCOVERED from the
  filesystem (vs headless_validator's stale hardcoded lists — B48 P3);
  MainWindow interface contract; NAV_ITEMS 4-tuple + shortcut checks;
  per-theme QSS render validation (selector regex + length) covering
  the B35 safe_substitute blind spot ✓. W4 note: port this discovery
  style into headless_validator's Phase 7/24.

## run_console_boot.py (165)
- REAL-boot validator: console.boot() + 5s stabilize + status-shape
  validation against the ACTUAL SystemState/ServiceStatus enums
  ("derive valid values — never hardcode" ✓); graceful shutdown in
  finally. T-DIAG-class (spawns daemons); correctly excluded from
  verify_all_safe's runnable set.

## verify_all_safe.py (135)
- Dynamic runner: executes every "safe" tools/*.py as a subprocess
  (120s timeout, output cap).
- **CARRY (P2 candidate)**: the safety filter is convention-based
  (skips deprecated + fix_/reset_/migrate_/patch_/cleanup_/force_
  prefixes + one hardcoded interactive name) — it MISSES wipe_*,
  repair_*, purge_*, populate_*, merge_*, seed_*, rescrape_*, sync_*,
  ingest_pro_demos, rebuild_monolith. Whether any of those MUTATES on
  bare no-arg invocation decides if this "validation" tool can wipe or
  rewrite DB state mid-run. Verdict at B52-B54 (no-arg behavior
  census per tool); register there if any live mutation exists.

## dev_health.py (115)
- Orchestrates headless (critical) + dead_code --strict (critical
  since the 2026-07-17 zero-orphan baseline — any regression is NEW
  dead code) + Feature_Audit + portability (--full). --quick tier.
  Clean; the strict dead-code stance is a W3/W4 asset.

## portability_test.py (1,573)
- 10-test in-house portability linter: hardcoded paths (w/ platform-
  guard back-walk + regex-string + PORTABILITY_OK suppressions),
  f-string path construction (file-extension + escape-stripping
  false-positive logic), import-side-effect AST scan (~100-entry
  substring allowlist — crude but warn-level), config portability,
  required files, critical imports, env-var fallback + secret scan,
  resource paths, cross-platform (CRLF = INFO), requirements checks.
- P3s: docstring-parity comment detection is naive (works for the
  common cases); SAFE_IMPORT_PATTERNS substring matching allowlists
  broadly ("print(" clears any line); --save-report writes
  portability_report.json to the REPO ROOT (stray-artifact check at
  W5/B76 gitignore read). Explains 4 of the S5 sys.path hits ✓.
