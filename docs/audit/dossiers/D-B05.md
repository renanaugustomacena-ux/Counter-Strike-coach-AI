# D-B05 — storage-data (5 files, 1,600 LOC) — pass 1 read 2026-08-14

## backend/storage/match_data_manager.py (1150)
- **Tier-3 shard architecture**: per-match SQLite `match_{id}.db` under
  config.MATCH_DATA_PATH; match_id = SHA-256(demo_name) % (2^63-1) — **L1 CONTRACT**
  with run_ingestion.py:1232 (mirror helper demo_name_to_match_id prevents drift;
  verified 255/255 demos 2026-04-26).
- **POV-RAP-FIX war story**: ORM was bound to `match_tick_state` while the writer
  wrote `matchtickstate` → every RAP batch silently dropped. Fixed: __tablename__
  realigned, id→rowid mapping, v2→v3 nullable-column migration; legacy empty twin
  table tolerated as inert (R2-03 unexpected-table check).
- Per-shard schema versioning (v3) with _MATCH_DB_MIGRATIONS; DB-01 defence-in-depth:
  identifier/col-type/DEFAULT-literal allowlists before any DDL f-string (the
  sanctioned parameterization-exempt sites for the S4 census).
- WR-14 device-id disconnect detection; AUDIT §8.8 REVERSAL: dangling symlink →
  MatchDataUnavailableError with recovery instructions (report, never recreate) —
  the subject of F-0003's tests. M-18 true-LRU engine cache (50). WAL pragmas = monolith.
- Memory window: MEMORY_WINDOW_SECONDS=5.0 converted per-demo via metadata tick_rate
  (26-TICK fix). **F-0012 (P1)**: :488/:830/:837 use undefined `logger` (module has
  `_logger` only) — no-metadata/invalid-rate fallback = NameError crash in
  get_player_tick_window/get_all_players_tick_window; branches untested. Also :823
  bare `rate = 64` (should be DEFAULT_TICK_RATE) + inline 32..256 dup of
  is_valid_tick_rate (L10).
- warn_if_shards_in_legacy_location: report-only (war story 2026-07-26: auto-migration
  + $HOME default filled root disk mid-pytest — now explicit migrate_match_data()).
- get_active_utilities: entity_id -1 sentinel skip w/ warning ✓. reset_match_data_manager
  for PRO_DEMO_PATH changes (who calls it on settings change? → L6).

## backend/storage/state_manager.py (275)
- DAO over CoachState singleton + ServiceNotification queue. RESOLVES B02 question:
  Scanner's `hltv_status == "Scanning"` gate — hltv_status IS the Hunter daemon
  status field (naming legacy only). R2-13 typed DaemonName + unknown-daemon raise
  (silent no-op war story); GLOBAL status validated against CoachStatus values.
- Process-local lock + cross-process safety via id=1 CHECK (P2-01) — second creator
  fails loudly (L3 row). SM-01 expunge-on-return; SM-02 telemetry escalation (5×);
  SM-03 notification cap 500 + P2-06 age prune (30d); DB-05 sanitized UI/LLM error
  details (07§no-internal-leak ✓). P0-04 lazy singleton (import-time engine war story).

## backend/storage/stat_aggregator.py (126)
- HLTV spider → ProPlayer/ProPlayerStatCard/ProTeam upserts (select-then-add; ok:
  HLTV manager sessions serialized per scrape). V-2 ratio-vs-percentage boundary
  normalization (>1.0 → /100) for kast/headshot_pct; S-07 blob size-guard truncates
  to core with warning (no-silent-cap ✓). Redundant explicit commit inside
  auto-committing get_session (F6-03 doc-rot family, harmless).

## backend/storage/maintenance.py (49)
- prune_old_metadata(30d): tick deletion by demo_name chunked at 500 (under
  SQLITE_MAX_VARIABLE_NUMBER 999 ✓), aggregates preserved, single transaction.
  Caller census → Phase C (who schedules it?).

## datasets/__init__.py (0) — empty placeholder (like models/) → L10.
