# D-B42 — tactical_viewer_screen.py solo (1,702 LOC) — pass 1 read 2026-08-14

Zero new register findings. F-0002 semantics settled (register annotated:
no upgrade). Two carries closed. The densest war-story file in qt_app.

## F-0002 verdict (the :416 bare 64)
- Two sites: `self._demo_tick_rate = 64` init placeholder +
  `_resolve_demo_tick_rate` fallback (loud warning). Real rate resolved
  per demo via run_ingestion._parse_demo_header_meta, sanity-banded
  32..256; 26-TICK war story in-file ("old default-64 path made
  128-tick demos play at half speed"). Time math (dual-progress offset,
  playback) uses the RESOLVED rate. Fix stays: import DEFAULT_TICK_RATE
  at both sites to satisfy the SSOT guard test.

## Carries closed
- **Trails (B39)**: _on_seek → clear_trails() ("a jump invalidates
  continuous movement history"); _switch_map → set_map (clears);
  ghost-mode activation clears. Halftime color staleness self-expires
  in seconds (40-point deque) — benign, CLOSED.
- **Chronovisor cancel (B36)**: cancel_scan() has NO caller — scans run
  to completion and deliver to the (possibly hidden) screen; harmless.
  The B36 P3 stands as dead-ish API (L10 tail).

## War-story museum (all verified in-file)
- Cancel-doesn't-discard: demoparser2 has no cooperative cancel; cancel
  now only hides the dialog and the eventual result IS surfaced —
  with the observed log timestamps (2026-04-24T11:54) of the old
  "loading finished, nothing happened" bug in the docstring.
- _DemoLoaderLogBridge: INFO-log → Qt-signal tap gives the progress
  dialog live phase text (user cancelled 4-min silent parses); R4 MED
  double-detach ordering bug documented and fixed; RuntimeError-guarded
  emit; handler emits from worker thread → queued to dialog ✓.
- R4 MED CM wiring: scan_match was NEVER called → transport buttons
  took clicks and did nothing; now scan starts post-load w/ DB match_id
  resolution, graceful skip for un-ingested demos, transport
  enable/disable + tooltips + timeline glyphs + footer count from real
  results. USE_RAP_MODEL=False → error path → stays disabled (honest).
- FE-03 (L9 showcase): file-picker path hardening — realpath, .dem
  extension check, DS-12 MIN_DEMO_SIZE enforcement at UI with the
  symlink→/etc/shadow rationale.
- Tofu-proofing: _TransportIconButton painted glyphs (U+23EE/23ED tofu
  offscreen); Play/Pause as TEXT (U+23F8 no fallback); ×-sign speeds.
- Others: blank-buttons QSS padding rule, negative-geometry overlay
  guard, modal-vs-inline error split (missed dark-bg errors),
  raw-shape logging on parse return (blank-viewer diagnosability),
  cache-format drift surfaced via rejected-keys INFO.

## Structure notes
- WebEngine marquee: lazy heavy imports; toggle+dist BOTH required else
  documented fallback (never blank); runtime flip needs re-enter
  (documented); bridge wired bidirectionally; web ghosts normalized via
  SpatialEngine for the active radar ✓ consistent space; nades
  Qt-native only (documented P4.0 gap).
- Ghost Mode (frame 14): payload seam public + defensive (Locked
  Decision 8); FIELD-GAP notes on divergence metrics, ghost library
  combo, sync offset, causal source; full i18n on ghost-owned strings;
  retranslate recomposes via mode re-entry trick ✓.
- FIELD-GAP score/bomb: InterpolatedFrame carries neither today —
  compose-on-superset with em-dashes (map score box hidden) ✓ honest.
- P3s: _switch_map calls QApplication.processEvents() (reentrancy
  window on rapid combo churn); open_moment uses win._show_toast
  private access; _update_tick_ui re-polishes the play button every
  100ms even when state unchanged; chronovisor footer template
  hardcodes "3 scales" claim (verify vs scanner config at Pass 2 L8).
- L7 ledger (the S3 ×9 verdict — genuine EN misses, all in the
  load/error flow): "Open Demo File" + filter string, "Loading Demo",
  progress texts, path-validation errors ×4, "Demo Load Failed" modal
  pair, "Loading map…", "Error:" prefix, CM/play tooltips. Everything
  else on the screen flows through tactical.* keys — same
  generalization target as match_detail's md_* pattern.
