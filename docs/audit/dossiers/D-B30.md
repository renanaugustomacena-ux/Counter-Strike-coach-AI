# D-B30 — coaching-dialogue (1 file, 1,974 LOC, solo) — pass 1 read 2026-08-14

## services/coaching_dialogue.py — the coaching chat brain
- **Anti-fabrication doctrine at its most mature**: WR-78 data-honesty prompt
  rules (no invented tactical narratives, inferences marked, no copy-paste
  round stories, block-scoped claims); tutor-mode doctrine (pro data NEVER
  narrated as "your"); CHAT-02 deterministic 2nd→3rd person transforms
  (ordered longest-first) applied at ALL THREE injection sites; WR-79
  username rule; SYSTEM_PROMPT fabrication bans w/ "say you don't have
  data" defaults.
- **Security posture (L9 showcase #2)**: BE-03 prompt-injection defenses w/
  DOCUMENTED attack surfaces (control-char strip + brace escape killing the
  str.format exploit; DB-sourced tool results sanitized + length-capped);
  DP-03 zero-trust tool phase — every LLM-supplied argument validated
  against the _known_demos whitelist, fragments char-filtered + capped,
  rounds bounded ints, errors returned as short strings never exception
  detail; _MAX_TOOL_ROUNDS budget + per-call tool cap.
- Concurrency: R4 LOW snapshot-then-release lock discipline (180s LLM call
  runs UNLOCKED; get_history/clear/cancel stay responsive); F5-06
  history-mutates-only-on-success (cleared session drops the exchange);
  F2 streaming w/ stall budget + DR-14 whole-message re-render + F2.3
  lock-free cancellation; CHAT-01 warmup daemon + hardware-realistic
  timeout budgets w/ env overrides.
- DP-02/DP-03 grounding: match inventory in system prompt ("cannot choose a
  match it has never been shown"); disambiguation blocks (predecessor
  silently narrated an arbitrary FaZe–Spirit demo); R4 MED round-regex
  word-boundary fix; R4 MED both-addends None-guard (TypeError discarded
  the whole analytical block).
- **F-0016 consumers #5+#6**: _retrieve_round_drill_down labels rounds
  WON/LOST via any(p.round_won); _format_player_analytics filters
  round_won.is_(True) for "BEST WINNING ROUNDS" — if the winner-dtype bug
  is real, chat reports every round LOST and best-rounds comes back EMPTY.
  Radius updated in FINDINGS at fix time.
- Census closures: round_reconstructor.format_for_llm consumer FOUND
  (WR-76, B11 row); F-0028's dead ml_preds param also threads through
  _get_ml_analysis_for_players (radius note).
- Notes: _get_ml_analysis_for_players constructs HybridCoachingEngine PER
  CALL on the player_query path (model+baseline reload; session path is
  cached — perf P3) and reaches into engine privates (friend coupling,
  L10); `except (TimeoutError, Exception)` redundant tuple (P3); EN
  throughout incl. offline fallbacks (L7 — LLM may mirror user language,
  system prompt is EN).

Zero new register findings; F-0016 blast radius materially widened.
