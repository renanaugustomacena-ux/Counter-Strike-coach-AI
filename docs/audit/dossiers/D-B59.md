# D-B59 — ptools: goliath + D2A aggregator (2 files, 2,058 LOC) — pass 1 read 2026-08-14

Zero new register findings. One F-0014 evidence member added; two L8
cross-refs surfaced for Pass 2.

## Goliath_Hospital.py (1,119) — census SAFE (heavy read-only)
- 11-department BaseValidator diagnostic; `-d` single-department mode;
  per-department crash isolation (CRITICAL check, run continues) ✓.
- **F-0014 evidence #3**: Endocrinology's entry-point list includes
  `main.py` — `SOURCE_ROOT/main.py` does not exist, so every run emits
  `entry:main.py FAIL`. Third tool carrying the dead reference (with
  dead_code_detector ENTRY_POINTS; the manifest is clean per B58).
- In-file R4 MED war story (Oncology): os.sep vs "/" mismatch meant the
  long-function exclusion list NEVER matched on Windows — fixed with
  rel_posix normalization ✓ documented.
- `_run_with_timeout` daemon-thread guard (10-20s) wraps every heavy
  probe: DB SELECT 1, CoachingService init, FeatureExtractor+DB
  roundtrip (skip-if-empty), TemporalBaselineDecay. NAIVE
  datetime.now() feeds the 45-day weight probe (S6 carry).
- ER: AST syntax sweep + forbidden patterns (Desktop paths, password=,
  api_key= — self-skip) + bare-import namespace check. Pathology: mock-
  indicator scan is noisy by design but non-blocking (always-True
  detail line); DB probe LIKEs %test%/%mock%/%MCIV%. Tool Clinic
  validates BOTH tools dirs (syntax/main-guard/docstring). Endocrin.:
  alembic = PROJECT_ROOT tree only (coherent w/ alembic.ini binding);
  JSON configs incl. map_config/map_tensors.
- L10: overlaps root goliath.py CLI + console `hospital` command
  (dispatch duplication already mapped at B47).

## aggregate_match_stats_sql.py (939) — D2A, exemplary gating
- DRY-RUN DEFAULT + --commit; --force / --really-force (5s countdown)
  ladder; d_track_running lock only in write mode w/ finally-release;
  --no-lock escape; checkpoint + JSON report; --reconcile read-only
  diff vs 'complete' rows (5% field tol / 10% row halt → exit 2;
  nosec B608 justified — module constants).
- EIGHT in-file R4 war stories: MR12 band [13,36] (old CSGO set
  mis-flagged 81% of demos, 2026-05-05); cumulative MAX(_total) vs
  per-round SUM undercount (35 vs 17 on m1-mirage — totals from
  cumulative, damage only per-round); rows_skipped_noise hardcoded-0
  fix; TERRORIST/CT full-name roster mapping (old "T" compare never
  matched); **R4 HIGH 26-TICK: trade window from persisted per-demo
  tick_rate (GAP-01), loud WARN fallback — hardcoded 64 had halved
  real-time windows on 128-tick demos**; RAW rating components (prior
  normalized draft corrupted every Z-score vs pro_baseline);
  impact_rounds ≠ rating_impact conflation fix; --force no-op fix
  (write path now consults the flag).
- Class taxonomy honest: 25 Class-A real; 11 Class-B explicit 0.0 w/
  per-field reasons (flash_detonate empty victim_name documented);
  Class-C deferred (D2C linker). data_quality vocabulary coherent
  with rebuild_monolith's D2B ladder ✓.
- **L8 cross-ref #1 — split SSOT**: stamps MD5-mod-100 70/15/15 by
  demo stem at insert; data_pipeline (P-DP-02) later re-derives
  TEMPORAL splits w/ player decontamination and bulk-overwrites
  dataset_split (F2-22 chunked UPDATE). Insert-time split is
  provisional; pipeline owns the column. CONTRACTS.md row.
- **L8 cross-ref #2 — KAST provenance**: full_sql rows carry
  estimate_kast_from_stats (the ~30%-trade closed form repair_kast
  documented as inflated vs event-accurate); ladder is estimator →
  event-accurate tied to data_quality. CONTRACTS.md row.
- P3: --match-id help says "stem hashes to N" but filter is filename
  equality (match_N.db); mixed naive/aware match_date fallbacks (S6).
- is_pro path-based override w/ documented rationale (metadata flag
  unreliable — known-pro Vitality sample read 0) ✓.
