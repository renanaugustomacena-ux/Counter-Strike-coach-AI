# D-B10 — ingestion-orchestration (3 files, 2,086 LOC) — pass 1 read 2026-08-14

## run_ingestion.py (1,649) — the ingestion heart
- **Dedup**: triple-check (IngestionTask path excl. error/processing → PlayerMatchStats
  stem → shard file existence) ✓; match_id derivation = DA-03-01 SHA-256 mod 2^63-1
  (L1 contract w/ match_data_manager mirror ✓, collision math documented).
- **Queue orchestration**: process_new_demos (refresh_settings per scan ✓ C-01-aware;
  priority via ResourceManager) → process_queued_tasks (F6-13 detached-instance
  discipline: re-attach per session ✓); progress % via CoachState.
- **_parse_demo_header_meta (:1092-1142)** — the F-0006 site in full context: GAP-01
  doctrine, [32,256] validation, safe defaults; `except Exception` :1108 (panic
  bypass); does NOT route through validate_demo_file (demo_loader is the template).
- **_interpolate_position**: alive-segment-scoped interpolation (no bleed across
  deaths), CIRCULAR yaw/pitch interpolation (sin/cos), (0,0,0)-poison awareness w/
  R4-14-01 telemetry, NaN→DB-compat fills. EXCELLENT numerical hygiene.
- **_EventExtractor**: F6-14-v2 MultiIndex state lookups (iterrows-dict perf war
  story), ±5-tick fallback, steamid→name map, GAP-02 throw origins from tick state,
  9 event types in fixed order; per-type _safe_parse isolation (Exception-catch —
  F-0006 family).
- **_save_sequential_data**: parse→interpolate→enrich(per-demo tick_rate GAP-01 ✓)
  →vectorized dual-write (to_sql chunks 2k/10k HP; 736s→15-20s war story).
  NO transaction across chunks — DELIBERATELY compensated: P4-A match_complete flag
  gates Teacher training; last_tick_processed enables resume. Writer targets
  `matchtickstate` (POV-RAP alignment consistent ✓).
- ML pipeline: profile-ready gate (MIN_DEMOS_FOR_COACHING), skill vector + curriculum
  level, RAP inference (graceful checkpoint-incompat skip), insight delete+rewrite
  idempotency, cross-DB (HLTV) reads EXPLICITLY kept off the ingestion hot path
  (documented boundary decision) ✓.
- Notes: get_session("knowledge") label is a no-op (deprecated param) → L10;
  blind-state F6.1/DQ-02 derivation consistent across both frames.

## hltv_sync_service.py (269)
- H4 cadence policy (7d full / 24h incremental / 1h rest / 6h dormant) documented;
  pre-flight chain docker→solver→connectivity; stop-signal file protocol w/
  1s-granular dormant sleep; DA-06-01 stale-PID recovery.
- **F-0014 (P1)**: start_detached targets DELETED main.py (:237) — child dies
  instantly to DEVNULL, PID file records a corpse; dormant path (:103-104) RETURNS
  after 6h sleep while notification promises retry — daemon exits.

## run_worker.py (167)
- Standalone queue worker: R3-M25 atomic claim-in-transaction ✓, IM-02 stale
  recovery, cold-start pro batching gate, archive-aware file lookup, failure
  bookkeeping.
- **F-0015 (P1)**: _STALE_THRESHOLD 5min contradicts P4-B doctrine (30min in
  session_engine — the 5-min value IS the documented duplicate-processing bug);
  skip-after-claim leaks pro tasks into eternal processing/requeue churn.
- Shared stop file with hltv_sync ("hltv_sync.stop") — stopping HLTV also stops
  the worker (documented but surprising coupling) → P3/CP0 note.
- Overlap with session_engine Digester (same queue, different recovery rules) —
  runtime-topology question for Phase C caller census.
