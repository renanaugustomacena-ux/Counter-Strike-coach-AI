# D-B24 — coach-manager + inference (6 files, 1,731 LOC) — pass 1 read 2026-08-14

## nn/coach_manager.py (1,362) — Teacher cycle manager
- **Production-scale war stories**: B1-XL 429M-row OOM (earlyoom exit 143) →
  id-space rejection sampling above 2M cap w/ determinism-preserving draw
  order + per-run scale cache (25-min COUNT amortized); R4 CRIT contiguous
  windows BOTH fetchers (JEPA anchor→window expansion; RAP per-player streams
  — "position deltas computed BETWEEN DIFFERENT PLAYERS"); P4-A None-vs-empty
  load-bearing distinction (259 readmitted demos war story; per-shard failure
  = incomplete, NOT missing signal); temporal split GROUPS BY DEMO (observed
  1-in-TRAIN/9-in-VAL split-through-a-demo); eligibility gates w/ "missing
  signal must not empty the pool" doctrine; MIN_COMPLETE_MAP_ROUNDS=13 w/
  observed fragment census; WR-76 suffix normalization; 26-SCHEMA-02 owner
  decision documented; R4 MED dead legacy RAP path DELETED w/ its
  LEAK-01-class defect named ✓✓.
- **Three-training-paths question CLOSED**: all alive by design —
  orchestrator (phases 1 JEPA + 4 RAP), legacy train_nn (phases 2-3
  aggregate-space baseline + user fine-tune), jepa_train CLI (standalone).
- **L1 row — two 25-dim spaces**: tick-level TRAINING_FEATURES vs
  MATCH_AGGREGATE_FEATURES (both length-checked vs METADATA_DIM at import).
  GAP-07 sidecars stamp tick-level FEATURE_NAMES even on aggregate-space
  "latest" checkpoints (validation harmless, metadata misleading — P3).
- Orchestrator manager contract VERIFIED: _fetch_jepa_windows /
  _fetch_rap_windows / _update_state all present ✓. StopIteration = cancel
  signal (context.check_state; Phase K session_engine contract).
- Notes: _fetch_jepa_windows N+1 anchor expansion (~4500 indexed LIMIT-11
  queries/epoch — perf row); _get_user_baseline_vector zero in-file callers
  (L10); get_interactive_overlay_data window-level advantage stamped per-tick
  (intentional); RAP_POSITION_SCALE used on overlay side ✓ F3-05.

## nn/inference/ghost_engine.py (239)
- R4 LOW None-vs-(0,0)-sentinel fix ✓; R4-04-01 channel-semantics gate
  (USE_POV_TENSORS, default legacy — matches trained channels) ✓; P-SR-01
  parity validation at the inference boundary ✓; F3-05/F3-06 canonical scale
  + key resolution ✓; SimpleNamespace dict-promotion honoring the tick-list
  contract; USE_RAP_MODEL gate; lobotomy telemetry.
- **F-0026 (P2, resolves B13 carry)**: inference tensors at DEFAULT
  resolution (get_tensor_factory singleton → 128 map / 224 view) vs training
  at TrainingTensorConfig 64×64. AdaptiveAvgPool2d hides the dim mismatch
  while ABSOLUTE-grid-unit spatial statistics shift (VELOCITY_FALLOFF_RADIUS
  20 cells = 31% of 64² vs 9% of 224²; blur sigmas ditto — B13 L8 row's
  consumers found). BOTH RAP inference sites affected: ghost predict_tick
  AND coach_manager overlay (RAPStateReconstructor() with tensor_config=None
  → get_tensor_factory(); the P-SR-02 injection exists precisely for this
  and neither call site passes it). Gated behind USE_RAP_MODEL.
- L1 sweep row: _build_knowledge_from_game_state uses PlayerKnowledgeBuilder()
  default 64-tick on the LIVE path (C1.2 threading covers only the
  orchestrator path; live MM is 64-tick today — note, not register).

## nn/layers/superposition.py (121)
- RAP-AUDIT-06 FiLM upgrade (multiplicative-only → γ·out+β, zero-init β =
  behavior-preserving) ✓; P1-09 Kaiming; NN-24 live-vs-detached gate tensors
  (grad retained for sparsity loss / detached for observability — one-batch
  retention, bounded, module-33 OK); periodic stats + observatory API ✓ clean.

## __init__.py ×3 (advanced/inference/layers) — 13 LOC total, trivial.
