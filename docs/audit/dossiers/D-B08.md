# D-B08 — external-sources (10 files, 2,065 LOC) — pass 1 read 2026-08-14

## hltv/stat_fetcher.py (969)
- HLTV scraper via FlareSolverr. Citizenship discipline: D-23 legal notice, robots.txt
  preflight w/ DP-04 Cloudflare-HTML detection, 2–7s random delays, HLTV_SCRAPING_ENABLED
  opt-out, robots-compliant discovery (ranking page, not disallowed stats query).
- Adaptive backoff: _consecutive_failures scales delay (+2s/failure cap +10, decay on
  success) — lightweight retry-budget (07§ ✓). R4 HIGH thousands-separator war story
  ("39,606"→39.606 poisoning ratios) → _THOUSANDS_RE. H2 min-viable-fields gate +
  rating>0 sanity + stale-response heuristic. CSS-fallback chains with LOUD activation
  warnings (layout-drift detection — no silent degradation ✓).
- kast/hs/opening pct→ratio normalization at save (consistent w/ stat_aggregator V-2).
- Notes: _consecutive_failures declared as class attr, mutated per-instance (works,
  style nit); sub-page URLs date-windowed 2021-06-01→today (R4 MED frozen-date story).

## hltv/flaresolverr_client.py (162)
- REST client: exponential backoff 5/15/45s, typed exception branches, last_error
  surface, session lifecycle w/ leak-aware destroy (R4). Italian log string :71 →
  P3 log-language batch.

## hltv/docker_manager.py (138)
- FlareSolverr container lifecycle: docker start → compose up fallback; NO shell=True
  (L9 ✓), timeouts on every subprocess, DS-05 path validation before cwd, health
  polling (45s budget). Italian log :136 → P3 batch.

## hltv_scraper.py (60)
- Thin sync-cycle entry: DS-06 preflight → discover → fetch_and_save ×limit. Broad
  except at boundary (daemon-facing, logged) — acceptable.

## steam_api.py (136)
- _request_with_retry: DS-03 TOTAL-timeout budget (monotonic deadline caps compounded
  backoff), per-request timeout capped to remaining budget, no-retry on HTTPError,
  loud raises — module-07-grade. R3-M04 Steam64 17-digit validation; vanity-URL
  auto-resolve; R4 dead-403-branch cleanup story (diagnostics moved to except).

## steam_demo_finder.py (257)
- Steam replay discovery: registry (R4 breadcrumb on failure) → dynamic drive-scan
  paths (ctypes GetLogicalDrives, platform-guarded) → LINUX_PATHS (deploy-aware ✓).
  tz-aware mtimes. F6-11 DOCUMENTED duplication w/ ingestion/steam_locator (path-
  precedence consolidation deferred) → L10 row.

## faceit_integration.py (305)
- Full FACEIT client: 6s spacing (10 req/min), 429 handling w/ R4 MED RFC-7231
  Retry-After-date fix + 300s cap + 3-retry limit, R3-09 match_id traversal sanitize,
  DS-08 HTTPS-only SSRF guard, size-capped streaming download reusing MAX_DEMO_SIZE.
- P3: `raise FACEITAPIError(...)` without `from e` (chain loss — 07§raise-from).

## faceit_api.py (38)
- Thin Elo/level fetcher. L10: second FACEIT module beside faceit_integration
  (raw function vs client class) — consolidate or document split.

## __init__.py ×2 (0) — empty.
