# D-B55 — eval tools (7 files, 2,361 LOC) — pass 1 read 2026-08-14

Zero new register findings. F-0039 census: +1 mutating member (total 12);
three heavy-but-non-DB-mutating; three safe.

## eval_harness.py (741) — GAP-04, the pre/post-retrain evidence tool
- Append-only timestamped reports/ JSON ("no overwrite, no silent
  mutation"); git-SHA + harness-version stamped; per-section graceful
  degradation (NOT_AVAILABLE/ERROR/SKIPPED never abort the report).
- War stories: B6.2 dry-run fast path ×2 (SBERT + embedding pass blew
  the 30s test budget — ONE uniform heavy-section skip rule); GAP-09
  column-scoped SELECT (survives pre-migration DBs); AC-32-01 dual-
  format embedding deserialization REUSED from ExperienceBank.
- Sections: drift monitor on demo ticks; self-retrieval recall@k + kNN
  purity; Brier + ECE w/ per-bin detail (Naeini 2015 cite); silhouette
  w/ hand-rolled sklearn fallback; strategy-label coverage (200-label
  target flag); MoE expert-utilization probe w/ balance thresholds.
  This is what run_full_training_cycle shells out to (B47 ✓ coherent).
- Census: bare run writes reports/*.json + loads SBERT — heavy but no
  DB mutation (quasi-safe).

## coach_answer_eval.py (329) — GAP-15/TASKS#48
- Fixtures built from LIVE DB ground truth (read-only builder);
  groundedness = fraction of expected facts in the answer. Two
  exceptional details: Unicode-dash normalization ("LLMs typeset ASCII
  hyphens as U+2011") and ROSTER-CLUSTER grounding (≥3 same-demo
  players proves real-match narration despite paraphrase). --dry-run;
  Ollama-absent → exit 2. Census: quasi-safe (LLM calls + report).

## mine_coaching_experience.py (235)
- **F-0039 member #12**: LIVE default (--dry-run available) — mines
  roundstats → ExperienceBank.add_experience (context-hash idempotent).
  The tool whose records mine_shard_strategies' scoped-DELETE war story
  protects ✓ coherent. **Map-SSOT member #12** (8-map set). P3: local
  _classify_round_phase duplicates round_utils.infer_round_phase (L10).

## validate_coaching_pipeline.py (252)
- Census SAFE (requires demo arg; bare = usage + exit 1). With args:
  the 6-step product proof (parse → profile ensure → stats upsert →
  deviations → corrections → narrative) with PASS/FAIL word-count gate.
  Writes real rows for the given demo — documented purpose.

## test_tactical_pipeline.py (443)
- 9-stage headless E2E of the tactical viewer pipeline: catches ALL
  errors (no first-crash), field-access matrices for player/nade
  attributes, mid-frame interpolation probe, coordinate-range checks,
  Qt-free render simulation. Census: bare run auto-picks a demo and
  parses (heavy CPU + .mcn cache write; no DB mutation) — quasi-safe.
  T-DIAG-adjacent quality.

## test_rap_lite.py (89) — census SAFE
- Forward-pass contract test: RAPMemoryLite identity, 7-key output
  shapes, finite-value Ghost/Chronovisor compatibility, checkpoint-name
  registration, JEPA non-regression ✓.

## drift_detector.py (272) — census SAFE (verify default)
- §63/C-DRIFT-01 skeleton: SHA-256 manifest of Programma_CS2_RENAN
  minus runtime artifacts; --baseline writes SECURITY/
  drift_baseline.json (explicit flag only); verify → exit 1 on drift w/
  IR-02/IR-05 pointers; honest Phase-1/Phase-3 roadmap.
