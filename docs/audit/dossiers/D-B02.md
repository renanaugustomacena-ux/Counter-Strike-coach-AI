# D-B02 — core-runtime (7 files, ~2,050 LOC) — pass 1 read 2026-08-14

## core/session_engine.py (612)
- **Purpose**: the daemon process — Scanner (FS→queue), Digester (queue→stats),
  Teacher (ML retrain), Pulse (heartbeat) + watchdog (30s, restarts dead daemons) +
  stdin parent-death monitor (EOF/STOP → shutdown; answers B01 stdin=PIPE question).
- **Contracts**: retrain trigger = cold-start ≥10 pro samples, then +10% growth
  (R4 HIGH fix); count persisted only AFTER training (R4 MED: creates CoachState row);
  Teacher shares _TRAINING_LOCK with ml_controller (private cross-module import —
  inappropriate-intimacy smell, pragmatic); backup failure → warn-once + continue;
  zombie tasks (processing >30min default, SE-04 validated setting) reset to queued;
  IM-03 wait-then-clear wakeup (2s safety timeout); OBS-07 correlation id per cycle.
- Notes (dossier-only): docstring "Started by main.py" stale (Kivy-era) :65; Pulse uses
  time.sleep(5) not _shutdown_event.wait(5) (shutdown latency, inconsistent w/ Teacher);
  Scanner gates demo scanning on state.hltv_status == "Scanning" — semantic coupling
  demo-scan↔HLTV naming, verify at B05 state_manager; sys.path bootstrap F6-06
  (documented debt, S5); broad except-Exception loops = daemon resilience (acceptable).
- **X-refs**: run_ingestion.process_new_demos/process_queued_tasks (B10); watcher,
  ResourceManager (B09); state_manager (B05); backup_manager (B06); coach_manager (B24);
  belief calibration G-07 (B17); init_knowledge_base (B27); pro_baseline meta-shift (B15).

## core/demo_frame.py (168)
- Typed frame model: PlayerState (DF-01 NaN/Inf sanitize incl. yaw — R4: Inf yaw hung
  interpolation), GhostState, NadeState (frozen; H-05 estimated-duration flag),
  GameEvent/EventType, BombState, KillEvent, DemoFrame. STRING Team enum (R1-02 twin).
- Notes: PlayerState not frozen but uses object.__setattr__ (harmless oddity);
  NadeState frozen with mutable list field trajectory → unhashable in practice (never
  hash it; L5 note). L1 shape anchor for playback + tactical widgets.

## core/playback_engine.py (263)
- Playback clock + interpolation. load_frames REQUIRES tick_rate kwarg (26-TICK R4 —
  both prod callers previously omitted and got silent 2× speed error on 128-tick).
  Binary-search seek via _ticks_cache (guarded by empty-_frames early return).
  CORE-10+R4: non-finite yaw guard before wrap-around loop (Inf hang fix).
- **Kivy residue**: docstring + play() imports kivy.clock (warns "subclass must
  override"); core→UI-framework layering smell; Qt subclass = qt_playback_engine (B35).
  W3 candidate: lift Clock out of core.
- Notes: end-of-frames → pause, play() restarts from 0; speed clamp [0.25, 8].

## core/spatial_engine.py (108)
- World↔normalized↔pixel transforms; RADAR_REFERENCE_SIZE=1024 MUST equal
  MapMetadata.world_to_radar default (L1 contract row). pixel_to_world inverse math
  hand-derived — L1: verify exact inverse vs spatial_data (B03). Y-flip delegated to
  rendering layer (comment cites Kivy-era TacticalMap; Qt real site = map_widget B39).
- F6-26: Z ignored (multi-level maps) — documented limitation. Anti-fabrication:
  unknown map → (0.5,0.5) + once-per-map warning.
- P3 (batch): class "docstring" placed AFTER first statement → dead string expr,
  SpatialEngine.__doc__ is None.

## core/map_manager.py (114)
- Thin wrapper over AssetAuthority + Kivy Loader (guarded; MM-01/MM-02 degrade paths,
  R4 MED: Loader=None guard on valid-asset branch too). Qt-era caller census → L10
  (load_map_async likely dead under Qt).

## core/asset_manager.py (273)
- AssetAuthority singleton: SmartAsset (path + lazy Kivy texture, Optional by contract
  in headless/Qt), theme suffix chain (dark/light → regular → checkered fallback),
  cache setdefault under lock (F6-32/R4 concurrency-safe), magenta/black checkered
  fallback (anti-fabrication — replaced old "Mirage fallback").
- **RISK to verify at B03**: _normalize_map_name partial match `key in clean or clean
  in key` is order-dependent and loose — if SPATIAL_REGISTRY holds prefix-similar names
  (de_dust vs de_dust2), wrong-map radar becomes possible → promote to register if so.
- Deprecated MapAssetManager shim → L10 removal candidate.

## core/localization.py (452)
- Kivy-era i18n singleton `i18n`: LOC-02 chain JSON(lang) → hardcoded(lang) →
  hardcoded(en) → raw key (+LOC-03 debug log). JSON = assets/i18n/{en,pt,it}.json with
  {home_dir} placeholder expansion; hardcoded trilingual fallback dict inline (en/pt/it).
  Lazy Kivy binding only if kivy already in sys.modules (no eager Kivy boot).
- Notes: hardcoded wizard_step1_desc f-strings bake WINDOWS backslash paths at import
  (Linux shows literal backslashes — only matters when JSON missing; P3 batch);
  get_text(trigger=) legacy param unused; set_language accepts lang known to either
  source. Relationship to qt_app i18n_bridge → B34. L7 anchor: JSON files are SSOT;
  parity test 565 keys ×3 langs.
