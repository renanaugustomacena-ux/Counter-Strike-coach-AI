# D-B57 — misc tools + fuzz + docs tooling (8 files, 2,042 LOC) — pass 1 read 2026-08-14

**F-0040 registered (P1)**: build_pipeline sanitizes BEFORE honoring
--test-only — and verify_all_safe's special case makes the wipe
deterministic under the "safe" runner. Register updated.

## build_pipeline.py (226) — **F-0040 discovery**
- `execute()` stage order: Stage 1 = `Sanitize_Project.py --yes`
  (unconditional), Stage 2 = venv, Stage 3 = tests, and only at the
  3→4 boundary `if self.test_only: return True`. So `console build run
  --test-only` / `goliath build --test-only` DELETE database.db,
  hltv_metadata.db, match_data/, models/, logs/ with zero warning.
- COMPOUNDS F-0039: verify_all_safe.py:25-27 special-cases
  build_pipeline with `["--test-only"]` — the flag reads as a safety
  valve but sanitize runs first, so the "safe" verifier wipes live data
  every sweep. Fix shape: move the sanitize stage AFTER the test_only
  early-return (register carries it).
- Otherwise sound: shlex.split + shell=False throughout ✓; venv_win
  pyinstaller fallback chain; audit_binaries hash-lock as post-gate.

## Sanitize_Project.py (198) — F-0040's payload
- Targets: database.db DELETE, hltv_metadata.db DELETE, match_data/
  CLEAR, models/ CLEAR, logs/ CLEAR, hltv_sync.pid DELETE. Gated by a
  Confirm prompt or `-y/--yes` — the gate is real interactively, but
  any automation passing --yes (build_pipeline does) bypasses it.
- Census note: bare invocation under verify_all_safe would BLOCK on the
  prompt (not wipe) — the danger path is exactly the special-cased
  build_pipeline route.

## dead_code_detector.py (581) — census SAFE
- FOUR R4 war stories in-file: unbound-`r` crash; `import a.b` binds
  only `a` (alias map fix); "Phase C reported nothing, ever"
  (content-without-imports scan fix); Phase-C-must-flip-verdict rule.
  Plus the caveman 125-false-positive war story (TASKS#57) that
  motivated the AST rewrite ✓.
- ENTRY_POINTS still lists SOURCE_DIR/main.py — the dead legacy
  entry point (F-0014 cross-ref): keeps its import closure "alive" in
  --strict CI, masking real orphans under it (W3 one-liner).

## Feature_Audit.py (208)
- Live mode real (imports the actual parser + DB probes). "Static" mode
  is a hardcoded 23-column list that nothing regenerates — drift-prone
  vs demo_parser's real output (P3, dossier-only).

## d4_disk_hygiene_audit.py (273) — census SAFE (read-only)
- Honest D4 doctrine: monolith + shards opened `mode=ro` URI; NOTHING
  deleted; JSON report to docs/. Tick-parity → KEEP / RM_AFTER_BACKUP /
  INVESTIGATE; -1 sentinels route conservative ✓.
- P3 retention subtlety: `files[:3]` runs on auto-backups-desc THEN
  pre-restoration appended — "3 most recent" is per-family, not global,
  and the setdefault month-winner claim breaks across families.
  Conservative direction (keeps extra), read-only anyway.

## tools/fuzz/fuzz_demo_parser.py (268) + __init__.py (4)
- §64/C-SBX-02 scaffold, honest Phase-1 status; atheris optional w/
  deterministic seed-42 random fallback; crash corpus sha256-named +
  .meta sidecars; exit 1 on any crash (CI-friendly).
- Windows quirk (P3): `_try_parse` hands `tf.name` to demoparser2 while
  the NamedTemporaryFile handle is still OPEN — on Windows the reopen
  fails, so every input would "crash" as PermissionError and flood
  .fuzz/crashes. Linux-CI-targeted tool, dossier-only.
- Outside verify_all_safe's blast radius (non-recursive tools/*.py
  glob skips the fuzz/ subdir) ✓.

## docs/tooling/generate_zh_pdfs.py (284) — census SAFE
- Personal docs tooling: Book-Coach-{1,2,3}.md → dark A4 PDFs
  (mermaid→SVG via mmdc, weasyprint, NotoTC CJK + zh alt text).
  Machine-specific Linux paths (chrome, fonts, /home/renan venv) with
  env overrides — honest for its purpose.
- Stale-move drift (P3): default DOCS_DIR = its own parent
  (docs/tooling/) but the Book-Coach files live in docs/ root and the
  docstring still says `python docs/generate_zh_pdfs.py` — bare run
  now [SKIP]s all three files unless DOCS_DIR is set. Self-degrading,
  no harm.
- Correct tempfile pattern here (delete=False + finally unlink) —
  contrast with the fuzzer's Windows-hostile delete=True ✓.
