# D-B54 — pipeline tools (3 files, 2,324 LOC) — pass 1 read 2026-08-14

**F-0039 UPGRADED P2→P1** on this batch's evidence (register updated).

## rebuild_monolith.py (1,065)
- **The F-0039 upgrade driver**: bare invocation = --phase full; Phase 2
  (rebuild_match_stats) DELETES every pro PlayerMatchStats row FIRST,
  accumulates replacements in memory, bulk-inserts ONLY at the end —
  verify_all_safe's 120s timeout kills it right after the delete (tick
  phase skips fast in incremental) → pro stats left EMPTY; the finally
  lock-release never runs (stale d_track_running); and
  save_user_setting("PRO_DEMO_PATH") mutates settings as a side effect
  of any run. Recovery = full multi-hour rebuild or backup restore.
- For its INTENDED operator use, the D1 engineering is superb:
  checkpoint/resume JSON (in_progress breadcrumb for kill diagnosis),
  disk-pressure abort (default 50GB), WAL truncate cycles, bulk-PRAGMA
  set/restore pairs, index drop/recreate cycle (post-R4 indexes kept in
  the cycle), column-listed INSERT w/ triple-update doc, synthetic-path
  match-dbs source mode (D1 — migrates matches whose .dem is gone),
  immutable ro probes, lock acquisition per docs/concurrency_policy.md
  w/ --no-lock escape hatch, R3-H09 NaN sanitization + CHECK clamps,
  D2B enrich-only phase w/ honest data_quality vocabulary
  (full_sql_dem_unreadable / _partial / complete) and demoparser2
  health checks ✓✓.
- **Map-SSOT member #11**: KNOWN_CS2_MAPS (8 maps).

## observe_training_cycle.py (572)
- **F-0039 member #11**: bare invocation runs a REAL 1-epoch JEPA
  training (TrainingOrchestrator.run_training) — model checkpoint
  writes included — plus full-repo *.pt rglob. Maps the 6-phase
  pipeline to the user's own Book-Coach-1.md diagram (Acquisizione →
  Elaborazione → Addestramento → Osservatorio → Conoscenza) ✓.
- Diagnostic quality is high: TRAIN-split↔tick cross-check (the
  _fetch_jepa_ticks empty-return gap called out), dead-feature column
  scan, ObserverCallback event capture, UNASSIGNED-split gap surfacing,
  pipeline-gap final report. T-DIAG-adjacent; belongs behind a
  --train opt-in flag (W3 one-liner).

## policy_runner.py (687) — census SAFE (read-only)
- §54 Policy-as-Code engine: SECURITY/policies/*.yaml discovery w/
  schema validation; 5 rule kinds (line_regex, text_regex, yaml_walker
  w/ minimal JSONPath subset, file_compare regex-extract equality,
  ast_walker Phase-2 stub honestly emitting info); time-bound waivers
  where EXPIRY fails the strict gate; hand-rolled `**` glob→regex;
  HARD_EXCLUDES defense-in-depth ("a misconfigured policy can't walk
  huge trees"); warn-mode default / --strict; JSON mode; exit 0/1/2.
  Mature governance tooling — B76 will read the policies it executes.
