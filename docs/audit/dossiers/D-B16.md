# D-B16 — analysis-core (5 files, 1,605 LOC) — pass 1 read 2026-08-14

## analysis/win_probability.py (674)
- A-12 checkpoint dim-validation (trainer 9-dim ≠ predictor 12-dim, "do NOT
  cross-load") ✓; torch.load weights_only=True ✓ (L5/L9); W-02 error-level
  untrained-inference visibility + _checkpoint_loaded caller gate; R4 MED
  get_win_predictor war story (production used to ALWAYS run Xavier-random
  weights; canonical path now wired, 26-HYB-01 load-or-degrade) ✓.
- 26-WINPROB-03 Platt Newton Hessian-sign war story (divergence to ±1e8) ✓;
  clipped exponents; coordinate-wise Newton OK. Deterministic boundary checks
  FIRST in heuristics (0 alive → 0/1) ✓; W-01 additive bomb adj; W-03 utility
  norm. Feature count verified = 12 ✓.
- R4 LOW "DORMANT BY DESIGN" doc on Elo/Platt (Phase-10/R9 deferral; placebo-on-
  random-weights rationale) — exemplary anti-delete note. Elo math standard.
- **Phase N cross-refs (B20)**: (1) predictor loads checkpoint as BARE
  state_dict — trainer must save exactly that format; (2) shared path contract
  MODELS_DIR/global/win_prob_predictor.pt. P3: 16000/115/5 literals (same
  family as vectorizer); __main__ self-test (S5 row).

## analysis/engagement_range.py (256)
- Additive design over core.map_callouts (documented re-export) ✓; O-03
  missing-map warning; per-event coordinate validation w/ warning; min-5-kills
  gate on role comparison ✓.
- **L10/L8 row — role vocabulary**: baselines keyed "awper/entry_fragger/
  support/lurker/igl/flex" vs rating.py priors "awper/entry/support/lurker/
  igl" vs PlayerRole enum values (B18) — a caller passing "entry" here gets
  silent [] observations. Census the vocabulary at B18 + Phase K.
- L7 row: coaching observation strings EN-only (stored via insights? K census).
  P3: "Unknown Position" string-coupled contract w/ coaching_service.

## analysis/momentum.py (243)
- R4 HIGH war story: dual half-switch reset (13 AND 16) → mr_format-derived ✓;
  DESIGN note gap=0 no-decay within half ✓; asymmetric ±streak increments and
  ROUND_TYPE_WEIGHT documented hand-tuning (P8-03 validation plan); clamps
  [0.7, 1.4]; R4 LOW factory-not-singleton docstring fix (consistent across
  momentum/blind_spots/entropy) ✓.
- **F-0016 blast radius widened**: from_round_stats consumes RoundStats.round_won
  — if winner-dtype bug is real, tracker sees eternal loss streaks → is_tilted
  permanently true → tilt-risk coaching spam. Add to F-0016 consumer list.
- Notes: OT (rounds >24, MR3 swaps) never resets momentum (modeling gap, minor);
  post-reset first round still gets gap-decay ~0.86 (bounded, harmless).

## analysis/blind_spots.py (235)
- B-01 per-round try/except + R4 LOW >5% skip-ratio WARNING (mirrors the
  P-VEC-02 telemetry pattern — good cross-module consistency) ✓; F4-03 public
  evaluate_single_action (no private game-tree access) ✓; priority =
  freq×impact; pattern aggregation per situation ✓.
- B17 contract deps: build_tree(state, depth)/get_best_action/
  evaluate_single_action. L7 row: EN-only training plan text. P3: advice dict
  rebuilt per iteration; game_tree param typed `object`.

## analysis/entropy_analysis.py (197)
- Thread-correct buffer reuse: AC-03-01 preallocated grid + E-01 lock w/
  try/finally; AC-06-01 tiny-clip before log2; per-map grid resolutions;
  P8-07 hand-estimated _MAX_DELTA with explicit validation plan ✓ honest.
- **L8 row — metric near-degeneracy**: entropy grid normalizes to the SAMPLE
  bounding box (min/max of the 1-5 enemy positions), not fixed map extent —
  for ≤5-point samples the measure mostly counts coincident cells, and pre/post
  box re-normalization erases absolute-concentration information (a smoke that
  bunches enemies registers ~0 delta unless they land in one cell). Fine for
  relative ranking of throws (current use); recommend fixed map-extent grid if
  it ever feeds decisions. Verify consumers at Phase K.
