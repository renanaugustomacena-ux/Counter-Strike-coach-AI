# D-B52 — db-maintenance tools (5 files, 2,347 LOC) — pass 1 read 2026-08-14

Zero new register findings. verify_all_safe no-arg census: 3 safe, 2
live-mutating (both idempotent-add) — running total of mutating members
is now 3 (with B51's miner).

## wipe_for_reingest_safe.py (541) — the gold-standard destructive tool
- Dry-run DEFAULT + mandatory --confirm-wipe + snapshot-or---no-snapshot
  waiver gate; HMAC-sealed snapshots (stdlib Phase 1 w/ honest Fernet
  Phase-2 note); JSONL audit trail (Windows USERNAME war story);
  DB-unlock preflight (os.rename probe replacing psutil open_files —
  the Windows access-violation hard-crash war story); restore path:
  member-name allowlist + tar filter="data" defense-in-depth + WAL/SHM
  sidecar removal with the mixed-state rationale. Doctrine-mapped
  (§57/§60, C-WIPE-01, WIPE_RUNBOOK); swap mode honestly
  NotImplementedError (v4 port never landed, v1-v4 deleted).
- Census: bare invocation = dry-run ✓ SAFE. Settings screen's CLI-only
  wipe claim verified consistent ✓.

## reset_pro_data.py (667)
- Interactive [y/N] (default N) + --yes; delete allowlist; FK-ordered
  clears; CoachState RESET-not-delete; PlayerProfile explicitly
  preserved ("belongs to the user"); preserve-hltv default; verify
  phase + VACUUM. Census: prompt-gated AND prefix-skipped ("reset_") ✓.
- **W3/CP0 note — safety-generation gap**: deletes MORE than the wipe
  tool (models, .mcn cache, shards, graph, sync state) with NO dry-run,
  NO snapshot, NO audit log. Retrofit the wipe tool's gates or fold
  into the runbook.

## db_health_diagnostic.py (584)
- READ-ONLY (census ✓ safe). Model-derived tables/columns/statuses
  ("cannot silently go stale") ✓; identifier-validated PRAGMAs; 10
  sections incl. EXPLAIN QUERY PLAN full-scan detection, schema-drift
  model-vs-DB, cross-DB shard↔matchresult reconciliation.
- P3s: section 4.2's orphan check uses `match_id NOT IN (subquery)`
  while populate_match_results leaves match_id NULL → SQL three-valued
  logic silently pins the count at 0 (the repo's OWN documented
  NULL-NOT-IN trap, fixed in match_history_vm, recurring here); 5.3's
  main_ids can carry None; "pass --full" message but no --full flag.

## populate_round_stats.py (287)
- War stories: Q1-02 (enrichment dict DISCARDED → 11 columns 0.0
  everywhere — fixed via the ENRICHMENT_TO_PLAYERMATCHSTATS SSOT
  import, F6-19); case-insensitive player match ("exact match silently
  skipped every mixed-case player"); DL-1 lineage commit-ordering
  deadlock rationale (NTFS). Idempotent INSERT OR IGNORE + skip-if-
  present; MIN(tick) bare-column SQLite semantics documented.
- Census: bare invocation = LIVE incremental run (parses missing demos,
  inserts + UPDATEs) — mutating member #2 (idempotent-add, heavy CPU).

## populate_match_results.py (268)
- ANTI-FABRICATION showcase: earlier revision "guessed winner = team_a
  … a coin flip recorded as fact" — now outcomes are per STARTING SIDE
  only; filename team names stored strictly as metadata; substitutes
  handled by max-across-group. Idempotent via demo: event_key.
- **F-0016 radius**: winner derivation reads roundstats.round_won —
  matchresult rows inherit any source dtype error (consumer list
  updated). **Map-SSOT member #10** (9-map set). Census: bare
  invocation = LIVE cheap insert run — mutating member #3
  (idempotent-add). P3: rows created without match_id (feeds the
  db_health 4.2 blind spot above).
