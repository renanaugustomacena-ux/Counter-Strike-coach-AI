# D-B40 — screens: home + history (4 files, 1,513 LOC) — pass 1 read 2026-08-14

## home_screen.py (989)
- Frame-05 composition per docstring; VM-driven (MatchHistoryVM +
  FocusInsightVM); composed-label helpers documented as the single
  source for build + retranslate ✓; cold-start onboarding stack swap ✓.
- War stories in-file: "…" matches-chip placeholder (hardcoded 0 misled
  users), coach-chip active-reset (transient "Analyzing" stuck), chip
  clobber gate (_matches_chip_populated vs the coarser total_matches
  signal), three FIELD-GAP captions (pending count / last-sync / corpus
  size have no signals — render "—").
- Ingestion buttons: single shared _ingestion_worker slot, both buttons
  disabled while busy, done/error restore + re-fetch ✓. Worker runs
  run_ingestion.process_new_demos IN-PROCESS on the global pool —
  cross-ref D-B09 (read; no daemon side effects) ✓ sanctioned manual
  path. notification_received emits from main thread ✓.
- **S3 census correction (L7 ledger)**: the S3 i18n sweep grepped only
  setter calls — CONSTRUCTOR-arg literals escaped it. Raw EN here:
  "RECENT MATCHES" caption, "View all →", onboarding EmptyState
  (title/desc/both CTAs), completion/failure toast messages. None are
  covered by retranslate(). Joins the Pass-2 L7 systemic finding.
- P3: _navigate duplicates widgets_helpers.navigate_to.

## match_history_screen.py (464)
- **B41 cancel-carry RESOLVED**: on_leave → vm.cancel(). Residual (P3,
  W3 one-liner): a cancelled load still emits matches_changed([]) —
  quick leave-and-return can flash a FALSE "No matches found" until the
  fresh load lands (self-correcting in most orderings; fix = cancel
  path returns None, which _on_loaded already skips).
- **F-0035-SAFE**: both EmptyStates built via constructor with their
  CTAs; only set_title/set_description called later — CTA rows never
  enter the vanish path ✓.
- Grouping: elapsed-time buckets documented (24h/7d, not calendar);
  delta-chip baseline = WHOLE personal list, zero-ratings excluded as
  failed parses (both documented) ✓; count caption via shared
  count_personal_and_pro ✓; map chips top-8 cap commented.
- Filter chips: single-select re-check loop is idempotent (set_checked
  no-ops on same value) ✓.
- **Theme-staleness ledger** (systemic, register at Pass 2): NO
  theme_changed wiring — FilterChips, pro banner, path chips, status
  captions keep old-token instance styles after a live theme switch
  (renders are per-theme fresh constructions, so the matrix can't see
  it). Hosts so far: match_history, home (chips/banner/captions),
  roster cards (B39).
- **Effects-doctrine tension ledger** (Pass 2 L2): _render_filtered
  ends with Animator.fade_in(container) IMMEDIATELY after bulk row
  insertion — opacity effect animating through the post-insert
  layout/first-paint pass; offscreen CI green, real-display Linux is
  the doctrine's concern. Census remaining fade_in call sites at
  B41-B45.
- L7 ledger additions: bucket headers rendered from _BUCKET_ORDER
  literals ("TODAY"/"THIS WEEK"/"EARLIER"), source chips
  ("All"/"Personal"/"Pro"), "All maps", the pro-only banner sentence,
  empty/error state texts ("No matches found", "Open Dashboard",
  "Clear filters", "Couldn't load matches").
- _to_aware naive→UTC assumption — same S6 producer-TZ carry as
  match_mini_card._relative_time.

## placeholder.py (59) — LIVE, not dead
- app.py:343 uses create_placeholder_screens() as the per-screen
  ImportError fallback layer (D-B34's deferred-import pattern) ✓.
  13 EN title/desc pairs are degraded-mode-only surfaces (L7 tail).
- W6 note: all three qt_app READMEs (:32) call the class
  "EmptyPlaceholderScreen" — actual name is PlaceholderScreen.

## screens/__init__.py (1) — docstring only.
