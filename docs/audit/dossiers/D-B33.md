# D-B33 — reporting (5 files, 881 LOC) — pass 1 read 2026-08-14

## reporting/visualizer.py (361)
- **F-0017 SCOPE SETTLED**: render_differential_overlay builds its own grid
  (no flip) + imshow(origin="lower") + consistent extents; map background
  origin-upper matches radar row order — BOTH internally coherent →
  orientation on the LIVE overlay path is CORRECT. heatmap_engine's dead
  rgba remains the only mis-oriented surface (F-0017 stands as scoped).
- VZ-02 path-traversal guard on config image paths (L9 ✓); VZ-01/DA-VZ-01
  try/finally figure closes ✓; TwoSlopeNorm diverging map; activity mask.
- Rows: (1) FIFTH map-geometry duplication — _get_bounds hardcodes 6 maps'
  bounds independent of spatial_data pos/scale (L1/L10 map-SSOT list);
  (2) **S2 shim LIVE CONSUMER**: generate_highlight_report imports
  backend.nn.rap_coach.chronovisor_scanner (old path) — W3 shim removal
  must update it; (3) positionless critical moments rendered along a fake
  line at 80% map height OVER the radar — visually implies locations
  (RAP-cluster CP0 note); (4) MatchVisualizer default output "reports/
  assets" is CWD-relative (L6) → **report image links break when CWD ≠
  USER_DATA_ROOT** (RG-02 relative_to ValueError → name-only link to a
  file that isn't co-located; fix: pass anchored output_dir). P3:
  generate_highlight_report treats ScanResult as a list (truthiness works
  — dataclass always truthy → proceeds even on failure result; annotations
  built from .critical_moments? NO — iterates the ScanResult? `for cm in
  critical_moments` — ScanResult is NOT iterable → TypeError caught by
  broad except → returns None. Dead-ish path under RAP gate; fold into
  RAP cluster.)

## reporting/report_generator.py (102)
- DA-RG-01/RG-01 anchored + validated output dir; loud empty-parse guards ✓.
  P3 rows: deaths extracted per-frame (health==0 every tick — mass
  duplicates) then UNUSED; naive datetime.now (S6); static "clustering at
  X, Y" placeholder text in the report body (template honesty — flag W6).

## backend/reporting/analytics.py (418)
- Pro-overview fallback doctrine documented; SA-03 Row-indexing; R4 HIGH
  war story (rating-only history → pro percentiles ranked user ~0th);
  anti-fabrication: empty pro utility baseline rather than invented values
  ✓; calculate_deviations consumer #2; hltv2 breakdown reuses rating SSOT
  constants ✓.
- Rows: (1) **get_hltv2_breakdown omits dpr from compute_impact_rating**
  → survival penalty missing → dashboard Impact inflated ~0.1-0.2 vs
  stored rating_impact (the exact divergence rating.py's contract warns
  against — L8 row, W2/W3 one-liner); (2) get_skill_radar skips the
  _player_filter fallback (empty radar for pro-overview mode — P3
  inconsistency); (3) module-level `analytics = AnalyticsEngine()` eager
  import-time instantiation (import-side-effect family, L10); (4) SIXTH
  divergent known-map list + regex (map SSOT row now 6 sites).

## 2 empty __init__.py.
