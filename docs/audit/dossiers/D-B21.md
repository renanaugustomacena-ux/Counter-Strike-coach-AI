# D-B21 — jepa-training (2 files, 1,435 LOC) — pass 1 read 2026-08-14

## nn/jepa_train.py (818) — legacy/CLI training pipeline
- **Resolves 4 carried verifies favorably**: (1) _moe_aux_loss IS consumed —
  R4 MED war story at :649 ("never added to ANY objective — protection inert
  AND its autograd graph retained between forwards" = the module-33 lesson,
  found and fixed in-repo); (2) amp contract honored (forward+loss inside
  config.amp_autocast bf16, backward/step outside) both paths; (3)
  is_pretrained persisted + restored via save/load_jepa_model ✓; (4) loss
  accumulation via .item() throughout — no graph retention.
- ML-hygiene highlights: DATA-01 refuses per-tick broadcast of match-aggregate
  avg_kast (future-information leak + train/serve skew — feature 16 stays 0.0
  both sides); J-1 tick-level sequences w/ I-JEPA citation (kills the
  round-aggregate index collision); WR-53 repeat-last-tick padding (zero-pad =
  impossible states corrupting LSTM); R4 LOW exclusive-bound tail-bias fix;
  R4 LOW processed-batches denominator fix; P1-05/NN-35 O(B) shifted-randperm
  negative sampling (self-exclusion correct; degenerate path unreachable);
  V-4 per-demo DataQualityError catch; M3 atomic checkpoint save; REPR-01 EMA
  schedule persisted + W1.4 legacy-checkpoint warning; weights_only=True.
- **CP0 status item**: --mode finetune is DELIBERATELY DISABLED — 26-RANGE-01
  raises loudly (y=25-dim last-tick vs head OUTPUT_DIM=10; TASKS#64 contract
  undefined; proposed fix in the error text). JEPA fine-tuning unavailable by
  design until the target contract lands.
- Notes: _DB_PATH derived from __file__ (frozen-app portability, L6 row);
  raw sqlite3 WAL+busy_timeout ✓ documented ORM bypass for bulk reads;
  checkpoint FORMAT here is a full dict (≠ persistence.save_nn bare
  state_dict + sidecar) — **B24 must census which loader inference uses**.

## nn/jepa_trainer.py (617) — modern trainer class
- **F-0023 (P2, resolves B20 verify)**: train_step_vl feeds RAW cosine
  concept_logits ∈ [-1,1] to vl_jepa_concept_loss's BCE-with-logits →
  sigmoid range [0.27, 0.73] can never fit labels 0.0/0.8-1.0 (irreducible
  loss floor, gradient saturates toward ±1); inference reads concepts via
  temperature-scaled SOFTMAX — a different function of the same logits.
  Ordinal signal survives; calibration + train/readout coherence broken.
- Quality architecture: KT-05 concept-param 0.05× LR group (collapse
  prevention per VL-JEPA §4.6); NN-36 target-encoder excluded from optimizer;
  Phase 4A-D AMP + grad accumulation (correct leftover flush, 1/accum loss
  scaling) + clipping on trainable-only; J-6 EMA cosine schedule w/ V-3 reset
  fix AND R4 MED optimizer-step-unit fix (batches vs steps war story);
  REPR-01 rehydration + W1.4 loud legacy warning; P9-02 detector wired
  per-epoch (raises — **B22 must verify orchestrator lets it bubble**);
  J-2 no-RoundStats skip path trains InfoNCE alone (no leakage) with
  LabelSourceMonitor sliding-window telemetry (1%-over-5min alarm) ✓;
  R4 MED train_step_vl contract-parity war story (shape/device checks).
- Module-33 lens: CLEAN (.item() everywhere, enqueue detached, no tensor
  accumulation; epoch_variances are floats).
- L8/L10 consistency row: this trainer uses fp16 + GradScaler (bare
  torch.amp.autocast default) while jepa_train.py uses config.amp_autocast
  bf16-no-scaler — two AMP strategies across sibling trainers; the config
  docstring argues bf16 is THE path for the ROCm target. Unify at W3.
- DriftMonitor/should_retrain consumers FOUND here (closes B15 census row):
  check_val_drift → 3-of-5 → retrain_if_needed (fresh scheduler, EMA reset,
  collapse-detector reset, drift history cleared) ✓.
