# D-B22 — training-orchestrator (1 file, 1,611 LOC, solo) — pass 1 read 2026-08-14

## nn/training_orchestrator.py — the Teacher training brain
- **War-story density is exceptional**: B4 dry-run Law-7 fix (dry-run used to
  OVERWRITE production checkpoints); R4 CRIT contiguous-window feed (old flat
  random ticks made JEPA context/target unrelated rows); V-1 next-step target
  (was overlap or 89-ticks-distant); R4 MED val-features-leaked-into-negative-
  pool fix; R4 HIGH skip-denominator fix (non-comparable val losses corrupted
  best-checkpoint + early stopping); TASKS#59 zero-batch scheduler skip;
  B3.2 best-val sidecar restore (resume no longer auto-overwrites best);
  G-01 real round_number mapping (old tick//(64·115) estimate = 26-TICK
  violation + "fabricated concept labels" — their words); LEAK-01 masked
  value targets (never train on future round_outcome); T-2 ≥50% POV density
  (zero-fallback windows taught "vision is uninformative"); RAP-AUDIT-02
  position deltas (head trained at last); RAP-AUDIT-05 + C1.2 per-demo dt
  for the LTC ODE; POV-RAP-FIX-2 match_id fallback; TASKS#60 once-per-demo
  map warning; 26-ORCH-02 loud tick-rate fallbacks; P3-C/P3-D abort gates;
  W2.5/DR-17 drift wiring (CLOSES the ST-1 "no caller" gap +
  TickFeatureDriftMonitor consumer FOUND — B15 row closed).
- Module-33 lens: CLEAN — _neg_pool bounded 500 w/ detached CPU tensors;
  per-batch caches scoped to the call; losses handled as floats.
- **F-0024 (P2)**: the P9-02 embedding-collapse HARD-STOP is bypassed on the
  production path. Orchestrator drives its own epoch loop via
  trainer.train_step(); JEPATrainer.train_epoch — the ONLY site feeding
  embedding_collapse_detector.update() — is reached only from the
  drift-retrain path. Per-batch embedding_variance returns in the result
  dict and is discarded (callbacks only). The exact "advisory only" state
  P9-02 mandated fixing, reproduced one level up (early_stopping.py
  docstring even promises "the TrainingOrchestrator inspects").
- **F-0025 (P2)**: RAP value/strategy labels computed under a team attribute
  the input rows don't have. EMPIRICAL: monolith playertickstate has NO
  `team` column ("no such column: team"); shards use {'CT','TERRORIST'}.
  `getattr(item, "team", "CT")` → every sample treated as CT →
  _compute_advantage returns CT-perspective advantage as the value label for
  T-side players (anti-correlated targets for ~half the corpus); bomb_factor
  `== "T"` never fires; _classify_tactical_role is_ct always True → T-side
  role branches unreachable, CT+bomb→RETAKE stamped on T samples. Gated
  behind USE_RAP_MODEL (default off) — silent poison if ever enabled. Fix:
  resolve side from the shard row / knowledge.own_team ('TERRORIST' full
  name — map to T; keeps comparison vocabulary consistent shard-vs-shard).
- L1 sweep rows (F-0002 family): _rap_prefetch_caches per-tick event slice
  uses fixed 320/64 literals (:976-977) while its bulk bounds are rate-
  derived; _build_sample_knowledge event fetch repeats tick−320/+64.
- Phase-K/B24 carries: manager contract (_fetch_jepa_windows /
  _fetch_rap_windows / _update_state — coach_manager); RAPTrainer
  criterion_val + train_step dict contract (B25); AMP divergence noted at
  B21 applies here via JEPATrainer.
