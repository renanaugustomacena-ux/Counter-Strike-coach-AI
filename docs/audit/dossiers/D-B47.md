# D-B47 — root CLIs (5 files, 1,204 LOC) — pass 1 read 2026-08-14

Zero new register findings. F-0037 evidence widened to FIVE surfaces
(batch_ingest joins). Cross-cutting L3/L10 rows recorded.

## goliath.py (301)
- F7-29 child cleanup (signal + atexit, terminate→kill ladder) ✓;
  F7-07 %s-logging fix comments ×4; shell-free subprocess ✓.
- P3s: hand-rolled JSON-line log format doesn't escape quotes in
  messages (malformed JSON lines — L4 row); run_db(force) accepts but
  ignores --yes; **L10 row: goliath duplicates console v3's build/
  integrity/audit/db/doctor/baseline surface — console's own docstring
  claims sole-entry doctrine ("If it's not in this console, the project
  doesn't need it") → CP0/W3 decision: deprecate goliath or demote to
  alias.**

## schema.py (326)
- L9-strong: _IDENTIFIER_RE + _SAFE_COL_TYPE_RE validation on every
  dynamic identifier; bracket quoting; parametrized master lookups;
  sequences reset BACKS UP to _sqlite_sequence_backup before DELETE
  with an ID-reuse warning ✓.
- **L3 row (migration-path plurality)**: _apply_column_migration is a
  hot-patch mechanism OUTSIDE the canonical alembic chain (alongside
  db_migrate.ensure_database_current and the deprecated migrate_db) —
  additive-only today, but version history diverges from applied state.
- P3s: `fix --type knowledge` accepted but is a no-op (comment says the
  fix moved — the CLI choice remains); DB_PATH hardcoded relative,
  bypassing core.config DATABASE_URL (L6 row: relocated DBs inspect the
  wrong file).

## run_full_training_cycle.py (247)
- War stories verified in-file: DET-01 (entry point shipped WITHOUT
  set_global_seed — fixed with commit ref e89df58); PRE-11
  (MaturityObservatory was never registered → zero MaturitySnapshots;
  now wired w/ honest note that most signals are RAP-only).
- B6.1 pre/post eval snapshots in a SUBPROCESS (SBERT/FAISS never share
  the 4GB GPU budget; failures logged loudly, never block training) ✓;
  GPU reclaim between phases (del + gc + empty_cache + mem_get_info
  log) ✓; deprecated --resume documented no-op; callbacks.close_all()
  in finally ✓. This is the ORCHESTRATOR training path (phases 1/4 of
  the Phase-N three-paths map — consistent).

## batch_ingest.py (325)
- **F-0037 fifth surface** (register amended): already-ingested set
  snapshotted once; ProcessPool workers then parse fully — the
  default-ON daemon can race the whole batch. Single-operator
  assumption is implicit, unenforced.
- Otherwise the STRONGEST ingestion code audited: finally-block task
  state machine w/ the Apr-2026 "0 failed after 301 hard failures"
  silent-desync war story; error_message truncation (512); failed→
  queued retry reset; max_tasks_per_child=1 worker recycling
  (DataFrame-leak mitigation, 3.10 fallback per CLAUDE.md); psutil-not-
  sysconf war story (2026-07-17 E2E crash); RAM/CPU worker autoscale;
  DP-06 pro-path auto-recovery; symlink-excluding rglob ✓.
- Auto-train after ingest = the LEGACY train path (phases 2/3) —
  consistent with the three-paths map; P3: calls private
  manager._train_phase from a CLI.

## setup.py (5) — pyproject shim ✓ trivial.
