# D-B19 — nn-foundations (10 files, 1,486 LOC) — pass 1 read 2026-08-14

Study companion: 33-profiling-memoria-gc (STUDY_LOG row added this commit).

## nn/config.py (218)
- Reproducibility: AR-6/P1-02 set_global_seed (all four RNGs + cudnn flags),
  DET-02 warn_only deterministic algorithms + CS2_NONDETERMINISTIC escape,
  DET-01 seeded_generator for DataLoader workers ✓ course-grade determinism.
- Hardware: discrete-GPU-preferred selection (VRAM score, integrated-keyword
  penalty), CUDA_DEVICE override, cached device, ROCm gfx1201 MIOpen war story
  (rocrand JIT headers missing → cudnn backend disabled → ATen kernels) ✓;
  WR-09 CPU-fallback user notification w/ R4 LOW breadcrumb.
- amp_autocast: bf16-only (no GradScaler; ROCm fp32 GEMM 1.3-vs-118-TFLOPS
  rationale) with "backward/step OUTSIDE the context" contract — **B21 must
  verify call sites honor it**.
- **L1 anchors**: INPUT_DIM = METADATA_DIM (canonical 25); RAP_POSITION_SCALE
  = 500.0 SSOT (F3-05 — GhostEngine + overlay consistency; verify B24/U).
- P3: intensity comment mentions "Background" level absent from maps; mid-file
  METADATA_DIM import (E402 family).

## nn/training_config.py (69) — clean dataclass split (base + JEPA); batch_size=1
  documented (per-match RAP). No issues.

## nn/dataset.py (63) — clone/detach discipline ✓; SelfSupervisedDataset sliding
  window w/ ctor ValueError on short data + F3-34 documented guard. Slices are
  views (read-only OK). No issues.

## nn/data_quality.py (156) — Phase 3D pre-training gate
- Counts + split distribution + zero-position rate + match completeness →
  pass/fail verdict; whole check wrapped (failed report, never crashes
  orchestrator) ✓. P3: "sample up to 10K" comment lies — `sample_total`
  computed then UNUSED; query counts the full table (correct, possibly slow;
  fix comment or implement sampling).

## nn/early_stopping.py (174)
- EmbeddingCollapseDetector: P9-02 promotion from advisory warning to HARD
  ABORT (2 consecutive collapsed val epochs → EmbeddingCollapseError) with
  NaN-explicit check (`v != v`) and a 4-lead investigation message ✓ exemplary.
  EarlyStopping standard w/ correct min_delta semantics. __main__ demo (S5).

## nn/ema.py (132)
- THREE aliasing bugs pre-fixed: NN-16 clone-on-apply_shadow, documented
  clone-on-restore, F3-30 cloned state_dict ✓. Update formula standard;
  late-registered params handled. __main__ demo (S5).

## nn/factory.py (142)
- Central instantiation for 6 model types w/ lazy imports; _require_int
  ctor-time coercion (kills cryptic mid-forward shape errors); canonical
  checkpoint names; loud ValueError with valid list ✓ clean.

## nn/model.py (223)
- **GAP-10/MOE-02 expert-collapse war story**: dense softmax gate let gradient
  reach every expert → collapse; now raw logits → top-K(2) sparse routing
  (_topk_sparse_gate: topk → softmax over selected → scatter; k=E degenerates
  to dense as sanity property) with DOCUMENTED state_dict key change
  (gate.0.weight → gate.weight; old checkpoints → StaleCheckpointError) ✓.
- P1-12 self-describing checkpoints (architecture dict in version metadata);
  role-bias averaging preserves sum-1; input-dim validation w/ explicit
  unsqueeze contract; RAP optional-deps ImportError guard ✓.
- Notes: ModelManager.save_version writes non-atomically and skips CTF-1 hash
  registration (persistence.save_nn is the hardened path — L9/L5 row: census
  which save path production uses at B21/B22); TeacherRefinementNN deprecated
  alias documented.

## nn/persistence.py (309) — CAMPAIGN-BEST checkpoint infrastructure
- StaleCheckpointError doctrine ("random weights never acceptable"); 4-tier
  path fallback (user → global → factory-user → factory-global); NN-14
  FileNotFoundError instead of silent random weights ✓.
- CTF-1 hash registry: chunked SHA-256; **R4 MED war story** (corrupt registry
  was silently rewritten with only the current entry → now .corrupt backup +
  ERROR). Permissive-by-default verification (unregistered/missing registry
  passes — documented factory-bundle allowance; local-app threat model, L9 note).
- GAP-07 sidecar: schema_version + metadata_dim + EXACT FEATURE_NAMES match →
  StaleCheckpointError BEFORE model touch (closes the heuristic-config-edited-
  after-training train/serve skew); legacy checkpoint → warn ✓.
- save_nn: atomic tmp+replace on BOTH files, ordered promotion (weights first,
  rationale documented), BaseException cleanup-then-reraise (the course-07
  LEGITIMATE BaseException pattern ✓); load_nn: weights_only=True +
  map_location=cpu + strict=True ✓.
- W5/B76 note: registry "committed to git" comment — verify gitignore
  treatment of models/checkpoint_hashes.json.

Zero register findings — strongest infrastructure batch of the campaign.
