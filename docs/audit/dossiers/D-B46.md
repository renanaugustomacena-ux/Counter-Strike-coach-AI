# D-B46 — console.py solo (1,671 LOC) — pass 1 read 2026-08-14

Zero new register findings. F-0037 evidence AMENDED: console `ingest
start` → ingest_manager.scan_all is the FOURTH trigger surface over the
shared IngestionTask queue, via a different consumer than
run_ingestion's — claim-semantics parity is a Pass-2 L3 reconciliation
item (with F-0015's stale-threshold territory).

## Security/hygiene showcase (L9 rows)
- Venv guard at import; F7-12 documented sys.path bootstrap; Windows
  UTF-8 reconfigure; rich hard-required with actionable error.
- P7-01: API keys via getpass w/ explicit process-list warning when
  passed as args; P7-02: dispatch-level exception sanitizer redacts
  STEAM/FACEIT/STORAGE keys from error text; F7-30: settings view masks
  keys to last-4; `set config` gated by an ALLOWLIST.
- maint clear-cache: PROJECT_ROOT marker check (Programma_CS2_RENAN dir
  + console.py file) before the rmtree walk — destructive-op guard ✓.
- P3 (L9 tail): `svc spawn <name>` joins user input onto the two tools
  dirs without a traversal check (`../` escapes) — operator-local tool,
  low risk, one-line normpath fix.

## Architecture
- CommandRegistry (category/subcmd, slots) + dual dispatch (TUI
  interactive parser + argparse CLI w/ extractor table); `help` handled
  pre-argparse; exit code sniffed from the [error] tag (crude, works).
- Lazy backend loader (no import-time side effects — boot doctrine ✓);
  ROOT-08: CLI mode boots services before dispatch (warning on failure;
  boots even for read-only commands — P3 heaviness note; console IS the
  sanctioned boot owner per D1-D10).
- _run_tool / _run_tool_live: shell-free subprocess, PYTHONPATH
  injection, cwd pinning, timeouts (named constants), output trim,
  kill-on-timeout ✓. test suite = 4 validators sequentially w/
  last-10-lines on failure.
- svc spawn: stderr → timestamped spawn log; parent's fd closed
  post-Popen with the inherit rationale documented ✓.

## TUI
- StatusPoller background thread (lock + Event + join(3)) so the render
  loop never blocks on get_system_status ✓; dirty-flag + status-hash
  change detection + throttled refresh (flicker fix); psutil primed;
  ">>>"-marked results pause Live for scrollback then resume; SIGINT →
  clean shutdown; Unix termios saved/restored in finally; platform
  input split (msvcrt vs select/tty cbreak) ✓.
- maint clear-queue: purges queued tasks (operator override of the
  F-0037 queue — fine as a tool); P0-03 auto-commit comment accurate.
- P3s: sys baseline table shows an arbitrary first-12-sorted-8 key
  subset; TUI has no input history; `_cmd_tool_user` help text claims
  "[personalize|customize]" but the handler is read-only (doc drift).
