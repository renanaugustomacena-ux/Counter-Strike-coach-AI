# =============================================================================
# Macena CS2 Analyzer — Pinned Dependencies
# DEP-1: Exact pins for reproducible builds (tested 2026-04-12)
# P5-03: Unused PDF/analysis packages removed (zero imports found)
# =============================================================================

# --- UI Framework (Qt) ---
PySide6==6.11.0

# --- Demo Parsing ---
demoparser2==0.41.1

# --- Data & ORM ---
sqlmodel==0.0.38
sqlalchemy==2.0.49
alembic==1.18.4
pydantic==2.12.5
pandas==2.3.3

# --- ML & Scientific ---
torch>=2.1.0,<3.0  # platform-specific (ROCm/CUDA/CPU); range pin intentional
numpy==2.4.3
scipy==1.17.1
scikit-learn==1.8.0

# --- Web & API ---
fastapi==0.135.3
uvicorn==0.44.0
requests==2.33.1
httpx==0.28.1

# --- Scraping & Browser ---
playwright==1.58.0
beautifulsoup4==4.14.3

# --- System & Monitoring ---
psutil==7.2.2
watchdog==5.0.3
keyring==25.7.0
sentry-sdk==2.57.0

# --- Media & Visualization ---
opencv-python==4.13.0.92
pillow==11.3.0
matplotlib==3.10.8

# --- NLP (optional — graceful fallback if missing) ---
sentence-transformers==3.4.1

# --- Vector Search (optional — graceful fallback to brute-force if missing) ---
faiss-cpu==1.13.2

# --- Optional: RAP Coach experimental architecture ---
# Required ONLY when USE_RAP_MODEL=True in settings.
# Install via: pip install -r requirements-rap.txt
